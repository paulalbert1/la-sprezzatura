// src/components/admin/settings/HeroSlideshowEditor.test.tsx
// Nyquist Wave 0 stub — Phase 34 Plan 01.
// Implementation lands in Plan 03 (settings-surface). Each pending
// stub becomes an active test when the corresponding component is built.
// Source of truth: .planning/phases/34-settings-and-studio-retirement/34-UI-SPEC.md § Hero Slideshow editor

import { describe, it } from "vitest";

describe("HeroSlideshowEditor (Phase 34 Plan 03)", () => {
  it.todo("empty state renders dashed border card with 'No slides yet' copy");
  it.todo("arrayMove reorders slides by dragging");
  it.todo("save is disabled when any slide has empty alt text");
  it.todo("empty state is hidden when slides.length > 0");
  it.todo("delete button triggers optimistic removal and Slide removed toast");
  it.todo("upload callback receives the full Sanity asset document, not a blob pathname");
  it.todo("drag handle hover color is #9A7B4B");
  it.todo("keyboard sensor: Space picks up, arrows move, Space drops");
});
