// src/components/admin/ui/CollapsibleSection.test.tsx
// Nyquist Wave 0 stub — Phase 34 Plan 01.
// Implementation lands in Plan 02 (foundation-primitives). Each pending
// stub becomes an active test when the corresponding component is built.
// Source of truth: .planning/phases/34-settings-and-studio-retirement/34-RESEARCH.md § 8 Validation Architecture

import { describe, it } from "vitest";

describe("CollapsibleSection (Phase 34 Plan 02)", () => {
  it.todo("renders header with title; chevron rotates 180 degrees when expanded");
  it.todo("body is not rendered in DOM when collapsed");
  it.todo("clicking header toggles expanded state");
  it.todo("Enter key on header toggles expanded state");
  it.todo("Space key on header toggles expanded state");
  it.todo("defaultOpen prop controls initial expanded state");
});
