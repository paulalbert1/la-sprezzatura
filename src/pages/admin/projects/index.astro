---
export const prerender = false;

import AdminLayout from "../../../layouts/AdminLayout.astro";
import ProjectList from "../../../components/admin/ProjectList.tsx";
import { getAdminProjects } from "../../../sanity/queries";

const projects = await getAdminProjects();
---

<AdminLayout title="Projects | La Sprezzatura" pageTitle="Projects">
  <ProjectList client:load projects={projects} />
</AdminLayout>
