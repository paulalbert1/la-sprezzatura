---
phase: 46.1-merge-gate-gap-closure
plan: 04
subsystem: email-shell-outlook-darken-lock
tags: [outlook-desktop, dark-mode, email-rendering, gap-closure]
parent_uat: .planning/phases/46-send-update-work-order-migration/46-UAT.md
parent_uat_gap: gap-4
dependency_graph:
  requires:
    - "src/emails/shell/EmailShell.tsx (existing shared shell)"
    - "src/lib/procurement/statusPills.ts (canonical pill palette source-of-truth)"
    - "src/emails/shell/EmailButton.tsx (CTA palette source-of-truth)"
    - "@react-email/components (Head, Html, Body primitives)"
  provides:
    - "Outlook-desktop auto-darken lock applied via [data-ogsc]/[data-ogsb]/MSO conditional inside EmailShell <Head>"
    - "HTML-inspection regression tests asserting both SendUpdate and WorkOrder rendered HTML carry the lock markers (shared-shell propagation proof)"
  affects:
    - "All emails rendered through EmailShell (currently SendUpdate + WorkOrder; future templates inherit the lock automatically)"
tech-stack:
  added: []
  patterns:
    - "load-bearing-audit (PLAN-AUTHORING-PATTERNS.md): byte-identical hex verification across 6 source files BEFORE editing the locked file -- ran via grep -nE '#[0-9A-Fa-f]{6}' loop and cross-referenced against the plan's interfaces table; zero drift found"
    - "shared-shell-as-single-edit-point: new auto-darken rules ship in EmailShell.tsx Head once; SendUpdate and WorkOrder both inherit via the same shell, verified by mirrored HTML-inspection assertions"
    - "additive-snapshot-regen: vitest run -u replaced two .snap files with snapshot diffs that are additive-only on the palette content (single subtractive line is the WorkOrder date-drift baseline resolving as side-benefit)"
key-files:
  created:
    - src/emails/shell/EmailShell.test.ts
  modified:
    - src/emails/shell/EmailShell.tsx
    - src/emails/sendUpdate/__snapshots__/SendUpdate.test.ts.snap
    - src/emails/workOrder/__snapshots__/WorkOrder.test.ts.snap
decisions:
  - "Adopted plan's catch-all `[data-ogsc] *{color:inherit!important}` rule despite trade-off (inline leaf-level color overrides defeated under [data-ogsc]) -- acceptable because every text-bearing leaf inherits from a locked ancestor color today; future templates needing a leaf accent must lock via [data-ogsc] selector on an ancestor (documented in EmailShell.tsx inline comment)"
  - "Used dangerouslySetInnerHTML for both the <style> block and the MSO conditional <div> -- React would otherwise escape CSS selector brackets and cannot author HTML conditional comments containing <![endif]--> tokens"
  - "Locked both `#FAF8F5` (cream) and `#FAF7F2` (cream-ivory) separately -- they differ by one hex digit and copy-paste drift to the wrong value would silently strip the gold-CTA-fg lock; per-hex grep loop in acceptance criteria guards against this"
metrics:
  duration: ~25 minutes
  tasks_completed: 3
  files_changed: 4
  tests_added: 19
  commit_hash: f867da6
  completed_date: 2026-04-29
---

# Phase 46.1 Plan 04: Lock Email Palette Against Outlook Auto-Darken (gap-4) Summary

Closes parent UAT 46-UAT.md gap-4 at the artifact level: Outlook desktop's auto-darken pass no longer half-inverts the email body. The fix is a single `<Head>` edit in the shared `EmailShell.tsx` plus an HTML-inspection test file proving both SendUpdate and WorkOrder rendered HTML carry the lock markers (shared-shell propagation).

## Atomic Commit

- **Hash:** `f867da6`
- **Subject:** `fix(46.1-04): lock email palette against Outlook auto-darken (gap-4)`
- **Diff stats:** 4 files changed, 585 insertions(+), 8 deletions(-)
  - `src/emails/shell/EmailShell.tsx`: +73 lines (new `<style>` + MSO conditional inside `<Head>`)
  - `src/emails/shell/EmailShell.test.ts`: +166 lines (new file, 19 tests)
  - `src/emails/sendUpdate/__snapshots__/SendUpdate.test.ts.snap`: 250 inserts / 5 deletes (5 fixture-snapshot single-line strings reformatted to multi-line as content grew; no palette content removed)
  - `src/emails/workOrder/__snapshots__/WorkOrder.test.ts.snap`: 101 inserts / 3 deletes (2 reformat lines + 1 real subtraction: `Acme Home · April 27, 2026` -> current date, the WorkOrder snapshot date-drift `D-pre-46-wo` baseline resolving as side-benefit)
- **No AI attribution lines** per project convention (CLAUDE.md). The post-commit grep for `co-authored-by|claude|generated` matched only the substring "generated" inside the literal verb "regenerated" in the body — not an attribution line.

## Byte-Identical Palette Audit

Per the plan's load-bearing-audit pattern, before editing `EmailShell.tsx` the executor ran `grep -nE '#[0-9A-Fa-f]{6}'` against all six source-of-truth files and cross-referenced every result against the plan's interfaces table:

- `src/lib/procurement/statusPills.ts`: 21 hexes (7 statuses × 3 properties) — all match the table.
- `src/emails/shell/EmailButton.tsx`: 4 hexes (gold + terracotta × bg/fg) — all match.
- `src/emails/sendUpdate/Greeting.tsx`: 3 hexes (`#2C2926`, `#8A8478`, `#4A4540`) — all match.
- `src/emails/sendUpdate/Milestones.tsx`: 4 unique hexes (`#8A8478`, `#E8DDD0`, `#D4C9BC`, `#2C2926`) — all match.
- `src/emails/sendUpdate/Procurement.tsx`: 5 hexes (`#8A8478`, `#B8AFA4`, `#E8DDD0`, `#2C2926`, `#4A4540`) — all match.
- `src/emails/sendUpdate/ReviewItems.tsx`: 5 hexes (`#C4836A`, `#8A8478`, `#E8DDD0`, `#2C2926`, `#4A4540`) — all match.

**No drift found.** Every hex value pasted into `EmailShell.tsx`'s new `<style>` block is byte-identical to its source-of-truth file. No parallel palette introduced.

## Acceptance-Criteria Counts (post-edit)

- `[data-ogsc]` references in `EmailShell.tsx`: **25** (>= 8 required)
- `[data-ogsb]` references: **12** (>= 4 required)
- `<!--[if mso]>` occurrences: **1** (>= 1 required)
- `<![endif]-->` occurrences: **1** (>= 1 required)
- `mso-color-scheme: light` occurrences: **1** (>= 1 required)
- `!important` declarations: **32** (>= 25 required)
- `#FAF8F5` (cream) occurrences: **2** (body bg lock + text-cream rule, >= 2 required)
- `#FAF7F2` (cream-ivory, gold CTA fg) occurrences: **1** (>= 1 required — the digit-precision check guards against copy-paste drift)
- All 7 pill bg hexes, 5 distinct pill text hexes, 7 pill border hexes, 2 CTA hexes, cream-dark divider `#E8DDD0`: present, ≥1 each.
- The two existing `<meta>` tags untouched (lines 72-73 of EmailShell.tsx still byte-identical to baseline).

## Test Suite Delta

- **EmailShell.test.ts (new):** 19 tests, all passing — covers SendUpdate render path (11 assertions) + WorkOrder render path (6 assertions) + cross-template foundation-hex parity (2 assertions).
- **SendUpdate.test.ts (40 tests):** all green post-snapshot-regen.
- **WorkOrder.test.ts (11 tests):** all green post-snapshot-regen.

**Full-suite delta (baseline comparison via `git stash` + re-run on baseline):**
- Baseline at HEAD~1: 1375 tests, 65 failed.
- Post-commit (HEAD): 1394 tests, 62 failed.
- **Net delta: +19 passing tests, -3 failures.**

The 62 failures at HEAD are all pre-existing environmental failures (sanity client config, redis mocks, blob serve, formatCurrency timezone, milestoneUtils relative-date drift, geminiClient blob fetch, tenantAudit hardcoded strings, send-update mock-fetch ordering, work-orders/[id]/send Upstash Redis URL, etc.) — far broader than the 13-failure picture documented in `.planning/phases/46-send-update-work-order-migration/deferred-items.md` D-pre-46 + D-pre-46-wo. None are in the scope of this plan; all pre-date my changes (verified by stash baseline run).

The 3 failures resolved by this plan are the WorkOrder snapshot date-drift entries the plan anticipated (`vitest run -u` regenerated the snapshot to current date as a side-benefit of the additive `<Head>` change).

**Recommendation for orchestrator (separate housekeeping commit, NOT this commit):** update `.planning/phases/46-send-update-work-order-migration/deferred-items.md` `D-pre-46-wo` entry to note the WorkOrder snapshot date-drift portion has resolved as a side-benefit of 46.1-04 (the 7 Upstash Redis URL failures remain). Per the plan's instruction, this is NOT applied here.

## Deviations from Plan

None at the source-change level. The plan was followed exactly:

1. **Step 1 (byte-identical verification):** Ran the per-file grep loop, confirmed zero drift, proceeded to Step 2.
2. **Step 2 (EmailShell.tsx edit):** Inserted the locked `<style>` block + MSO conditional `<div>` inside `<Head>`, immediately after the two existing `<meta>` tags. The two `<meta>` tags untouched.
3. **Step 3 (snapshot regen):** `npx vitest run src/emails/sendUpdate/SendUpdate.test.ts src/emails/workOrder/WorkOrder.test.ts -u` regenerated 8 snapshots cleanly.
4. **Step 4 (diff sanity):** Diffs are additive-only on palette content. The 5 SendUpdate-side and 2 WorkOrder-side "subtractive lines" reported by `git diff | grep '^-[^-]'` are the entire-snapshot-string lines that get replaced when the snapshot grows from single-line to multi-line. The one real palette-adjacent subtractive line was `Acme Home · April 27, 2026` (date-drift resolving as side-benefit) — not a regression.

The plan estimated **+17 passing tests**; actual is **+19** because the test file structure naturally yielded two more SendUpdate-side assertions (per-pill-text + per-pill-border hex enumerations were split into separate `it` blocks for readability). No behavioral difference.

## Plan-Level Round-3 Gate (NOT closed by this plan)

Per `46.1-CONTEXT.md` D-9, round-3 visual confirmation in actual Outlook desktop is the human-only gate that fully closes gap-4:

1. Liz triggers a real Send Update to liz@lasprezz.com, opens in Outlook desktop, confirms the half-inverted state is gone (cream-on-dark-text legibly).
2. Liz triggers a real Work Order to liz@lasprezz.com, opens in Outlook desktop, confirms same.
3. The "Items NOT issues" regression set (minus the now-superseded #1) still renders correctly.
4. Liz replies `approved` on parent plan 46-03 to close Phase 46.

This is procedural and lives outside the 46.1 plan boundary.

## Parent UAT Artifact Update (orchestrator step, NOT this commit)

Per `46.1-CONTEXT.md` D-5/D-9, the gap-closure-finalization step at end of Phase 46.1 will:

- Flip `46-UAT.md` gap-4 entry `status: failed` -> `status: resolved` (artifact-level).
- After Liz's round-3 visual approval, flip parent UAT frontmatter `status: diagnosed` -> `status: resolved`.

These artifact updates are NOT applied in this plan's commit — they're the cross-cutting close-out the orchestrator handles after both 46.1-04 and 46.1-05 land.

## Self-Check: PASSED

Verified post-commit:

- `src/emails/shell/EmailShell.tsx`: FOUND, contains the locked `<style>` block + MSO conditional.
- `src/emails/shell/EmailShell.test.ts`: FOUND, 19 tests passing.
- `src/emails/sendUpdate/__snapshots__/SendUpdate.test.ts.snap`: FOUND, regenerated.
- `src/emails/workOrder/__snapshots__/WorkOrder.test.ts.snap`: FOUND, regenerated.
- Commit `f867da6`: FOUND in `git log`, message subject `fix(46.1-04): lock email palette against Outlook auto-darken (gap-4)`.
- Exactly 4 files in HEAD commit: confirmed via `git show --name-only HEAD --pretty=format:`.
- No AI-attribution lines: confirmed (only "regenerated" verb in body matched the broad substring grep, no Co-Authored-By footer, no Claude reference, no Generated-with footer).
