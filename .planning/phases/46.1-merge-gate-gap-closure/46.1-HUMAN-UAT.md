---
status: issues_found
phase: 46.1-merge-gate-gap-closure
source:
  - 46.1-VERIFICATION.md
  - 46-UAT.md (parent — round-3 visual gate per 46.1-CONTEXT.md D-9)
started: 2026-04-28T21:20:00Z
updated: 2026-04-29T13:00:00Z
round_3_outcome: 2 of 6 items failed (test 3 gap-4 dark-mode persists; test 4 gap-5 left-align landed but exposed gap-6 layout bug)
new_gaps_surfaced:
  - gap-6  # procurement layout: ETA cells not horizontally aligned across rows + Item column too narrow
  - gap-7  # Outlook (Mac) desktop dark-mode persists despite 46.1-04 lock
---

## Current Test

[round-3 complete 2026-04-29; 2 of 6 items failed; routing to /gsd-plan-phase 46.1 --gaps for round-3 fix plans]

## Tests

### 1. Round-1 carry-over — gap-1 greeting strip regression

expected: Single "Hi Victoria," greeting at the top of the SendUpdate render; the personalNote body follows without repeating the greeting line.
result: passed (round-3 web-preview screenshot 2026-04-29 shows single "Hi Victoria," only)

### 2. Round-1 carry-over — gap-2 empty-ETA regression

expected: ETA cell for a procurement row with both `installDate` and `expectedDeliveryDate` null renders blank — no `Invalid Date` text visible.
result: passed (round-3 web-preview shows row 3 "Italian Pendant Lights" PENDING ORDER pill with no `Invalid Date` text in ETA position)

### 3. Round-2 — gap-4 Outlook desktop dark-mode lock

expected: Send Update AND Work Order both render the locked light palette in Outlook desktop dark mode — body bg cream `#FAF8F5`, text colors stone/dark/mid as authored; no half-inversion (dark bg + still-light text).
result: FAILED — Outlook desktop (Mac, "New Outlook" UX) still renders the half-inverted state: dark gray body bg + cream-on-dark text. The `[data-ogsc]`/`[data-ogsb]`/MSO lock from 46.1-04 (commit `f867da6`) appears to not be honored by New Outlook for Mac. See parent UAT gap-7 for new fix path.

### 4. Round-2 — gap-5 left-aligned table cells

expected: Milestone dates render left-aligned immediately following the milestone pill (not flushed to the right edge). Procurement Status pills and ETA cells also left-aligned in the same render.
result: PARTIAL PASS — left-alignment landed correctly per D-7 (Milestones dates and Procurement Status/ETA all left-aligned). But the alignment fix exposed an underlying layout problem: Procurement ETA values are not horizontally aligned across rows because Item column is too narrow and rows wrap to uneven heights. Visual zig-zag of ETA values is the new finding. See parent UAT gap-6 for new fix path.

### 5. Regression check — `Items NOT issues` set (minus now-superseded #1)

expected: Work Order render, terracotta CTA, formal signoff, reply line, milestone pills with strikethrough, and vendor sub-lines all render identically to round-1 with no regressions from gap-4 + gap-5 fixes.
result: passed (visible in round-3 light-mode web preview — terracotta OPEN PORTAL CTA, "You can reply to this email directly." line, COMPLETE strikethrough on Initial Consultation + Concept Presentation, vendor sub-lines on Custom Sectional Sofa + Handknotted Rug all render correctly)

### 6. Round-3 close-out reply

expected: Liz replies `approved` on parent plan 46-03 once gap-1..gap-5 visual checks all pass; closes parent Phase 46.
result: blocked (cannot close — gap-6 + gap-7 surfaced; round-4 needed after 46.1 round-3 plan lands)

## Summary

total: 6
passed: 3       # tests 1, 2, 5
partial: 1      # test 4 — fix landed correctly but exposed gap-6
issues: 1       # test 3 — gap-4 fix insufficient, surfaced gap-7
pending: 0
skipped: 0
blocked: 1      # test 6 — gated on 46.1 round-3

## Gaps

- **gap-6** (parent UAT 46-UAT.md) — Procurement table layout: ETA cells not horizontally aligned across rows + Item column too narrow. Surfaced by test 4.
- **gap-7** (parent UAT 46-UAT.md) — Outlook (Mac) desktop dark-mode contrast inversion persists despite [data-ogsc]/[data-ogsb] lock. Surfaced by test 3.
