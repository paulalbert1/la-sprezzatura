---
phase: 46.1-merge-gate-gap-closure
plan: 09
type: execute
wave: 4
depends_on: []
files_modified:
  - src/emails/shell/EmailShell.tsx
  - src/emails/shell/EmailShell.test.ts
  - src/emails/shell/EmailButton.tsx
  - src/emails/sendUpdate/Procurement.tsx
  - src/emails/sendUpdate/Body.tsx
  - src/emails/sendUpdate/Body.test.ts
  - src/emails/sendUpdate/SendUpdate.test.ts
  - src/emails/sendUpdate/__snapshots__/SendUpdate.test.ts.snap
  - src/lib/email/personalNoteMarkdown.ts
  - src/components/admin/SendUpdateModal.tsx
autonomous: true
requirements: []
requirements_addressed:
  - "CR-R3-01 (round-3 code review BLOCKER -- 46.1-REVIEW.md)"
  - "WR-01 / WR-02 (round-2 carryover -- stripLeadingGreeting tightening)"
  - "WR-03 (round-2 carryover -- 600-char enforcement in admin compose UI)"
  - "WR-04 (round-2 carryover -- [data-ogsc] * catch-all deletion)"
  - "WR-05 (round-2 carryover -- MSO conditional moved out of <div> in <head>)"
  - "WR-06 (round-2 carryover -- pill borderRadius reconciled with Phase 46 D-3)"
  - "WR-R3-02 / WR-R3-04 / IN-R3-02 (round-3 carryovers -- test/CSS-gen reshape)"
  - "D-15 (className strings on pill <span> + EmailButton CTA)"
  - "D-16 (delete dead [data-ogsc] .text-* + .border-cream-dark rules)"
  - "D-17 (delete [data-ogsc] * { color: inherit !important } catch-all)"
  - "D-18 (className-match assertions + invariant valign:top floor + DRY CSS-gen)"
  - "D-19 (round-2 WR carryovers folded -- WR-01..06)"
  - "D-20 (single atomic commit; all 10 files diff together)"
  - "D-21 (round-5 re-trigger gate)"
parent_uat: .planning/phases/46-send-update-work-order-migration/46-UAT.md
parent_uat_gap: "round-4 BLOCKER + round-2 WR carryovers"
must_haves:
  truths:
    - "Procurement pill rendered HTML carries class=\"pill-${status}\" alongside the existing inline style (D-15)"
    - "EmailButton CTA rendered HTML carries class=\"cta-${variant}\" alongside the existing inline style (D-15)"
    - "EmailShell.tsx no longer contains [data-ogsc] .text-* per-class rules (D-16; rules deleted because Tailwind utilities never bind)"
    - "EmailShell.tsx no longer contains [data-ogsc] .border-cream-dark rule (D-16)"
    - "EmailShell.tsx no longer contains the [data-ogsc] * { color: inherit !important } catch-all (D-17)"
    - "Procurement.tsx pillStyle() no longer sets borderRadius (WR-06; pill renders flat-edge rectangle)"
    - "personalNoteMarkdown.ts exports MAX_LEN as a named export (WR-03)"
    - "SendUpdateModal.tsx personalNote textarea has maxLength={MAX_LEN} attribute and a {personalNote.length} / {MAX_LEN} character counter (WR-03)"
    - "stripLeadingGreeting takes a clientFirstName parameter and only strips when the captured word matches (case-insensitive) (WR-01/02)"
    - "EmailShell.tsx generates the [data-ogsc] pill rules + @media pill rules at module-load time from STATUS_PILL_STYLES (D-18 IN-R3-02)"
    - "EmailShell.tsx no longer injects the MSO conditional via <div style={display:none}> in <head> (WR-05); MSO payload lives in a sibling <style dangerouslySetInnerHTML> instead"
    - "EmailShell.test.ts has className-binding assertions for each procurement status pill (one per status) and each CTA variant (D-18 WR-R3-02)"
    - "SendUpdate.test.ts:506-527 valign:top floor is invariant-shaped (per-row count, not magic number 9) (D-18 WR-R3-04)"
    - "Body.test.ts covers hyphenated firstName, 'Hi all,' negative case (clientFirstName=Sarah), case-insensitive match (D-19 WR-01/02)"
    - "All 19 existing 46.1-04 lock-marker assertions either pass unchanged or are rewritten to className-binding shape per D-18 #1 -- the gap-4 contract is preserved by behavior even when the assertion shape changes"
    - "Existing 4 D-7 align tests (46.1-05) still pass; existing 6 D-12/D-13 column-width tests (46.1-07) still pass; existing 5 SendUpdate snapshot tests + plain-text snapshot still pass; WorkOrder snapshot tests still pass"
    - "npm test exits with green-or-baseline-only failures -- zero NEW failures introduced; baseline = the documented 13 from deferred-items.md D-pre-46 + D-pre-46-wo + WorkOrder snapshot date-drift"
    - "Single atomic commit lands all 10 files; commit message follows the round-1..round-3 convention (no AI attribution)"
  artifacts:
    - path: src/emails/sendUpdate/Procurement.tsx
      provides: "Procurement section with className-bound pill chrome (D-15) and pillStyle() with borderRadius removed (WR-06)"
      contains: "className={`pill-${"
    - path: src/emails/shell/EmailButton.tsx
      provides: "EmailButton CTA with className-bound variant (D-15)"
      contains: "className={`cta-${"
    - path: src/emails/shell/EmailShell.tsx
      provides: "Email shell with deleted dead class rules (D-16/D-17), MSO moved out of div-in-head (WR-05), and module-load-time CSS generation from STATUS_PILL_STYLES (D-18 IN-R3-02)"
      contains: "buildPillRules"
    - path: src/emails/shell/EmailShell.test.ts
      provides: "Test suite rewritten to className-binding shape for pill + CTA bindings (D-18 WR-R3-02); existing gap-4 / gap-7 lock markers asserted via the new shape"
      contains: "pill-ordered"
    - path: src/lib/email/personalNoteMarkdown.ts
      provides: "Constrained markdown serializer with MAX_LEN exported as named export (WR-03)"
      contains: "export const MAX_LEN"
    - path: src/components/admin/SendUpdateModal.tsx
      provides: "Send Update compose UI with maxLength={MAX_LEN} on personalNote textarea + character counter (WR-03)"
      contains: "maxLength={"
    - path: src/emails/sendUpdate/Body.tsx
      provides: "stripLeadingGreeting tightened to exact firstName match (WR-01/02)"
      contains: "clientFirstName"
    - path: src/emails/sendUpdate/Body.test.ts
      provides: "Body tests covering hyphenated firstName + 'Hi all,' negative + case-insensitive match (WR-01/02)"
      contains: "Mary-Anne"
    - path: src/emails/sendUpdate/SendUpdate.test.ts
      provides: "valign:top floor rewritten to invariant-shaped per-row count (D-18 WR-R3-04)"
      contains: "bodyRowCount"
    - path: src/emails/sendUpdate/__snapshots__/SendUpdate.test.ts.snap
      provides: "Regenerated snapshot reflecting className additions on pill <span> + CTA + Procurement.tsx borderRadius removal + EmailShell.tsx CSS-generation refactor + MSO conditional relocation"
      contains: "class=\"pill-"
  key_links:
    - from: src/emails/shell/EmailShell.tsx
      to: src/lib/procurement/statusPills.ts
      via: "buildPillRules() imports STATUS_PILL_STYLES and generates [data-ogsc]/@media pill rules at module-load time"
      pattern: "import .*STATUS_PILL_STYLES"
    - from: src/components/admin/SendUpdateModal.tsx
      to: src/lib/email/personalNoteMarkdown.ts
      via: "imports MAX_LEN to set textarea maxLength + counter"
      pattern: "import .*MAX_LEN.*personalNoteMarkdown"
    - from: src/emails/sendUpdate/Procurement.tsx
      to: src/lib/procurement/statusPills.ts
      via: "imports ProcurementStatus + STATUS_PILL_STYLES + STATUS_LABELS (existing); pill <span> now binds className={`pill-${it.status}`}"
      pattern: "className=\\{`pill-\\$\\{it\\.status\\}`\\}"
threat_model:
  trust_boundaries:
    - boundary: "email render -> recipient inbox"
      description: "Rendered HTML reaches an external mail client. (Pre-existing.)"
  notes:
    - "Tenant isolation: N/A -- UI/render only; no tenant-scoped data flows."
    - "Input validation: WR-03 maxLength + WR-01/02 firstName-match strip both REDUCE input-handling risk."
    - "Auth/AuthZ: N/A."
    - "XSS / injection: className strings in D-15 are derived from typed enums (ProcurementStatus from statusPills.ts; \"terracotta\" | \"gold\" literal union from EmailButton.tsx) -- no user-controlled string flows into className. Safe."
    - "No new secrets, no new env vars, no new external deps."
  threats:
    - id: T-46.1-09-01
      category: T
      component: "EmailShell.tsx existing lock surface (gap-4 + gap-7 markers)"
      disposition: mitigate
      mitigation: "Acceptance criteria assert each lock marker survives the rewrite via grep. The gap-4 + gap-7 contracts are preserved by behavior; the assertion shape changes (D-18 #1) but the behavioral guarantees do not."
    - id: T-46.1-09-02
      category: T
      component: "Snapshot regen scope -- accidental body content drift"
      disposition: mitigate
      mitigation: "Acceptance criteria require the SendUpdate snapshot diff to include exactly: (a) class=\"pill-${status}\" additions on procurement <span>s; (b) class=\"cta-${variant}\" addition on the CTA Button; (c) <head> rewrite to module-load-time CSS-gen output; (d) MSO conditional relocation. No body prose changes (Greeting/Body/Milestones/ReviewItems/footer are byte-identical)."
    - id: T-46.1-09-03
      category: I
      component: "Pill text/border colors in Outlook desktop dark mode"
      disposition: accept
      mitigation: "Round-5 Liz visual re-test on Outlook for Mac dark + Outlook for Windows dark + Outlook web dark is the human gate (D-21). If pill text inverts despite the className binding, fall through to a follow-up phase per D-16 knock-on note (Option C data-attribute hooks). Documented as known risk in CONTEXT.md D-16, not a blocker for round-4 ship."
    - id: T-46.1-09-04
      category: T
      component: "react-email <Button> potentially stripping className"
      disposition: mitigate
      mitigation: "Task 1 verification gate (D-15) re-renders the snapshot after the className edit and greps for `class=\"cta-` substrings. If matches == 0, the executor STOPs before continuing to D-16 and surfaces a planner-time pivot. The verification snapshot grep is a HARD gate at task-1 acceptance, not deferred."
    - id: T-46.1-09-05
      category: E
      component: "CI cost / new external deps"
      disposition: mitigate
      mitigation: "Acceptance criteria check `git diff -- package.json package-lock.json` for any new runtime dep additions. Q8 lock from prior rounds (no Litmus / Email-on-Acid / Playwright dark-mode emulation) is structurally enforced -- no new deps expected. The CSS-generation refactor is in-process module-load-time; no new build step."
---

<objective>
Close round-4 of phase 46.1: the **CR-R3-01 BLOCKER** from the round-3 code review (46.1-REVIEW.md round-3 section, commit `a59cf25`) plus the **6 round-2 WR carryovers** (WR-01..06) plus 3 round-3 quality items (WR-R3-02 / WR-R3-04 / IN-R3-02) -- all in a single atomic commit per D-20.

**The defect (CR-R3-01):** the Phase 46.1-04 dark-mode lock palette in `src/emails/shell/EmailShell.tsx` targets className hooks (`pill-*`, `cta-*`, `text-stone`, `text-charcoal`, `border-cream-dark`, etc.) that **never appear on rendered elements**. Tailwind compiles utility classes (`bg-cream`, `text-stone`, `border-cream-dark`) into inline styles and strips the className from the rendered HTML. The literal pill class hooks (`pill-scheduled`, `pill-ordered`, etc.) are not applied as classNames anywhere in source code -- `Procurement.tsx:118` returns `<span style={pillStyle(it.status)}>` with no `className`; `EmailButton.tsx:38-58` returns `<Button>` with inline `style` only. Verified: `grep -cE 'class="[^"]*(pill-|cta-)' src/emails/sendUpdate/__snapshots__/SendUpdate.test.ts.snap` returns 0. The body-bg paint via `<table bgcolor>` + inline `<body style>` does work; everything else (pill chrome, CTA bg, text colors) falls to Outlook auto-darken in Mac/Windows dark mode -- the original gap-4 contrast inversion is unfixed.

**The fix (D-15..D-20):** apply explicit literal className strings (`pill-${status}` and `cta-${variant}`) -- not Tailwind utility classes -- to the pill `<span>` and CTA `<Button>` so the per-class lock rules in `EmailShell.tsx` actually bind. Delete dead `[data-ogsc] .text-*` and `.border-cream-dark` rules (Tailwind utilities; never bind). Delete the `[data-ogsc] * { color: inherit !important }` catch-all (it actively cancels every inline color in dark mode). Refactor the hand-mirrored `[data-ogsc]` + `@media` palette into module-load-time CSS generation from `STATUS_PILL_STYLES` (single source of truth; eliminates ~50 lines of mirrored CSS). Rewrite `EmailShell.test.ts` hex-substring assertions to className-binding shape (the existing tests pass on dead rules; rewriting them to class-bound assertions is the regression guard the next round needs). Rewrite `SendUpdate.test.ts:506-527` valign:top floor from magic-number-9 to per-row count (invariant-shaped). Fold all six round-2 WR carryovers (tighter `stripLeadingGreeting`, exported `MAX_LEN` + `maxLength` enforcement, MSO conditional moved out of `<div>`-in-`<head>`, pill `borderRadius` deleted). Single atomic commit per D-20. Round-5 code review then faces zero outstanding findings before Liz's round-5 visual re-test on Outlook for Mac dark + Windows dark + web dark (D-21).

**Hard constraints:**

1. **CONTEXT.md is locked.** Every locked decision (D-15..D-21) translates to a specific edit. The plan does NOT re-decide any of them. If the executor finds an edge case that conflicts with CONTEXT.md, STOP and surface to Paul -- do not improvise.

2. **D-15 verification gate is HARD.** After applying the className edits in Task 1 and regenerating the snapshot, grep for `class="pill-` and `class="cta-` substrings. If grep returns 0 matches, react-email's `<Button>` is stripping the className -- STOP and surface as a planner-time pivot (CONTEXT.md D-15 verification gate). Continuing to D-16/D-17 deletion BEFORE D-15 binding is verified is the failure mode that ships round-3-redux for round-5.

3. **Round-2 baseline failure count.** `npm test` baseline is 62 failures (per CONTEXT.md D-18 "tests preserved" -- the documented 13 from `deferred-items.md` D-pre-46 + D-pre-46-wo + WorkOrder snapshot date-drift, expanded by the round-3 plans' new tests). Final test gate: zero NEW failures vs the round-3 baseline at commit `9f9cd05`.

4. **Atomic commit.** All 10 files in `files_modified` ship in a single commit per D-20. The snapshot is regenerated EXACTLY ONCE at the end of Task 5 -- intermediate snapshot regenerations during Task 1's verification gate are local-only (do not commit). The single final regen agrees with all of D-15+D-16+D-17+D-18+D-19 simultaneously.

Output: 9 source-file edits + 1 snapshot regen + 1 atomic commit + 1 SUMMARY.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/PLAN-AUTHORING-PATTERNS.md
@.planning/phases/46.1-merge-gate-gap-closure/46.1-CONTEXT.md
@.planning/phases/46.1-merge-gate-gap-closure/46.1-REVIEW.md
@.planning/phases/46.1-merge-gate-gap-closure/46.1-SPIKE-OUTLOOK-MAC.md
@.planning/phases/46.1-merge-gate-gap-closure/46.1-04-PLAN.md
@.planning/phases/46.1-merge-gate-gap-closure/46.1-08-PLAN.md
@.planning/phases/46-send-update-work-order-migration/46-UAT.md
@src/emails/shell/EmailShell.tsx
@src/emails/shell/EmailShell.test.ts
@src/emails/shell/EmailButton.tsx
@src/emails/sendUpdate/Procurement.tsx
@src/emails/sendUpdate/Body.tsx
@src/emails/sendUpdate/Body.test.ts
@src/emails/sendUpdate/SendUpdate.test.ts
@src/emails/_theme.ts
@src/lib/procurement/statusPills.ts
@src/lib/email/personalNoteMarkdown.ts
@src/components/admin/SendUpdateModal.tsx

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase at plan-time. -->

From src/lib/procurement/statusPills.ts:
```ts
export type ProcurementStatus =
  | "scheduled"
  | "warehouse"
  | "ordered"
  | "in-transit"
  | "pending"
  | "delivered"
  | "installed";

export const PROCUREMENT_STATUSES: ProcurementStatus[] = [...];

export interface PillStyle {
  bg: string;
  text: string;
  border: string;
}

export const STATUS_PILL_STYLES: Record<ProcurementStatus, PillStyle> = {
  scheduled:    { bg: "#F3EFE9", text: "#6B5E52", border: "#E0D5C5" },
  warehouse:    { bg: "#F3EDE3", text: "#6B5E52", border: "#D4C8B8" },
  "in-transit": { bg: "#FBF2E2", text: "#8A5E1A", border: "#E8CFA0" },
  ordered:      { bg: "#E8F0F9", text: "#2A5485", border: "#B0CAE8" },
  pending:      { bg: "#FDEEE6", text: "#9B3A2A", border: "#F2C9B8" },
  delivered:    { bg: "#EDF5E8", text: "#3A6620", border: "#C4DBA8" },
  installed:    { bg: "#EDF5E8", text: "#3A6620", border: "#A8C98C" },
};

export const STATUS_LABELS: Record<ProcurementStatus, string> = { ... };
```

From src/emails/shell/EmailButton.tsx:
```ts
const VARIANT_COLORS = {
  gold:       { bg: "#9A7B4B", fg: "#FAF7F2" },   // WorkOrder default
  terracotta: { bg: "#C4836A", fg: "#FAF8F5" },   // SendUpdate (46-04 D-19)
} as const;

export type EmailButtonVariant = keyof typeof VARIANT_COLORS;
// = "gold" | "terracotta"
```

From src/emails/sendUpdate/Procurement.tsx (line 118 -- the pill <span>):
```tsx
<Column width="22%" align="left" style={{ verticalAlign: "top" }}>
  <span style={pillStyle(it.status)}>{STATUS_LABELS[it.status]}</span>
</Column>
```

From src/emails/sendUpdate/Procurement.tsx (line 90 -- the WR-06 target):
```tsx
function pillStyle(status: ProcurementStatus): CSSProperties {
  const palette = STATUS_PILL_STYLES[status];
  return {
    display: "inline-block",
    backgroundColor: palette.bg,
    color: palette.text,
    border: `0.5px solid ${palette.border}`,
    padding: "3px 10px",
    borderRadius: "2px",   // longhand only -- Phase 46 D-3   <-- DELETE per WR-06 (line 90)
    fontSize: 11,
    fontWeight: 600,
    textTransform: "uppercase",
    letterSpacing: "0.06em",
  };
}
```

From src/lib/email/personalNoteMarkdown.ts (line 23 -- the WR-03 target):
```ts
const MAX_LEN = 600;            // <-- promote to `export const MAX_LEN = 600`
```

From src/components/admin/SendUpdateModal.tsx (lines 451-484 -- the WR-03 + helper-text target):
```tsx
<textarea
  id="send-update-note"
  rows={4}
  className="luxury-input w-full"
  placeholder="Hi, here's the latest on your project..."
  value={personalNote}
  onChange={(e) => setPersonalNote(e.target.value)}
  disabled={sending}
  style={{ resize: "vertical", fontFamily: "var(--font-sans)" }}
/>
<p style={{ margin: "6px 0 0", fontSize: "11px", color: "#9E8E80", fontFamily: "var(--font-sans)" }}>
  Appears at the top of the email, above the status sections.
</p>
<p style={{ margin: "4px 0 0", fontSize: "11px", color: "#9E8E80", fontStyle: "italic", fontFamily: "var(--font-sans)" }}>
  Don't include &quot;Hi {"{firstName}"},&quot; — it's added automatically.
</p>
```

From src/emails/sendUpdate/Body.tsx (lines 23-27 -- the WR-01/02 target):
```ts
const LEADING_GREETING_RE = /^Hi\s+\w+[,!]?\s*\n+/;

export function stripLeadingGreeting(personalNote: string): string {
  return personalNote.replace(LEADING_GREETING_RE, "");
}
```

Body.tsx callsite (line 35):
```tsx
const stripped = stripLeadingGreeting(personalNote);
```

The current Body.tsx receives only `personalNote` as a prop. Plan adopts: widen Body's props to add `clientFirstName: string` (required), pass through to `stripLeadingGreeting`. SendUpdate.tsx composition root has `clientFirstName` in scope (existing SendUpdate prop; baseInput fixture in Body.test.ts already supplies it via line 103: `clientFirstName: "Victoria"`).

From src/emails/sendUpdate/Body.test.ts:
- 12 existing `it()` blocks in `describe("stripLeadingGreeting (46.1 D-1 -- gap-1 fix)")`
- 1 integration test in `describe("Body integration: double-greeting fix (gap-1)")` (line 96-110) -- already passes `clientFirstName: "Victoria"` via `baseInput()`; no test-body change needed beyond verifying it still passes after Body widens its prop interface.
- All 12 stripLeadingGreeting tests need a `clientFirstName` argument added. Each existing test's expected behavior under the new signature:
  - "strips 'Hi Victoria,'" -- pass `"Victoria"` -> still strips (firstName match)
  - "strips 'Hi Sarah!'" -- pass `"Sarah"` -> still strips (match)
  - "strips 'Hi Marco'" -- pass `"Marco"` -> still strips (match)
  - "does NOT strip 'hi victoria,'" -- pass `"Victoria"` -> still no strip (regex case-sensitive on "Hi" itself; lowercase "hi" never matches the regex regardless of firstName comparison)
  - "does NOT strip 'Hello/Dear/Hey Victoria,'" -- pass `"Victoria"` -> still no strip (greeting word mismatch)
  - "does NOT strip 'Hi everyone, this is the body'" -- pass any name -> still no strip (no trailing newline)
  - "does NOT strip mid-paragraph 'Hi Victoria,'" -- pass `"Victoria"` -> still no strip (anchored)
  - "strips ONLY the first leading occurrence" -- pass `"Victoria"` -> still strips first occurrence only
  - "returns empty string unchanged" / "whitespace-only unchanged" -- pass `""` (no firstName needed for these inputs)

NEW tests added per CONTEXT.md D-19 #1:
  - "strips 'Hi Mary-Anne,' when clientFirstName='Mary-Anne' (hyphenated firstName, exact match)" -- positive
  - "does NOT strip 'Hi all,' when clientFirstName='Sarah' (intentional generic salutation preserved; WR-02 fix)" -- negative
  - "strips 'Hi sarah,' when clientFirstName='Sarah' (case-insensitive firstName match)" -- positive (note: greeting word stays "Hi" capital; only firstName is lowered)
  - "does NOT strip 'Hi Victoria,' when clientFirstName='Sarah' (different firstName, mismatch; WR-02 fix's other direction)" -- negative

Existing 46.1-04 lock surface in EmailShell.tsx (verified at plan-time, lines 69-184):
  - 7 [data-ogsb] .pill-* { background-color: ... !important } rules  -- KEEP, generated via buildPillRules() per D-18 IN-R3-02
  - 5 distinct [data-ogsc] .pill-* { color: ... !important } rules    -- KEEP, generated via buildPillRules()
  - 7 [data-ogsc] .pill-* { border-color: ... !important } rules      -- KEEP, generated via buildPillRules()
  - 2 [data-ogsb] .cta-* { background-color: ... !important } rules   -- KEEP (terracotta + gold; class hooks now bind via D-15)
  - 1 [data-ogsb] body, [data-ogsb] .bg-cream { background-color: ... } rule  -- KEEP (body element selector binds; .bg-cream tail dead but cheap)
  - 8 [data-ogsc] .text-* + .border-cream-dark { ... } rules  -- DELETE per D-16
  - 1 [data-ogsc] * { color: inherit !important } catch-all   -- DELETE per D-17
  - @media (prefers-color-scheme: dark) block (~30 rules from 46.1-08)  -- KEEP, but pill rules generated via buildPillRules(""); .text-* / .border-cream-dark inside @media also DELETED per D-16
  - <meta name="color-scheme" content="only light"> + <meta name="supported-color-schemes" content="light">  -- KEEP
  - <table bgcolor="#FAF8F5"> body wrapper + inline <Body style={{backgroundColor: "#FAF8F5"}}>  -- KEEP (gap-7 working markers)
  - MSO conditional via <div style={{display:none}} dangerouslySetInnerHTML>  -- DELETE per WR-05; replace with sibling <style dangerouslySetInnerHTML={{__html: '<!--[if mso]>body { mso-color-scheme: light !important; }<![endif]-->'}}/>
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: D-15 -- Bind explicit literal class names on pill &lt;span&gt; + CTA &lt;Button&gt;; verify with snapshot regen + grep gate</name>
  <files>
    - src/emails/sendUpdate/Procurement.tsx
    - src/emails/shell/EmailButton.tsx
  </files>
  <read_first>
    - src/emails/sendUpdate/Procurement.tsx (full file -- the pill <span> at line 118; the pillStyle() function at lines 82-96; STATUS_PILL_STYLES + STATUS_LABELS imports at line 15)
    - src/emails/shell/EmailButton.tsx (full file -- the <Button> at lines 38-58; VARIANT_COLORS at lines 22-25; EmailButtonVariant type at line 27)
    - src/lib/procurement/statusPills.ts (the ProcurementStatus enum + STATUS_PILL_STYLES record -- the values className strings derive from)
    - .planning/phases/46.1-merge-gate-gap-closure/46.1-CONTEXT.md `## Round 4` section, specifically D-15 (the locked verification gate text)
    - src/emails/sendUpdate/__snapshots__/SendUpdate.test.ts.snap (current state -- baseline grep counts: `grep -cE 'class="[^"]*pill-' SendUpdate.test.ts.snap` should currently return 0; after this task it should return >= 3)
  </read_first>
  <action>

**Step 1 -- Edit `src/emails/sendUpdate/Procurement.tsx`.**

Pill `<span>` is at line 118. Current source:

```tsx
<Column width="22%" align="left" style={{ verticalAlign: "top" }}>
  <span style={pillStyle(it.status)}>{STATUS_LABELS[it.status]}</span>
</Column>
```

Change to add `className` ALONGSIDE existing `style` (do NOT remove the inline style; both are needed -- inline style paints default light-mode color, className binds the dark-mode lock rule):

```tsx
<Column width="22%" align="left" style={{ verticalAlign: "top" }}>
  <span className={`pill-${it.status}`} style={pillStyle(it.status)}>{STATUS_LABELS[it.status]}</span>
</Column>
```

`it.status` is typed `ProcurementStatus = "scheduled" | "warehouse" | "ordered" | "in-transit" | "pending" | "delivered" | "installed"` -- resulting className is one of exactly seven literal values. NOT a Tailwind utility (no matching utility pattern), so the `<Tailwind>` compiler leaves it intact.

**Step 2 -- Edit `src/emails/shell/EmailButton.tsx`.**

Add `className={`cta-${variant}`}` to the `<Button>` element (lines 38-58) alongside `href` and `style`:

```tsx
export function EmailButton({ href, variant = "gold", children }: EmailButtonProps) {
  const colors = VARIANT_COLORS[variant];
  return (
    <Button
      href={href}
      className={`cta-${variant}`}
      style={{
        display: "inline-block",
        backgroundColor: colors.bg,
        color: colors.fg,
        // ...remaining inline style props unchanged...
      }}
    >
      {children}
    </Button>
  );
}
```

`variant` is `EmailButtonVariant = "gold" | "terracotta"` -- className is exactly `cta-gold` or `cta-terracotta`.

**Step 3 -- HARD verification gate (CONTEXT.md D-15).**

Regenerate the SendUpdate snapshot LOCALLY:

```bash
npx vitest run src/emails/sendUpdate/SendUpdate.test.ts -u 2>&1 | tail -10
```

Then grep:

```bash
PILL_HITS=$(grep -cE 'class="[^"]*pill-' src/emails/sendUpdate/__snapshots__/SendUpdate.test.ts.snap)
CTA_HITS=$(grep -cE 'class="[^"]*cta-' src/emails/sendUpdate/__snapshots__/SendUpdate.test.ts.snap)
echo "PILL_HITS=$PILL_HITS  CTA_HITS=$CTA_HITS"
```

**Expected:** `PILL_HITS >= 3` (FIXTURES.full() has 3 procurement rows); `CTA_HITS >= 1`.

**If either count is 0, STOP IMMEDIATELY.** react-email's `<Button>` (or the Tailwind compiler) is stripping the className -- D-15 strategy needs a planner-time pivot. Surface to Paul with the exact grep output and the rendered snapshot fragment; do NOT proceed to Task 2.

If both counts are non-zero, proceed.

**Step 4 -- Do not commit yet.** Tasks 2-4 will produce additional snapshot deltas. Task 5 does the single final regen + atomic commit.

  </action>
  <verify>
    <automated>npx vitest run src/emails/sendUpdate/SendUpdate.test.ts -u 2>&1 | tail -5; grep -cE 'class="[^"]*(pill-|cta-)' src/emails/sendUpdate/__snapshots__/SendUpdate.test.ts.snap</automated>
  </verify>
  <acceptance_criteria>
    - Procurement.tsx pill `<span>` now binds `className={`pill-${it.status}`}`:
      `grep -cF 'className={`pill-${it.status}`}' src/emails/sendUpdate/Procurement.tsx` returns `1`
    - EmailButton.tsx Button now binds `className={`cta-${variant}`}`:
      `grep -cF 'className={`cta-${variant}`}' src/emails/shell/EmailButton.tsx` returns `1`
    - Existing inline style on Procurement.tsx pill `<span>` preserved:
      `grep -cF 'style={pillStyle(it.status)}' src/emails/sendUpdate/Procurement.tsx` returns `1`
    - Existing inline style block on EmailButton.tsx Button preserved (`backgroundColor: colors.bg` line still present):
      `grep -cF 'backgroundColor: colors.bg' src/emails/shell/EmailButton.tsx` returns `1`
    - **D-15 verification gate (HARD):** locally-regenerated SendUpdate snapshot now contains `class="pill-*"` substrings:
      `grep -cE 'class="[^"]*pill-' src/emails/sendUpdate/__snapshots__/SendUpdate.test.ts.snap` returns at least `3`
    - **D-15 verification gate (HARD):** locally-regenerated SendUpdate snapshot now contains `class="cta-*"` substring:
      `grep -cE 'class="[^"]*cta-' src/emails/sendUpdate/__snapshots__/SendUpdate.test.ts.snap` returns at least `1`
    - No commit yet -- working tree dirty:
      `git status --porcelain | grep -cE '^.M (src/emails/sendUpdate/Procurement\.tsx|src/emails/shell/EmailButton\.tsx|src/emails/sendUpdate/__snapshots__/SendUpdate\.test\.ts\.snap)'` returns `3`
  </acceptance_criteria>
  <done>
    Procurement.tsx pill <span> binds className={`pill-${it.status}`} alongside its existing inline style; EmailButton.tsx Button binds className={`cta-${variant}`} alongside its existing inline style; locally-regenerated SendUpdate snapshot contains both class="pill-*" (>= 3) and class="cta-*" (>= 1) substrings. The hard CONTEXT.md D-15 verification gate has passed -- react-email is preserving the literal class strings through compilation. Working tree dirty with 3 modified files; no commit yet (Task 5 does the atomic commit). Ready to proceed to Task 2.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: D-16 + D-17 + WR-05 -- Delete dead [data-ogsc] .text-* / .border-cream-dark rules; delete [data-ogsc] * catch-all; relocate MSO conditional out of &lt;div&gt;-in-&lt;head&gt;</name>
  <files>
    - src/emails/shell/EmailShell.tsx
  </files>
  <read_first>
    - src/emails/shell/EmailShell.tsx (full file -- the existing 46.1-04 lock surface at lines 69-184; the catch-all at line 119; the MSO conditional <div> at lines 178-183)
    - .planning/phases/46.1-merge-gate-gap-closure/46.1-CONTEXT.md (D-16 + D-17 + D-19 WR-05 locked text)
    - .planning/phases/46.1-merge-gate-gap-closure/46.1-REVIEW.md (round 3 CR-R3-01 + WR-04 + WR-05)
    - src/lib/procurement/statusPills.ts (still source of truth; the per-class .pill-* rules KEEP; only .text-* and .border-cream-dark go in D-16)
  </read_first>
  <action>

**Step 1 -- D-16: Delete the per-class [data-ogsc] .text-* and .border-cream-dark rules.**

In EmailShell.tsx `<style dangerouslySetInnerHTML>` block, delete these 6 rule-lines (currently lines ~100-106):

```css
[data-ogsc] .text-stone { color: #8A8478 !important; }
[data-ogsc] .text-stone-light { color: #B8AFA4 !important; }
[data-ogsc] .text-charcoal,
[data-ogsc] .text-dark { color: #2C2926 !important; }
[data-ogsc] .text-mid { color: #4A4540 !important; }
[data-ogsc] .text-cream { color: #FAF8F5 !important; }
[data-ogsc] .text-cream-ivory { color: #FAF7F2 !important; }
```

(6 lines, 7 rules because `.text-charcoal,\n[data-ogsc] .text-dark` share a body. All seven Tailwind text-utility selectors go away.)

ALSO delete the `.border-cream-dark` rule (line ~123):

```css
[data-ogsc] .border-cream-dark { border-color: #E8DDD0 !important; }
```

ALSO delete the `/* Text colors */` and `/* Procurement pill text ... */` and `/* Borders -- ... */` comment blocks where they introduce the deleted rules. Re-balance comments cleanly.

ALSO delete the parallel `.text-*` and `.border-cream-dark` rules inside the `@media (prefers-color-scheme: dark) { ... }` block (lines ~140-170). Mirror byte-identical from `[data-ogsc]` block per 46.1-08; ALL dead. Delete:

```css
.text-stone { color: #8A8478 !important; }
.text-stone-light { color: #B8AFA4 !important; }
.text-charcoal, .text-dark { color: #2C2926 !important; }
.text-mid { color: #4A4540 !important; }
.text-cream { color: #FAF8F5 !important; }
.text-cream-ivory { color: #FAF7F2 !important; }
.border-cream-dark { border-color: #E8DDD0 !important; }
```

**Keep:**
- `[data-ogsb] body, [data-ogsb] .bg-cream { background-color: #FAF8F5 !important; }` (body element selector binds; CONTEXT.md does not flag .bg-cream tail for deletion)
- `[data-ogsb] .cta-terracotta`, `[data-ogsb] .cta-gold` (now bind via Task 1)
- All 7 `[data-ogsb] .pill-*` background rules (now bind via Task 1)
- All 5 distinct `[data-ogsc] .pill-*` text rules (now bind via Task 1)
- All 7 `[data-ogsc] .pill-*` border rules (now bind via Task 1)
- The corresponding `@media` body/cta/pill rules
- The two `<meta>` tags
- The 46.1-08 `<table bgcolor>` wrapper + inline `<Body style={{backgroundColor:"#FAF8F5"}}>`

Task 3 will replace the surviving hand-written `.pill-*`/`.cta-*` rules with `buildPillRules()` output. For now, leave them as hand-written.

**Step 2 -- D-17: Delete the catch-all rule.**

Delete this line (currently line 119):

```css
[data-ogsc] * { color: inherit !important; }
```

Also delete the surrounding comment block (currently lines 115-118) framing it as "catch-all auto-darken cancellation". Per CONTEXT.md D-17, the catch-all CANCELS every inline color in dark mode -- harmful, not protective.

**Step 3 -- D-19 WR-05: Relocate the MSO conditional out of &lt;div&gt;-in-&lt;head&gt;.**

Current (lines 178-183):

```tsx
{/* MSO conditional -- older Outlook desktop builds where [data-ogs*] doesn't reach. */}
<div
  style={{ display: "none" }}
  dangerouslySetInnerHTML={{
    __html: `<!--[if mso]><style type="text/css">body { mso-color-scheme: light !important; }</style><![endif]-->`,
  }}
/>
```

`<div>` invalid in `<head>` per HTML5. Replace with sibling `<style>`:

```tsx
{/* MSO conditional -- older Outlook desktop builds. WR-05 (round-2 carryover):
    moved out of <div> wrapper (invalid in <head>) into a sibling <style>
    element whose payload is wrapped in the MSO conditional comment. The
    <!--[if mso]> + <![endif]--> + mso-color-scheme: light substrings still
    appear in the rendered HTML (test surface preserved). */}
<style
  dangerouslySetInnerHTML={{
    __html: `<!--[if mso]>body { mso-color-scheme: light !important; }<![endif]-->`,
  }}
/>
```

**Notes:**
- `mso-color-scheme: light` substring preserved (test line 60 still passes)
- `<!--[if mso]>` and `<![endif]-->` substrings preserved (tests lines 50, 53, 122-130 still pass)
- The inner `<style type="text/css">` wrapper inside the MSO conditional is dropped (we're already inside a `<style>` element). If the executor finds an existing test asserting `<style type="text/css">` substring (verified at task start: NOT in EmailShell.test.ts), removal is safe.

**Step 4 -- No commit; proceed to Task 3.**

  </action>
  <verify>
    <automated>grep -cE '\[data-ogsc\] \.text-(stone|charcoal|mid|cream|dark)|\[data-ogsc\] \.border-cream-dark|\[data-ogsc\] \*' src/emails/shell/EmailShell.tsx</automated>
  </verify>
  <acceptance_criteria>
    - All 7 [data-ogsc] .text-* rules deleted from EmailShell.tsx:
      `grep -cE '\[data-ogsc\] \.text-(stone|stone-light|charcoal|dark|mid|cream|cream-ivory)' src/emails/shell/EmailShell.tsx` returns `0`
    - .border-cream-dark rule under [data-ogsc] deleted:
      `grep -cE '\[data-ogsc\] \.border-cream-dark' src/emails/shell/EmailShell.tsx` returns `0`
    - Catch-all rule deleted:
      `grep -cE '\[data-ogsc\] \*' src/emails/shell/EmailShell.tsx` returns `0`
    - @media-block .text-* parallel rules also deleted (excluded comment lines + selectors that include [data-ogsc] are filtered):
      `grep -vE '\[data-ogsc\]' src/emails/shell/EmailShell.tsx | grep -cE '^\s+\.text-(stone|charcoal|mid|cream|dark)' | tr -d ' '` returns `0`
    - @media-block .border-cream-dark parallel rule also deleted:
      `grep -vE '\[data-ogsc\]' src/emails/shell/EmailShell.tsx | grep -cE '^\s+\.border-cream-dark' | tr -d ' '` returns `0`
    - The surviving 46.1-04 [data-ogsb] body/bg-cream rule preserved:
      `grep -cE '\[data-ogsb\] body, \[data-ogsb\] \.bg-cream' src/emails/shell/EmailShell.tsx` returns `1`
    - MSO conditional moved out of <div>-in-<head>; no top-level <div> in EmailShell JSX:
      `grep -cF '<div' src/emails/shell/EmailShell.tsx | tr -d ' '` returns `0`
    - MSO conditional payload preserved:
      `grep -cF '<!--[if mso]>' src/emails/shell/EmailShell.tsx` returns at least `1`
      `grep -cF '<![endif]-->' src/emails/shell/EmailShell.tsx` returns at least `1`
      `grep -cF 'mso-color-scheme: light' src/emails/shell/EmailShell.tsx` returns at least `1`
    - Existing 46.1-04 surviving lock infrastructure intact (selectors still present in the file, possibly via buildPillRules generation in Task 3 -- this acceptance is a floor enforced post-Task-2 BEFORE Task 3 generation runs; values are: 1 body/bg-cream rule + 2 cta rules + 7 pill bg rules + 7 pill border rules + 5 pill text rules):
      `grep -c 'data-ogsb' src/emails/shell/EmailShell.tsx` returns at least `9`
      `grep -c 'data-ogsc' src/emails/shell/EmailShell.tsx` returns at least `12`
    - No commit yet:
      `git diff --cached --name-only | wc -l | tr -d ' '` returns `0`
  </acceptance_criteria>
  <done>
    EmailShell.tsx has the 7 [data-ogsc] .text-* rules deleted, .border-cream-dark rule deleted, [data-ogsc] * catch-all deleted, the parallel rules inside @media block deleted to match, and the MSO conditional relocated from a <div> wrapper into a sibling <style> element. The body-element selector + .bg-cream + .cta-* + .pill-* rules survive intact (Task 3 will refactor them to module-load-time generation). Working tree dirty; no commit yet.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: D-18 + WR-R3-04 -- Refactor EmailShell.tsx to module-load-time CSS generation from STATUS_PILL_STYLES; rewrite EmailShell.test.ts hex-substring assertions to className-binding shape; rewrite SendUpdate.test.ts:506-527 valign:top floor + width-tests to invariant-shaped per-row count</name>
  <files>
    - src/emails/shell/EmailShell.tsx
    - src/emails/shell/EmailShell.test.ts
    - src/emails/sendUpdate/SendUpdate.test.ts
  </files>
  <read_first>
    - src/emails/shell/EmailShell.tsx (post-Task-2 state -- surviving .pill-*/.cta-* hand-written rules to refactor; imports at lines 27-40 to extend with STATUS_PILL_STYLES)
    - src/lib/procurement/statusPills.ts (STATUS_PILL_STYLES record)
    - src/emails/_theme.ts (no CTA token there; CTA hex literals stay inline -- buildPillRules handles pills only)
    - src/emails/shell/EmailShell.test.ts (full file -- the 19 existing lock-marker assertions)
    - src/emails/sendUpdate/SendUpdate.test.ts lines 470-545 (existing D-12/D-13 column-width and valign:top tests)
    - .planning/phases/46.1-merge-gate-gap-closure/46.1-CONTEXT.md D-18 (three coupled improvements; literal example code blocks)
  </read_first>
  <action>

**Step 1 -- D-18 #3: Refactor EmailShell.tsx CSS to module-load-time generation.**

CTA rules (only 2 variants) stay hand-written. Pill rules generate from `STATUS_PILL_STYLES`.

Add import at top of EmailShell.tsx:

```tsx
import { STATUS_PILL_STYLES } from "../../lib/procurement/statusPills";
```

Add module-level CSS generator above `EmailShell` component (after imports, before `export type SignoffStyle`):

```tsx
/**
 * Build a string of pill-class CSS rules from STATUS_PILL_STYLES.
 * Generates the [data-ogsc]/[data-ogsb] dark-mode lock palette + the
 * @media (prefers-color-scheme: dark) parallel block at module-load time
 * (no build step). Single source of truth across admin UI + email render +
 * dark-mode lock; drift is structurally impossible.
 *
 * 46.1 D-18 IN-R3-02 -- replaces ~40 lines of hand-mirrored CSS.
 */
function buildPillRules(prefix: string, scope: "bg" | "text" | "border" | "all"): string {
  const rules: string[] = [];
  for (const status of Object.keys(STATUS_PILL_STYLES) as Array<keyof typeof STATUS_PILL_STYLES>) {
    const p = STATUS_PILL_STYLES[status];
    const sel = prefix ? `${prefix} .pill-${status}` : `.pill-${status}`;
    const decls: string[] = [];
    if (scope === "bg" || scope === "all") decls.push(`background-color: ${p.bg} !important`);
    if (scope === "text" || scope === "all") decls.push(`color: ${p.text} !important`);
    if (scope === "border" || scope === "all") decls.push(`border-color: ${p.border} !important`);
    rules.push(`${sel} { ${decls.join("; ")}; }`);
  }
  return rules.join("\n              ");
}
```

Inside the component body (above `return (`):

```tsx
const ogscPillTextRules = buildPillRules("[data-ogsc]", "text");
const ogscPillBorderRules = buildPillRules("[data-ogsc]", "border");
const ogsbPillBgRules = buildPillRules("[data-ogsb]", "bg");
const mediaPillRules = buildPillRules("", "all");
```

Replace the surviving hand-written `.pill-*` rule blocks inside the `<style dangerouslySetInnerHTML>` payload with template-literal interpolation:

```tsx
<style
  dangerouslySetInnerHTML={{
    __html: `
      /* Body bg + bg-cream class hook (body element selector + dead .bg-cream tail) */
      [data-ogsb] body, [data-ogsb] .bg-cream { background-color: #FAF8F5 !important; }

      /* CTA backgrounds (hand-written -- only 2 variants) */
      [data-ogsb] .cta-terracotta { background-color: #C4836A !important; }
      [data-ogsb] .cta-gold { background-color: #9A7B4B !important; }

      /* Procurement pill backgrounds -- generated from STATUS_PILL_STYLES */
      ${ogsbPillBgRules}

      /* Procurement pill text -- generated from STATUS_PILL_STYLES */
      ${ogscPillTextRules}

      /* Procurement pill borders -- generated from STATUS_PILL_STYLES */
      ${ogscPillBorderRules}

      /* @media (prefers-color-scheme: dark) -- gap-7 (46.1 D-10/D-11) */
      @media (prefers-color-scheme: dark) {
        body, .bg-cream { background-color: #FAF8F5 !important; }
        .cta-terracotta { background-color: #C4836A !important; }
        .cta-gold { background-color: #9A7B4B !important; }
        ${mediaPillRules}
      }
    `,
  }}
/>
```

Verify post-edit:
- Rendered CSS contains all 7 status keys (`pill-scheduled`, `pill-warehouse`, `pill-ordered`, `pill-in-transit`, `pill-pending`, `pill-delivered`, `pill-installed`)
- Rendered CSS contains 7 `background-color`, 7 `color`, 7 `border-color` declarations under `[data-ogs*]` (in the three calls), and 7 of each under `@media`
- Each color value byte-identical to STATUS_PILL_STYLES (no parallel palette)

**Step 2 -- D-18 #1 (WR-R3-02): Rewrite EmailShell.test.ts hex-substring assertions to className-binding shape.**

Current EmailShell.test.ts patterns to rewrite:

1. Test on line 80-85 (`"all 7 procurement pill bg hex values appear in rendered HTML"`): currently iterates `REQUIRED_PILL_BG_HEXES` -- vacuous after CR-R3-01. Replace with one `it()` block PER status. The executor confirms `FIXTURES.full()` exercises all 7 statuses by reading `src/emails/sendUpdate/fixtures.ts` at task start; if only a subset is covered, scope assertions to that subset and document.

```ts
const PILL_PALETTE: Array<[ProcurementStatus, string]> = [
  ["scheduled",  "#F3EFE9"],
  ["warehouse",  "#F3EDE3"],
  ["in-transit", "#FBF2E2"],
  ["ordered",    "#E8F0F9"],
  ["pending",    "#FDEEE6"],
  ["delivered",  "#EDF5E8"],
  ["installed",  "#EDF5E8"],
];

// inside describe block
it.each(PILL_PALETTE)(
  "Procurement %s pill carries class=\"pill-%s\" colocated with inline background-color (D-18 WR-R3-02)",
  async (status, expectedHex) => {
    const html = await render(createElement(SendUpdate, SU_FIXTURES.full()));
    const escapedHex = expectedHex.replace(/[#]/g, "\\$&");
    const pillClassRe = new RegExp(`<span[^>]*class="[^"]*pill-${status}[^"]*"[^>]*style="[^"]*background-color:\\s*(?:${escapedHex}|rgb)`, "i");
    expect(html).toMatch(pillClassRe);
  },
);
```

If `it.each` generic-typing with `ProcurementStatus` is awkward, fall back to 7 explicit `it()` blocks (same shape; one per status).

CTA: replace test on line 101-108 (`"both CTA hex values..."`) with two explicit blocks (note: react-email Button renders as `<a>`, not `<span>`):

```ts
it("SendUpdate CTA carries class=\"cta-terracotta\" colocated with inline background-color #C4836A (D-18 WR-R3-02)", async () => {
  const html = await render(createElement(SendUpdate, SU_FIXTURES.full()));
  expect(html).toMatch(/<a[^>]*class="[^"]*cta-terracotta[^"]*"[^>]*style="[^"]*background-color:\s*(?:#C4836A|rgb\(\s*196\s*,\s*131\s*,\s*106)/i);
});

it("WorkOrder CTA carries class=\"cta-gold\" colocated with inline background-color #9A7B4B (D-18 WR-R3-02)", async () => {
  const html = await render(createElement(WorkOrder, buildWO()));
  expect(html).toMatch(/<a[^>]*class="[^"]*cta-gold[^"]*"[^>]*style="[^"]*background-color:\s*(?:#9A7B4B|rgb\(\s*154\s*,\s*123\s*,\s*75)/i);
});
```

Existing assertions that stay valid (no rewrite needed):
- `[data-ogsc]` selector present (line 38) -- still in generated CSS
- `[data-ogsb]` selector present (line 43) -- still in generated CSS
- MSO conditional open/close + mso-color-scheme (lines 48-61, 122-130) -- substrings still in rendered HTML post-WR-05
- `!important` substring (line 63) -- still present
- `#FAF8F5` inside [data-ogsb] window (line 68-78) -- body rule unchanged

DELETE from test file (because hex-substring shape is vacuous):
- `REQUIRED_PILL_BG_HEXES` constant + iterating test
- `REQUIRED_PILL_TEXT_HEXES` constant + iterating test (replace with per-status text-color colocation tests, mirror of pill bg blocks)
- `REQUIRED_PILL_BORDER_HEXES` constant + iterating test (replace with per-status border-color colocation tests, mirror of pill bg blocks)
- `REQUIRED_CTA_HEXES` constant + iterating test (replaced by 2 explicit cta blocks above)

WorkOrder mirror block: same shape (per-status pill colocation tests don't apply -- WO has no procurement; the WO mirror block focuses on CTA cta-gold colocation + the existing data-ogs* / MSO / cream-bg foundation tests).

The gap-7 (46.1-08 D-10/D-11) describe block at the END of the file (lines 185-256) is unchanged.

**Step 3 -- D-18 #2 (WR-R3-04): Rewrite SendUpdate.test.ts:506-527 valign:top floor + lines 487/494/502 width tests to invariant-shaped per-row count.**

Replace the body of the valign:top test at lines 506-527 (preserve the it() name with new `(... -- invariant per-row count, WR-R3-04)` suffix):

```ts
it("Procurement BODY rows compile verticalAlign:top to compiled HTML (D-12 -- invariant per-row count, WR-R3-04)", async () => {
  const html = await render(createElement(SendUpdate, FIXTURES.full()));
  const procStart = html.indexOf(">Procurement</p>");
  expect(procStart).toBeGreaterThan(-1);
  const procSlice = html.slice(procStart);
  // Body rows carry the border-bottom sentinel (per ROW_STYLE in Procurement.tsx).
  // Header tr does NOT carry it. Decoupled from fixture row count.
  const bodyRowCount = (procSlice.match(/<tr[^>]*style="[^"]*border-bottom:\s*0\.5px solid #E8DDD0/g) || []).length;
  expect(bodyRowCount).toBeGreaterThanOrEqual(3);
  const valignAttr = (procSlice.match(/<td[^>]*valign="top"/g) || []).length;
  const valignStyle = (procSlice.match(/<td[^>]*style="[^"]*vertical-align:\s*top/gi) || []).length;
  const valignHits = valignAttr + valignStyle;
  expect(valignHits).toBeGreaterThanOrEqual(bodyRowCount);
});
```

Apply same per-row-count pattern to the width tests (lines 487/494/502). Each currently `>= 4` floor (1 header + 3 body for FIXTURES.full(), exact match no headroom -- brittle). Refactor each:

```ts
it("Procurement section emits <td width=\"60%\"> on Item column for header + each body row (D-13 -- invariant per-row count, WR-R3-04)", async () => {
  const html = await render(createElement(SendUpdate, FIXTURES.full()));
  const procStart = html.indexOf(">Procurement</p>");
  const procSlice = html.slice(procStart);
  const bodyRowCount = (procSlice.match(/<tr[^>]*style="[^"]*border-bottom:\s*0\.5px solid #E8DDD0/g) || []).length;
  expect(bodyRowCount).toBeGreaterThanOrEqual(3);
  const widthMatches = (procSlice.match(/<td[^>]*width="60%"/g) || []).length;
  expect(widthMatches).toBeGreaterThanOrEqual(bodyRowCount + 1);
});
```

Repeat for `width="22%"` (Status column) and `width="18%"` (ETA column).

**Step 4 -- No commit yet; proceed to Task 4.**

  </action>
  <verify>
    <automated>npx vitest run src/emails/shell/EmailShell.test.ts --reporter=basic 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - EmailShell.tsx imports STATUS_PILL_STYLES:
      `grep -cE 'import.*STATUS_PILL_STYLES.*procurement/statusPills' src/emails/shell/EmailShell.tsx` returns at least `1`
    - EmailShell.tsx defines buildPillRules() function:
      `grep -cF 'function buildPillRules' src/emails/shell/EmailShell.tsx` returns `1`
    - buildPillRules() invoked at least 4 times (ogsc text + ogsc border + ogsb bg + media all):
      `grep -cF 'buildPillRules(' src/emails/shell/EmailShell.tsx` returns at least `4`
    - All 7 status keys are referenced at least once in EmailShell.test.ts (test code, comment-excluded):
      `grep -vE '^\s*(//|\*)' src/emails/shell/EmailShell.test.ts | grep -oE 'pill-(scheduled|warehouse|in-transit|ordered|pending|delivered|installed)' | sort -u | wc -l | tr -d ' '` returns `7`
    - EmailShell.test.ts CTA className-colocation assertions present (cta-terracotta + cta-gold):
      `grep -cF 'cta-terracotta' src/emails/shell/EmailShell.test.ts` returns at least `1`
      `grep -cF 'cta-gold' src/emails/shell/EmailShell.test.ts` returns at least `1`
    - Old vacuous hex-iterating constants deleted:
      `grep -cE '^const REQUIRED_PILL_(BG|TEXT|BORDER)_HEXES|^const REQUIRED_CTA_HEXES' src/emails/shell/EmailShell.test.ts` returns `0`
    - SendUpdate.test.ts valign:top test uses bodyRowCount variable:
      `grep -cF 'bodyRowCount' src/emails/sendUpdate/SendUpdate.test.ts` returns at least `1`
    - SendUpdate.test.ts width tests use bodyRowCount + 1 floor:
      `grep -cE 'bodyRowCount\s*\+\s*1' src/emails/sendUpdate/SendUpdate.test.ts` returns at least `3` (60% / 22% / 18% widths)
    - Old magic-number `>= 9` floor removed:
      `grep -cE 'toBeGreaterThanOrEqual\(9\)' src/emails/sendUpdate/SendUpdate.test.ts` returns `0`
    - The full EmailShell.test.ts gap-4 + gap-7 describe blocks pass (no FAIL markers):
      `npx vitest run src/emails/shell/EmailShell.test.ts --reporter=basic 2>&1 | grep -ciE 'failed|✘' | tr -d ' '` returns `0`
    - SendUpdate.test.ts D-12/D-13 + valign tests still pass post-rewrite:
      `npx vitest run src/emails/sendUpdate/SendUpdate.test.ts -t "Procurement column widths" --reporter=basic 2>&1 | grep -ciE 'fail' | tr -d ' '` returns `0`
    - No commit yet:
      `git diff --cached --name-only | wc -l | tr -d ' '` returns `0`
  </acceptance_criteria>
  <done>
    EmailShell.tsx imports STATUS_PILL_STYLES, defines buildPillRules() function, and invokes it 4 times (ogsc text + ogsc border + ogsb bg + media all) -- the hand-mirrored ~40 lines of pill CSS are gone. EmailShell.test.ts has per-status pill className-colocation assertions on SendUpdate render plus 2 CTA className-colocation assertions; old REQUIRED_*_HEXES constants are deleted. SendUpdate.test.ts:506-527 valign:top floor + the 3 width-floor tests are rewritten to invariant-shaped per-row count. EmailShell.test.ts + SendUpdate.test.ts column-width/valign tests pass green. Working tree dirty; no commit yet.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 4: D-19 -- Fold round-2 WR carryovers (WR-01/02 stripLeadingGreeting tightening, WR-03 MAX_LEN export + textarea enforcement, WR-06 pill borderRadius deletion + audit)</name>
  <files>
    - src/emails/sendUpdate/Body.tsx
    - src/emails/sendUpdate/Body.test.ts
    - src/emails/sendUpdate/SendUpdate.tsx
    - src/lib/email/personalNoteMarkdown.ts
    - src/components/admin/SendUpdateModal.tsx
    - src/emails/sendUpdate/Procurement.tsx
  </files>
  <read_first>
    - src/emails/sendUpdate/Body.tsx (full file -- LEADING_GREETING_RE at line 23, stripLeadingGreeting() lines 25-27, Body component lines 33-51)
    - src/emails/sendUpdate/Body.test.ts (full file -- 12 existing it() blocks + 1 integration test)
    - src/emails/sendUpdate/SendUpdate.tsx (composition root; <Body personalNote=...> callsite)
    - src/lib/email/personalNoteMarkdown.ts (line 23: `const MAX_LEN = 600`; promote to named export)
    - src/components/admin/SendUpdateModal.tsx lines 451-484 (textarea + helper text region)
    - src/emails/sendUpdate/Procurement.tsx line 90 (borderRadius: "2px" pill style)
    - .planning/phases/46.1-merge-gate-gap-closure/46.1-CONTEXT.md D-19 (four sub-edits with locked content)
  </read_first>
  <action>

**Step 1 -- WR-01/02: Tighten stripLeadingGreeting.**

Edit `src/emails/sendUpdate/Body.tsx`:

(a) Replace regex at line 23 to capture firstName word:

```ts
// Captures the post-"Hi" word as group 1; matches optional ,/!; >=1 trailing newline.
// Case-sensitive on "Hi" (CONTEXT.md D-1 -- never "hi"/"hello"/"dear").
// Char class widened to [\w'-]+ for hyphenated/apostrophe firstNames (Mary-Anne, O'Brien).
const LEADING_GREETING_RE = /^Hi\s+([\w'-]+)[,!]?\s*\n+/;
```

(b) Widen function signature:

```ts
export function stripLeadingGreeting(personalNote: string, clientFirstName: string): string {
  return personalNote.replace(LEADING_GREETING_RE, (match, capturedFirstName: string) => {
    // Only strip when the captured word equals clientFirstName (case-insensitive).
    // Narrows the strip to the exact double-greeting case CONTEXT.md D-1 describes;
    // preserves intentional "Hi all," when recipient firstName is something else.
    if (capturedFirstName.toLowerCase() === clientFirstName.toLowerCase()) {
      return "";
    }
    return match;
  });
}
```

(c) Widen Body component props + callsite:

```ts
export interface BodyProps {
  personalNote: string;
  clientFirstName: string;   // NEW -- required per WR-01/02
}

export function Body({ personalNote, clientFirstName }: BodyProps) {
  if (!personalNote.trim()) return null;
  const stripped = stripLeadingGreeting(personalNote, clientFirstName);
  // ...rest unchanged...
}
```

(d) Update `<Body>` callsite in `src/emails/sendUpdate/SendUpdate.tsx` to pass `clientFirstName`. Composition root has `clientFirstName` in scope already (existing SendUpdate prop). The exact JSX edit:

```tsx
{/* before */} <Body personalNote={personalNote} />
{/* after  */} <Body personalNote={personalNote} clientFirstName={clientFirstName} />
```

If callsite uses prop-spread, thread `clientFirstName` accordingly.

(e) Update `src/emails/sendUpdate/Body.test.ts`:

For each of the 12 existing `stripLeadingGreeting()` calls, add the matching `clientFirstName` argument per the interfaces table in `<context>`. ADD 4 new it() blocks at the end of the `stripLeadingGreeting` describe block:

```ts
it("strips 'Hi Mary-Anne,' when clientFirstName='Mary-Anne' (hyphenated firstName, exact match)", () => {
  expect(stripLeadingGreeting("Hi Mary-Anne,\n\nBody.", "Mary-Anne")).toBe("Body.");
});

it("does NOT strip 'Hi all,' when clientFirstName='Sarah' (intentional generic salutation, mismatch)", () => {
  const input = "Hi all,\n\nBody.";
  expect(stripLeadingGreeting(input, "Sarah")).toBe(input);
});

it("strips 'Hi sarah,' when clientFirstName='Sarah' (case-insensitive firstName match)", () => {
  // Greeting word stays "Hi" capital (regex case-sensitive on greeting). Only firstName lowered.
  expect(stripLeadingGreeting("Hi sarah,\n\nBody.", "Sarah")).toBe("Body.");
});

it("does NOT strip 'Hi Victoria,' when clientFirstName='Sarah' (different firstName, mismatch)", () => {
  const input = "Hi Victoria,\n\nBody.";
  expect(stripLeadingGreeting(input, "Sarah")).toBe(input);
});
```

The integration test on line 96-110 already passes `clientFirstName: "Victoria"` via `baseInput()`. No additional edit needed beyond the Body widening (verify by reading `src/emails/sendUpdate/fixtures.ts` to confirm `baseInput` threads `clientFirstName` through).

**Step 2 -- WR-03: Export MAX_LEN; enforce maxLength + character counter.**

Edit `src/lib/email/personalNoteMarkdown.ts` line 23:

```ts
// before
const MAX_LEN = 600;

// after
export const MAX_LEN = 600;
```

Edit `src/components/admin/SendUpdateModal.tsx`:

(a) Add import at top of file:

```ts
import { MAX_LEN as PERSONAL_NOTE_MAX_LEN } from "../../lib/email/personalNoteMarkdown";
```

(Aliased to avoid naming collisions.)

(b) Add `maxLength={PERSONAL_NOTE_MAX_LEN}` to the textarea (lines 451-463 region):

```tsx
<textarea
  id="send-update-note"
  rows={4}
  maxLength={PERSONAL_NOTE_MAX_LEN}     // NEW -- WR-03
  className="luxury-input w-full"
  placeholder="Hi, here's the latest on your project..."
  value={personalNote}
  onChange={(e) => setPersonalNote(e.target.value)}
  disabled={sending}
  style={{
    resize: "vertical",
    fontFamily: "var(--font-sans)",
  }}
/>
```

(c) Add character-counter `<p>` AFTER the existing helper-text paragraphs (after line 484, before the section's closing `</div>`):

```tsx
<p
  style={{
    margin: "4px 0 0",
    fontSize: "11px",
    color: "#9E8E80",
    fontFamily: "var(--font-sans)",
  }}
>
  {personalNote.length} / {PERSONAL_NOTE_MAX_LEN} characters
</p>
```

The counter updates on every keystroke; maxLength prevents typing past 600. `parsePersonalNote` `OVER_LIMIT` throw becomes unreachable for admin-typed content.

**Step 3 -- WR-06: Delete pill borderRadius from pillStyle().**

Edit `src/emails/sendUpdate/Procurement.tsx` lines 82-96 (`pillStyle()` function):

After:
```tsx
function pillStyle(status: ProcurementStatus): CSSProperties {
  const palette = STATUS_PILL_STYLES[status];
  // No borderRadius: pills render as flat-edge rectangles per Phase 46 D-3 (which
  // explicitly reserves `border-radius: 2px` for the CTA <a> only). 46.1 D-19 WR-06
  // round-2 carryover removes the violating shorthand assignment that the round-2
  // review flagged (shorthand form on a non-CTA element).
  return {
    display: "inline-block",
    backgroundColor: palette.bg,
    color: palette.text,
    border: `0.5px solid ${palette.border}`,
    padding: "3px 10px",
    fontSize: 11,
    fontWeight: 600,
    textTransform: "uppercase",
    letterSpacing: "0.06em",
  };
}
```

(Misleading `// longhand only -- Phase 46 D-3` comment goes with deleted line. New comment block above explains intentional absence.)

**Audit other email components for stray border-radius.** Per CONTEXT.md D-19 #4:

```bash
grep -rnE 'border-radius|borderRadius' src/emails/ --include='*.tsx' --include='*.ts'
```

Expected post-edit hits:
- `src/emails/shell/EmailButton.tsx:53` -- `borderRadius: "2px"` on CTA Button (KEEP per Phase 46 D-3)
- `src/emails/shell/EmailButton.tsx:11-13` (header comment about longhand) -- KEEP (legitimate prose)
- `src/emails/sendUpdate/Procurement.tsx:90` -- DELETED in this step

If grep returns any other source-code occurrences (excluding header comments + EmailButton), STOP and surface to Paul.

**Step 4 -- No commit yet; proceed to Task 5.**

  </action>
  <verify>
    <automated>npx vitest run src/emails/sendUpdate/Body.test.ts --reporter=basic 2>&1 | tail -5; grep -cF 'export const MAX_LEN' src/lib/email/personalNoteMarkdown.ts; grep -cE 'borderRadius|border-radius' src/emails/sendUpdate/Procurement.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Body.tsx LEADING_GREETING_RE captures the firstName word with [\w'-]+:
      `grep -cE "LEADING_GREETING_RE\s*=\s*/\^Hi\\\\s\\+\\(\\[\\\\w'\\-\\]\\+\\)" src/emails/sendUpdate/Body.tsx` returns at least `1`
    - Body.tsx stripLeadingGreeting takes clientFirstName parameter:
      `grep -cE 'stripLeadingGreeting\s*\(\s*personalNote:\s*string,\s*clientFirstName:\s*string\s*\)' src/emails/sendUpdate/Body.tsx` returns `1`
    - Body.tsx case-insensitive comparison present:
      `grep -cF '.toLowerCase()' src/emails/sendUpdate/Body.tsx` returns at least `2`
    - Body component widened with clientFirstName prop:
      `grep -cE 'clientFirstName:\s*string' src/emails/sendUpdate/Body.tsx` returns at least `2`
    - SendUpdate.tsx Body callsite passes clientFirstName:
      `grep -cE '<Body[^/>]*clientFirstName' src/emails/sendUpdate/SendUpdate.tsx` returns at least `1`
    - Body.test.ts has 4 new it() blocks per CONTEXT.md D-19 #1:
      `grep -cF 'Mary-Anne' src/emails/sendUpdate/Body.test.ts` returns at least `1`
      `grep -cF "'Hi all,'" src/emails/sendUpdate/Body.test.ts` returns at least `1`
      `grep -cE 'case[- ]insensitive' src/emails/sendUpdate/Body.test.ts` returns at least `1`
      `grep -cF 'different firstName' src/emails/sendUpdate/Body.test.ts` returns at least `1`
    - Body.test.ts existing 12 stripLeadingGreeting tests still pass post-signature-update:
      `npx vitest run src/emails/sendUpdate/Body.test.ts --reporter=basic 2>&1 | grep -ciE 'fail' | tr -d ' '` returns `0`
    - personalNoteMarkdown.ts MAX_LEN promoted to named export:
      `grep -cF 'export const MAX_LEN' src/lib/email/personalNoteMarkdown.ts` returns `1`
    - personalNoteMarkdown.ts MAX_LEN value unchanged at 600:
      `grep -cE 'export const MAX_LEN\s*=\s*600' src/lib/email/personalNoteMarkdown.ts` returns `1`
    - SendUpdateModal.tsx imports MAX_LEN:
      `grep -cE 'import.*MAX_LEN.*personalNoteMarkdown' src/components/admin/SendUpdateModal.tsx` returns `1`
    - SendUpdateModal.tsx textarea has maxLength attribute:
      `grep -cE 'maxLength=\{(MAX_LEN|PERSONAL_NOTE_MAX_LEN)\}' src/components/admin/SendUpdateModal.tsx` returns `1`
    - SendUpdateModal.tsx character counter present:
      `grep -cF '{personalNote.length}' src/components/admin/SendUpdateModal.tsx` returns at least `1`
    - Procurement.tsx pillStyle no longer sets borderRadius:
      `grep -cE 'borderRadius|border-radius' src/emails/sendUpdate/Procurement.tsx` returns `0`
    - Border-radius audit clean across src/emails/ source code (snapshot files + header-comment prose + EmailButton.tsx CTA excluded):
      `grep -rnE 'borderRadius|border-radius' src/emails/ --include='*.tsx' --include='*.ts' | grep -vE '__snapshots__|src/emails/shell/EmailButton\.tsx' | wc -l | tr -d ' '` returns `0`
    - No commit yet:
      `git diff --cached --name-only | wc -l | tr -d ' '` returns `0`
  </acceptance_criteria>
  <done>
    Body.tsx widened: LEADING_GREETING_RE captures firstName word with [\w'-]+; stripLeadingGreeting takes clientFirstName and only strips on case-insensitive match; Body component prop widened; SendUpdate.tsx callsite passes it. Body.test.ts has 12 existing it() blocks updated to pass clientFirstName + 4 new it() blocks. personalNoteMarkdown.ts exports MAX_LEN; SendUpdateModal.tsx textarea has maxLength={MAX_LEN} + character counter. Procurement.tsx pillStyle no longer sets borderRadius. Border-radius audit confirms only EmailButton.tsx CTA retains property per Phase 46 D-3. All Body.test.ts tests pass. Working tree dirty; no commit yet.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 5: D-20 -- Final snapshot regen, full-suite test gate, atomic single-commit landing all 10 files</name>
  <files>
    - src/emails/shell/EmailShell.tsx
    - src/emails/shell/EmailShell.test.ts
    - src/emails/shell/EmailButton.tsx
    - src/emails/sendUpdate/Procurement.tsx
    - src/emails/sendUpdate/Body.tsx
    - src/emails/sendUpdate/Body.test.ts
    - src/emails/sendUpdate/SendUpdate.tsx
    - src/emails/sendUpdate/SendUpdate.test.ts
    - src/emails/sendUpdate/__snapshots__/SendUpdate.test.ts.snap
    - src/lib/email/personalNoteMarkdown.ts
    - src/components/admin/SendUpdateModal.tsx
  </files>
  <read_first>
    - .planning/phases/46.1-merge-gate-gap-closure/46.1-CONTEXT.md D-20 (single-atomic-commit constraint + the 10-file table)
    - .planning/phases/46.1-merge-gate-gap-closure/46.1-04-SUMMARY.md (commit message style for round-2)
    - .planning/phases/46.1-merge-gate-gap-closure/46.1-08-PLAN.md (atomic-commit format pattern from round-3)
    - Recent git log to confirm baseline failure count: `git log --oneline -5` showing round-3 commits (af52ca5 / 016c41c / 9f9cd05)
  </read_first>
  <action>

**Step 1 -- Final snapshot regeneration.**

Single final regen agreeing with all of D-15 + D-16 + D-17 + D-18 + D-19:

```bash
npx vitest run src/emails/sendUpdate/SendUpdate.test.ts -u 2>&1 | tail -10
```

Verify regenerated snapshot reflects ALL of:
- `class="pill-${status}"` substrings on each procurement `<span>` (D-15)
- `class="cta-terracotta"` substring on the CTA `<a>` (D-15)
- `<style>` block in `<head>` no longer contains `[data-ogsc] .text-stone`/`.text-charcoal`/`.text-mid`/`.text-cream`/`.text-cream-ivory`/`.text-stone-dark`/`.border-cream-dark` (D-16) -- in either `[data-ogsc]/[data-ogsb]` block or `@media` block
- `<style>` block no longer contains `[data-ogsc] *` catch-all (D-17)
- MSO conditional appears INSIDE a `<style>` element (not `<div style="display:none">`) (WR-05)
- Procurement pill `<span>` HTML no longer carries `border-radius:2px` (WR-06)
- Pill rules under `[data-ogs*]` and `@media` selectors present in rendered CSS for all 7 statuses (D-18 IN-R3-02 generated from STATUS_PILL_STYLES)

Inspect the snapshot diff:

```bash
git diff src/emails/sendUpdate/__snapshots__/SendUpdate.test.ts.snap | head -80
```

Expected diff regions:
1. `<head>` content -- generated CSS replaces hand-written rules (~40 lines deleted, similar count added; net delta small)
2. MSO conditional moved out of `<div>` wrapper into `<style>` element
3. Procurement pill `<span>` open tags now include `class="pill-X"` attributes
4. Procurement pill `<span>` style attribute drops `border-radius:2px` (or compiled equivalent)
5. CTA `<a>` open tag includes `class="cta-terracotta"` attribute

Diff should NOT include changes to:
- Greeting/Body prose, Milestones text, ReviewItems text, footer signoff (body content byte-identical)

If the diff includes unexpected body-content changes, STOP and inspect.

**Note re: WorkOrder.test.ts.snap.** EmailShell shared-shell propagation reaches WorkOrder. CONTEXT.md D-20 file table omits WO snapshot, but the shared-shell edits in this plan WILL diff WO snapshot (CSS-gen + MSO relocation propagate; CTA className `cta-gold` adds). The executor MUST regenerate WO snapshot too:

```bash
npx vitest run src/emails/workOrder/WorkOrder.test.ts -u 2>&1 | tail -10
```

If `git diff` shows WO snapshot deltas, INCLUDE the WO snapshot in the atomic commit -- otherwise `npm test` post-commit will fail on snapshot mismatch. The actual file count for the commit may be 11, not 10. The SUMMARY notes this as a planner-time correction.

**Step 2 -- Full-suite test gate.**

Capture baseline FIRST (before any commit). The executor records baseline at HEAD = `9f9cd05` by stashing pending changes, running `npm test`, capturing the failure count, then `git stash pop`:

```bash
# Save current edits
git stash push -u -m "46.1-09 in progress"

# Get baseline at HEAD = 9f9cd05
BASELINE=$(npm test 2>&1 | grep -E '^\s*Tests' | tail -1 | grep -oE '[0-9]+ failed' | grep -oE '[0-9]+')
echo "BASELINE=$BASELINE"

# Restore edits
git stash pop
```

Then run the full suite with edits applied:

```bash
npm test 2>&1 | tee /tmp/46.1-09-test.log | tail -50
ACTUAL=$(grep -E '^\s*Tests' /tmp/46.1-09-test.log | tail -1 | grep -oE '[0-9]+ failed' | grep -oE '[0-9]+')
echo "ACTUAL=$ACTUAL  BASELINE=$BASELINE"
```

**Pass condition: `ACTUAL <= BASELINE` (zero NEW failures).**

If `ACTUAL > BASELINE`, STOP. Inspect the new failures:
- New EmailShell.test.ts failures -> Task 3 className-binding regex didn't match rendered HTML; check exact class compilation
- New SendUpdate.test.ts width/valign failures -> Task 3 invariant-shaped rewrite has off-by-one; recheck `bodyRowCount`
- New Body.test.ts failures -> Task 4 stripLeadingGreeting signature change broke an existing call without parameters; ensure all 12 existing tests pass `clientFirstName`
- New WO snapshot failures -> regen WO snapshot per Step 1 note

**Step 3 -- Atomic single commit.**

Stage all 10-or-11 files:

```bash
git add \
  src/emails/shell/EmailShell.tsx \
  src/emails/shell/EmailShell.test.ts \
  src/emails/shell/EmailButton.tsx \
  src/emails/sendUpdate/Procurement.tsx \
  src/emails/sendUpdate/Body.tsx \
  src/emails/sendUpdate/Body.test.ts \
  src/emails/sendUpdate/SendUpdate.tsx \
  src/emails/sendUpdate/SendUpdate.test.ts \
  src/emails/sendUpdate/__snapshots__/SendUpdate.test.ts.snap \
  src/lib/email/personalNoteMarkdown.ts \
  src/components/admin/SendUpdateModal.tsx

# Add WO snapshot if regen needed:
if ! git diff --quiet src/emails/workOrder/__snapshots__/WorkOrder.test.ts.snap 2>/dev/null; then
  git add src/emails/workOrder/__snapshots__/WorkOrder.test.ts.snap
fi
```

(Note: SendUpdate.tsx is included because Task 4 Step 1d edited the `<Body>` callsite to pass `clientFirstName`. The CONTEXT.md D-20 file table did NOT explicitly list SendUpdate.tsx; the executor SHOULD add it because the Body prop widening requires the callsite update. Surface in SUMMARY as a planner-time correction. The frontmatter `files_modified` list is the authoritative D-20 mapping plus SendUpdate.tsx and possibly WO snapshot.)

Verify staged file count:

```bash
git diff --cached --name-only | wc -l
```

Expected: 11 (the 10 D-20 files + SendUpdate.tsx) OR 12 (+ WO snapshot if regen needed).

Commit with the atomic round-4 message (no AI attribution per project CLAUDE.md):

```bash
git commit -m "fix(46.1-09): close CR-R3-01 BLOCKER + WR-01..06 carryovers (round-4)

Round-3 code review (a59cf25 in 46.1-REVIEW.md) flagged CR-R3-01 BLOCKER:
the dark-mode lock palette in EmailShell.tsx targets className hooks
(pill-*, cta-*, text-stone, text-charcoal, border-cream-dark) that never
appear on rendered elements. Tailwind utility classes are inlined as
styles and stripped from className; literal pill/cta classes were never
applied as className= props. Verified: grep for class=\"pill-*\" /
class=\"cta-*\" returned 0 matches in the rendered snapshot. Result:
gap-4 contrast inversion is unfixed; only the body-bg paint via
<table bgcolor> + inline <body style> works in Outlook auto-darken.

Per 46.1-CONTEXT.md round-4 (D-15..D-21):

D-15 -- apply explicit literal className strings (pill-\${status} on
Procurement.tsx pill <span>; cta-\${variant} on EmailButton.tsx Button)
so the per-class lock rules in EmailShell.tsx actually bind. Literal
class strings are NOT Tailwind utilities; they pass through compilation
unchanged. Verified at planner time + at task-1 verification gate.

D-16 -- delete dead [data-ogsc] .text-* and .border-cream-dark rules
(Tailwind utility classes; never bind in rendered HTML). Same deletion
mirrored in @media (prefers-color-scheme: dark) block.

D-17 -- delete the [data-ogsc] * { color: inherit !important } catch-all
that actively cancels every inline color in dark mode. With D-15
binding the pill className hooks, per-class !important rules re-pin
inline colors after the catch-all reset; deleting the catch-all means
inline non-!important colors win by default.

D-18 #1 (WR-R3-02) -- rewrite EmailShell.test.ts hex-substring assertions
to className-binding shape. Old tests substring-matched hex values that
appeared inside dead style block declarations themselves; new tests
match class=\"pill-\${status}\" colocated with inline background-color.
Same coverage; assertion shape proves the rules ACTUALLY BIND on
rendered elements rather than just exist in the <style> block.

D-18 #2 (WR-R3-04) -- rewrite SendUpdate.test.ts:506-527 valign:top
floor from magic-number-9 to per-row count derived from procurement
body slice. Same shape applied to width-tests on lines 487-503.

D-18 #3 (IN-R3-02) -- refactor EmailShell.tsx hand-mirrored CSS into
module-load-time generation from STATUS_PILL_STYLES via buildPillRules().
Single source of truth across admin UI + email render + dark-mode lock;
drift is structurally impossible. No new build step.

D-19 (round-2 WR carryovers folded):
  WR-01/02 -- stripLeadingGreeting takes clientFirstName parameter,
    case-insensitive exact-match check, only strips on match. Hyphenated
    firstNames now caught (Mary-Anne); intentional generic salutations
    preserved (Hi all, when firstName=Sarah).
  WR-03 -- export MAX_LEN from personalNoteMarkdown.ts; add maxLength
    + character counter to SendUpdateModal personalNote textarea.
    PersonalNoteParseError(OVER_LIMIT) becomes unreachable from admin UI.
  WR-05 -- relocate MSO conditional out of <div style=display:none> in
    <head> (invalid HTML5) into a sibling <style dangerouslySetInnerHTML>.
  WR-06 -- delete borderRadius:2px from Procurement pillStyle()
    (shorthand form on a non-CTA element; Phase 46 D-3 reserves
    border-radius:2px for the CTA <a> only).

D-20 -- single atomic commit lands all D-20 files (+ SendUpdate.tsx
callsite update + WorkOrder snapshot if regen needed). The snapshot
diff is one load-bearing artifact that must agree with all five locked
decisions simultaneously.

D-21 -- round-5 re-trigger: /gsd-code-review against round-3 file set
+ new files (SendUpdateModal.tsx, personalNoteMarkdown.ts, Body.tsx)
must report findings.blocker == 0 before scheduling Liz's round-5
visual UAT on Outlook for Mac dark + Windows dark + web dark + light-
mode regression check.

Closes round-2 WR-01/02/03/05/06 + round-3 CR-R3-01 + WR-R3-02 +
WR-R3-04 + IN-R3-02. Round-4 of phase 46.1; round-5 visual gate is
Liz's responsibility per D-21."
```

**Step 4 -- Post-commit verification.**

```bash
git log --oneline -1
git show --stat HEAD | head -20
```

**Step 5 -- Write SUMMARY.md** per `<output>` section below.

  </action>
  <verify>
    <automated>npm test 2>&1 | grep -E '^\s*Tests' | tail -1; git log --oneline -1; git show --name-only HEAD --pretty=format: | grep -vE '^$'</automated>
  </verify>
  <acceptance_criteria>
    - Final snapshot regen committed; SendUpdate snapshot contains className bindings:
      `git show HEAD -- src/emails/sendUpdate/__snapshots__/SendUpdate.test.ts.snap | grep -cE 'class="[^"]*pill-' | tr -d ' '` returns at least `3`
      `git show HEAD -- src/emails/sendUpdate/__snapshots__/SendUpdate.test.ts.snap | grep -cE 'class="[^"]*cta-' | tr -d ' '` returns at least `1`
    - Snapshot no longer contains the dead [data-ogsc] .text-* / .border-cream-dark / catch-all CSS:
      `grep -cE '\[data-ogsc\] \.text-(stone|charcoal|mid|cream)' src/emails/sendUpdate/__snapshots__/SendUpdate.test.ts.snap` returns `0`
      `grep -cE '\[data-ogsc\] \.border-cream-dark' src/emails/sendUpdate/__snapshots__/SendUpdate.test.ts.snap` returns `0`
      `grep -cE '\[data-ogsc\] \*' src/emails/sendUpdate/__snapshots__/SendUpdate.test.ts.snap` returns `0`
    - Snapshot no longer contains <div style="display:none"> MSO wrapper:
      `grep -cF '<div style="display:none">' src/emails/sendUpdate/__snapshots__/SendUpdate.test.ts.snap` returns `0`
    - Snapshot still contains MSO conditional payload:
      `grep -cF '<!--[if mso]>' src/emails/sendUpdate/__snapshots__/SendUpdate.test.ts.snap` returns at least `1`
      `grep -cF 'mso-color-scheme: light' src/emails/sendUpdate/__snapshots__/SendUpdate.test.ts.snap` returns at least `1`
    - Snapshot still contains gap-7 markers (table bgcolor + media query + inline body bg):
      `grep -cE '<table[^>]*bgcolor="#FAF8F5"' src/emails/sendUpdate/__snapshots__/SendUpdate.test.ts.snap` returns at least `1`
      `grep -cF '@media (prefers-color-scheme: dark)' src/emails/sendUpdate/__snapshots__/SendUpdate.test.ts.snap` returns at least `1`
    - Snapshot pill <span> no longer carries border-radius:
      `grep -oE '<span[^>]*class="pill-[^"]+"[^>]*style="[^"]+"' src/emails/sendUpdate/__snapshots__/SendUpdate.test.ts.snap | head -3 | grep -ciE 'border-radius'` returns `0`
    - All EmailShell.test.ts tests pass:
      `npx vitest run src/emails/shell/EmailShell.test.ts --reporter=basic 2>&1 | grep -ciE '(failed|✘)' | tr -d ' '` returns `0`
    - All SendUpdate.test.ts tests pass:
      `npx vitest run src/emails/sendUpdate/SendUpdate.test.ts --reporter=basic 2>&1 | grep -ciE '(failed|✘)' | tr -d ' '` returns `0`
    - All Body.test.ts tests pass (16 total: 12 updated + 4 new):
      `npx vitest run src/emails/sendUpdate/Body.test.ts --reporter=basic 2>&1 | grep -ciE '(failed|✘)' | tr -d ' '` returns `0`
    - WorkOrder.test.ts tests pass:
      `npx vitest run src/emails/workOrder/WorkOrder.test.ts --reporter=basic 2>&1 | grep -ciE '(failed|✘)' | tr -d ' '` returns `0`
    - Full-suite npm test gate -- zero NEW failures vs baseline at 9f9cd05:
      Executor records `BASELINE` from `git stash` + `npm test` against HEAD~ before edits; post-commit `ACTUAL = npm test failure count`. Pass when `ACTUAL <= BASELINE`.
    - Atomic commit landed with locked subject line:
      `git log --oneline -1 | grep -cF 'fix(46.1-09): close CR-R3-01 BLOCKER + WR-01..06 carryovers (round-4)'` returns `1`
    - Commit contains 10-12 files (10 D-20 + SendUpdate.tsx + optional WO snapshot):
      `git show --name-only HEAD --pretty=format: | grep -vE '^$' | sort -u | wc -l | tr -d ' '` returns `10`, `11`, or `12`
    - Each of the 10 D-20 paths is present in commit:
      `git show --name-only HEAD --pretty=format: | grep -cE '^(src/emails/shell/EmailShell\.tsx|src/emails/shell/EmailShell\.test\.ts|src/emails/shell/EmailButton\.tsx|src/emails/sendUpdate/Procurement\.tsx|src/emails/sendUpdate/Body\.tsx|src/emails/sendUpdate/Body\.test\.ts|src/emails/sendUpdate/SendUpdate\.test\.ts|src/emails/sendUpdate/__snapshots__/SendUpdate\.test\.ts\.snap|src/lib/email/personalNoteMarkdown\.ts|src/components/admin/SendUpdateModal\.tsx)$' | tr -d ' '` returns `10`
    - No AI-attribution in commit message:
      `git log -1 --pretty=%B | grep -ciE 'co-authored-by|generated with claude|claude code|🤖'` returns `0`
    - No new package.json deps (Q8 lock from prior rounds):
      `git diff HEAD~1 HEAD -- package.json package-lock.json 2>&1 | grep -ciE 'litmus|email-on-acid|emailonacid|playwright.*colorScheme' | tr -d ' '` returns `0`
    - 46.1-09-SUMMARY.md created:
      `test -f .planning/phases/46.1-merge-gate-gap-closure/46.1-09-SUMMARY.md && echo OK` returns `OK`
  </acceptance_criteria>
  <done>
    Single atomic commit `fix(46.1-09): close CR-R3-01 BLOCKER + WR-01..06 carryovers (round-4)` lands all D-20 files (+ SendUpdate.tsx callsite + WO snapshot if regen needed); commit subject + body match round-1..round-3 conventions; no AI attribution. SendUpdate snapshot reflects className additions, dead-rule deletions, MSO relocation, CSS-generation refactor, and Procurement borderRadius removal -- all in one regenerated artifact. EmailShell.test.ts gap-4 + gap-7 describes both pass with className-binding-shape assertions. SendUpdate.test.ts D-12/D-13 column-width + valign:top tests pass with invariant-shaped per-row count. Body.test.ts has 16 tests passing (12 updated + 4 new). Full-suite `npm test` failure count <= baseline (zero NEW failures introduced). 46.1-09-SUMMARY.md written. Round-5 code review (D-21) is the next gate.
  </done>
</task>

</tasks>

<verification>
Post-commit, the following commands prove all decisions D-15..D-21 landed:

- D-15: `grep -cE 'class="[^"]*pill-' src/emails/sendUpdate/__snapshots__/SendUpdate.test.ts.snap` >= `3`; `grep -cE 'class="[^"]*cta-' src/emails/sendUpdate/__snapshots__/SendUpdate.test.ts.snap` >= `1`
- D-16: `grep -E '\[data-ogsc\] \.text-(stone|charcoal|mid|cream)' src/emails/shell/EmailShell.tsx` returns 0; `grep -E '\[data-ogsc\] \.border-cream-dark' src/emails/shell/EmailShell.tsx` returns 0
- D-17: `grep -E '\[data-ogsc\] \*' src/emails/shell/EmailShell.tsx` returns 0
- D-18 #1: `grep -vE '^\s*(//|\*)' src/emails/shell/EmailShell.test.ts | grep -oE 'pill-(scheduled|warehouse|in-transit|ordered|pending|delivered|installed)' | sort -u | wc -l` returns 7
- D-18 #2: `grep -cF 'bodyRowCount' src/emails/sendUpdate/SendUpdate.test.ts` >= `1`; `grep -cE 'toBeGreaterThanOrEqual\(9\)' src/emails/sendUpdate/SendUpdate.test.ts` returns `0`
- D-18 #3: `grep -cF 'function buildPillRules' src/emails/shell/EmailShell.tsx` returns `1`; `grep -cE 'import.*STATUS_PILL_STYLES' src/emails/shell/EmailShell.tsx` >= `1`
- D-19 WR-01/02: `grep -cE 'stripLeadingGreeting\s*\(\s*personalNote:\s*string,\s*clientFirstName' src/emails/sendUpdate/Body.tsx` returns `1`; `grep -cF 'Mary-Anne' src/emails/sendUpdate/Body.test.ts` >= `1`
- D-19 WR-03: `grep -cF 'export const MAX_LEN' src/lib/email/personalNoteMarkdown.ts` returns `1`; `grep -cE 'maxLength=\{(MAX_LEN|PERSONAL_NOTE_MAX_LEN)\}' src/components/admin/SendUpdateModal.tsx` returns `1`; `grep -cF '{personalNote.length}' src/components/admin/SendUpdateModal.tsx` >= `1`
- D-19 WR-05: `grep -cF '<div' src/emails/shell/EmailShell.tsx` returns `0`; `grep -cF '<!--[if mso]>' src/emails/shell/EmailShell.tsx` >= `1`
- D-19 WR-06: `grep -cE 'borderRadius|border-radius' src/emails/sendUpdate/Procurement.tsx` returns `0`; `grep -rnE 'borderRadius|border-radius' src/emails/ --include='*.tsx' --include='*.ts' | grep -vE '__snapshots__|src/emails/shell/EmailButton\.tsx' | wc -l` returns `0`
- D-20: `git log --oneline -1 | grep -cF 'fix(46.1-09): close CR-R3-01 BLOCKER'` returns `1`; commit file count is 10-12
- D-21: 46.1-09-SUMMARY.md flags round-5 code review as the next gate

Test gate (post-commit, against baseline-at-9f9cd05):

- `npx vitest run src/emails/shell/EmailShell.test.ts` -- gap-4 + gap-7 describes pass with className-binding-shape assertions
- `npx vitest run src/emails/sendUpdate/SendUpdate.test.ts` -- 5 snapshot + plain-text + behavioral + 4 D-7 + 6 D-12/D-13 (invariant-shaped) all pass
- `npx vitest run src/emails/sendUpdate/Body.test.ts` -- 16 tests pass (12 updated + 4 new)
- `npx vitest run src/emails/workOrder/WorkOrder.test.ts` -- snapshot regenerated; tests pass
- `npm test` -- total failure count <= round-3 baseline; zero NEW failures
- `git log -1 --pretty=%B | grep -ciE 'co-authored-by|generated with claude|claude code|🤖'` returns `0`
- `git diff HEAD~1 HEAD -- package.json package-lock.json | grep -ciE 'litmus|email-on-acid|playwright.*colorScheme'` returns `0`
</verification>

<success_criteria>
1. Procurement.tsx pill `<span>` renders with `class="pill-${status}"` AND its existing inline `style={pillStyle(it.status)}`; both halves of dark-mode protection ship together (D-15)
2. EmailButton.tsx Button renders with `class="cta-${variant}"` AND its existing inline style block (D-15)
3. EmailShell.tsx no longer contains the 7 dead `[data-ogsc] .text-*` rules, the 1 `.border-cream-dark` rule, or the `[data-ogsc] *` catch-all -- in either the `[data-ogsc]/[data-ogsb]` block OR the `@media` block (D-16, D-17)
4. EmailShell.tsx generates the `[data-ogsc]/[data-ogsb]/@media` pill rules at module-load time from `STATUS_PILL_STYLES` via `buildPillRules()` -- no hand-mirrored CSS for the 7 statuses' bg/text/border (D-18 #3 IN-R3-02)
5. EmailShell.test.ts gap-4 lock describe block has 7 per-status pill className-colocation assertions on SendUpdate render + 2 CTA className-colocation assertions (terracotta SU + gold WO) -- replacing old vacuous hex-iterating tests; the 4 `REQUIRED_*_HEXES` constants are deleted (D-18 #1 WR-R3-02)
6. SendUpdate.test.ts:506-527 valign:top floor + the 3 width-floor tests (lines 487-503) are rewritten to invariant-shaped per-row count derived from the procurement body slice (D-18 #2 WR-R3-04)
7. Body.tsx `stripLeadingGreeting` takes `clientFirstName: string` parameter; only strips when captured firstName equals `clientFirstName` case-insensitively; LEADING_GREETING_RE widened to `[\w'-]+` for hyphenated firstNames (D-19 WR-01/02)
8. Body component widened with `clientFirstName: string` prop; SendUpdate.tsx `<Body>` callsite passes it (D-19 WR-01/02)
9. Body.test.ts has 12 existing tests updated to pass `clientFirstName` argument + 4 new tests (hyphenated firstName positive, "Hi all," generic-salutation negative, case-insensitive positive, different-firstName negative) (D-19 WR-01/02)
10. personalNoteMarkdown.ts exports `MAX_LEN`; SendUpdateModal.tsx imports it and applies `maxLength={MAX_LEN}` to the personalNote textarea + character counter `{personalNote.length} / {MAX_LEN} characters` (D-19 WR-03)
11. EmailShell.tsx MSO conditional moved out of `<div style="display:none">` (invalid HTML5 in `<head>`) into a sibling `<style dangerouslySetInnerHTML>`; substring assertions on `<!--[if mso]>` and `mso-color-scheme: light` still pass (D-19 WR-05)
12. Procurement.tsx `pillStyle()` no longer sets `borderRadius` (per Phase 46 D-3, only the CTA `<a>` retains border-radius longhand); audit confirms no other email source file has stray border-radius (D-19 WR-06)
13. Single atomic commit `fix(46.1-09): close CR-R3-01 BLOCKER + WR-01..06 carryovers (round-4)` lands all 10 D-20 files (+ SendUpdate.tsx callsite + WorkOrder snapshot if regen needed); no AI attribution per project convention (D-20)
14. Full-suite `npm test` shows zero NEW failures vs the round-3 baseline at commit `9f9cd05`; gap-4 + gap-7 + Body + Procurement + WorkOrder + EmailShell test surfaces all pass green
15. 46.1-09-SUMMARY.md written; STATE.md / ROADMAP.md updates handled by gsd-execute-phase finalization (round-5 code review per D-21 is the next gate; round-5 Liz visual UAT on Outlook for Mac dark + Windows dark + web dark + light-mode regression is the human gate after that)
</success_criteria>

<output>
After completion, create `.planning/phases/46.1-merge-gate-gap-closure/46.1-09-SUMMARY.md` capturing:

- The atomic commit hash + diff stats (`git show --stat HEAD`)
- The chosen architecture per CONTEXT.md round-4 (one-line summary referencing D-15..D-20)
- Verification of each decision landed by content (paste literal grep output for D-15 className bindings, D-16 dead-rule deletion, D-17 catch-all deletion, D-18 #3 buildPillRules import, D-18 #1/#2 test rewrites, D-19 four sub-edits, WR-06 borderRadius audit clean)
- Test-suite delta: list each test file's pre/post pass count; final baseline-vs-actual failure count delta is 0 (zero new failures)
- Snapshot diff scope summary: which sections of the SendUpdate + WorkOrder snapshots changed (head/wrapper/className additions); body content unchanged
- Confirmation no Q8 violations: paste `git diff HEAD~1 HEAD -- package.json package-lock.json` output (expected: empty or version-only changes; no Litmus / Email-on-Acid / Playwright dark-mode emulation deps)
- Note for round-5 code review (D-21): "Run `/gsd-code-review 46.1` against round-3 file set + new files (`SendUpdateModal.tsx`, `personalNoteMarkdown.ts`, `Body.tsx`). Code review must report `findings.blocker == 0` AND no new BLOCKER from the className-binding refactor (e.g., react-email's `<Button>` accidentally stripping the className -- the verification gate in Task 1 catches this at planning time, but round-5 review independently confirms in rendered output). On clean review, parent UAT 46-UAT.md replays for round-5 Liz visual re-test on Outlook for Mac dark + Outlook for Windows dark + Outlook web dark + light-mode regression check."
- Note for round-5 visual gate: "Open Outlook for Mac dark-mode + Outlook for Windows dark-mode + Outlook web dark-mode + light mode regression and verify (a) procurement pill chrome (bg + text + border) renders with locked colors -- no half-inversion; (b) terracotta CTA renders with `#C4836A` bg; (c) gap-4 / gap-5 / gap-6 / gap-7 fixes from prior rounds still render correctly; (d) admin compose UI shows live character counter under personalNote textarea + maxLength enforcement (try typing past 600 chars -- input stops). If round-5 fails, re-open phase 46.1 with the specific marker that didn't survive."
- Carryover-resolution mapping: list each round-2 WR finding (WR-01, WR-02, WR-03, WR-05, WR-06) + each round-3 finding (CR-R3-01, WR-R3-02, WR-R3-04, IN-R3-02) and the line/file the resolution lives in.
</output>
