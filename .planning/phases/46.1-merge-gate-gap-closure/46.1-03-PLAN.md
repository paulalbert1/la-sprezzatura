---
phase: 46.1-merge-gate-gap-closure
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/PLAN-AUTHORING-PATTERNS.md
autonomous: true
requirements: []
requirements_addressed:
  - "gap-3 (parent UAT 46-UAT.md, Finding 3a)"
  - "D-3 (46.1-CONTEXT.md)"
parent_uat: .planning/phases/46-send-update-work-order-migration/46-UAT.md
parent_uat_gap: gap-3
must_haves:
  truths:
    - "PLAN-AUTHORING-PATTERNS.md 'Cheap verifications on load-bearing audit assumptions' section contains a bold admonition that the pattern applies to EVERY enum/type/contract referenced — not just the most prominent one (D-3)"
    - "PLAN-AUTHORING-PATTERNS.md contains a Phase 46 example block referencing the `ProcurementStatus` 7-vs-3 audit gap and the resulting merge-gate UAT rejection (D-3)"
    - "PLAN-AUTHORING-PATTERNS.md carries a checklist nudge at the top of the section instructing audit authors to enumerate every enum/type/contract before approval (D-3)"
  artifacts:
    - path: .planning/PLAN-AUTHORING-PATTERNS.md
      provides: "Strengthened 'Cheap verifications on load-bearing audit assumptions' entry with three discrete additions: bold admonition, Phase 46 ProcurementStatus example, checklist nudge"
      contains: "Apply this pattern to EVERY enum"
  key_links: []
---

<objective>
Close parent UAT gap-3 (audit-completeness gap, Finding 3a). The migration-diff audit at `46-MIGRATION-DIFF.md` listed only 3 of the 7 canonical `ProcurementStatus` values as pill colors — Ordered (blue), In Transit (amber), Delivered (green) — and described them as "the canonical palette" without verifying enum closure. When Liz hit a `pending` status pill in Outlook, the warm-red chrome read as "off-palette" because the audit had under-described the palette. The pattern that would have caught this — "Cheap verifications on load-bearing audit assumptions", added to `PLAN-AUTHORING-PATTERNS.md` two hours before this UAT at commit `3b1fc0f` — was not applied to `ProcurementStatus` (only to `MilestoneState`).

Per CONTEXT.md D-3, this is a docs-only fix: strengthen the existing pattern entry with three discrete additions:

1. **Bold admonition** at the top of the entry: `Apply this pattern to EVERY enum, type, or contract the audit references. The most-prominent assumption is rarely the only assumption.`
2. **Phase 46 example block** showing the `ProcurementStatus` 7-vs-3 audit gap concretely — what was enumerated, what was missed, what the failure mode was, and the 30-second grep that would have caught it.
3. **Checklist nudge** to be added near the top of the section: `Before approval: list every enum, type, or branded type the audit's claims rest on. Verify each. Failed verifications block approval.`

No code changes. No tests. Single file edit. Single atomic commit.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/PLAN-AUTHORING-PATTERNS.md
@.planning/phases/46.1-merge-gate-gap-closure/46.1-CONTEXT.md
@.planning/phases/46-send-update-work-order-migration/46-UAT.md
@.planning/phases/46-send-update-work-order-migration/46-MIGRATION-DIFF.md
@src/lib/procurement/statusPills.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Strengthen 'Cheap verifications on load-bearing audit assumptions' entry</name>
  <files>.planning/PLAN-AUTHORING-PATTERNS.md</files>
  <read_first>
    - .planning/PLAN-AUTHORING-PATTERNS.md (the entire existing entry — section "Cheap verifications on load-bearing audit assumptions" starts at line 163, ends at line 192 in the current file)
    - .planning/phases/46.1-merge-gate-gap-closure/46.1-CONTEXT.md (D-3 — locks the three insertions verbatim)
    - .planning/phases/46-send-update-work-order-migration/46-UAT.md (Finding 3a — the failure mode being captured)
    - src/lib/procurement/statusPills.ts (lines 17–50 — confirms the 7-value `ProcurementStatus` enum: scheduled / warehouse / in-transit / ordered / pending / delivered / installed)
  </read_first>
  <action>
Edit `.planning/PLAN-AUTHORING-PATTERNS.md`. Make THREE discrete edits to the existing "Cheap verifications on load-bearing audit assumptions" section (currently lines 163–192). All three insertions are NEW content; do NOT delete or rewrite any existing prose. The existing **Problem**, **Where this surfaced**, **Why it recurs**, **Default to ship**, **Where to put the assumption list**, **Exceptions**, and **Surfaced by** subsections all stay verbatim.

---

### Insertion 1 — Bold admonition (verbatim per D-3)

Add this line as a new bold paragraph IMMEDIATELY AFTER the existing **Problem.** paragraph (which currently ends with "...near-zero cost." at the end of line 165) and BEFORE the **Where this surfaced.** subsection. The new paragraph reads:

> **Apply this pattern to EVERY enum, type, or contract the audit references. The most-prominent assumption is rarely the only assumption.**

The line must be a standalone paragraph (blank line above and below) so it visually pops. Use `**...**` markdown bold around the entire sentence, including the period.

---

### Insertion 2 — Phase 46 ProcurementStatus example block

Add this block as a new subsection IMMEDIATELY AFTER the existing **Where this surfaced.** paragraph (which discusses the `MilestoneState` case) and BEFORE the **Why it recurs.** subsection. Insert with a blank line above and below.

The literal content to insert (markdown-formatted):

```markdown
**Where this DIDN'T get applied — Phase 46 merge-gate UAT (gap-3, 2026-04-28).** Two hours after the `MilestoneState` example above shipped, the same audit (`46-MIGRATION-DIFF.md`) made a parallel claim about procurement status pills: "the canonical palette is Ordered (blue), In Transit (amber), Delivered (green)." That sentence rests on a load-bearing assumption — that `ProcurementStatus` is closed at those three values. It isn't. `src/lib/procurement/statusPills.ts:17-50` defines SEVEN canonical statuses: `scheduled / warehouse / in-transit / ordered / pending / delivered / installed`. When Liz tested in Outlook desktop, a row hit `pending` — peach/red chrome (`bg #FDEEE6 / text #9B3A2A / border #F2C9B8`) — and reported it as "off-palette" because the audit said the canonical palette was three colors. The render was correct; the audit was under-described. A 30-second `grep -E '^export const PROCUREMENT_STATUSES' src/lib/procurement/statusPills.ts` (or `grep -nE '^\s+(scheduled|warehouse|in-transit|ordered|pending|delivered|installed):' src/lib/procurement/statusPills.ts`) would have surfaced the seven-value reality and blocked the audit from shipping with a three-value enumeration. The pattern that the `MilestoneState` example documents was applied to one enum and not the other; the failure surfaced as a merge-gate UAT rejection requiring a gap-closure phase (46.1) to remediate. The pattern works; the discipline of applying it to *every* enum the audit's claims rest on is what was missing — which is exactly what the bold admonition above is meant to enforce.
```

Use the literal text block verbatim — no paraphrasing, no editorial tightening, no insertion of links beyond what's shown.

---

### Insertion 3 — Checklist nudge

Add this as a new bulleted entry at the START of the **Default to ship.** subsection, BEFORE the existing code block (the `# Each load-bearing assumption gets a one-line check...` block). Insert as a single bullet line on its own paragraph:

> - **Before approval:** list every enum, type, or branded type the audit's claims rest on. Verify each. Failed verifications block approval.

The nudge must use the bold prefix `**Before approval:**` so it reads as an admonition, not a generic note. The em-dash (— U+2014) is not used here; the period after "approval" closes the prefix and the rest is plain prose.

---

Do NOT touch any OTHER section of `PLAN-AUTHORING-PATTERNS.md`. Do NOT renumber the existing five sections (Identity-token grep, Plan-listed test counts, Acceptance criteria that grep test names, Carry-over content presence, Cheap verifications). Do NOT change the **Surfaced by** footer line at the end of the strengthened entry — that line continues to point to "Phase 46 Plan 03 Checkpoint 1 (2026-04-28)" because the original surfacing is the `MilestoneState` discovery; the gap-3 UAT is a re-emergence in the SAME pattern that motivated the original entry, not a new pattern.

After the edits, verify the section reads coherently: existing **Problem.** → new bold admonition (Insertion 1) → existing **Where this surfaced.** → new Phase 46 example block (Insertion 2) → existing **Why it recurs.** → existing **Default to ship.** with new checklist nudge bullet at top (Insertion 3) → existing code block → existing **Where to put the assumption list.** → existing **Exceptions.** → existing **Surfaced by.**.

Stage and commit:
```bash
git add .planning/PLAN-AUTHORING-PATTERNS.md
git commit -m "docs(46.1-03): strengthen load-bearing-audit pattern (gap-3)

Three additions to PLAN-AUTHORING-PATTERNS.md 'Cheap verifications on
load-bearing audit assumptions' per 46.1-CONTEXT.md D-3:

1. Bold admonition immediately after the Problem paragraph: 'Apply
   this pattern to EVERY enum, type, or contract the audit
   references. The most-prominent assumption is rarely the only
   assumption.'

2. Phase 46 example block after the existing MilestoneState case,
   documenting how the same audit (46-MIGRATION-DIFF.md) shipped a
   three-value ProcurementStatus enumeration when the actual enum is
   seven-valued (scheduled/warehouse/in-transit/ordered/pending/
   delivered/installed). The 'pending' pill in Liz's Outlook test
   read as off-palette because the audit had under-described the
   palette. A 30-second grep on PROCUREMENT_STATUSES would have
   surfaced the seven-value reality before the audit shipped.

3. Checklist nudge at the top of the 'Default to ship' subsection:
   'Before approval: list every enum, type, or branded type the
   audit's claims rest on. Verify each. Failed verifications block
   approval.'

No code changes; no test changes. Closes parent UAT 46-UAT.md gap-3
Finding 3a (status: failed -> resolved at the end of phase 46.1).

The deferred design conversation about re-toning the 'pending' pill
away from the warm-red family that visually competes with the
terracotta CTA (Finding 3b) remains explicitly out of 46.1 scope per
CONTEXT.md."
```

Per project convention (CLAUDE.md): no AI attribution, no `Co-Authored-By` footer.
  </action>
  <verify>
    <automated>git log --oneline -1 | grep -F "docs(46.1-03)"</automated>
  </verify>
  <acceptance_criteria>
    - Insertion 1 (bold admonition) present verbatim. Literal-string grep on the locked D-3 admonition:
      `grep -cF "Apply this pattern to EVERY enum, type, or contract the audit references. The most-prominent assumption is rarely the only assumption." .planning/PLAN-AUTHORING-PATTERNS.md` returns `1`
    - Insertion 1 is bold (markdown `**...**` form):
      `grep -cF "**Apply this pattern to EVERY enum" .planning/PLAN-AUTHORING-PATTERNS.md` returns `1`
    - Insertion 2 (Phase 46 example block) present — anchored on its load-bearing phrasing rather than on commonly-occurring tokens like "ProcurementStatus" or "Phase 46" (PLAN-AUTHORING-PATTERNS.md "over-broad grep" pattern — pick a string the rest of the file does not already contain):
      `grep -cF "Where this DIDN'T get applied — Phase 46 merge-gate UAT (gap-3, 2026-04-28)" .planning/PLAN-AUTHORING-PATTERNS.md` returns `1`
      `grep -cF "scheduled / warehouse / in-transit / ordered / pending / delivered / installed" .planning/PLAN-AUTHORING-PATTERNS.md` returns `1`
      `grep -cF "bg #FDEEE6 / text #9B3A2A / border #F2C9B8" .planning/PLAN-AUTHORING-PATTERNS.md` returns `1`
    - Insertion 3 (checklist nudge) present verbatim:
      `grep -cF "Before approval:** list every enum, type, or branded type the audit's claims rest on. Verify each. Failed verifications block approval." .planning/PLAN-AUTHORING-PATTERNS.md` returns `1`
    - The five existing section headings are still present (no accidental deletion):
      `grep -cE "^## (Identity-token grep checks are over-broad by default|Plan-listed test counts are floors, not ceilings|Acceptance criteria that grep test names couple structure to text|Carry-over content presence checks for structural-rewrite audits|Cheap verifications on load-bearing audit assumptions)$" .planning/PLAN-AUTHORING-PATTERNS.md` returns `5`
    - The pre-existing `MilestoneState` example sentence is still present (we are ADDING, not replacing):
      `grep -cF "MilestoneState being a 2-valued type" .planning/PLAN-AUTHORING-PATTERNS.md` returns `1`
    - The pre-existing **Surfaced by** footer for this section is still present and unchanged:
      `grep -cF "Surfaced by:** Phase 46 Plan 03 Checkpoint 1 (2026-04-28). Logged at first occurrence" .planning/PLAN-AUTHORING-PATTERNS.md` returns `1`
    - Atomic commit landed:
      `git log --oneline -1 | grep -F "docs(46.1-03)"` returns one match
    - Exactly one file in HEAD:
      `git show --name-only HEAD | grep -E "^\.planning/PLAN-AUTHORING-PATTERNS\.md$" | wc -l` returns `1`
      `git show --name-only HEAD | grep -vE "^(commit|Author|Date|docs|fix|$|\.planning/PLAN-AUTHORING-PATTERNS\.md)" | wc -l` returns `0`
    - No AI-attribution footer in commit message:
      `git log -1 --pretty=%B | grep -ciE "co-authored-by|generated with|claude code"` returns `0`
    - No code or test files modified by this commit:
      `git show --name-only HEAD | grep -cE "^src/" ` returns `0`
    - `npm test` was NOT required for this task (docs-only). Confirmed by absence of any test in the commit. (No verification step beyond the explicit greps.)
  </acceptance_criteria>
  <done>
    PLAN-AUTHORING-PATTERNS.md "Cheap verifications on load-bearing audit assumptions" entry contains all three D-3 insertions verbatim. The five existing sections remain intact and unchanged. One atomic commit `docs(46.1-03): ...` lands a single docs file. No code changes, no test changes.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none material) | This plan modifies one planning-docs file. No code, no schema, no input/output surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-46.1-03-01 | I (Information disclosure) | `.planning/PLAN-AUTHORING-PATTERNS.md` is in `.planning/` directory which may or may not be tracked in git per repo policy | accept | Per CLAUDE.md (workspace), `.planning/` gitignore status varies per repo; this repo's status confirmed by inspection (the file is tracked — `git ls-files .planning/PLAN-AUTHORING-PATTERNS.md` returns the path). The file contains no secrets, no credentials, no PII — it is pure planning-process documentation. Committing it is the intended workflow per the existing 5+ entries in the file. No new disclosure surface. |

(No other STRIDE categories apply. No tampering surface beyond the normal git-commit boundary; no DoS/repudiation/privilege-escalation surfaces; no spoofing.)
</threat_model>

<verification>
- All three insertions present verbatim per the literal-string greps in `<acceptance_criteria>`
- Five existing section headings still present (no accidental deletion)
- Pre-existing `MilestoneState` example and **Surfaced by** footer unchanged
- One atomic commit `docs(46.1-03): ...` touches exactly one file: `.planning/PLAN-AUTHORING-PATTERNS.md`
- No code or test files modified (`git show --name-only HEAD | grep -cE "^src/"` returns `0`)
- No AI-attribution footer in commit message
- `npm test` not run (docs-only); no impact on test gate
</verification>

<success_criteria>
1. `PLAN-AUTHORING-PATTERNS.md` "Cheap verifications on load-bearing audit assumptions" entry contains the bold admonition immediately after **Problem.** paragraph
2. Phase 46 example block (referencing `ProcurementStatus`, the 7-value enum, and the gap-3 UAT rejection) inserted after the existing `MilestoneState` example
3. Checklist nudge inserted at the top of **Default to ship.** subsection
4. Five existing section headings + all pre-existing prose unchanged (additive edit only)
5. One atomic commit lands a single docs file with no AI attribution per project convention
</success_criteria>

<output>
After completion, create `.planning/phases/46.1-merge-gate-gap-closure/46.1-03-SUMMARY.md` capturing:
- The atomic commit hash + diff stats (+~25 lines, 0 deletions in `.planning/PLAN-AUTHORING-PATTERNS.md`)
- Confirmation all three D-3 insertions are present verbatim (cite the literal-string grep output for each)
- Note for the gap-closure-finalization step that parent UAT `46-UAT.md` gap-3 should flip `status: failed` → `status: resolved`
- Reminder: the deferred design conversation about re-toning `pending` palette (Finding 3b) is explicitly OUT of 46.1 scope per CONTEXT.md and remains open as a future polish item
</output>
