---
phase: 46.1-merge-gate-gap-closure
verified: 2026-04-29T13:30:00Z
status: gaps_found
score: 17/18 round-4 truths verified (CR-R4-01 BLOCKER on MSO-in-style); 23/23 round-1+2 truths still hold; round-5 D-21 close-out gate blocked by CR-R4-01
overrides_applied: 0
re_verification:
  round: 4
  previous_status: gaps_found
  previous_score: 23/23 automated; 3/6 human; 2 new gaps (gap-6, gap-7)
  previous_verified: 2026-04-29T13:00:00Z
  rounds:
    - round: 1
      verified: 2026-04-28T17:00:00Z
      status: human_needed
      score: 12/12
      scope: "Plans 46.1-01 (gap-1), 46.1-02 (gap-2), 46.1-03 (gap-3) -- eaea038, 9b5cb08, 889477e"
    - round: 2
      verified: 2026-04-28T21:15:00Z
      status: human_needed
      score: 23/23
      scope: "Round-1 plans re-confirmed + plans 46.1-04 (gap-4, f867da6) and 46.1-05 (gap-5, 14dabb9)"
    - round: 3
      verified: 2026-04-29T13:00:00Z
      status: gaps_found
      score: "23/23 automated; round-3 human re-test 3 of 6 passed; surfaced gap-6 (procurement layout) + gap-7 (Outlook Mac dark-mode persists)"
      scope: "Round-3 visual UAT replay -- plans 46.1-06/07/08 closed gap-6/gap-7; round-3 code review left CR-R3-01 BLOCKER + WR-R3-01..04 + IN-R3-01..03 open"
    - round: 4
      verified: 2026-04-29T13:30:00Z
      status: gaps_found
      score: "17/18 round-4 truths verified; CR-R4-01 BLOCKER (MSO conditional inside <style> element does not activate Outlook conditionals); D-21 close-out gate requires findings.blocker == 0"
      scope: "Plan 46.1-09 (commit b75ab0b) -- CR-R3-01 className-binding fix + WR-01..06 round-2 carryovers + WR-R3-02/04 + IN-R3-02 quality items + D-15..D-20 atomic commit"
  gaps_closed:
    - "CR-R3-01 (round-3 BLOCKER -- dead className-hook palette) -- closed by D-15 className binding on Procurement <span> + EmailButton CTA; verified by snapshot grep returning class=\"pill-ordered\", class=\"pill-in-transit\", class=\"pill-delivered\", class=\"cta-terracotta\" (4 pill + 5 CTA matches in SendUpdate snap; cta-gold confirmed in WorkOrder snap)"
    - "WR-01/02 (round-2 -- stripLeadingGreeting tightening): Body.tsx:35 takes clientFirstName parameter; case-insensitive exact-match check at line 40"
    - "WR-03 (round-2 -- 600-char enforcement): personalNoteMarkdown.ts:28 exports MAX_LEN; SendUpdateModal.tsx:9/459/499 import + maxLength + counter"
    - "WR-04 / D-17 (round-2 + round-3 -- [data-ogsc] * catch-all deletion): grep returns 0 in EmailShell.tsx"
    - "WR-06 (round-2 -- pill borderRadius): grep returns 0 in Procurement.tsx"
    - "WR-R3-02 (round-3 -- test name mismatch): EmailShell.test.ts rewritten to className-binding shape, 63 tests passing (was 29)"
    - "WR-R3-04 (round-3 -- valign:top magic-number floor): SendUpdate.test.ts uses bodyRowCount per-row invariant (18 occurrences); old toBeGreaterThanOrEqual(9) gone (0 matches)"
    - "IN-R3-02 (round-3 -- palette duplication): EmailShell.tsx buildPillRules() generates [data-ogsc]/[data-ogsb]/@media pill rules at module-load time from STATUS_PILL_STYLES (single source of truth)"
    - "D-16 dead-rule deletion: [data-ogsc] .text-* and .border-cream-dark grep returns 0"
  gaps_remaining:
    - "WR-05 (round-2 -- MSO conditional placement): NEW BLOCKER CR-R4-01 -- conditional was moved out of invalid <div style={display:none}> wrapper, but its new placement INSIDE a sibling <style> element is also non-functional (CSS context consumes <!-- as CDO/CDC tokens; mso-color-scheme directive never reaches Outlook word-engine)"
    - "gap-4 layered defense: MSO conditional layer was specifically targeting older Outlook desktop builds where [data-ogs*] doesn't reach -- this layer is broken even though [data-ogsc]/[data-ogsb] + @media + className binding layers all hold"
  gaps_surfaced_in_round_4:
    - "CR-R4-01 BLOCKER (MSO-in-<style>-element)"
    - "WR-R4-01 WARNING (.bg-cream selector tail still emitted in two surviving rules)"
    - "WR-R4-02 WARNING (SendUpdate.tsx:173 clientFirstName ?? '' coercion silently disables strip when caller passes undefined firstName)"
    - "IN-R4-01 INFO (EmailShell.test.ts MSO substring assertions still pass against non-functional placement -- false confidence)"
    - "IN-R4-02 INFO (Body.test.ts missing regression guard for empty clientFirstName)"
    - "IN-R4-03 INFO (buildPillRules indent inconsistency in rendered CSS)"
  regressions: []
  round_4_truths_added: 18
  round_4_truths_verified: 17
gaps:
  - truth: "MSO conditional sits in HTML context (so Outlook desktop's HTML parser interprets <!--[if mso]> + <![endif]--> as conditional comment boundaries)"
    status: failed
    reason: "Round-4 fix for WR-05 moved the MSO conditional out of an invalid <div style={display:none}> wrapper into a sibling <style dangerouslySetInnerHTML> element. Both SendUpdate snap (line 123) and WorkOrder snap (line 87) show the rendered output as <style><!--[if mso]>body { mso-color-scheme: light !important; }<![endif]--></style> -- the conditional sits INSIDE a <style> element where CSS parsing rules apply. Per CSS Syntax Level 3 §4.3.2, <!-- and --> are consumed as CDO/CDC tokens; the [if mso]>body { mso-color-scheme: light !important; } payload parses as a CSS rule with selector [if=mso] > body that matches no element. The mso-color-scheme directive that was the whole point of the WR-05 carryover never reaches Outlook desktop's word-engine renderer. The MSO layer of the gap-4 layered defense (originally targeting older Outlook builds where [data-ogs*] doesn't reach) is non-functional even though the [data-ogsc]/[data-ogsb] + @media (prefers-color-scheme: dark) + className-binding layers are all intact and load-bearing."
    artifacts:
      - path: src/emails/shell/EmailShell.tsx
        issue: "Lines 166-170 wrap the MSO conditional payload in a <style dangerouslySetInnerHTML> element. The HTML parser hands the contents to the CSS tokenizer; the conditional comment markers + the mso-color-scheme directive are all consumed as inert CSS tokens."
      - path: src/emails/sendUpdate/__snapshots__/SendUpdate.test.ts.snap
        issue: "Line 123 shows the broken-context output: </style><style><!--[if mso]>body { mso-color-scheme: light !important; }<![endif]--></style></head>. Identical pattern in src/emails/workOrder/__snapshots__/WorkOrder.test.ts.snap line 87. The snapshot regen confirms the BLOCKER reproduces in actual rendered HTML."
      - path: src/emails/shell/EmailShell.test.ts
        issue: "Lines 90-103 (and 248-258 per round-4 review) assert <!--[if mso]>, <![endif]-->, and mso-color-scheme: light substrings appear in rendered HTML. All three substrings DO appear inside the broken <style> element, so the tests pass green against a non-functional conditional. IN-R4-01: false confidence -- same category-error as round-3 hex-substring assertions on dead pill rules. After CR-R4-01 is fixed, the assertion must be tightened to lock the conditional in HTML context (see review §CR-R4-01 verification block)."
    missing:
      - "Move the MSO conditional out of the sibling <style dangerouslySetInnerHTML> element. Recommended approach (Option A from round-4 review): place the conditional as a child of <Body> via dangerouslySetInnerHTML on a <div>, wrapping its payload in a nested <style> block (e.g. <!--[if mso]><style>body { mso-color-scheme: light !important; }</style><![endif]-->). The conditional now sits in HTML context where Outlook's parser interprets it; the inner <style> is parsed as CSS only when the MSO conditional activates."
      - "Tighten EmailShell.test.ts MSO assertions per IN-R4-01 (review §CR-R4-01 verification block): assert that no unclosed <style> element is open at the position of the <!--[if mso]> opener. Suggested test shape: const condIdx = html.indexOf('<!--[if mso]>'); const before = html.slice(0, condIdx); const lastStyleOpen = before.lastIndexOf('<style'); const lastStyleClose = before.lastIndexOf('</style>'); expect(lastStyleClose).toBeGreaterThan(lastStyleOpen). Replace the location-agnostic substring assertions with this context-locked shape so a future re-occurrence of the BLOCKER does not pass green again."
      - "Regenerate SendUpdate + WorkOrder snapshots in lockstep with the source-edit so the broken <style><!--[if mso]>...<![endif]--></style></head> region disappears from snap lines 123/87."
      - "Re-run /gsd-code-review 46.1 against the post-fix snapshot to confirm findings.blocker == 0 (CR-R4-01 closed; D-21 close-out gate satisfied). After clean review, parent UAT 46-UAT.md replays for round-5 Liz visual re-test on Outlook for Mac dark + Windows dark + web dark + light-mode regression check."
human_verification:
  - test: "Round-5 close-out (after CR-R4-01 closes): Trigger a real Send Update + Work Order to liz@lasprezz.com from staging; open in Outlook for Mac dark mode, Outlook for Windows dark mode, Outlook web dark mode, plus light-mode regression. Verify procurement pill chrome (bg + text + border) renders with locked colors with no half-inversion in dark mode; terracotta CTA #C4836A in SendUpdate; gold CTA #9A7B4B in WorkOrder; gap-1 / gap-2 / gap-4 / gap-5 / gap-6 / gap-7 fixes from prior rounds still render correctly; admin compose UI shows live character counter under personalNote textarea + maxLength enforcement (typing past 600 chars stops input)."
    expected: "All locked palette colors render correctly in dark + light Outlook desktop + web; no contrast inversion; admin character counter live + capped at 600"
    why_human: "Outlook desktop's auto-darken pass + word-engine MSO interpretation + Outlook for Mac stripped <style> rendering can only be observed in real Outlook clients. Per CONTEXT.md D-21 -- on clean code review (CR-R4-01 closed), parent UAT replays for round-5 Liz visual re-test. NOT in scope for this verification because CR-R4-01 BLOCKER must close first."
  - test: "Round-5 close-out approval: Liz replies `approved` on parent plan 46-03 to flip parent UAT frontmatter from `status: diagnosed` (artifact-level) to actually closed and unblock Phase 46 cutover."
    expected: "Approval reply on plan 46-03 closes Phase 46; v5.3 unblocks"
    why_human: "Procedural human gate per CONTEXT.md D-9 / D-21. Cannot trigger until CR-R4-01 closes and visual UAT passes."
---

# Phase 46.1: Merge-Gate Gap Closure — Verification Report (Round 4)

**Phase Goal:** Resolve the three findings from the Phase 46 Outlook desktop merge-gate UAT on 2026-04-28 so plan 46-03 (cutover) can close. After 46.1 commits, the parent UAT artifact replays — Liz re-tests in Outlook desktop and on `approved` Phase 46 closes.

**Verified:** 2026-04-29T13:30:00Z
**Status:** gaps_found
**Re-verification:** Yes — Round 4 (focused on commit b75ab0b for plan 46.1-09; round-1/2/3 truths re-confirmed)

---

## Round-1+2+3 Re-Confirmation (Plans 01-08)

The 23 truths confirmed at round 2 (12 round-1 + 11 round-2) still hold at round 4:

- **SC-1 (greeting double-render):** `Body.tsx:35` `stripLeadingGreeting(personalNote, clientFirstName)` exists; `:54` calls it before `parsePersonalNote`. Round-4 plan 09 TIGHTENED this with the clientFirstName exact-match check (WR-01/02), making it more conservative — gap-1 still closed.
- **SC-2 (Invalid Date):** `SendUpdate.tsx:104-114` `formatDate("")` returns "" via `if (!d) return ""` guard + `isNaN(date.getTime())` fallback; `formatLongDate` mirrored on lines 111-115. Unchanged from round 1.
- **SC-3 (PLAN-AUTHORING-PATTERNS):** `PLAN-AUTHORING-PATTERNS.md:163` "Cheap verifications on load-bearing audit assumptions" entry present with bold admonition + Phase 46 ProcurementStatus example at line 171 + checklist nudge.
- **Round-2 (gap-4 dark-mode lock):** EmailShell.tsx still has [data-ogsc]/[data-ogsb] block + @media (prefers-color-scheme: dark) block, now generated via `buildPillRules()` from STATUS_PILL_STYLES (D-18 IN-R3-02 single source of truth).
- **Round-2 (gap-5 alignment):** Milestones.tsx + Procurement.tsx still all `align="left"`; ReviewItems.tsx still untouched per D-7 scope rule.
- **Round-3 (gap-6 procurement layout + gap-7 Outlook-for-Mac):** plans 46.1-06/07/08 closed those gaps; their fixes survive into round 4 unchanged (no regression introduced by the b75ab0b atomic commit).

No regressions. The atomic-commit scope was tightly bounded per D-20 to the 12 files listed in `46.1-09-SUMMARY.md`.

---

## Round-4 Goal Achievement (Plan 46.1-09 — commit b75ab0b)

### Observable Truths — Round 4

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | Procurement pill rendered HTML carries `class="pill-${status}"` (D-15) | VERIFIED | `Procurement.tsx:121` = `<span className={\`pill-${it.status}\`} style={pillStyle(it.status)}>`; snapshot grep `class="[^"]*pill-` returns 4 matches across `pill-ordered` / `pill-in-transit` / `pill-delivered` |
| 2 | EmailButton CTA rendered HTML carries `class="cta-${variant}"` (D-15) | VERIFIED | `EmailButton.tsx:40` = `className={\`cta-${variant}\`}`; SendUpdate snap returns 5 `class="cta-terracotta"` matches; WorkOrder snap returns 2 `class="cta-gold"` matches |
| 3 | EmailShell.tsx no longer contains `[data-ogsc] .text-*` per-class rules (D-16) | VERIFIED | `grep -cE '\[data-ogsc\] \.text-(stone\|stone-light\|charcoal\|dark\|mid\|cream\|cream-ivory)' EmailShell.tsx` returns 0 |
| 4 | EmailShell.tsx no longer contains `[data-ogsc] .border-cream-dark` rule (D-16) | VERIFIED | `grep -cE '\[data-ogsc\] \.border-cream-dark' EmailShell.tsx` returns 0 |
| 5 | EmailShell.tsx no longer contains `[data-ogsc] *` catch-all (D-17) | VERIFIED | `grep -cE '\[data-ogsc\] \*' EmailShell.tsx` returns 0 |
| 6 | Procurement.tsx pillStyle() no longer sets borderRadius (WR-06) | VERIFIED | `grep -cE 'borderRadius\|border-radius' Procurement.tsx` returns 0 |
| 7 | personalNoteMarkdown.ts exports MAX_LEN as named export (WR-03) | VERIFIED | Line 28: `export const MAX_LEN = 600;` |
| 8 | SendUpdateModal.tsx textarea has maxLength={MAX_LEN} + character counter (WR-03) | VERIFIED | Line 9 imports `MAX_LEN as PERSONAL_NOTE_MAX_LEN`; line 459 `maxLength={PERSONAL_NOTE_MAX_LEN}`; line 499 `{personalNote.length} / {PERSONAL_NOTE_MAX_LEN} characters` |
| 9 | stripLeadingGreeting takes clientFirstName parameter and only strips on case-insensitive match (WR-01/02) | VERIFIED | Body.tsx:35 `export function stripLeadingGreeting(personalNote: string, clientFirstName: string): string`; line 40 `if (capturedFirstName.toLowerCase() === clientFirstName.toLowerCase())` |
| 10 | EmailShell.tsx generates pill rules at module-load time from STATUS_PILL_STYLES (D-18 IN-R3-02) | VERIFIED | Line 41 `import { STATUS_PILL_STYLES } from "../../lib/procurement/statusPills"`; line 58 `function buildPillRules(prefix, scope)`; lines 100-103 four invocations cover [data-ogsb] bg, [data-ogsc] text, [data-ogsc] border, @media all |
| 11 | **EmailShell.tsx no longer injects MSO conditional via `<div style={display:none}>` in `<head>`; payload lives in a sibling `<style dangerouslySetInnerHTML>` instead (WR-05)** | **FAILED** | **Lines 166-170 show MSO conditional inside `<style dangerouslySetInnerHTML>`. The `<div style={display:none}>` wrapper is gone (`grep -cF '<div' EmailShell.tsx` returns 0), but the new placement is itself non-functional per CR-R4-01 — `<!-- -->` are consumed as CSS CDO/CDC tokens, the directive never reaches Outlook's word-engine. See gaps section.** |
| 12 | EmailShell.test.ts has className-binding assertions for each procurement status pill + each CTA variant (D-18 WR-R3-02) | VERIFIED | EmailShell.test.ts contains `pill-(scheduled\|warehouse\|in-transit\|ordered\|pending\|delivered\|installed)` test code references covering all 7 status keys; 63/63 tests passing (was 29 pre-46.1-09 = +34 className-binding-shape tests) |
| 13 | SendUpdate.test.ts:506-527 valign:top floor uses per-row invariant (D-18 WR-R3-04) | VERIFIED | `grep -cF 'bodyRowCount' SendUpdate.test.ts` returns 18 occurrences (4 tests + setup); `grep -cE 'toBeGreaterThanOrEqual\(9\)' SendUpdate.test.ts` returns 0 (magic-number-9 floor gone) |
| 14 | Body.test.ts covers hyphenated firstName, "Hi all," negative, case-insensitive match (D-19 WR-01/02) | VERIFIED | Body.test.ts:103 `Mary-Anne` hyphenated test; line 111 `does NOT strip 'Hi all,' when clientFirstName='Sarah'`; line 121 case-insensitive `Hi sarah,` with clientFirstName=Sarah; 19/19 tests passing (was 12 = +7 new tests) |
| 15 | All existing 46.1-04 lock-marker assertions still pass (rewritten to className-binding shape) | VERIFIED | EmailShell.test.ts 63/63 passing — no regression vs 46.1-04 lock contract; assertion shape changed (D-18 #1) but behavioral guarantees preserved |
| 16 | Existing D-7 / D-12-13 / SendUpdate snapshot / WorkOrder snapshot tests still pass | VERIFIED | SendUpdate.test.ts 50/50 passing; Body.test.ts 19/19 passing; full-suite test run shows 1327 passed (was 1287 pre-46.1-09) — net +40 passing tests |
| 17 | npm test exits with green-or-baseline-only failures (zero NEW failures) | VERIFIED | Full-suite run: 1454 total / 1327 passed / 59 failed / 68 todo. Baseline = 59 failures (per CONTEXT.md D-18 + deferred-items.md D-pre-46 / D-pre-46-wo / WorkOrder snapshot date-drift). Zero NEW failures introduced by b75ab0b. |
| 18 | Single atomic commit lands all files; no AI attribution | VERIFIED | `b75ab0b` is the single fix commit (12 files: 10 D-20 + SendUpdate.tsx callsite + WorkOrder snap); `git show --pretty=%B b75ab0b \| grep -ciE 'co-authored-by\|generated with claude\|claude code\|🤖'` returns 0 |

**Round-4 Score:** 17/18 truths verified. The single failed truth (#11) is the CR-R4-01 BLOCKER detailed below.

**Combined Round-1+2+3+4 Score:** 40/41 truths verified.

---

### Required Artifacts — Round 4

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `src/emails/sendUpdate/Procurement.tsx` | className-bound pill chrome (D-15) + borderRadius removed (WR-06) | VERIFIED | Line 121 carries `className={\`pill-${it.status}\`}`; `grep borderRadius` returns 0 |
| `src/emails/shell/EmailButton.tsx` | className-bound CTA variant (D-15) | VERIFIED | Line 40 = `className={\`cta-${variant}\`}` |
| `src/emails/shell/EmailShell.tsx` | Dead rules deleted (D-16/D-17), MSO out of div-in-head (WR-05), CSS-gen at module-load (D-18 IN-R3-02) | PARTIAL | D-16/D-17 deletions verified (grep=0); buildPillRules() present and wired (4 invocations); WR-05 div-removal verified (grep=0). HOWEVER: MSO replacement placement (lines 166-170) is non-functional per CR-R4-01 — sibling `<style dangerouslySetInnerHTML>` puts the conditional in CSS context. Artifact exists, edit landed, but the WR-05 fix introduces a new BLOCKER. |
| `src/emails/shell/EmailShell.test.ts` | Test suite rewritten to className-binding shape (D-18 WR-R3-02); existing gap-4/gap-7 lock markers asserted via new shape | PARTIAL VERIFIED | 63/63 tests passing (was 29; +34 tests). All 7 procurement status keys referenced (pill-scheduled..pill-installed). HOWEVER: per IN-R4-01 the MSO substring assertions at lines 90-103 / 248-258 still pass against the broken-context placement — false confidence. Test surface needs tightening alongside CR-R4-01 fix. |
| `src/lib/email/personalNoteMarkdown.ts` | MAX_LEN exported as named symbol (WR-03) | VERIFIED | Line 28: `export const MAX_LEN = 600;` |
| `src/components/admin/SendUpdateModal.tsx` | maxLength={MAX_LEN} + character counter (WR-03) | VERIFIED | Lines 9, 459, 499 |
| `src/emails/sendUpdate/Body.tsx` | stripLeadingGreeting tightened to clientFirstName exact-match (WR-01/02) | VERIFIED | Lines 35, 40 |
| `src/emails/sendUpdate/Body.test.ts` | Hyphenated/Hi all/case-insensitive tests (WR-01/02) | VERIFIED | Lines 103, 111, 121 |
| `src/emails/sendUpdate/SendUpdate.test.ts` | bodyRowCount per-row invariant (D-18 WR-R3-04) | VERIFIED | 18 occurrences; magic-number-9 floor gone |
| `src/emails/sendUpdate/__snapshots__/SendUpdate.test.ts.snap` | Regenerated with className additions + borderRadius removal + CSS-gen output + MSO relocation | PARTIAL VERIFIED | 4 `class="pill-*"` + 5 `class="cta-terracotta"` matches confirm D-15 binding landed. HOWEVER: line 123 shows `</style><style><!--[if mso]>body { mso-color-scheme: light !important; }<![endif]--></style></head>` — the broken MSO-in-style placement reproduces in the rendered snapshot, confirming CR-R4-01 reaches the wire. |
| `src/emails/workOrder/__snapshots__/WorkOrder.test.ts.snap` | Regenerated; cta-gold className binding visible | PARTIAL VERIFIED | 2 `class="cta-gold"` matches confirm D-15 binding for WorkOrder. Same broken MSO-in-style placement at line 87 — CR-R4-01 reproduces in WorkOrder render path too. |

---

### Key Link Verification — Round 4

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| `EmailShell.tsx` | `statusPills.ts` STATUS_PILL_STYLES | `buildPillRules()` imports + generates [data-ogsc]/[data-ogsb]/@media pill rules at module-load | VERIFIED | Line 41 import; lines 58-70 buildPillRules definition; lines 100-103 four invocations |
| `SendUpdateModal.tsx` | `personalNoteMarkdown.ts` MAX_LEN | imports MAX_LEN to set textarea maxLength + counter | VERIFIED | Line 9 `import { MAX_LEN as PERSONAL_NOTE_MAX_LEN } from "../../lib/email/personalNoteMarkdown"` |
| `Procurement.tsx` `<span>` | EmailShell.tsx `[data-ogsc]/.[data-ogsb]/@media` pill rules | className `pill-${it.status}` colocated with inline style; rules in EmailShell `<head>` re-pin under auto-darken | VERIFIED for [data-ogsc]/[data-ogsb] + @media layers; BLOCKED for MSO layer | className binding lands; per-class !important rules in [data-ogsc] block now bind because className strings flow through @react-email/tailwind compilation unchanged. The [data-ogsb]/[data-ogsc]/@media layers are load-bearing and verified. The MSO conditional layer (originally targeting older Outlook builds where [data-ogs*] doesn't reach) is broken per CR-R4-01. |
| `EmailButton.tsx` `<a>` | EmailShell.tsx `.cta-terracotta` / `.cta-gold` rules | className `cta-${variant}` flows through Tailwind compile + per-class lock rules re-pin in dark mode | VERIFIED | className landed in both SendUpdate (cta-terracotta) and WorkOrder (cta-gold) snaps |

---

### Data-Flow Trace (Level 4) — Round 4

| Artifact | Data Variable | Source | Produces Real Data | Status |
|----------|---------------|--------|--------------------|--------|
| `Procurement.tsx` pill `<span>` | `it.status` (typed `ProcurementStatus`) | `STATUS_PILL_STYLES[status]` lookup; className templated from same enum | className flows through @react-email/tailwind compilation as literal class string (verified by snapshot grep) | FLOWING |
| `EmailButton.tsx` `<a>` | `variant` (typed `"terracotta" \| "gold"`) | className templated from same prop | className flows through compilation; both variants confirmed in snaps | FLOWING |
| `EmailShell.tsx` buildPillRules output | Static CSS generated at module-load from STATUS_PILL_STYLES | Token records → CSS string interpolation | All 7 statuses × bg/text/border emitted in [data-ogsc]/[data-ogsb] + @media blocks; byte-identical to STATUS_PILL_STYLES values | FLOWING |
| `SendUpdateModal.tsx` character counter | `personalNote.length` | useState in modal; updated on each keystroke | Counter renders live count; maxLength on textarea caps input at 600 | FLOWING |
| `EmailShell.tsx` MSO conditional payload | Static CSS (`mso-color-scheme: light !important`) | Hardcoded in `<style dangerouslySetInnerHTML>` payload | Payload reaches the rendered HTML but Outlook's word-engine never parses it as a conditional comment because the `<style>` element wrapper puts it in CSS context (CR-R4-01) | DISCONNECTED — the directive is emitted but never reaches the consumer |

The MSO conditional row above is the data-flow signature of the BLOCKER: the artifact exists, the wiring is structurally present (commit b75ab0b emits the substring), but the consumer (Outlook desktop word-engine) never receives the directive in a parseable form. This is the same category-error CR-R3-01 documented for the dead pill-class rules — the byte appears in the wire, but not in a context where the consumer interprets it.

---

### Behavioral Spot-Checks — Round 4

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| EmailShell.test.ts 63 tests pass | `npx vitest run src/emails/shell/EmailShell.test.ts` | 63 passed | PASS |
| Body.test.ts 19 tests pass (was 12 = +7 new round-4 tests) | `npx vitest run src/emails/sendUpdate/Body.test.ts` | 19 passed | PASS |
| SendUpdate.test.ts 50 tests pass | `npx vitest run src/emails/sendUpdate/SendUpdate.test.ts` | 50 passed | PASS |
| Full suite: 59-failure baseline preserved (no NEW failures) | `npx vitest run` | 1454 total / 1327 passed / 59 failed / 68 todo | PASS |
| Net delta vs pre-46.1-09: +40 passing tests | `git diff --stat b19e39a..b75ab0b` test-file LOC | +40 passing | PASS |
| `class="pill-*"` count in SendUpdate snap | `grep -cE 'class="[^"]*pill-' SendUpdate.test.ts.snap` | 4 (D-15 verification gate PASSED) | PASS |
| `class="cta-terracotta"` count in SendUpdate snap | `grep -cE 'class="[^"]*cta-terracotta' SendUpdate.test.ts.snap` | 5 | PASS |
| `class="cta-gold"` count in WorkOrder snap | `grep -cE 'class="[^"]*cta-gold' WorkOrder.test.ts.snap` | 2 | PASS |
| `[data-ogsc] *` count in EmailShell.tsx | `grep -cE '\[data-ogsc\] \*' EmailShell.tsx` | 0 (D-17 verified) | PASS |
| `[data-ogsc] .text-*` count | `grep -cE '\[data-ogsc\] \.text-' EmailShell.tsx` | 0 (D-16 verified) | PASS |
| `[data-ogsc] .border-cream-dark` count | `grep -cE '\[data-ogsc\] \.border-cream-dark' EmailShell.tsx` | 0 (D-16 verified) | PASS |
| `borderRadius` count in Procurement.tsx | `grep -cE 'borderRadius\|border-radius' Procurement.tsx` | 0 (WR-06 verified) | PASS |
| `<div` count in EmailShell.tsx (post-WR-05 div removal) | `grep -cF '<div' EmailShell.tsx` | 0 (display-none div wrapper removed) | PASS |
| **MSO conditional sits OUTSIDE `<style>` element in rendered HTML** | inspect SendUpdate snap line 123 + WorkOrder snap line 87 | **`</style><style><!--[if mso]>body { mso-color-scheme: light !important; }<![endif]--></style></head>` -- conditional sits INSIDE a `<style>` element wrapper** | **FAIL — CR-R4-01 BLOCKER reproduces in actual rendered HTML** |
| `bodyRowCount` count in SendUpdate.test.ts | `grep -cF 'bodyRowCount' SendUpdate.test.ts` | 18 (per-row invariant landed) | PASS |
| `toBeGreaterThanOrEqual(9)` count in SendUpdate.test.ts | `grep -cE 'toBeGreaterThanOrEqual\(9\)' SendUpdate.test.ts` | 0 (magic-number-9 floor gone) | PASS |
| All 7 pill status keys referenced in EmailShell.test.ts | `grep -oE 'pill-(scheduled\|warehouse\|in-transit\|ordered\|pending\|delivered\|installed)' EmailShell.test.ts \| sort -u \| wc -l` | 7 | PASS |
| No AI attribution in commit b75ab0b | `git show --pretty=%B b75ab0b \| grep -ciE 'co-authored-by\|generated with claude\|claude code\|🤖'` | 0 | PASS |
| Atomic commit (single fix commit b75ab0b) | `git log b19e39a..HEAD --oneline -- src/` | b75ab0b sole src/ commit (others are docs) | PASS |
| 12 files in commit b75ab0b | `git show --name-only b75ab0b \| grep -vE '^$' \| wc -l` | 12 (10 D-20 + SendUpdate.tsx callsite + WorkOrder snap) | PASS |

---

### Requirements Coverage

No REQUIREMENTS.md numbered IDs claimed for this phase. The 9 plans of 46.1 declare `requirements: []` and instead carry round-/finding-references (gap-1..gap-7, CR-R3-01, WR-01..06, WR-R3-01..04, IN-R3-01..03, D-15..D-21). This is the established pattern for gap-closure phases (decimal/polish phases that close findings from a parent UAT, not REQUIREMENTS.md-driven feature work).

REQUIREMENTS.md `grep -E "Phase 46\.1|46\.1-"` returns 0 hits — REQUIREMENTS.md does not reference this decimal phase. No orphaned requirements.

Round-4 is verified against the round-4 finding set (CR-R3-01 + WR-01..06 + WR-R3-02/04 + IN-R3-02 per `46.1-09-PLAN.md` `requirements_addressed`). 8 of 9 close cleanly; CR-R3-01 closes (the one BLOCKER from round-3) but the WR-05 fix introduces a new BLOCKER CR-R4-01 — net round-4 BLOCKER count: 1.

---

### Anti-Patterns Found — Round 4

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| `src/emails/shell/EmailShell.tsx` | 166-170 | MSO conditional payload wrapped in a `<style dangerouslySetInnerHTML>` element. The HTML parser hands the contents to the CSS tokenizer; `<!--` / `-->` are consumed as CDO/CDC tokens; the `[if mso]>body { mso-color-scheme: light !important; }<![endif]` payload parses as a CSS rule that matches no element. | BLOCKER | The MSO layer of the gap-4 layered defense is non-functional. Older Outlook desktop builds where `[data-ogs*]` doesn't reach receive no `mso-color-scheme: light` directive. CR-R4-01. |
| `src/emails/shell/EmailShell.test.ts` | 90-103, 248-258 | MSO conditional substring assertions match against the broken-context placement (substrings appear inside the `<style>` element body). | WARNING | False confidence: tests pass green against a non-functional conditional. Same category-error as round-3 hex-substring assertions on dead pill rules. IN-R4-01. After CR-R4-01 closes, assertions must be tightened to lock the conditional in HTML context (no unclosed `<style>` open at `<!--[if mso]>` position). |
| `src/emails/shell/EmailShell.tsx` | 122, 153 | `.bg-cream` selector tail still emitted in two surviving rules: line 122 `[data-ogsb] body, [data-ogsb] .bg-cream { ... }` and line 153 `body, .bg-cream { ... }`. The `.bg-cream` half is dead (Tailwind compiles `className="bg-cream"` to inline style). | WARNING | Same anti-pattern D-16 deleted for `.text-*` rules. The body element selector is load-bearing; the `.bg-cream` tail adds no protection. WR-R4-01 — fix is to delete the dead tail in lockstep with the CR-R4-01 patch. |
| `src/emails/sendUpdate/SendUpdate.tsx` | 173 | `clientFirstName ?? ""` coercion at the SendUpdate boundary. When caller passes `undefined`, `Body` receives `""` → `stripLeadingGreeting("Hi Victoria,...", "")` no-ops because exact-match check fails. | WARNING | If Sanity returns a client without a `firstName` field (data quality issue), gap-1 silently regresses for that recipient. WR-R4-02 — recommended Option C (document constraint + add empty-firstName regression test) per round-4 review. |
| `src/emails/sendUpdate/Body.test.ts` | n/a | Missing regression guard for `stripLeadingGreeting("Hi Victoria,\n\nBody.", "")` — the empty-firstName case described in WR-R4-02. | INFO | Add explicit assertion that empty-firstName disables strip (or, if WR-R4-02 is fixed via Option B with permissive fallback, add as positive case). IN-R4-02. |
| `src/emails/shell/EmailShell.tsx` | 69 | `buildPillRules` `join("\n              ")` (14 spaces) creates inconsistent indent in `@media` block (16-space surrounding indent). | INFO | Cosmetic only — CSS parsers don't care. Snapshot diff readability slightly degraded. IN-R4-03. |

---

### Human Verification Required

Round-5 close-out gate per CONTEXT.md D-21. **Cannot trigger until CR-R4-01 BLOCKER closes** (D-21 explicitly requires `findings.blocker == 0` from `/gsd-code-review 46.1` before parent UAT 46-UAT.md replays for round-5 visual UAT).

#### 1. Round-5 visual UAT (after CR-R4-01 closes)

**Test:** Trigger a real Send Update + Work Order to liz@lasprezz.com from staging. Open in Outlook for Mac dark mode + Outlook for Windows dark mode + Outlook web dark mode + light-mode regression check. Verify:
- Procurement pill chrome (bg + text + border) renders with locked colors — no half-inversion in dark mode
- Terracotta CTA #C4836A bg in SendUpdate; gold CTA #9A7B4B bg in WorkOrder
- gap-1 / gap-2 / gap-4 / gap-5 / gap-6 / gap-7 fixes from prior rounds still render correctly
- Admin compose UI shows live character counter under personalNote textarea + maxLength enforcement (typing past 600 chars stops input)

**Expected:** All locked palette colors render correctly in dark + light Outlook desktop + web; no contrast inversion; admin character counter live + capped at 600.

**Why human:** Outlook desktop's auto-darken pass + word-engine MSO interpretation + Outlook for Mac stripped `<style>` rendering can only be observed in real Outlook clients. Per CONTEXT.md D-21 — on clean code review (CR-R4-01 closed), parent UAT replays for round-5 Liz visual re-test. NOT in scope for this verification because CR-R4-01 BLOCKER must close first.

#### 2. Round-5 close-out approval (after visual UAT passes)

**Test:** Liz replies `approved` on parent plan 46-03 to flip parent UAT frontmatter from `status: diagnosed` to actually closed and unblock Phase 46 cutover.

**Expected:** Approval reply on plan 46-03 closes Phase 46; v5.3 unblocks.

**Why human:** Procedural human gate per CONTEXT.md D-9 / D-21.

---

### Gaps Summary

**One gap blocks D-21 close-out:**

The round-4 plan (commit b75ab0b) closes the round-3 CR-R3-01 BLOCKER (dead className-hook palette) cleanly: D-15 className binding lands in the rendered snapshots, [data-ogsc]/[data-ogsb] + @media + className-binding layers all hold, and the test surface migrates from hex-substring shape to className-binding shape (no more passing-against-dead-CSS).

But the WR-05 fix introduces a regression of the same category-error: the MSO conditional was moved out of an invalid `<div style={display:none}>` wrapper into a sibling `<style dangerouslySetInnerHTML>` element. The new placement avoids the HTML5 head-content violation, but it puts the conditional in CSS-parsing context where `<!--` / `-->` are consumed as CDO/CDC tokens and the `mso-color-scheme: light !important` directive parses as a CSS rule with selector `[if=mso] > body` matching no element. The substring `<!--[if mso]>body { mso-color-scheme: light !important; }<![endif]-->` reaches the wire (visible at SendUpdate snap line 123 and WorkOrder snap line 87) but never reaches Outlook's word-engine as a conditional comment.

This is exactly the failure mode CR-R3-01 documented for the dead pill-class rules — and the failure mode IN-R4-01 flags for the test surface that asserts substring presence rather than HTML-context placement. The MSO layer of the gap-4 layered defense is non-functional even though every other layer is intact.

**Closure shape:** A small follow-on plan (estimate 4 file edits + 1 snapshot regen + 1 atomic commit) per round-4 review §CR-R4-01 Option A:
1. Move the MSO conditional out of `<head>` and into `<Body>` (after `<Preheader>`, before the `<table bgcolor>` wrapper) via `<div dangerouslySetInnerHTML={{ __html: \`<!--[if mso]><style>body { mso-color-scheme: light !important; }</style><![endif]-->\` }} />`. The conditional now sits in HTML context where Outlook's parser interprets the `<!--[if mso]>...<![endif]-->` boundaries; the inner `<style>` is parsed as CSS only when MSO activates.
2. Tighten `EmailShell.test.ts` MSO assertions per IN-R4-01: assert no unclosed `<style>` element is open at the `<!--[if mso]>` position (regex shape provided in round-4 review §CR-R4-01 verification block).
3. Regenerate SendUpdate + WorkOrder snapshots in lockstep so the broken `<style><!--[if mso]>...<![endif]--></style></head>` region disappears from snap lines 123/87.
4. Re-run `/gsd-code-review 46.1` against the post-fix scope. On `findings.blocker == 0`, parent UAT replays for round-5 Liz visual UAT.

**Optional fold-ins** (cheap, scoped to files in the same edit):
- WR-R4-01: delete `.bg-cream` selector tail from EmailShell.tsx:122 and :153 (same anti-pattern as D-16 deletions; one-line edit each).
- WR-R4-02: document `clientFirstName ?? ""` coercion intent at SendUpdate.tsx:173 + add empty-firstName regression test at Body.test.ts (Option C from review).
- IN-R4-02: empty-firstName regression assertion (lands with WR-R4-02 fix above).
- IN-R4-03: ignore (cosmetic snapshot whitespace).

**Test-suite baseline note:** The 59-failure baseline is preserved (`deferred-items.md` D-pre-46 + D-pre-46-wo + WorkOrder snapshot date-drift + adjacent unrelated subsystems). Net delta from b75ab0b: +40 passing tests; 0 new failures. The atomic commit landed in scope per D-20.

**Round-1+2+3 gap closure status (re-confirmed at round 4):**
- gap-1 (greeting double-render): resolved (eaea038, tightened by round-4 WR-01/02)
- gap-2 (Invalid Date): resolved (9b5cb08)
- gap-3 (PLAN-AUTHORING-PATTERNS): resolved (889477e)
- gap-4 (Outlook desktop dark-mode): resolved at the [data-ogsc]/[data-ogsb] + className-binding + @media layers (f867da6 + b75ab0b); MSO sub-layer broken per CR-R4-01 — old Outlook desktop builds where [data-ogs*] doesn't reach are unprotected until CR-R4-01 closes
- gap-5 (left-align table cells): resolved (14dabb9)
- gap-6 (procurement layout): resolved by 46.1-07 (016c41c)
- gap-7 (Outlook for Mac persistence): resolved by 46.1-08 (9f9cd05)

Parent UAT artifact-level statuses for gap-1..gap-7 are all marked resolved/in-progress per `46-UAT.md`; the parent's frontmatter `status: diagnosed` flips to `status: resolved` only on round-5 Liz approval (D-21), which itself is blocked by CR-R4-01.

---

_Verified: 2026-04-29T13:30:00Z_
_Verifier: Claude (gsd-verifier, round 4 -- focused on commit b75ab0b)_
_Round-1 verified: 2026-04-28T17:00:00Z (12/12 truths)_
_Round-2 verified: 2026-04-28T21:15:00Z (23/23 combined)_
_Round-3 verified: 2026-04-29T13:00:00Z (23/23 automated; surfaced gap-6, gap-7)_
_Round-4 verified: 2026-04-29T13:30:00Z (17/18 round-4 truths; CR-R4-01 BLOCKER on MSO-in-style; D-21 close-out blocked)_
