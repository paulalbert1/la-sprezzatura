# Phase 46.1: Merge-Gate Gap Closure - Discussion Log (Round 4)

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in `46.1-CONTEXT.md` (Round 4 section, D-15..D-21) — this log preserves the alternatives considered.

**Date:** 2026-04-29
**Phase:** 46.1-merge-gate-gap-closure
**Round:** 4 (re-opened after `a59cf25` code review surfaced CR-R3-01 BLOCKER)
**Areas discussed:** CR-R3-01 fix strategy, WR-R3-01 catch-all rule, Test surface tightening + IN-R3-02 DRY, Round-2 WR carryovers

---

## CR-R3-01 fix strategy

| Option | Description | Selected |
|--------|-------------|----------|
| Option A: explicit class strings | Add `className=\`pill-${status}\`` to Procurement.tsx pill `<span>`, `className=\`cta-${variant}\`` to EmailButton.tsx `<Button>`. Minimal change. Preserves existing CSS rules. | ✓ |
| Option B: drop class-hook lock | Remove all `.pill-*`, `.cta-*`, `.text-*` rules from EmailShell.tsx. Trust body-bg + `<table bgcolor>` + inline-styled colors + `@media (prefers-color-scheme: dark)` only. Pill text/border colors fall to Outlook auto-darken (the original bug). | |
| Option C: data-attribute hooks | Refactor lock to `data-color-token="stone"` / `data-pill-status="ordered"` attributes. Tailwind compiler does NOT strip data-attributes. Most robust against Tailwind drift. Larger refactor. | |

**User's choice:** Option A.
**Notes:** Smallest viable change that closes the BLOCKER. Verification grep `class="pill-` / `class="cta-` against the regenerated snapshot is part of the plan (catches react-email `<Button>` stripping className if it happens). Option B was rejected because pill text/border falls to Outlook auto-darken — exactly the gap-4 inversion. Option C was rejected as larger refactor without payoff once Option A is verified to survive snapshot.

### Follow-up: Tailwind text class handling

| Option | Description | Selected |
|--------|-------------|----------|
| Delete dead text rules | Remove `[data-ogsc] .text-stone`, `.text-charcoal`, `.text-cream`, `.text-stone-dark`, `.border-cream-dark` rules from EmailShell.tsx. Trust inline + `@media` only. | ✓ |
| Add literal sibling classes | Add `className="text-stone email-text-stone"` everywhere; lock targets `email-text-*`. Larger blast radius. | |
| Defer to follow-up phase | Round-4 ships only pill + CTA fix; dead `.text-*` rules stay as no-ops. | |

**User's choice:** Delete dead text rules.
**Notes:** Tailwind utility classes are stripped from rendered HTML, so the per-class lock cannot bind regardless. Deleting is the honest answer. WorkOrder.tsx text-stone-dark is now intentionally unprotected at the class layer — relies on inline + `@media`. Documented as known risk for round-4 visual UAT in D-16.

---

## WR-R3-01 catch-all rule

| Option | Description | Selected |
|--------|-------------|----------|
| Delete the catch-all | Remove `[data-ogsc] * { color: inherit !important }` from EmailShell.tsx:119. Inline non-`!important` colors survive Outlook desktop dark mode. New per-class pill rules (with `!important`) re-pin pill chrome. | ✓ |
| Keep + force inline `!important` | Retain catch-all; convert every inline `style={{color}}` to use a CSS-string injection that includes `!important` (dangerouslySetInnerHTML or string-literal style attribute). Larger surface, more fragile. | |
| Replace with body-only lock | Swap `[data-ogsc] *` for `[data-ogsc] body { color: #2C2926 !important }`. Sets a sane default text color at body level. Per-class pill rules still re-pin pill colors. | |

**User's choice:** Delete the catch-all.
**Notes:** With D-16's text-rule deletion, no per-class rule re-pins inline text colors after the catch-all overrides them — the catch-all becomes a self-foot-shoot. Deleting lets inline non-`!important` colors win by default, matching the "force light mode" intent.

---

## Test surface tightening + IN-R3-02 DRY

| Option | Description | Selected |
|--------|-------------|----------|
| WR-R3-02 + WR-R3-04 in; IN-R3-02 deferred | Rewrite className-match tests + tighten `>= 9` floor to per-row invariant. Defer build-time CSS generation. | |
| All three in scope | Also land build-time CSS generation from `statusPills.ts` — single source of truth for `[data-ogsc]/[data-ogsb]` and `@media` blocks. Eliminates ~50 lines of mirrored CSS. | ✓ |
| WR-R3-02 only | Round-4 ships the minimum: className-match tests for the BLOCKER. WR-R3-04 brittle floor and IN-R3-02 DRY both deferred. | |

**User's choice:** All three in scope.
**Notes:** Co-located with the EmailShell.tsx + EmailShell.test.ts BLOCKER fix; bundling avoids a second commit cycle for the same file set. Generation runs at module-load time (in-process, no new build step).

---

## Round-2 WR carryovers (multi-select)

User selected ALL four open round-2 WARNINGs to fold into 46.1-09:

| WR | Description | Selected |
|----|-------------|----------|
| WR-03 admin maxLength=600 | Add `maxLength={MAX_LEN}` + character counter to SendUpdateModal `<textarea>`; export MAX_LEN from personalNoteMarkdown.ts. | ✓ |
| WR-05 MSO conditional fix | Move MSO conditional out of `<div>` inside `<head>` and into existing `<style dangerouslySetInnerHTML>`. | ✓ |
| WR-01/02 strip regex tightening | Either widen char class or pass clientFirstName for exact match. | ✓ |
| WR-06 borderRadius shorthand audit | Resolve shorthand-vs-comment mismatch on Procurement.tsx:90 per Phase 46 D-3. | ✓ |

### WR-01/02 follow-up

| Option | Description | Selected |
|--------|-------------|----------|
| Pass clientFirstName, exact match only | Change `stripLeadingGreeting` signature to accept clientFirstName. Only strip when captured word equals it (case-insensitive). Fixes both WR-01 (hyphenated) and WR-02 (`"Hi all,"` elision) simultaneously. | ✓ |
| Widen char class to `[\w'-]+` only | Smaller change; covers Mary-Anne / O'Brien but does NOT fix the `"Hi all,"` elision risk. | |
| Document blind spot + add test | Keep regex; add intentional-blind-spot test. No behavior change. | |

**User's choice:** Pass clientFirstName, exact match only.
**Notes:** Most correct. Tightens strip to the exact double-greeting case CONTEXT.md D-1 actually describes.

### WR-06 resolution (Claude's discretion)

Phase 46 D-3 (re-read in CONTEXT-46): "Keep longhand `border-radius: 2px` on the CTA `<a>` button only ... No other element retains border-radius." The pill `<span>` violates D-3 on two axes (shorthand + wrong element). Resolution captured in D-19: **delete `borderRadius` from `pillStyle()` entirely**, rendering pills as flat-edge rectangles per Phase 46 D-3 intent. If Liz wants 2px back as a design choice, that's a separate polish phase + Phase 46 D-3 amendment.

---

## Claude's Discretion

- **D-18 IN-R3-02 generation timing**: chose module-load (in-process) over build-step / file-write. Rationale: matches the existing `statusPills.ts` import pattern; avoids adding a new generated-file commit gate.
- **D-15 verification gate**: planner re-renders snapshot and greps `class="pill-` / `class="cta-` BEFORE locking the plan. If react-email `<Button>` strips the className, plan pivots to hand-authored `<a>` (not deferred to round-5 review).
- **D-16 WorkOrder.tsx text-stone-dark known risk**: documented as a fall-through condition for round-5 visual UAT. If it inverts in Outlook desktop dark mode, that's a follow-up phase (Option C data-attribute territory), not a round-5 of 46.1.

---

## Deferred Ideas

- **Option C data-attribute lock refactor** — full migration to `data-color-token` / `data-pill-status` attribute-based lock rules. Most robust against Tailwind compiler version drift. Deferred unless round-5 UAT proves Option A insufficient.
- **WorkOrder text-color protection** — if D-16's "delete dead text rules" leaves WorkOrder text inverted in Outlook desktop, a follow-up phase reshapes the lock (likely Option C).
- **Pill border-radius design decision** — if Liz wants 2px radius on pills back, future polish phase + Phase 46 D-3 amendment.
- **Universal cleanup of `border-radius` shorthand across all section components** — D-19 WR-06 audits Greeting/Milestones/etc but only fixes Procurement; if any other shorthand surfaces, planner decides scope at audit time.

---

## Round-3 INFO items not surfaced as gray areas

(These were considered low-priority and not raised; documented here for traceability.)

- **IN-R3-01** Procurement outer Section paddingTop:16 composition smell — sibling sections also own their own top spacing; not unique enough to warrant a refactor in 46.1-09. Defer to v6.0 multi-tenant section-ordering refactor per `MEMORY.md project_admin_pivot.md`.
- **IN-R3-03** body-bg test regex anchor tightening — low practical risk; can roll into D-18's test rewrite if planner finds it cheap, otherwise defer.
- **Round-2 IN-01..07** — test name miscount (IN-01 — superseded by D-18 className rewrite which deletes the affected tests), tenant-leak hardcoded CC string (IN-02 — out of scope, v6.0), milestones initial state (IN-03), modal stale-state risk (IN-04), regex composition (IN-05 — superseded by D-19 WR-01/02 rewrite), `??` empty-string fallthrough (IN-06), array-index keys (IN-07). All deferred.
