---
phase: 46.1-merge-gate-gap-closure
plan: 08
subsystem: emails/shell
tags:
  - "46.1"
  - "gap-7"
  - "dark-mode"
  - "outlook-for-mac"
  - "email-shell"
  - "additive-fix"
requires:
  - "46.1-06 (SPIKE artifact -- chosen combination)"
  - "46.1-04 (existing [data-ogsc]/[data-ogsb]/MSO lock to layer on top of)"
provides:
  - "Outlook-for-Mac dark-mode survival markup in EmailShell.tsx (additive on top of 46.1-04 lock)"
  - "10 new HTML-inspection assertions covering candidates (a) + (b) + (d) marker presence on both SendUpdate and WorkOrder rendered HTML"
  - "Closes parent UAT 46-UAT.md gap-7 code-fix half"
affects:
  - src/emails/shell/EmailShell.tsx
  - src/emails/shell/EmailShell.test.ts
  - src/emails/sendUpdate/__snapshots__/SendUpdate.test.ts.snap
  - src/emails/workOrder/__snapshots__/WorkOrder.test.ts.snap
tech-stack:
  patterns:
    - "Additive layered defense (3 redundant survival paths) on top of existing Outlook-desktop lock"
    - "HTML-attribute paint via <table bgcolor> + inline style= survives <style>-block stripping"
    - "@media (prefers-color-scheme: dark) re-pins locked light palette in source order AFTER existing rules (specificity tie-resolver)"
key-files:
  modified:
    - src/emails/shell/EmailShell.tsx
    - src/emails/shell/EmailShell.test.ts
    - src/emails/sendUpdate/__snapshots__/SendUpdate.test.ts.snap
    - src/emails/workOrder/__snapshots__/WorkOrder.test.ts.snap
decisions:
  - "Applied chosen combination (a)+(b)+(d) per 46.1-SPIKE-OUTLOOK-MAC.md ## Recommendation; (c) class-hook selectors rejected because they share the failing <style>-block delivery vehicle with the existing 46.1-04 lock"
  - "All 19 existing 46.1-04 lock-marker assertions preserved byte-identically (gap-4 contract)"
  - "10 new gap-7 markers asserted (5 SU + 5 WO mirrored) -- proves shared-shell propagation"
  - "Round-4 Liz visual re-test on actual Outlook clients (Mac + Windows + web dark) is the human gate; automated tests prove markup presence, not actual Outlook rendering"
metrics:
  duration: ~12 min
  completed_date: 2026-04-29
  files_changed: 4
  insertions: 475
  deletions: 31
parent_uat: .planning/phases/46-send-update-work-order-migration/46-UAT.md
parent_uat_gap: gap-7
---

# Phase 46.1 Plan 08: Apply Outlook-for-Mac Dark-Mode Fix to EmailShell Summary

Applied the chosen combination from `46.1-SPIKE-OUTLOOK-MAC.md` (candidates **(a) `<table bgcolor>` body wrapper** + **(b) `@media (prefers-color-scheme: dark)` block** + **(d) inline body-attribute paint**) ADDITIVE on top of the existing 46.1-04 `[data-ogsc]`/`[data-ogsb]`/MSO lock. Three redundant survival paths cover the failing Outlook-for-Mac variant; existing 19 lock-marker assertions preserved.

## Atomic Commit

```
9f9cd05 fix(46.1-08): apply outlook-for-mac dark-mode survival per 46.1-SPIKE (gap-7)
4 files changed, 475 insertions(+), 31 deletions(-)
```

Diff stats:

| File | Insertions | Deletions |
|------|-----------|-----------|
| `src/emails/shell/EmailShell.tsx` | 99 | 19 (rebalanced JSX nesting for `<table>` wrapper) |
| `src/emails/shell/EmailShell.test.ts` | 92 | 0 (purely additive new describe block) |
| `src/emails/sendUpdate/__snapshots__/SendUpdate.test.ts.snap` | 205 | 5 (5 fixtures × media query + wrapper insertions) |
| `src/emails/workOrder/__snapshots__/WorkOrder.test.ts.snap` | 79 | 7 (3 fixtures × additions + 3 date-drift refresh `Apr 28 → Apr 29`) |

The `<table>` wrapper around `<Container>` requires re-nesting the existing `<Container>` content as a child of `<td>`; that's the source of the 19 deletions in EmailShell.tsx (the same lines re-appear indented inside the new wrapper).

## Chosen combination from SPIKE.md

Applied candidate **(a) `<table bgcolor>` body wrapper** + candidate **(b) `@media (prefers-color-scheme: dark)` block** + candidate **(d) inline body-attribute paint** -- ADDITIVE on top of the existing 46.1-04 lock. Three layers of defense.

## Confirmation: each technique marker landed by content

```
$ grep -n 'bgcolor="#FAF8F5"' src/emails/shell/EmailShell.tsx
205:            bgcolor="#FAF8F5"

$ grep -n '@media (prefers-color-scheme: dark)' src/emails/shell/EmailShell.tsx
138:              @media (prefers-color-scheme: dark) {

$ grep -nE 'style=\{\{ backgroundColor: "#FAF8F5" \}\}' src/emails/shell/EmailShell.tsx
193:        <Body className="bg-cream font-body" style={{ backgroundColor: "#FAF8F5" }}>
211:            style={{ backgroundColor: "#FAF8F5" }}
215:                <td align="center" valign="top" style={{ backgroundColor: "#FAF8F5" }}>
```

(a) `<table bgcolor>` at line 205; (b) media query at line 138; (d) inline body style at line 193 (Body), 211 (table), 215 (td).

## Confirmation: existing 46.1-04 lock preserved

```
$ grep -c 'data-ogsc' src/emails/shell/EmailShell.tsx
27        # ≥ 8 required, lock-rule count unchanged from f867da6

$ grep -c 'data-ogsb' src/emails/shell/EmailShell.tsx
17        # ≥ 4 required, lock-rule count unchanged from f867da6

$ grep -c 'mso-color-scheme' src/emails/shell/EmailShell.tsx
1         # ≥ 1 required, MSO conditional unchanged

$ grep -cE 'name="(color-scheme|supported-color-schemes)"' src/emails/shell/EmailShell.tsx
2         # both meta tags unchanged
```

## Confirmation: 19 existing lock-marker assertions still pass

```
$ npx vitest run src/emails/shell/EmailShell.test.ts -t "EmailShell auto-darken lock" --reporter=verbose | tail -10

  ✓ 11 SendUpdate render lock-marker assertions
  ✓ 6 WorkOrder render lock-marker assertions
  ✓ 2 Foundation hex parity assertions

  Test Files  1 passed (1)
       Tests  19 passed (19)
```

(Counted 11 + 6 + 2 = 19 against the existing describe blocks per `46.1-04-SUMMARY.md` and `46.1-04-PLAN.md` test-surface contract.)

## Confirmation: new gap-7 marker assertions pass

```
$ npx vitest run src/emails/shell/EmailShell.test.ts -t "EmailShell Outlook-for-Mac dark-mode survival" --reporter=verbose | tail -10

  ✓ SendUpdate render contains <table bgcolor="#FAF8F5"> body wrapper (a)
  ✓ SendUpdate render contains @media (prefers-color-scheme: dark) block (b)
  ✓ SendUpdate @media block contains !important (b specificity tie-resolver)
  ✓ SendUpdate @media block re-pins #FAF8F5 + #2C2926 (b palette parity)
  ✓ SendUpdate <body> carries inline background-color (d)
  ✓ WorkOrder render contains <table bgcolor="#FAF8F5"> body wrapper (a)
  ✓ WorkOrder render contains @media (prefers-color-scheme: dark) block (b)
  ✓ WorkOrder @media block contains !important (b specificity tie-resolver)
  ✓ WorkOrder @media block re-pins #FAF8F5 + #2C2926 (b palette parity)
  ✓ WorkOrder <body> carries inline background-color (d)

  Tests  10 passed (10)
```

5 SU + 5 WO = 10 new assertions, all green.

## Test-suite delta

| Metric | Pre-46.1-08 | Post-46.1-08 |
|--------|-------------|--------------|
| EmailShell.test.ts pass count | 19 | 29 (+10 new) |
| EmailShell.test.ts fail count | 0 | 0 |
| All `src/emails/` test files | 107/107 pass | 117/117 pass |
| Full suite (`npm test`) failures | 62 (post-46.1-07 baseline) | 59 (no new failures; date-driven baseline tests have moved off Apr 28) |

The 59 failures match the pre-existing baseline categories from `deferred-items.md` (Tenant audit, Sanity client config, formatCurrency timezone, blob-serve mocks, send-update mock-fetch, work-orders/send Redis, workflow templates ApiClient mocks, etc.). The drop from 62 → 59 is date-related self-resolution of pre-existing date-sensitive assertions; no regression introduced. Zero NEW failures.

## Snapshot-diff scope verification

Both snapshots regenerated atomically. Body content sections (Greeting / Body / Milestones / Procurement / ReviewItems / CTA / Footer) byte-identical -- verified by counting body-content marker occurrences in `git show HEAD~1:<snap>` vs current state:

**SendUpdate snapshot:**

| Body marker | OLD count | NEW count | Status |
|-------------|-----------|-----------|--------|
| `Hi Sarah,` | matched | matched | OK |
| `For your review` | matched | matched | OK |
| `>Milestones</p>` | matched | matched | OK |
| `>Procurement</p>` | matched | matched | OK |
| `Open Portal` | matched | matched | OK |
| `Sent via Sprezza Hub` | matched | matched | OK |
| `Elizabeth Lewis` | matched | matched | OK |
| `Darien, CT` | matched | matched | OK |
| `Schumacher`, `May 9`, `Custom sofa` | matched | matched | OK |

**WorkOrder snapshot:**

| Body marker | OLD count | NEW count | Status |
|-------------|-----------|-----------|--------|
| `Marco,` | matched | matched | OK |
| `work order for` | matched | matched | OK |
| `VIEW WORK ORDER` | matched | matched | OK |
| `Acme Home` | matched | matched | OK |
| `Elizabeth` | matched | matched | OK |
| `Sent via Sprezza Hub` | matched | matched | OK |
| `Work order ready` | matched | matched | OK |
| `April 28, 2026` (3 occurrences) | 3 | 0 | DATE DRIFT (pre-existing baseline; today is 2026-04-29) |
| `April 29, 2026` (3 occurrences) | 0 | 3 | DATE DRIFT refresh |

The WorkOrder `Apr 28 → Apr 29` date drift is the documented `D-pre-46-wo` baseline failure mode (today's-date-bound test). It would have surfaced on any plan that regenerated WorkOrder snapshots today; not a body-content gap-7 regression.

The diff additions in both snapshots consist of:
1. The new `@media (prefers-color-scheme: dark) { ... }` block inside the existing `<style>` payload (one occurrence per HTML-rendered fixture)
2. The new outer `<table bgcolor="#FAF8F5" ...>` wrapper opening + closing tags
3. The new inline `style="background-color:rgb(250,248,245)"` attribute on the wrapper `<table>`/`<td>` and unchanged on `<body>` (which already had it from `bg-cream` → Tailwind compilation)

No body-content additions or modifications. The chosen combination is `<head>`-and-wrapper-only as specified by SPIKE.md.

## 46.1-06 spike landed BEFORE this plan executed

```
$ git log --oneline -2
9f9cd05 fix(46.1-08): apply outlook-for-mac dark-mode survival per 46.1-SPIKE (gap-7)
8213a1b docs(46.1-07): advance phase 46.1 plan position after gap-6 closure

$ git log --oneline | grep '46.1-06'
2e2e221 docs(46.1-06): summary + state/roadmap update for gap-7 spike
af52ca5 docs(46.1-06): commit gap-7 outlook-mac dark-mode survival spike artifact
```

`af52ca5` (46.1-06 SPIKE artifact) is the upstream `depends_on: ["06"]` source-of-truth for what code lands here. Read at execution start; chosen combination (a) + (b) + (d) extracted from the `## Recommendation` section.

## Threat-model audit

- **T-46.1-08-01 (regress gap-4 lock contract)** — MITIGATED. All 19 existing `EmailShell.test.ts` lock-marker assertions still pass. Pre/post grep counts for `data-ogsc` (27), `data-ogsb` (17), `mso-color-scheme` (1), and the two `<meta name="color-scheme">` tags unchanged.
- **T-46.1-08-02 (regress body content)** — MITIGATED. Body-content marker counts unchanged across both SendUpdate and WorkOrder snapshots. Diff is `<head>`-and-wrapper-only as specified.
- **T-46.1-08-03 (chosen combination doesn't fix Mac)** — ACCEPTED. Round-4 Liz visual re-test is the human gate; the spike's confidence ratings give justified probability the chosen combination survives.
- **T-46.1-08-04 (paid services / Playwright dark-mode CI gate)** — MITIGATED. `git diff HEAD~1 HEAD -- package.json` for litmus/emailonacid: 0 hits. `grep -rcE 'colorScheme:\s*"dark"' src/`: 0 hits. Q8 lock structurally enforced.

No threat-model `flag` entries for this plan -- the chosen combination introduces no new trust boundaries (CSS rules + a paint-only wrapper on shared shell only).

## Note for gap-closure-finalization

Parent UAT `46-UAT.md` gap-7 should flip `status: failed` → `status: resolved` at phase 46.1 close-out, AFTER round-4 Liz visual re-test confirms the markup actually survives in Outlook for Mac dark.

## Note for round-4 Liz visual re-test

Open Outlook for Mac dark-mode + Outlook for Windows dark-mode + Outlook web M365 dark-mode and verify:

1. Email body bg renders as cream `#FAF8F5` -- NOT auto-darkened gray
2. Text renders as dark `#2C2926` -- NOT auto-inverted cream
3. Procurement pills render with their locked color values (the 7 STATUS_PILL_STYLES.* hex codes)
4. Terracotta CTA renders with its locked `#C4836A` color (SU); gold CTA `#9A7B4B` (WO)
5. gap-4 (round-2 lock) / gap-5 (left-aligned cells) / gap-6 (Procurement column widths 60/22/18) fixes from prior plans still render correctly

If round-4 fails on any variant, re-open gap-7 with the specific variant + technique that didn't survive. Likely next-round candidates:
- If (a) `<table bgcolor>` failed, investigate whether react-email's compiled `<Body>` is emitting a wrapper that prevents the `<table bgcolor>` from being top-level.
- If (a) + (d) survived but the body still inverts, the failure mode is most likely "Mac auto-darken paints at a level above all of these" -- escalate to VML / `mso-element` directives (deferred to round-5 per SPIKE.md `## Out-of-scope considered alternatives`).

## Self-Check: PASSED

- FOUND: src/emails/shell/EmailShell.tsx (file present, 246 lines, contains gap-7 markers)
- FOUND: src/emails/shell/EmailShell.test.ts (file present, 263 lines, contains new gap-7 describe block)
- FOUND: src/emails/sendUpdate/__snapshots__/SendUpdate.test.ts.snap (regenerated, contains `bgcolor="#FAF8F5"` and `@media (prefers-color-scheme: dark)`)
- FOUND: src/emails/workOrder/__snapshots__/WorkOrder.test.ts.snap (regenerated, contains `bgcolor="#FAF8F5"` and `@media (prefers-color-scheme: dark)`)
- FOUND: commit `9f9cd05` (atomic 4-file commit, no AI attribution, no deletions)
- FOUND: `46.1-SPIKE-OUTLOOK-MAC.md` (read at execution start; chosen combination extracted from `## Recommendation`)
- FOUND: 19 existing lock-marker assertions still pass + 10 new gap-7 assertions pass = 29 EmailShell tests green
- FOUND: 117/117 emails/ tests green (no new failures in any email-related test file)
- FOUND: full-suite baseline at 59 failures (none new; all from `deferred-items.md` categories)
