---
phase: 46.1-merge-gate-gap-closure
type: context
parent_phase: 46
parent_uat: .planning/phases/46-send-update-work-order-migration/46-UAT.md
status: locked
created: 2026-04-28
---

# Phase 46.1 — Merge-Gate Gap Closure

**Decimal/polish phase.** Closes the three gaps surfaced by the Phase 46 Outlook desktop merge-gate UAT on 2026-04-28. Phase 46 plan 46-03 (cutover) cannot close until these three gaps resolve. After 46.1 commits, the parent UAT artifact replays (Liz re-tests in Outlook desktop) and on `approved` Phase 46 closes properly.

**Locked source of truth:** `46-UAT.md` in the parent phase directory. Read that first — every gap below references its UAT entry directly.

---

## Goal

Resolve the three findings from the parent UAT so Phase 46's merge-gate passes on the next test cycle:

1. **gap-1** — SendUpdate greeting double-render (real bug, code change + admin UX helper)
2. **gap-2** — `formatDate` / `formatLongDate` "Invalid Date" string for empty/invalid input (real bug, one-line guard + parity)
3. **gap-3** — `PLAN-AUTHORING-PATTERNS.md` enum-closure verification strengthening (audit-pattern docs change, not a code bug)

The three are independent — no dependency ordering between plans, can ship in parallel waves.

**Out of scope:** the design conversation about re-toning `pending` procurement palette away from warm-red. That's a separate design thread (46-04 retro / future polish), explicitly NOT covered by 46.1 plans.

---

## Decisions (locked)

### D-1 — Defensive strip for greeting collision is the safety net; admin UX helper is the prevention. Ship both.

**Why:** Defensive strip handles the immediate test-send today and any past Sanity drafts where personalNote starts with "Hi {Name},". Admin UX helper prevents the habit from re-forming. Either alone is insufficient — strip-only leaves users mystified about why their typed greeting "disappears", helper-only fails when users skip past the helper text. Both together are tight.

**Strip pattern (locked):** `^Hi\s+\w+[,!]?\s*\n+`

- Anchored at the very start of personalNote
- Matches "Hi" literal (exact case — never strip "hi", "hello", "hey" — too aggressive)
- Single word after "Hi" (the firstName)
- Optional comma or exclamation point
- One or more trailing newlines (the line that separates greeting from body)

Strips ONE leading match. Does not iterate. Does not strip mid-paragraph occurrences. Does not strip greetings that don't follow the literal "Hi {Name}," shape (e.g., "Dear Victoria,", "Hello Victoria,", "Hi everyone,"). The pattern is deliberately narrow — it only matches the exact shape the structural Greeting component emits, so legitimate prose that happens to start with "Hi {someone}," but is NOT the doubled-greeting case never matches.

**Where the strip lives:** `src/emails/sendUpdate/Body.tsx` (or new helper colocated with Body). NOT in `SendUpdate.tsx` composition root — keep composition root declarative; mutation logic belongs in the section component that owns the personalNote rendering.

**Helper-text copy (locked):** `Don't include "Hi {firstName}," — it's added automatically.`

The helper renders below the personalNote textarea in the admin compose UI. firstName interpolates the live recipient first name when known (compose UI has access to selected client); falls back to literal `Hi {firstName}` when no client is selected yet (e.g., during template-edit before recipient pick).

### D-2 — `formatDate` / `formatLongDate` guard returns empty string `""`, not a placeholder

**Why:** Empty string renders as a quiet empty cell in the procurement table. A placeholder ("—", "TBD", "n/a") commits to a specific UX that's better resolved at the section component level if needed (Procurement.tsx could later choose to render "TBD" for missing ETAs, but that's a section-component decision, not a formatter decision). Formatter's job is "format a date or get out of the way." `""` is the cleanest get-out-of-the-way return.

**Guard order (locked):**
```ts
export function formatDate(d: string | Date): string {
  if (!d) return "";                                // null, undefined, ""
  const date = typeof d === "string" ? new Date(d) : d;
  if (isNaN(date.getTime())) return "";             // unparseable string, etc.
  return date.toLocaleDateString("en-US", { month: "short", day: "numeric" });
}
```

Apply identical guard to `formatLongDate` at the same file. Same return-empty contract.

**Tests (locked):**
- `formatDate("")` → `""`
- `formatDate(undefined as unknown as string)` → `""` (guards against typescript-bypass)
- `formatDate("not-a-date")` → `""`
- `formatDate("2026-05-14")` → `"May 14"` (regression-protect existing behavior)
- `formatDate(new Date("2026-05-14"))` → `"May 14"` (regression-protect Date overload)
- Mirror four tests for `formatLongDate` (same inputs, expect either `""` or "May 14, 2026" / "April 28, 2026" pattern).

### D-3 — Patterns-doc strengthening is docs-only; no code or test changes

**Why:** The audit-completeness gap is a discipline-of-the-pattern issue, not a missing pattern. The "Cheap verifications on load-bearing audit assumptions" entry exists at PLAN-AUTHORING-PATTERNS.md (committed 2026-04-28 at `3b1fc0f`). What's missing is a stronger admonition to apply the pattern to EVERY enum the audit references, not just the most prominent one — this UAT exposed the orchestrator checking `MilestoneState` (2-valued, lossless) but not `ProcurementStatus` (7-valued, partly enumerated in the audit).

**Edit scope (locked):**

In `PLAN-AUTHORING-PATTERNS.md` under the "Cheap verifications on load-bearing audit assumptions" section, add:

1. A bold admonition: *"Apply this pattern to EVERY enum, type, or contract the audit references. The most-prominent assumption is rarely the only assumption."*
2. A new explicit Phase 46 example block referencing the `ProcurementStatus` 7-vs-3 audit gap and the resulting merge-gate UAT rejection — concrete enough that a future reader sees the failure mode, not just an abstract rule.
3. A small checklist nudge at the top of audit-document templates: *"Before approval: list every enum, type, or branded type the audit's claims rest on. Verify each. Failed verifications block approval."*

No code changes. No tests. One file edit. Single commit.

### D-4 — All three plans run in parallel (Wave 1)

**Why:** No file overlap between the three plans:

- gap-1 plan: `src/emails/sendUpdate/Body.tsx` (or new helper) + admin compose UI file (TBD in plan-phase) + tests
- gap-2 plan: `src/emails/sendUpdate/SendUpdate.tsx` (the formatter helpers, lines ~104-112) + tests
- gap-3 plan: `.planning/PLAN-AUTHORING-PATTERNS.md` (docs only)

`SendUpdate.tsx` is touched by gap-2 only (formatter functions are exported from there but lived alongside the composition root). gap-1 touches `Body.tsx`, not `SendUpdate.tsx`. Parallelization-safe.

### D-5 — Re-trigger merge-gate UAT after 46.1 closes

**Why:** The UAT in `46-UAT.md` is `status: diagnosed` with three gaps marked `failed`. After 46.1 plans commit, the executor (or the orchestrator at gap-closure-finalization) updates each gap's `status: failed` → `status: resolved`, then updates the UAT frontmatter `status: diagnosed` → `status: resolved`. This auto-resolution is part of the `close_parent_artifacts` step in the gap-closure execute-phase workflow.

But that's the artifact-side resolution. The actual visual confirmation requires Liz to re-run the merge-gate test:
1. Trigger a real Send Update to liz@lasprezz.com
2. Trigger a real Work Order to liz@lasprezz.com
3. Open both in Outlook desktop and verify:
   - **gap-1:** Single "Hi Victoria," — no duplication
   - **gap-2:** Pendant Lights ETA renders empty (if Sanity still has no date for that row), not "Invalid Date"
   - **gap-3:** N/A in the visual gate — verified by reading the docs commit, not by the email render
4. Confirm the seven items previously listed as "NOT issues" (dark-mode, Work Order, terracotta CTA, formal signoff, reply line, milestone pills, vendor sub-lines) still render correctly — no regressions from the gap fixes
5. Capture new screenshots (replace or supplement the existing eight in `screenshots/`)
6. Reply `approved` on plan 46-03 to close it.

This re-trigger is procedural and lives outside the 46.1 plans themselves — it's the gate that 46.1's success unblocks.

---

## Constraints

- **Atomicity:** each plan ships as a single atomic commit (the cutover-style discipline still applies, even at smaller scope).
- **Test gates:** gap-1 and gap-2 plans must add the test cases listed in D-1/D-2; full `npm test` must remain green-or-baseline-only-failures (the 13 pre-existing failures from `deferred-items.md` D-pre-46 + D-pre-46-wo + the WorkOrder snapshot date-drift).
- **No regressions:** post-46.1, the Outlook merge-gate's "items NOT issues" list (UAT section) must still render correctly. The defensive strip and the formatter guards are narrow on purpose — they should not affect any other surface.
- **No scope creep:** the design conversation about re-toning `pending` palette is explicitly out of scope. If the planner is tempted to add a fourth plan for it, the planner should stop and surface that as a separate phase or deferred item.

---

## Files modified (estimate, planner refines)

| Plan | Files |
|------|-------|
| 46.1-01 (gap-1) | `src/emails/sendUpdate/Body.tsx` (or new helper module), `src/emails/sendUpdate/SendUpdate.test.ts` (or `Body.test.ts`), one admin compose UI file TBD by planner (likely under `src/components/admin/`) |
| 46.1-02 (gap-2) | `src/emails/sendUpdate/SendUpdate.tsx` (formatter helpers), `src/emails/sendUpdate/SendUpdate.test.ts` (or sibling) |
| 46.1-03 (gap-3) | `.planning/PLAN-AUTHORING-PATTERNS.md` |

---

## Acceptance

- [ ] gap-1 plan committed: defensive strip + admin UX helper, tests cover the strip pattern's narrow scope (literal "Hi {Name}," only, NOT "Hello/Dear/Hey/etc."), `npm test` green-or-baseline
- [ ] gap-2 plan committed: `formatDate` + `formatLongDate` guard, tests cover empty / undefined / unparseable / valid string / valid Date inputs, `npm test` green-or-baseline
- [ ] gap-3 plan committed: PLAN-AUTHORING-PATTERNS.md updated with the bold admonition, the Phase 46 example block, and the checklist nudge
- [ ] 46-UAT.md gaps updated `status: failed` → `status: resolved` for all five (gap-1..gap-5); UAT frontmatter `status: diagnosed` → `status: resolved`
- [ ] Phase 46.1 closes; control returns to Phase 46 plan 46-03 awaiting Liz's re-test

---

## Round 2 — re-test 2026-04-28 surfaced gap-4 + gap-5

The round-1 commits (eaea038, 9b5cb08, 889477e) closed gap-1/2/3 at the artifact level. Liz re-tested in Outlook desktop ~16:53 ET on 2026-04-28 and visually confirmed gap-1 (single greeting renders correctly). Two new findings emerged in the same re-test cycle, captured here rather than spun out to Phase 46.2 because they belong to the same merge-gate close-out:

- **gap-4** — Outlook desktop dark-mode contrast inversion (REVERSES the round-1 "Items NOT issues #1" classification — the cream-on-dark half-inversion is unreadable, not "acceptable")
- **gap-5** — right-aligned date/ETA columns push content to the far edge of the email card; user wants left-aligned cells throughout

Two new plans, both Wave 2 (parallel-safe — no file overlap with each other or with 46.1-01..03).

### D-6 — Force light-mode rendering in Outlook desktop via `[data-ogsc]` / `[data-ogsb]` selectors + MSO conditional

**Why:** `EmailShell.tsx:72-73` already declares `<meta name="color-scheme" content="only light">` and `<meta name="supported-color-schemes" content="light">`. New Outlook (web/M365) and Apple Mail respect those tags and skip auto-darken. **Outlook desktop ignores them** and runs its own auto-darken pass via the `[data-ogsc]` (text-color) and `[data-ogsb]` (background-color) selector hooks, which only spare colors the email author has explicitly locked through those same selectors. The result Liz saw — dark gray body + still-light text — is the half-inverted state that emerges when the author has locked nothing. Lock the existing palette against inversion; do NOT design a dark variant.

**Edit scope (locked):**

In `src/emails/shell/EmailShell.tsx` `<Head>` (immediately after the existing two `<meta>` tags), add a `<style>` block containing:

1. **`[data-ogsc]` text-color overrides** for every text color the templates use:
   - `[data-ogsc] .text-stone, [data-ogsc] .text-stone-light` → re-pin to `#8A8478` / `#B8AFA4`
   - `[data-ogsc]` rules for inline-styled text colors `#2C2926` (dark text), `#4A4540` (mid text), `#9B3A2A` (procurement pending pill text), and the seven `STATUS_PILL_STYLES.*.text` values
   - Use `color: ... !important` on each rule (Outlook auto-darken specificity requires `!important`)

2. **`[data-ogsb]` background-color overrides** for every background:
   - `[data-ogsb] body, [data-ogsb] .bg-cream` → `background-color: #FAF8F5 !important`
   - `[data-ogsb]` rules for the seven `STATUS_PILL_STYLES.*.bg` values
   - Border colors via `[data-ogsc]` since Outlook auto-darken treats border as text-adjacent: `border-color: #E8DDD0 !important` for the cream-dark divider; pill borders via the seven `STATUS_PILL_STYLES.*.border` values
   - CTA backgrounds: terracotta `#C4836A` (SU) and gold `#9A7B4B` (WO)

3. **MSO conditional block** for older Outlook desktop builds where `[data-ogs*]` doesn't reach:
   ```html
   <!--[if mso]>
   <style type="text/css">
     body { mso-color-scheme: light !important; }
   </style>
   <![endif]-->
   ```

**Source of truth for color values:** import from existing tokens — do NOT hardcode a parallel palette. The CSS rules can list the literal hex values (CSS doesn't support imports here), but the values must be byte-identical to:
- Cream/stone/dark-text: existing inline styles in `EmailShell.tsx`, `Greeting.tsx`, `Body.tsx`, `Milestones.tsx`, `Procurement.tsx`, `ReviewItems.tsx`
- Pill chrome: `src/lib/procurement/statusPills.ts` (7 statuses × 3 properties = 21 values)
- CTA: `src/emails/shell/EmailButton.tsx` (terracotta + gold from `_theme.ts`)

If the planner finds palette drift between sources, that's a separate finding — STOP and surface it before locking the D-6 CSS values.

**Where the style lives:** inside `<Head>` of `EmailShell.tsx`, NOT a separate file. Locating the override in the shared shell ensures both SendUpdate and WorkOrder benefit; locating it inline (vs. external stylesheet) is required because email clients strip `<link rel="stylesheet">`.

**Tests (locked):**
- HTML inspection: rendered SendUpdate HTML contains the `[data-ogsc]` selector (at least one occurrence)
- HTML inspection: rendered SendUpdate HTML contains the `[data-ogsb]` selector
- HTML inspection: rendered SendUpdate HTML contains the MSO conditional block (`<!--[if mso]>` ... `<![endif]-->` literal)
- HTML inspection: at least one `!important` declaration is present in the new style block
- HTML inspection: the cream `#FAF8F5` value appears in the `[data-ogsb]` rule (verifies the body bg lock specifically)
- Same set of inspections for WorkOrder rendered HTML (proves the shared shell exports the fix to both templates)
- Snapshot regeneration: existing `__snapshots__/SendUpdate.test.ts.snap` and any WorkOrder snapshot will diff. The diff is expected and load-bearing — committed as part of the plan.

Visual confirmation in actual Outlook desktop is round-3 manual re-test, not automated.

### D-7 — All table cell content left-aligned

**Why:** Right-aligned dates push them to the email card's far edge, visually disconnecting them from their row. Centered status pill compounds the issue in Procurement. User decision: all cells read left-to-right with no right-anchored or centered cells.

**Edit scope (locked):**

| File | Line | Before | After |
|------|------|--------|-------|
| `src/emails/sendUpdate/Milestones.tsx` | 107 | `<Column align="right" ...>` (date) | `<Column align="left" ...>` |
| `src/emails/sendUpdate/Procurement.tsx` | 105 | `<Column align="center" ...>` (Status header) | `<Column align="left" ...>` |
| `src/emails/sendUpdate/Procurement.tsx` | 106 | `<Column align="right" ...>` (ETA header) | `<Column align="left" ...>` |
| `src/emails/sendUpdate/Procurement.tsx` | 116 | `<Column align="center" ...>` (Status cell) | `<Column align="left" ...>` |
| `src/emails/sendUpdate/Procurement.tsx` | 119 | `<Column align="right" ...>` (ETA cell) | `<Column align="left" ...>` |

Five literal `align=` attribute changes. No other code in either file changes. The `verticalAlign: "middle"` and `width: "50%"` style properties on those columns are unrelated and stay as-is.

**Tests (locked):**
- Snapshot regeneration: existing `__snapshots__` will diff for both fixtures. Expected and load-bearing.
- HTML inspection: rendered SendUpdate has zero `align="right"` and zero `align="center"` attributes within the Milestones and Procurement sections (asserts the change is complete, not partial).
- Existing tests in `Body.test.ts` and `SendUpdate.test.ts` still pass — alignment changes don't affect text content or the strip behavior.

### D-8 — Round-2 plans run as Wave 2 (parallel-safe)

**Why:** No file overlap between gap-4 and gap-5:
- gap-4 (46.1-04): `src/emails/shell/EmailShell.tsx` + tests inspecting rendered HTML (likely `src/emails/shell/EmailShell.test.ts` new file or sibling)
- gap-5 (46.1-05): `src/emails/sendUpdate/Milestones.tsx` + `src/emails/sendUpdate/Procurement.tsx` + snapshot updates

`EmailShell.tsx` is touched only by gap-4. Milestones/Procurement only by gap-5. Snapshots regen on both plans but the updated `.snap` files diff in different test blocks. Parallelization-safe.

Wave assignment:
- Wave 1: 46.1-01, 46.1-02, 46.1-03 (already complete: eaea038, 9b5cb08, 889477e)
- Wave 2: 46.1-04, 46.1-05 (new — to be planned by `/gsd-plan-phase 46.1`)

### D-9 — Re-trigger merge-gate UAT after Wave 2 closes (round 3)

**Why:** Same logic as D-5 but for round 3. After 46.1-04 + 46.1-05 commit, the parent UAT artifact replays — Liz re-tests in Outlook desktop. On `approved` Phase 46 closes for real.

Round-3 visual checks:
- **gap-4:** SendUpdate body in Outlook desktop dark mode renders cream-on-dark-text legibly (no half-inversion). WorkOrder same.
- **gap-5:** Milestone dates render left-aligned, immediately following their pill rather than at the email card's right edge. Procurement Status pill and ETA cell also left-aligned.
- **Regression:** the "Items NOT issues" list — minus the now-superseded #1 — still renders correctly.

---

## Round 3 — re-test 2026-04-29 surfaced gap-6 + gap-7

Liz re-tested in Outlook for Mac on 2026-04-29. Tests 1 (gap-1 regression), 2 (gap-2 regression), and 5 (items-not-issues) passed. Test 3 (gap-4) FAILED — Outlook for Mac dark-mode persists despite the [data-ogsc]/[data-ogsb]/MSO lock from `f867da6`. Test 4 (gap-5) PARTIAL — left-alignment landed correctly but exposed a procurement layout problem the right-alignment had visually masked. Two new findings, both Wave 3 (parallel-safe within the wave; gap-7 fix depends on a spike that runs alongside gap-6 fix).

- **gap-6** — Procurement table layout regression. Item column too narrow + uneven row heights + verticalAlign:middle = ETA cells form a zig-zag instead of a horizontal stripe.
- **gap-7** — Outlook for Mac dark-mode contrast inversion persists. The 46.1-04 lock targets classic Outlook for Windows hooks; New Outlook for Mac uses a different rendering pipeline and is not honoring `[data-ogsc]`/`[data-ogsb]`.

Three new plans, two waves. See `46.1-PLANNING-QUESTIONS.md` for the briefing that locked the four decisions below.

### D-10 — gap-7 packaged as inline spike + fix; gap-6 ships parallel-safe in the same wave

**Why:** "Spike first" is the strategy choice (Q5 option A in `46.1-PLANNING-QUESTIONS.md`). Packaging the spike as plan 46.1-06 keeps the findings inside the phase artifact trail, finishes everything in one `/gsd-execute-phase` run, and gives 46.1-08 (the gap-7 fix) a concrete deliverable to read instead of a freeform "go figure it out."

**Plan structure (locked):**

| Plan | Scope | Wave | Depends on | Output |
|------|-------|------|------------|--------|
| 46.1-06 | Outlook-for-Mac dark-mode spike — literature review + prototype renders | 1 | — | `46.1-SPIKE-OUTLOOK-MAC.md` committed to phase dir |
| 46.1-07 | gap-6 Procurement layout fix | 1 | — | `Procurement.tsx` + `SendUpdate.test.ts` + snapshot regen |
| 46.1-08 | gap-7 Outlook-for-Mac dark-mode fix | 2 | `[46.1-06]` | `EmailShell.tsx` + `EmailShell.test.ts` + SendUpdate/WorkOrder snapshot regen |

Wave 1 plans run in parallel: 46.1-06 only writes the SPIKE.md findings file; 46.1-07 only touches Procurement.tsx + SendUpdate test surface. Zero file overlap.

**Spike scope (locked):**
- Variants to research: New Outlook for Mac (Liz's daily driver, the failing variant), classic Outlook for Windows (where 46.1-04's lock IS sufficient — must not regress), Outlook web M365, Apple Mail. Document for each variant which technique(s) survive.
- Candidate techniques to evaluate at minimum: (a) `<table bgcolor>` body wrapper, (b) `@media (prefers-color-scheme: dark)` block, (c) `class~="email-shell"` selector hooks with `!important`, (d) inline body-attribute paint as fallback if `<style>` is stripped.
- Sources: htmlemail.io, Email on Acid blog, Litmus blog, Stack Overflow tagged outlook-mac-dark-mode.
- **Archaeology check (added 2026-04-29 by Paul):** Before recommending a gap-7 fix combination, the spike MUST `grep -n 'align=' src/emails/sendUpdate/Procurement.tsx` and confirm gap-5/gap-6's left-alignment is not being overridden by a parent Column or wrapper attribute. If the spike finds a parent override, raise it as a separate finding — round-4 should not surface a fourth gap that turns out to be gap-5's tail.
- **Output:** structured findings table (one row per variant × candidate technique with PASS/FAIL/UNKNOWN + cited source URL), a chosen combination with rationale and confidence level, and any open risks. The output file MUST be specific enough that the executor of 46.1-08 can author code from it without re-doing the research.

### D-11 — gap-6 outer Section paddingTop:16 (mirrors ReviewItems pattern)

**Why:** Today Procurement has `paddingTop: 10` (smaller than its siblings: Milestones 12, ReviewItems 12 + outer Section paddingTop 8). Liz asked for "a bit more vertical margin above the Procurement section." The cleanest fix is mirror ReviewItems' established outer-wrapper pattern: wrap the Procurement render in `<Section style={{ paddingTop: 16 }}>` while leaving the inner `paddingTop: 10` untouched. Total visual gap above Procurement becomes ~26px vs current ~10px without disturbing inner padding.

**Edit scope:**
- `src/emails/sendUpdate/Procurement.tsx` — wrap the existing `<Section>` render in an outer `<Section style={{ paddingTop: 16 }}>`. No other changes to inner padding.
- Match the pattern at `src/emails/sendUpdate/ReviewItems.tsx:75` (`<Section style={{ paddingLeft: 40, paddingRight: 40, paddingBottom: 24, paddingTop: 8 }}>`) — the Procurement outer Section follows the same wrapping shape but uses `paddingTop: 16`.

This pairs with the column-width + verticalAlign edits in the same plan; the three changes ship in a single atomic commit per the 46.1 plan-atomicity discipline.

### D-12 — gap-6 test surface: snapshot regen + section-scoped HTML inspection

**Why:** Snapshot diff alone is too noisy — a future unrelated edit could re-roll the snapshot and silently break a gap-6 invariant. Three explicit assertions guard the load-bearing values; snapshots back them up.

**Tests (locked):**
- New section-scoped describe block in `src/emails/sendUpdate/SendUpdate.test.ts` Procurement section. Use the element-scoped regex pattern established in 46.1-05 (`<td[^>]*align="left">`), NOT global string matching.
- Three new `it()` blocks asserting (against rendered HTML for the SendUpdate template):
  1. Item column declared width is the largest of the three Procurement columns (60% per the locked ratios; planner verifies the rendered output uses `width="60%"` or react-email's compiled equivalent).
  2. Procurement row body Columns set `valign="top"` (or react-email's compiled equivalent, e.g. `style="...vertical-align:top..."`). Header row stays as authored.
  3. Outer Procurement Section's compiled HTML carries the paddingTop value from D-11 (16, expressed in whatever unit react-email compiles to — likely `padding-top:16px` in the inline style attribute).
- Snapshot file `src/emails/sendUpdate/__snapshots__/SendUpdate.test.ts.snap` regenerates. Diff is expected and load-bearing — committed as part of the plan. Verify the diff scope is Procurement-only (no Milestones, ReviewItems, Greeting, or Body changes).
- Existing tests preserved: `Body.test.ts` 13 still pass, `EmailShell.test.ts` 19 still pass, full suite stays at the 62-failure baseline.

### D-13 — gap-7 test surface: HTML inspection only + round-4 human visual gate

**Why:** Mirrors the D-6 / 46.1-04 test pattern. HTML inspection proves the new spike-validated technique markers landed in BOTH SendUpdate AND WorkOrder rendered HTML (proves shared-shell propagation). Round-4 visual confirmation in actual Outlook clients is the only gate that proves the fix survived in real rendering pipelines — automated tests cannot replicate Outlook for Mac's dark-mode pass.

**Tests (locked):**
- New HTML-inspection `it()` blocks in `src/emails/shell/EmailShell.test.ts`, one per technique marker the spike's chosen combination introduces. Examples (final list determined by SPIKE.md):
  - If spike selects bgcolor wrapper: assert rendered HTML contains a top-level `<table bgcolor="#FAF8F5">` (or equivalent) in BOTH SendUpdate and WorkOrder render paths.
  - If spike selects `@media (prefers-color-scheme: dark)`: assert rendered HTML's `<head>` contains `@media (prefers-color-scheme: dark)` and the override block names the locked palette colors.
  - If spike selects `class~="email-shell"` hooks: assert rendered HTML's `<head>` contains the new selector(s) with `!important` declarations.
- The 19 existing 46.1-04 lock-marker assertions stay as-is and continue to pass — the gap-7 fix is additive, not a replacement.
- Round-4 human gate (cannot be automated): Liz triggers real Send Update + Work Order to liz@lasprezz.com, opens in Outlook for Mac dark mode, Outlook for Windows dark mode, and Outlook web dark mode. Confirms cream `#FAF8F5` body bg + authored text colors render correctly in all three; no half-inversion. Light-mode in all three confirmed unregressed.
- **Hard exclusions:** No paid services (Litmus, Email on Acid). No Playwright dark-mode emulation as a verification gate (does not reproduce Outlook for Mac's specific pipeline; would give false confidence that a real round-4 test would shatter).

### D-14 — Round 4 re-trigger after Wave 3 closes

Same logic as D-5 / D-9 but for round 4. After 46.1-06 / 46.1-07 / 46.1-08 commit, the parent UAT artifact replays. Liz re-tests in Outlook desktop on Mac and Windows + Outlook web. On `approved`, Phase 46 closes for real and v5.3 unblocks.
