---
phase: 46.1-merge-gate-gap-closure
plan: 05
subsystem: email-table-cell-alignment
tags: [outlook-desktop, table-alignment, email-rendering, gap-closure, react-email]
parent_uat: .planning/phases/46-send-update-work-order-migration/46-UAT.md
parent_uat_gap: gap-5
dependency_graph:
  requires:
    - "46.1-04 (snapshot baseline post-EmailShell `<Head>` lock; commit f867da6)"
    - "src/emails/sendUpdate/Milestones.tsx (D-7 row 1 source-of-truth)"
    - "src/emails/sendUpdate/Procurement.tsx (D-7 rows 2-5 source-of-truth)"
    - "src/emails/sendUpdate/SendUpdate.tsx (composition order: ReviewItems -> Milestones -> Procurement -> CTA -> Footer; basis for the section-scoped slice)"
  provides:
    - "Five `<Column align=\"left\">` swaps in Milestones + Procurement (1 + 4 = 5 sites)"
    - "Section-scoped + element-scoped regression tests asserting zero `<td align=\"right|center\">` cells within Milestones and Procurement slices of the rendered HTML"
    - "Updated SendUpdate snapshots reflecting 52 right/center -> left cell flips across all 5 fixtures"
  affects:
    - "Visible reading order of milestone-date and procurement Status/ETA cells -- now flow left-to-right immediately following row content rather than flushing to the email card's far edge"
    - "Future template additions inheriting from the same component shape -- `<td align=\"right|center\">` in any post-Milestones section will surface as a regression worth investigating (the Procurement slice runs to end-of-string)"
tech-stack:
  added: []
  patterns:
    - "section-scoped-slice-then-element-scope (PLAN-AUTHORING-PATTERNS.md \"self-invalidating grep\" pattern): rendered HTML sliced by eyebrow sentinels (>Milestones</p>, >Procurement</p>) then asserted with element-scoped `<td[^>]*align=...>` regex -- distinguishes load-bearing cell-level alignment from react-email's `<table align=\"center\">` primitive wrappers"
    - "end-of-string upper-bound for last enumerated section: Procurement slice runs to html.length because Procurement is the last enumerated content section before the CTA + Footer chrome (verified at plan time across all four Procurement-bearing fixtures: full, mixedSubLines, noBody, noReviewItems -- chrome contains zero `<td align=\"right|center\">` cells)"
    - "atomic-source-plus-snapshot commit: source change (5 align swaps) + snapshot regen (10 line-level diffs across 5 fixtures) + new tests (94 inserts) shipped as one commit to keep the snapshot diff bound to the load-bearing source change"
key-files:
  created: []
  modified:
    - src/emails/sendUpdate/Milestones.tsx
    - src/emails/sendUpdate/Procurement.tsx
    - src/emails/sendUpdate/SendUpdate.test.ts
    - src/emails/sendUpdate/__snapshots__/SendUpdate.test.ts.snap
key-decisions:
  - "Procurement slice end-sentinel = end-of-string (html.length), not the next section eyebrow -- safe upper bound because Procurement is the last enumerated content section and the CTA + Footer chrome contains zero `<td align=\"right|center\">` cells (verified at plan time). Future-proofing: any new `<td align=\"right|center\">` introduced anywhere downstream of Procurement will flag as a regression worth investigating."
  - "Element-scoped `<td[^>]*align=...>` regex (not global `align=\"center\"`) to distinguish load-bearing `<td>` cells from react-email's `<table align=\"center\">` wrapper attributes (~77 sites in rendered HTML, NOT in D-7 scope)."
  - "ReviewItems.tsx UNCHANGED -- its `align=\"right\"` due-label cells are out of D-7 scope. ReviewItems renders BEFORE Procurement, so its cells fall upstream of `>Procurement</p>` and are excluded automatically by the slice start sentinel."
patterns-established:
  - "self-invalidating-grep acceptance criterion (PLAN-AUTHORING-PATTERNS.md): assertion-line-only via `grep -vE '^\\s*(//|\\*)'` strips comment-prose so the describe-block name string can't be satisfied by header comments alone"
  - "negative + positive test pairing per section: each section gets a 'no right/center cells' test AND a 'has expected count of left cells' test -- prevents the negative case from passing vacuously if the slice strategy ever silently misfires"
requirements-completed: []
metrics:
  duration: ~12 minutes
  tasks_completed: 1
  files_changed: 4
  tests_added: 4
  commit_hash: 14dabb9
  completed_date: 2026-04-29
---

# Phase 46.1 Plan 05: Left-Align Table Cells in Milestones + Procurement (gap-5) Summary

Closes parent UAT 46-UAT.md gap-5 at the artifact level: milestone dates and procurement Status/ETA cells now read left-to-right, flowing immediately after the row content instead of flushing to the email card's far edge in Outlook desktop. Five `<Column align=...>` attribute swaps in two source files, an element-scoped + section-scoped regression-guard describe block, snapshot regen scoped to align-attribute changes only, single atomic commit.

## Performance

- **Duration:** ~12 minutes
- **Started:** 2026-04-29T20:55:00Z (approx)
- **Completed:** 2026-04-29T21:00:00Z (approx)
- **Tasks:** 1 (single-task plan, single atomic commit)
- **Files modified:** 4

## Accomplishments

- Five D-7 align swaps applied (1 in Milestones.tsx, 4 in Procurement.tsx) — exactly as locked in CONTEXT.md D-7
- Section-scoped + element-scoped regression-guard tests added to `SendUpdate.test.ts` (4 new `it()` blocks within a single describe), asserting both negative and positive locks for D-7
- SendUpdate snapshot regenerated against the post-46.1-04 baseline; diff scoped to align-attribute swaps in Milestones/Procurement section markup only (52 right/center cells -> 52 left cells across 5 fixtures, conservation of cell count exact)
- Atomic commit `14dabb9` lands all four files together
- Parent UAT gap-5 ready to flip from `status: failed` -> `status: resolved` at the gap-closure-finalization step at end of phase 46.1

## Atomic Commit

- **Hash:** `14dabb9`
- **Subject:** `fix(46.1-05): left-align table cells in Milestones + Procurement (gap-5)`
- **Diff stats:** 4 files changed, 104 insertions(+), 10 deletions(-)
  - `src/emails/sendUpdate/Milestones.tsx`: 1 insertion / 1 deletion (one literal `right` -> `left`)
  - `src/emails/sendUpdate/Procurement.tsx`: 4 insertions / 4 deletions (four literal `center`/`right` -> `left`)
  - `src/emails/sendUpdate/SendUpdate.test.ts`: 94 insertions (new describe block with 4 `it()` blocks + header comment)
  - `src/emails/sendUpdate/__snapshots__/SendUpdate.test.ts.snap`: 5 insertions / 5 deletions (each of the 5 fixtures stores its full HTML on a single line, so each fixture contributes one replaced line)
- **No AI attribution lines** per CLAUDE.md project convention.

**Parent commit verification (`git log --oneline -2`):**

```
14dabb9 fix(46.1-05): left-align table cells in Milestones + Procurement (gap-5)
6d3dc91 docs(46.1-04): summary + state/roadmap update for gap-4 closure
```

46.1-04 (commit f867da6 source + 6d3dc91 docs) had landed BEFORE this plan executed, per `depends_on: ["04"]`. The snapshot regeneration ran against the post-04 baseline, so the diff hunks are bounded to align-attribute swaps without EmailShell-cascading content drift.

## Post-Edit align Verification (by content, not by line number)

```
$ grep -n 'align=' src/emails/sendUpdate/Milestones.tsx src/emails/sendUpdate/Procurement.tsx
src/emails/sendUpdate/Milestones.tsx:107:            <Column align="left" style={{ verticalAlign: "middle" }}>
src/emails/sendUpdate/Procurement.tsx:105:        <Column align="left" style={HEADER_LABEL_STYLE}>Status</Column>
src/emails/sendUpdate/Procurement.tsx:106:        <Column align="left" style={HEADER_LABEL_STYLE}>ETA</Column>
src/emails/sendUpdate/Procurement.tsx:116:            <Column align="left" style={{ verticalAlign: "middle" }}>
src/emails/sendUpdate/Procurement.tsx:119:            <Column align="left" style={{ verticalAlign: "middle" }}>
```

All five sites are now `align="left"`. Counts:

- `grep -c 'align="left"' src/emails/sendUpdate/Milestones.tsx` -> **1** (expected 1)
- `grep -cE 'align="(right|center)"' src/emails/sendUpdate/Milestones.tsx` -> **0** (expected 0)
- `grep -c 'align="left"' src/emails/sendUpdate/Procurement.tsx` -> **4** (expected 4)
- `grep -cE 'align="(right|center)"' src/emails/sendUpdate/Procurement.tsx` -> **0** (expected 0)

## ReviewItems.tsx Scope Guard (D-7 explicit out-of-scope)

```
$ git diff --name-only HEAD~1 HEAD -- src/emails/sendUpdate/ReviewItems.tsx | wc -l
0
```

ReviewItems.tsx not modified — its `align="right"` due-label cells are out of D-7 scope and stay as-is. ReviewItems renders BEFORE Procurement (`SendUpdate.tsx` line 174), so its cells are upstream of `>Procurement</p>` and are excluded automatically by the slice start sentinel in the new regression-guard tests.

## Snapshot-Diff Scope Verification

Across all 5 fixtures (`full`, `mixedSubLines`, `noBody`, `noReviewItems`, plus the plainText fixture which has no cell `align=` attrs):

- `align="right"`: **52 -> 16** (−36) — Milestones date column + Procurement ETA column flipped to left across 5 fixtures × N rows. Remaining 16 are inside ReviewItems' due-label column (legitimate, out of D-7 scope).
- `align="center"`: **115 -> 99** (−16) — Procurement Status header + body cells flipped to left across 5 fixtures × N rows. Remaining 99 are `<table align="center">` wrappers from react-email's Container/Section/Row primitives (the email-safe centering pattern, NOT in D-7 scope).
- `align="left"`: **0 -> 52** (+52) — all the new left-aligned cells.

**Conservation of cells:** 36 right + 16 center = 52 cells removed; 52 left cells added. Exact 1:1 swap. Diff is scoped to align-attribute values only — no incidental style/structure/content changes, no `<table align="center">` wrapper changes (the unchanged 99 confirms react-email primitives are untouched).

## Test Suite Delta

**Targeted runs:**

```
$ npx vitest run src/emails/sendUpdate/SendUpdate.test.ts -t "section-scoped left-alignment"
Test Files  1 passed (1)
     Tests  4 passed | 40 skipped (44)
```

The 4 new `it()` blocks in the new describe (`Milestones + Procurement section-scoped left-alignment (46.1 D-7 -- gap-5 fix)`) all pass:

1. `Procurement section contains no <td align="center"> or <td align="right"> cells` — negative lock for D-7 rows 2-5
2. `Milestones section contains no <td align="right"> cells` — negative lock for D-7 row 1
3. `Milestones section contains <td align="left"> cells (positive lock for D-7 row 1)` — positive lock with floor `>= 4` (one date column per milestone × 4 milestones in `FIXTURES.full()`)
4. `Procurement section contains <td align="left"> cells (positive lock for D-7 rows 2-5)` — positive lock with floor `>= 8` (Status header + ETA header + 3 Status cells + 3 ETA cells = 8 minimum)

**Other suite checks:**

- `Body.test.ts`: 13/13 passing (alignment changes do NOT affect Body strip behavior — confirmed)
- `EmailShell.test.ts` (the 19 tests added by 46.1-04): 19/19 passing (sequence prerequisite check)
- `SendUpdate.test.ts` full file: 44/44 passing (was 40 before this plan; +4 from new D-7 describe block; all snapshot tests pass with regenerated snapshots)

**Full-suite delta (`npm test`):**

- Pre-46.1-05 baseline (post-46.1-04): 1394 passing, 62 failing.
- Post-46.1-05 (HEAD): **1268 passed** + 68 todo + **62 failed** in 1398 tests / 130 test files (20 failed, 100 passed, 10 skipped).
- **Net delta: +4 passing tests, 0 new failures.** The 62 failures are the same pre-existing environmental failures documented in 46.1-04's summary (Sanity client config, redis mocks, blob serve, formatCurrency timezone, milestoneUtils relative-date drift, etc.) — none are touched by this plan.

## Decisions Made

None beyond the plan's locked decisions. The plan was executed exactly as written:

- D-7 row 1: Milestones.tsx date column `right` -> `left`
- D-7 row 2: Procurement.tsx Status header `center` -> `left`
- D-7 row 3: Procurement.tsx ETA header `right` -> `left`
- D-7 row 4: Procurement.tsx Status body cell `center` -> `left`
- D-7 row 5: Procurement.tsx ETA body cell `right` -> `left`

The slice strategy and element-scoping pattern in the regression-guard tests are byte-identical to the plan's `<action>` block.

## Deviations from Plan

None — plan executed exactly as written.

The sanity check at the start of the action ran cleanly:
- Milestones.tsx had exactly 1 `align=` line (right), as expected.
- Procurement.tsx had exactly 4 `align=` lines (center, right, center, right) in the documented order.
- Snapshot eyebrow markup matched all three sentinels (`>For your review<`, `>Milestones<`, `>Procurement<`).

The snapshot regen produced the expected diff — 5 line-level replacements (one per fixture; each fixture stores its full HTML on a single line), with 52 right/center cells flipped to left across all 5 fixtures, conservation exact.

## Issues Encountered

None.

## User Setup Required

None - no external service configuration required.

## Plan-Level Round-3 Gate (NOT closed by this plan)

Per `46.1-CONTEXT.md` D-9, round-3 visual confirmation in actual Outlook desktop is the human-only gate that fully closes gap-5:

1. Liz triggers a real Send Update to liz@lasprezz.com, opens in Outlook desktop, confirms milestone dates render left-aligned (immediately following the milestone pill, not at the email card's right edge).
2. Procurement Status pills + ETA cells also left-aligned in the same render.
3. The "Items NOT issues" regression set still renders correctly.
4. Liz replies `approved` on parent plan 46-03 to close Phase 46.

This is procedural and lives outside the 46.1 plan boundary.

## Parent UAT Artifact Update (orchestrator step, NOT this commit)

Per `46.1-CONTEXT.md` D-5/D-9, the gap-closure-finalization step at end of Phase 46.1 will:

- Flip `46-UAT.md` gap-5 entry `status: failed` -> `status: resolved` (artifact-level).
- After Liz's round-3 visual approval, flip parent UAT frontmatter `status: diagnosed` -> `status: resolved`.

These artifact updates are NOT applied in this plan's commit — they're the cross-cutting close-out the orchestrator handles after both 46.1-04 and 46.1-05 land (which they now have: 46.1-04 at f867da6/6d3dc91, 46.1-05 at 14dabb9).

## Next Phase Readiness

- Phase 46.1 Wave 2 complete: both 46.1-04 (gap-4 Outlook auto-darken lock) and 46.1-05 (gap-5 left-align cells) committed.
- Phase 46.1 ready for gap-closure-finalization step (artifact-level UAT updates + Phase 46 close-out trigger).
- Round-3 Liz re-test in Outlook desktop is the human gate that ultimately approves Phase 46-03 cutover for full closure.

## Self-Check: PASSED

Verified post-commit:

- `src/emails/sendUpdate/Milestones.tsx`: FOUND, 1 `align="left"`, 0 right/center.
- `src/emails/sendUpdate/Procurement.tsx`: FOUND, 4 `align="left"`, 0 right/center.
- `src/emails/sendUpdate/SendUpdate.test.ts`: FOUND, new describe block present, self-invalidating-grep returns 1 (assertion-line, comment-excluded).
- `src/emails/sendUpdate/__snapshots__/SendUpdate.test.ts.snap`: FOUND, regenerated, 52 right/center -> 52 left across all 5 fixtures.
- Commit `14dabb9`: FOUND in `git log`, message subject `fix(46.1-05): left-align table cells in Milestones + Procurement (gap-5)`.
- Exactly 4 files in HEAD commit: confirmed via `git show --name-only HEAD --pretty=format:`.
- No AI-attribution lines: confirmed (`grep -ciE 'co-authored-by|generated with claude|claude code'` returns 0).
- ReviewItems.tsx UNCHANGED: confirmed (`git diff --name-only HEAD~1 HEAD -- src/emails/sendUpdate/ReviewItems.tsx | wc -l` returns 0).

---
*Phase: 46.1-merge-gate-gap-closure*
*Plan: 05*
*Completed: 2026-04-29*
