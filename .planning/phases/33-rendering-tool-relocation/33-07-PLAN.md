---
phase: 33-rendering-tool-relocation
plan: 07
type: execute
wave: 4
depends_on:
  - 33-02
  - 33-03
  - 33-04
  - 33-05
  - 33-06
files_modified: []
autonomous: false
requirements:
  - RNDR-01
  - RNDR-02
  - RNDR-03
  - RNDR-04
  - RNDR-05
  - RNDR-06
  - RNDR-07
  - RNDR-08
  - RNDR-09
  - RNDR-10
  - RNDR-11

must_haves:
  truths:
    - "Full Vitest suite passes green with zero failures"
    - "STUDIO_API_SECRET does not appear in the built dist/ output"
    - "AI generation creates a rendering (manual UAT confirms rendered image in ChatView)"
    - "Refinement iteration produces a new rendering (manual UAT confirms)"
    - "Promote creates a designOption document and closes the drawer (manual UAT confirms)"
    - "Usage badge reflects the correct count/limit after generation"
  artifacts:
    - path: "dist/"
      provides: "Production build output for T-33-01 security check"
  key_links:
    - from: "Manual UAT (admin)"
      to: "Same designOption document visible in client portal"
      via: "Promote action writes to shared Sanity dataset"
      pattern: "N/A — manual cross-app verification"
---

<objective>
Final phase gate: run the full Vitest suite, perform the T-33-01 build security check, and conduct the manual RNDR-11 UAT (D-24). This plan has no code changes — it is pure verification. If any test fails or the UAT reveals a bug, the executor creates targeted fix tasks before proceeding.

Purpose: Ensures all 11 RNDR requirements are satisfied before marking Phase 33 complete. Covers RNDR-11 (manual regression) and T-33-01 (security acceptance criterion).

Output: Green test suite, clean dist/ build, manual UAT sign-off.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/33-rendering-tool-relocation/33-CONTEXT.md
@.planning/phases/33-rendering-tool-relocation/33-RESEARCH.md
@.planning/phases/33-rendering-tool-relocation/33-VALIDATION.md
@.planning/phases/33-rendering-tool-relocation/33-02-SUMMARY.md
@.planning/phases/33-rendering-tool-relocation/33-03-SUMMARY.md
@.planning/phases/33-rendering-tool-relocation/33-04-SUMMARY.md
@.planning/phases/33-rendering-tool-relocation/33-05-SUMMARY.md
@.planning/phases/33-rendering-tool-relocation/33-06-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run full Vitest suite and build security check</name>
  <files></files>
  <read_first>
    - .planning/phases/33-rendering-tool-relocation/33-VALIDATION.md — per-task verification map and sampling requirements
    - .planning/phases/33-rendering-tool-relocation/33-RESEARCH.md — T-33-01 acceptance criterion
  </read_first>
  <action>
**Step 1 — Full Vitest suite:**
```bash
npx vitest run
```
Expected result:
- All pre-existing API tests (generate.test.ts, usage.test.ts) pass green
- All new Wave 0 stubs (WizardContainer.test.tsx, StepUpload.test.tsx, StepSetup.test.tsx, SessionListPage.test.tsx, refine.test.ts, promote.test.ts) show as pending (not failing)
- Zero test failures

If any test fails: do NOT proceed to Step 2. Read the failure output, identify which plan introduced the regression, fix it, and re-run until clean.

**Step 2 — Build security check (T-33-01 acceptance criterion):**
```bash
npx astro build
grep -r "STUDIO_API_SECRET" dist/ 2>/dev/null || echo "CLEAN: STUDIO_API_SECRET not found in dist/"
```

If `grep` finds any matches in `dist/`: the token is leaking into the client bundle. DO NOT proceed to UAT. Identify which component is importing `import.meta.env.STUDIO_API_SECRET` directly (it should only appear in .astro files). Fix the leaking component to receive the token as a prop, rebuild, and re-run grep until clean.

**Step 3 — TypeScript compilation final check:**
```bash
npx tsc --noEmit
```
Must pass with zero errors.

**Step 4 — Spot-check regression fixes:**

RNDR-06 regression:
```bash
grep -n "maxVisitedStep" src/components/admin/rendering/WizardContainer.tsx | head -5
```
Must return: state declaration + stepper render usage.

RNDR-07 regression:
```bash
grep -n "URL.createObjectURL\|revokeObjectURL" src/components/admin/rendering/StepUpload.tsx
```
Must return: at least 2 matches (create + revoke).

RNDR-08 regression (both StepUpload and StepClassify):
```bash
grep -n "overflow.*hidden.*ellipsis\|textOverflow.*ellipsis" src/components/admin/rendering/StepUpload.tsx src/components/admin/rendering/StepClassify.tsx
```
Must return matches in both files.

RNDR-09 regression:
```bash
grep -n "multiple" src/components/admin/rendering/StepUpload.tsx
```
Must return the file input `multiple` attribute.

RNDR-10 regression (Style Preset on Step 1 only, Design Vision on Step 4 only):
```bash
grep -n "STYLE_PRESETS\|Select a style" src/components/admin/rendering/StepSetup.tsx && echo "OK: Style Preset in StepSetup"
grep -n "Design vision" src/components/admin/rendering/StepDescribe.tsx && echo "OK: Design Vision in StepDescribe"
grep -c "STYLE_PRESETS\|Design vision" src/components/admin/rendering/StepDescribe.tsx src/components/admin/rendering/StepSetup.tsx
```
Verify: StepDescribe has 0 STYLE_PRESETS matches; StepSetup has 0 "Design vision" matches.

No Studio hooks remaining:
```bash
grep -rn "useCurrentUser\|useToolContext\|@sanity/ui\|@sanity/icons" src/components/admin/rendering/
```
Must return zero matches.

studioToken not in component env access:
```bash
grep -rn "import.meta.env" src/components/admin/rendering/
```
Must return zero matches.
  </action>
  <verify>
    <automated>cd /Users/paulalbert/Dropbox/GitHub/la-sprezzatura && npx vitest run 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run` exits with code 0, zero test failures (pending tests are acceptable)
    - `grep -r "STUDIO_API_SECRET" dist/` returns zero matches after `npx astro build`
    - `npx tsc --noEmit` exits with code 0
    - `grep -rn "@sanity/ui\|@sanity/icons" src/components/admin/rendering/` returns zero matches
    - `grep -rn "import.meta.env" src/components/admin/rendering/` returns zero matches
    - `grep "maxVisitedStep" src/components/admin/rendering/WizardContainer.tsx` returns 2+ matches
    - `grep "URL.createObjectURL" src/components/admin/rendering/StepUpload.tsx` returns 1+ match
    - `grep "multiple" src/components/admin/rendering/StepUpload.tsx` returns input attribute match
  </acceptance_criteria>
  <done>Full Vitest suite green. STUDIO_API_SECRET not in dist/. TypeScript clean. All spot-check regression greps return expected matches.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Manual UAT — RNDR-11 end-to-end regression (D-24)</name>
  <what-built>
    The complete rendering tool relocation: session list at /admin/rendering, 4-step wizard at /admin/rendering/new, chat refinement at /admin/rendering/[sessionId], promote drawer, and usage badge. All run from the admin app using the same backend API routes as Sanity Studio.
  </what-built>
  <how-to-verify>
    Perform the following steps (from RESEARCH.md D-24 and VALIDATION.md manual UAT section):

    **Step 1 — Create a new session:**
    1. Navigate to /admin/rendering
    2. Verify the page loads with the session list (or "No rendering sessions yet" empty state)
    3. Verify the "Your usage X / Y" badge appears in the header
    4. Click "New session"
    5. Verify the wizard loads at /admin/rendering/new with 4 step indicators (Setup, Upload, Classify, Describe)
    6. Verify the admin sidebar and top bar remain visible

    **Step 2 — Complete the wizard:**
    7. Step 1 (Setup): Enter a session title, select a project, choose an aspect ratio, select a style preset from the dropdown
    8. Click "Next: Upload"
    9. Step 2 (Upload): Drop or select 2 image files
    10. Verify thumbnail previews appear immediately (before upload completes) — RNDR-07
    11. Verify filenames are truncated with ellipsis if long — RNDR-08
    12. Verify both files upload (multi-file) — RNDR-09
    13. Click a completed step (step 1 circle) — verify you can jump back, then return to step 2 — RNDR-06
    14. Click "Next: Classify"
    15. Step 3 (Classify): Set image type for each image
    16. Click "Next: Describe"
    17. Step 4 (Describe): Verify "Style preset" is NOT on this step (only "Design Vision" textarea) — RNDR-10
    18. Enter a design vision description
    19. Click "Generate" — verify the spinner/overlay appears

    **Step 3 — Verify generation result:**
    20. Verify the page navigates to /admin/rendering/[sessionId] after generation completes (within ~60 seconds)
    21. Verify the rendered image appears in the left pane (full size)
    22. Verify the usage badge count has incremented by 1

    **Step 4 — Refinement:**
    23. In the right chat pane, type a refinement: "Make it warmer and add more natural light"
    24. Click "Send refinement"
    25. Verify the message appears in the thread optimistically
    26. Verify the polling indicator shows while generating
    27. Verify a new rendering appears after refinement completes (~30-60 seconds)
    28. Verify the thumbnail strip shows 2 renderings (original + refined)

    **Step 5 — Promote:**
    29. Click "Promote" in the chat header
    30. Verify the parchment drawer slides in from the right
    31. Verify the preview image is shown in the parchment band
    32. Enter a caption
    33. Click "Publish to gallery"
    34. Verify the drawer closes and the toast "Published to Design Options" appears briefly

    **Step 6 — Verify cross-app consistency (VALIDATION.md manual-only check):**
    35. Open Sanity Studio (both tools coexist per D-21)
    36. Navigate to the Rendering tool in Studio
    37. Verify the session created in admin appears in Studio's session list
    38. Verify the usage count in Studio matches what admin showed
    39. Open the client portal for the linked project — verify the promoted Design Option appears in the gallery

    **Step 7 — Project tab:**
    40. Navigate to /admin/projects/[projectId]
    41. Verify a "Rendering" tab appears
    42. Click it — verify it navigates to /admin/rendering?project={id} with the sessions pre-filtered

    Type "approved" when all steps pass, or describe any issues found.
  </how-to-verify>
  <resume-signal>Type "approved" to mark Phase 33 complete, or describe issues to create targeted fix tasks.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Build output (dist/) → client | Must not contain STUDIO_API_SECRET |
| Admin browser → Sanity Studio | Both tools write to same dataset; coexistence verified in UAT step 36-38 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-33-01 | Information Disclosure | STUDIO_API_SECRET in dist/ | mitigate | **Acceptance in this plan:** `grep -r "STUDIO_API_SECRET" dist/` returns zero matches after `npx astro build`. If matches found: identify and fix the leaking component before UAT. |
| T-33-02 | Information Disclosure | blob-serve auth in production | mitigate | UAT Step 6 confirms blob images load correctly in admin with session cookie. DevTools Network check: confirm blob requests return 200 (session cookie sent, not 401). |
| T-33-03 | Tampering | Prompt injection via vision field | accept | Same posture as Studio tool. UAT generates a real rendering — content policy working as expected. |
| T-33-04 | Elevation of Privilege | Admin session during UAT | accept (existing control) | UAT performed by authenticated admin. Middleware protection verified in Phase 29/30. |
</threat_model>

<verification>
This plan IS the verification. Success criteria drive the human checkpoint in Task 2.

Summary of automated checks completed in Task 1:
- `npx vitest run` — zero failures
- `grep -r "STUDIO_API_SECRET" dist/` — zero matches
- `npx tsc --noEmit` — zero errors
- Spot-check greps for all 5 regression fixes (RNDR-06 through RNDR-10)

Manual check in Task 2 (RNDR-11, D-24):
- End-to-end session creation, generation, refinement, promote
- Cross-app verification: admin sessions visible in Studio, promoted option in portal
</verification>

<success_criteria>
- `npx vitest run` exits 0 with zero failures
- `grep -r "STUDIO_API_SECRET" dist/` returns zero matches after production build
- `npx tsc --noEmit` exits 0
- No @sanity/ui or @sanity/icons imports in src/components/admin/rendering/
- No import.meta.env access in src/components/admin/rendering/
- Manual UAT approved: generation produces rendered image, refinement produces new rendering, promote creates designOption in Sanity, usage badge reflects correct count
- Admin-created sessions visible in Sanity Studio session list (D-21 coexistence)
</success_criteria>

<output>
After UAT approval, create `.planning/phases/33-rendering-tool-relocation/33-07-SUMMARY.md`

Update `.planning/STATE.md`:
- status: Phase 33 complete
- stopped_at: Phase 34 (Settings and Studio Retirement)

Update `.planning/ROADMAP.md`:
- Mark Phase 33 as complete
- Update plan count: "Plans: 7 plans"
</output>
