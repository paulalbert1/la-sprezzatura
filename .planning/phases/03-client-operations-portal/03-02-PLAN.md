---
phase: 03-client-operations-portal
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - src/components/portal/PortalLayout.astro
  - src/components/portal/StatusBadge.astro
  - src/components/portal/MilestoneTimeline.astro
  - src/pages/portal/[token].astro
  - astro.config.mjs
autonomous: false
requirements: [CLNT-01, CLNT-02]

must_haves:
  truths:
    - "A client clicking their unique portal URL sees their project name, status badge, current pipeline stage, and milestone timeline"
    - "Invalid or disabled portal tokens show a generic 'Project not found' page with no information leakage"
    - "The portal page renders server-side on every request (always fresh from Sanity)"
    - "The portal page matches the luxury aesthetic -- warm neutrals, Cormorant Garamond headings, DM Sans body, generous whitespace"
    - "Timeline is horizontal on desktop and vertical on mobile"
    - "Portal pages are excluded from the XML sitemap"
    - "Brute-force attempts are rate-limited"
  artifacts:
    - path: "src/components/portal/PortalLayout.astro"
      provides: "Minimal layout with no nav, branded footer, noindex meta"
      min_lines: 25
    - path: "src/components/portal/StatusBadge.astro"
      provides: "Pill-shaped badge showing current stage name with color coding"
      min_lines: 10
    - path: "src/components/portal/MilestoneTimeline.astro"
      provides: "Responsive stepper component (horizontal desktop, vertical mobile)"
      min_lines: 40
    - path: "src/pages/portal/[token].astro"
      provides: "SSR portal page with token lookup, rate limiting, 404 handling"
      contains: "prerender = false"
    - path: "astro.config.mjs"
      provides: "Sitemap filter excluding /portal/ routes"
      contains: "/portal"
  key_links:
    - from: "src/pages/portal/[token].astro"
      to: "src/sanity/queries.ts"
      via: "getProjectByPortalToken import"
      pattern: "getProjectByPortalToken"
    - from: "src/pages/portal/[token].astro"
      to: "src/lib/portalStages.ts"
      via: "STAGE_META import for stage descriptions"
      pattern: "STAGE_META"
    - from: "src/pages/portal/[token].astro"
      to: "src/lib/rateLimit.ts"
      via: "checkRateLimit import for brute-force protection"
      pattern: "checkRateLimit"
    - from: "src/pages/portal/[token].astro"
      to: "src/components/portal/MilestoneTimeline.astro"
      via: "component import"
      pattern: "MilestoneTimeline"
    - from: "src/pages/portal/[token].astro"
      to: "src/components/portal/StatusBadge.astro"
      via: "component import"
      pattern: "StatusBadge"
---

<objective>
Build the portal page and all its visual components. A client visits `/portal/[token]`, the server queries Sanity for the matching project, and renders a branded single-page experience with project name, status badge, milestone timeline, and stage description. Invalid tokens get a generic 404. Rate limiting protects against brute-force.

Purpose: This is the user-facing deliverable of Phase 3 -- the client portal page that Liz shares with active clients.
Output: Working SSR portal page with all visual components, sitemap exclusion, and rate limiting.
</objective>

<execution_context>
@/Users/paulalbert/.claude/get-shit-done/workflows/execute-plan.md
@/Users/paulalbert/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-client-operations-portal/03-CONTEXT.md
@.planning/phases/03-client-operations-portal/03-RESEARCH.md
@.planning/phases/03-client-operations-portal/03-01-SUMMARY.md

Source files in the code repo at /Users/paulalbert/Dropbox/GitHub/la-sprezzatura/:
@src/layouts/BaseLayout.astro
@src/styles/global.css
@astro.config.mjs

<interfaces>
<!-- From Plan 01 (must be completed first) -->

From src/lib/portalStages.ts:
```typescript
export type StageKey = "discovery" | "concept" | "design-development" | "procurement" | "installation" | "closeout";
export interface StageMeta { value: StageKey; title: string; description: string; }
export const STAGES: StageMeta[];
export const STAGE_META: Record<StageKey, StageMeta>;
```

From src/lib/generateToken.ts:
```typescript
export function generatePortalToken(length?: number): string;
```

From src/lib/rateLimit.ts:
```typescript
export function checkRateLimit(ip: string, options?: { maxRequests?: number; windowMs?: number }): void;
// Throws Error("Too many requests") when limit exceeded
```

From src/sanity/queries.ts (added in Plan 01):
```typescript
export async function getProjectByPortalToken(token: string): Promise<{
  title: string;
  clientName: string | null;
  pipelineStage: StageKey;
} | null>;
```

From src/styles/global.css (design tokens):
```css
--color-cream: #FAF8F5;
--color-cream-dark: #F5F0EB;
--color-stone: #8A8478;
--color-stone-light: #B8B0A4;
--color-charcoal: #2C2926;
--color-charcoal-light: #4A4540;
--color-terracotta: #C4836A;
--color-terracotta-light: #D4A08A;
--font-heading: "Cormorant Garamond", "Georgia", serif;
--font-body: "DM Sans", "system-ui", sans-serif;
```

From src/layouts/BaseLayout.astro (reference for layout pattern, DO NOT use for portal):
```typescript
interface Props { title: string; description?: string; ogImage?: string; noindex?: boolean; transparentHeader?: boolean; }
```

From astro.config.mjs (current sitemap filter):
```javascript
sitemap({ filter: (page) => !page.includes("/admin") })
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create portal visual components (PortalLayout, StatusBadge, MilestoneTimeline)</name>
  <files>src/components/portal/PortalLayout.astro, src/components/portal/StatusBadge.astro, src/components/portal/MilestoneTimeline.astro</files>
  <action>
    Create `src/components/portal/` directory with three Astro components.

    **PortalLayout.astro** -- Minimal HTML layout for the portal page. NOT the full BaseLayout (no Header, no MobileMenu, no Footer, no ScrollAnimations, no JsonLd). Structure:
    - Props: `{ title: string }`
    - `<head>`: charset, viewport, `<meta name="robots" content="noindex, nofollow">`, Font preload for --font-heading and --font-body (import `{ Font } from "astro:assets"`), `<title>` from prop
    - Import `../../styles/global.css` so Tailwind v4 + design tokens are available
    - `<body>`: `class="bg-cream text-charcoal font-body min-h-screen flex flex-col"`
    - `<slot />` for page content
    - Minimal footer at bottom: `class="mt-auto py-8 text-center"` with "La Sprezzatura" dot "Interior Design" in `text-xs text-stone tracking-widest uppercase`, and `liz@lasprezz.com` email link below with `hover:text-terracotta transition-colors`
    - Follow the exact pattern from 03-RESEARCH.md "Portal Layout" code example

    **StatusBadge.astro** -- Pill-shaped badge component:
    - Props: `{ stage: string; isComplete: boolean }` where `stage` is the human-readable stage title (e.g., "Design Development") and `isComplete` is true when stage is "closeout"
    - Render: `<span>` with pill shape using `inline-flex items-center px-4 py-1.5 rounded-full text-sm font-body font-medium tracking-wide`
    - Color: if `isComplete` use muted green (`bg-emerald-50 text-emerald-700`), otherwise use terracotta (`bg-terracotta/10 text-terracotta`)
    - Content: the stage title text

    **MilestoneTimeline.astro** -- Responsive stepper:
    - Props: `{ currentStage: StageKey }` -- import `StageKey` and `STAGES` from `../../lib/portalStages`
    - Calculate `currentIndex` from STAGES array
    - Each stage gets a status: `completed` (before current), `current` (equals current), `future` (after current)
    - Layout: `flex flex-col md:flex-row md:items-start gap-0` on the container. Each stage item is `flex md:flex-col items-center`. On mobile (default), stages stack vertically with dots on the left. On desktop (md:), stages flow horizontally with dots on top.
    - Connector lines between stages: vertical on mobile (w-0.5, h-full, positioned left of dot), horizontal on desktop (h-0.5, w-full, positioned above dots). Completed connectors use `bg-terracotta/40`, future use `bg-stone-light/30`.
    - Stage dot: `w-7 h-7 rounded-full flex items-center justify-center shrink-0 relative z-10`. Completed: `bg-terracotta text-white` with a simple checkmark (inline SVG, `w-3.5 h-3.5`). Current: `bg-terracotta text-white ring-4 ring-terracotta/20` with a filled inner dot. Future: `bg-cream-dark border-2 border-stone-light/40`.
    - Stage label: `text-xs tracking-wide font-body`. Current: `text-charcoal font-medium`. Others: `text-stone`. On desktop add `md:text-center md:mt-2`. On mobile add `ml-3 py-3`.
    - Current stage description: Below the entire timeline, show the current stage's description text from STAGE_META in `text-base text-charcoal-light leading-relaxed max-w-xl mx-auto text-center mt-8 font-body`.
    - Ensure adequate vertical spacing on mobile between stage items (min `py-3` on stage items, connectors fill the gap).
  </action>
  <verify>
    <automated>cd /Users/paulalbert/Dropbox/GitHub/la-sprezzatura && npx vitest run --reporter=verbose && ls -la src/components/portal/PortalLayout.astro src/components/portal/StatusBadge.astro src/components/portal/MilestoneTimeline.astro</automated>
  </verify>
  <done>Three portal components exist: PortalLayout (minimal layout, noindex, branded footer), StatusBadge (pill with terracotta/green color coding), MilestoneTimeline (responsive horizontal/vertical stepper with checkmarks, current ring, and stage description).</done>
</task>

<task type="auto">
  <name>Task 2: Create SSR portal page with token lookup, rate limiting, 404 handling, and sitemap exclusion</name>
  <files>src/pages/portal/[token].astro, astro.config.mjs</files>
  <action>
    **src/pages/portal/[token].astro** -- The SSR portal page:

    Frontmatter:
    - `export const prerender = false;` (CRITICAL -- must NOT use getStaticPaths)
    - Import `PortalLayout` from `../../components/portal/PortalLayout.astro`
    - Import `MilestoneTimeline` from `../../components/portal/MilestoneTimeline.astro`
    - Import `StatusBadge` from `../../components/portal/StatusBadge.astro`
    - Import `getProjectByPortalToken` from `../../sanity/queries`
    - Import `STAGE_META` and `type StageKey` from `../../lib/portalStages`
    - Import `checkRateLimit` from `../../lib/rateLimit`
    - Get `token` from `Astro.params`
    - Rate limiting: Extract IP from `Astro.request.headers.get("x-forwarded-for")` or `"x-real-ip"` or `"unknown"`. Call `checkRateLimit(ip, { maxRequests: 10, windowMs: 60_000 })` wrapped in try/catch. If rate limited, return a 429 response with a simple "Please try again later" message using PortalLayout.
    - Query: `const project = await getProjectByPortalToken(token)` -- returns null if no match or portalEnabled is false
    - If no project: set `Astro.response.status = 404` and render a "not found" page (details below)
    - If project found: extract `currentStage` (default to `"discovery"` if null), get stage metadata

    Template -- Not Found (404):
    - Wrap in `<PortalLayout title="Project Not Found">`
    - Centered content: "Project Not Found" heading (font-heading, text-3xl), brief message "This project link is not active. If you believe this is an error, please contact your designer." in text-stone. Include email link to liz@lasprezz.com.
    - Do NOT differentiate between "never existed", "disabled", or "wrong token" -- identical message for all (security: no information leakage).

    Template -- Project Found:
    - Wrap in `<PortalLayout title={`${project.title} | La Sprezzatura`}>`
    - Content container: `max-w-2xl mx-auto px-6 py-12 md:py-20`
    - La Sprezzatura branding: small logo-text at top, `text-xs text-stone tracking-[0.2em] uppercase font-body mb-8 text-center` -- "La Sprezzatura"
    - Project title: `<h1>` with `font-heading text-4xl md:text-5xl font-light text-charcoal text-center mb-2`
    - If `project.clientName`: greeting below title, "Welcome, {clientName}" in `text-lg text-stone font-body text-center mb-8`
    - If no clientName: just `mb-8` spacer
    - StatusBadge centered: `<div class="flex justify-center mb-12">` with `<StatusBadge stage={stageMeta.title} isComplete={currentStage === "closeout"} />`
    - Divider: `<hr class="border-stone-light/20 mb-12">`
    - Section heading: "Project Timeline" in `font-heading text-2xl text-charcoal text-center mb-8`
    - MilestoneTimeline component: `<MilestoneTimeline currentStage={currentStage} />`
    - Bottom CTA area: below the timeline, subtle `mt-16 text-center` section with "Questions about your project?" text and "Contact Elizabeth" mailto link styled as a subtle button (`inline-block border border-stone-light/40 px-6 py-2.5 text-sm tracking-widest uppercase text-stone hover:text-terracotta hover:border-terracotta transition-colors`)

    **astro.config.mjs** -- Update the sitemap filter:
    - Change `filter: (page) => !page.includes("/admin")` to `filter: (page) => !page.includes("/admin") && !page.includes("/portal")`
    - This prevents portal URLs from appearing in the XML sitemap
  </action>
  <verify>
    <automated>cd /Users/paulalbert/Dropbox/GitHub/la-sprezzatura && npx vitest run --reporter=verbose && npx astro check 2>&1 | tail -5</automated>
  </verify>
  <done>Portal page exists at src/pages/portal/[token].astro with prerender=false. Invalid tokens show generic 404 page. Valid tokens show branded portal with project name, status badge, milestone timeline, and stage description. Rate limiting active at 10 requests/min/IP. Sitemap excludes /portal/ routes. All tests still pass.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify complete client portal end-to-end</name>
  <action>
    Human verifies the complete portal experience: Sanity Studio portal fields, token generation, portal page rendering, responsive timeline, 404 handling, and portalEnabled toggle behavior.
  </action>
  <what-built>Complete client portal: SSR page at /portal/[token] with project lookup from Sanity, branded layout (no nav), status badge, responsive milestone timeline (horizontal on desktop, vertical on mobile), stage descriptions, and 404 handling for invalid tokens. Sanity Studio has new "Client Portal" field group with portalToken (auto-generated, read-only, copyable URL), clientName, portalEnabled toggle, and visible pipelineStage dropdown.</what-built>
  <how-to-verify>
    1. Start the dev server: `cd /Users/paulalbert/Dropbox/GitHub/la-sprezzatura && npm run dev`
    2. Visit Sanity Studio at http://localhost:4321/admin
    3. Open any existing project document -- verify the "Client Portal" tab appears with pipelineStage, portalToken, clientName, and portalEnabled fields
    4. Create a NEW project document -- verify portalToken auto-generates an 8-char code and the full URL is displayed
    5. On the new project: set a pipeline stage (e.g., "Design Development"), enter a client name, toggle "Portal Enabled" on, copy the portal URL
    6. Visit the copied portal URL in browser (e.g., http://localhost:4321/portal/xK9mP2qR) -- verify you see:
       - La Sprezzatura branding at top
       - Project title as large heading
       - "Welcome, [client name]" greeting
       - Terracotta status badge showing "Design Development"
       - Milestone timeline with Discovery and Concept filled/checked, Design Development highlighted with ring, Procurement/Installation/Closeout muted
       - Stage description text below timeline
       - "Questions about your project?" CTA at bottom
       - Branded footer with email
       - No site navigation or menu
    7. Resize browser to mobile width -- verify timeline flips from horizontal to vertical
    8. Visit http://localhost:4321/portal/INVALID -- verify generic "Project Not Found" page (no info leakage)
    9. Toggle "Portal Enabled" off in Sanity Studio, refresh the portal URL -- verify it now shows 404
  </how-to-verify>
  <verify>Human confirms portal works end-to-end</verify>
  <done>Human has verified: portal page renders correctly for valid tokens, 404 for invalid tokens, Sanity Studio fields work, timeline is responsive, and portalEnabled toggle controls access.</done>
  <resume-signal>Type "approved" or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
- `npx vitest run` -- all tests pass
- Portal page renders correctly at `/portal/[valid-token]` with all visual elements
- Invalid tokens show generic 404
- Sanity Studio shows portal fields in dedicated group
- Sitemap at `/sitemap-index.xml` does not include `/portal/` URLs
- Mobile responsive: timeline vertical, layout stacks properly
</verification>

<success_criteria>
- Client can visit their unique PURL and see project name, status badge, pipeline stage, and milestone timeline -- without any login
- Liz can update pipeline stage in Sanity Studio and client sees the change immediately (SSR, no cache)
- Guessing tokens shows no information about other projects
- Portal page matches the luxury aesthetic of the public site
- Timeline is horizontal on desktop, vertical on mobile
</success_criteria>

<output>
After completion, create `.planning/phases/03-client-operations-portal/03-02-SUMMARY.md`
</output>
