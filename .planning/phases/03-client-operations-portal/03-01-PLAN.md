---
phase: 03-client-operations-portal
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/generateToken.ts
  - src/lib/portalStages.ts
  - src/lib/rateLimit.ts
  - src/lib/generateToken.test.ts
  - src/lib/portalStages.test.ts
  - vitest.config.ts
  - src/sanity/schemas/project.ts
  - src/sanity/components/PortalUrlDisplay.tsx
  - src/sanity/queries.ts
autonomous: true
requirements: [CLNT-01, CLNT-03]

must_haves:
  truths:
    - "Token generation produces cryptographically random 8-char alphanumeric strings"
    - "Stage metadata defines all 6 pipeline stages with labels and descriptions"
    - "Sanity Studio shows portalToken (read-only), clientName, portalEnabled fields in a 'Client Portal' group"
    - "Sanity Studio displays the full portal URL in a copyable read-only field"
    - "pipelineStage field is visible in Sanity Studio (no longer hidden)"
    - "A GROQ query can look up a project by its portalToken"
  artifacts:
    - path: "src/lib/generateToken.ts"
      provides: "8-char alphanumeric token generator using crypto"
      exports: ["generatePortalToken"]
    - path: "src/lib/portalStages.ts"
      provides: "Stage metadata map with all 6 pipeline stages"
      exports: ["STAGES", "STAGE_META", "StageKey", "StageMeta"]
    - path: "src/lib/rateLimit.ts"
      provides: "Reusable rate limiter extracted from actions/index.ts"
      exports: ["checkRateLimit"]
    - path: "src/sanity/schemas/project.ts"
      provides: "Extended project schema with portal fields and field groups"
      contains: "portalToken"
    - path: "src/sanity/components/PortalUrlDisplay.tsx"
      provides: "Custom Sanity input showing full portal URL with copy button"
      exports: ["PortalUrlDisplay"]
    - path: "src/sanity/queries.ts"
      provides: "Portal token lookup query"
      exports: ["getProjectByPortalToken"]
  key_links:
    - from: "src/sanity/schemas/project.ts"
      to: "src/sanity/components/PortalUrlDisplay.tsx"
      via: "components.input on portalToken field"
      pattern: "components:.*input:.*PortalUrlDisplay"
    - from: "src/sanity/schemas/project.ts"
      to: "src/lib/generateToken.ts"
      via: "initialValue callback"
      pattern: "initialValue.*generatePortalToken"
    - from: "src/sanity/queries.ts"
      to: "portalToken field"
      via: "GROQ query filter"
      pattern: "portalToken.*==.*\\$token"
---

<objective>
Build the backend foundation for the client portal: token generation, stage metadata, Sanity schema extension with portal fields, custom Studio input component for URL display, and the GROQ query for token lookup. Also set up vitest and write unit tests for the pure utility modules.

Purpose: All portal data modeling and utilities must exist before the portal page can be built. This plan creates everything the SSR page will query and display.
Output: Extended Sanity schema with portal fields, utility modules with tests, portal token lookup query.
</objective>

<execution_context>
@/Users/paulalbert/.claude/get-shit-done/workflows/execute-plan.md
@/Users/paulalbert/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-client-operations-portal/03-CONTEXT.md
@.planning/phases/03-client-operations-portal/03-RESEARCH.md

Source files in the code repo at /Users/paulalbert/Dropbox/GitHub/la-sprezzatura/:
@src/sanity/schemas/project.ts
@src/sanity/schemas/index.ts
@src/sanity/queries.ts
@src/actions/index.ts

<interfaces>
<!-- Existing project schema structure (from src/sanity/schemas/project.ts) -->
<!-- The project schema is a defineType with fields array. pipelineStage exists at the end with hidden: true. -->
<!-- No field groups are currently defined -- must add groups when extending. -->

From src/sanity/queries.ts:
```typescript
import { sanityClient } from "sanity:client";
export async function getProjects(): Promise<...>
export async function getProjectBySlug(slug: string): Promise<...>
export async function getFeaturedProjects(): Promise<...>
export async function getSiteSettings(): Promise<...>
export async function getServices(): Promise<...>
```

From src/actions/index.ts (rate limiter pattern to extract):
```typescript
const rateLimitMap = new Map<string, { count: number; resetAt: number }>();
function checkRateLimit(ip: string): void {
  // 3 requests per minute per IP, throws ActionError on exceed
}
```

From src/sanity/schemas/index.ts:
```typescript
import { project } from "./project";
import { service } from "./service";
import { siteSettings } from "./siteSettings";
export const schemaTypes = [project, service, siteSettings];
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create utility modules with vitest tests (Wave 0 + implementation)</name>
  <files>vitest.config.ts, src/lib/generateToken.ts, src/lib/generateToken.test.ts, src/lib/portalStages.ts, src/lib/portalStages.test.ts, src/lib/rateLimit.ts</files>
  <behavior>
    - generatePortalToken() returns an 8-character string
    - generatePortalToken() output contains only alphanumeric characters [A-Za-z0-9]
    - generatePortalToken() produces unique tokens (1000 calls, no duplicates)
    - STAGES array has exactly 6 entries in order: discovery, concept, design-development, procurement, installation, closeout
    - Each StageMeta has value, title, and description (non-empty strings)
    - STAGE_META record maps each StageKey to its corresponding StageMeta
    - StageKey type covers all 6 stage values
  </behavior>
  <action>
    **vitest.config.ts** -- Create minimal vitest config at project root:
    ```typescript
    import { defineConfig } from "vitest/config";
    export default defineConfig({ test: { include: ["src/**/*.test.ts"] } });
    ```

    **src/lib/generateToken.ts** -- Token generation using Web Crypto API (`globalThis.crypto.getRandomValues`) which works in both Node.js 20+ and browsers (needed because Sanity Studio `initialValue` runs client-side). Charset: `ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz0123456789` (62 chars). Default length 8. Export `generatePortalToken(length?: number): string`.

    **src/lib/portalStages.ts** -- Stage metadata as specified in 03-RESEARCH.md "Stage Metadata Map" code example. Export `StageKey` type, `StageMeta` interface, `STAGES` array, and `STAGE_META` record. Use the exact stage descriptions from the research doc.

    **src/lib/rateLimit.ts** -- Extract the rate limiter from `src/actions/index.ts` into a reusable module. The function should accept `ip: string` and `options?: { maxRequests?: number; windowMs?: number }` with defaults of 3 requests per 60 seconds. It should throw a plain Error (not ActionError, since the portal page is not an Astro Action). Update `src/actions/index.ts` to import from `src/lib/rateLimit.ts` instead of defining inline -- BUT keep the existing behavior (throw ActionError) by wrapping the call. Actually, to avoid modifying the working actions file unnecessarily, just create the new rateLimit.ts as a standalone utility that throws a generic Error with message "Too many requests". The portal page will use this directly. The actions file can be refactored later if desired.

    **Tests** -- Write tests FIRST (red), then implement (green). Follow TDD cycle.
  </action>
  <verify>
    <automated>cd /Users/paulalbert/Dropbox/GitHub/la-sprezzatura && npx vitest run --reporter=verbose</automated>
  </verify>
  <done>All vitest tests pass. generateToken produces valid 8-char alphanumeric tokens. portalStages exports all 6 stages with metadata. rateLimit exports a reusable rate limiter function.</done>
</task>

<task type="auto">
  <name>Task 2: Extend Sanity project schema with portal fields and add token lookup query</name>
  <files>src/sanity/schemas/project.ts, src/sanity/components/PortalUrlDisplay.tsx, src/sanity/queries.ts</files>
  <action>
    **src/sanity/schemas/project.ts** -- Modify the existing project schema:
    1. Add `groups` to the defineType options: `[{ name: "content", title: "Content", default: true }, { name: "portal", title: "Client Portal" }]`
    2. Add `group: "content"` to ALL existing fields (title through order, including pipelineStage)
    3. Change `pipelineStage` field: remove `hidden: true`, add `group: "portal"` (override content group), update the comment to reflect Phase 3 activation
    4. Add three new fields AFTER pipelineStage (all with `group: "portal"`):
       - `portalToken`: type string, title "Portal Token", readOnly true, initialValue calls `generatePortalToken()` from `../../lib/generateToken`, components.input set to the PortalUrlDisplay component. Import PortalUrlDisplay from `../components/PortalUrlDisplay`.
       - `clientName`: type string, title "Client Name", description "Client's name shown on their portal page"
       - `portalEnabled`: type boolean, title "Portal Enabled", initialValue false, description "Toggle to activate this project's client portal link"

    Important: The `initialValue` for portalToken uses `generatePortalToken()` which relies on `globalThis.crypto.getRandomValues` -- this works in the browser (Sanity Studio) without needing Node.js crypto. Import at the top of the file.

    **src/sanity/components/PortalUrlDisplay.tsx** -- Create directory `src/sanity/components/` and the React component as specified in 03-RESEARCH.md Pattern 3. The component:
    - Takes `StringInputProps` from `sanity`
    - Reads `value` (the token) from props
    - Uses `useFormValue(["portalEnabled"])` to check if portal is active
    - Displays full URL as `{siteUrl}/portal/{value}` in a read-only TextInput
    - Has a "Copy" button using `navigator.clipboard.writeText()`
    - Shows "Token will be generated when you create this project." if no value
    - Shows "Enable 'Portal Enabled' toggle to activate this link." if not enabled
    - Use the site URL from `import.meta.env.PUBLIC_SITE_URL || "https://lasprezz.com"`. Since this runs in Sanity Studio (React), use a hardcoded fallback. Actually, Sanity Studio in Astro runs in the browser and does NOT have access to `import.meta.env`. Just use `"https://lasprezz.com"` as the base URL (per astro.config.mjs `site` setting). This is fine because the URL is for Liz to copy and share with clients.

    **src/sanity/queries.ts** -- Add a new exported async function:
    ```typescript
    export async function getProjectByPortalToken(token: string) {
      return sanityClient.fetch(
        `*[_type == "project" && portalToken == $token && portalEnabled == true][0]{
          title, clientName, pipelineStage
        }`,
        { token }
      );
    }
    ```
    Place it after the existing `getProjectBySlug` function. Do not modify existing functions.
  </action>
  <verify>
    <automated>cd /Users/paulalbert/Dropbox/GitHub/la-sprezzatura && npx vitest run --reporter=verbose && npx astro check 2>&1 | head -20</automated>
  </verify>
  <done>Sanity project schema has groups (content + portal), pipelineStage is visible in portal group, portalToken/clientName/portalEnabled fields added. PortalUrlDisplay component exists with copy functionality. getProjectByPortalToken query added to queries.ts. All existing tests still pass.</done>
</task>

</tasks>

<verification>
- `npx vitest run` passes all tests (token generation, stage metadata)
- `src/sanity/schemas/project.ts` contains portalToken, clientName, portalEnabled fields with group: "portal"
- `src/sanity/schemas/project.ts` no longer has `hidden: true` on pipelineStage
- `src/sanity/components/PortalUrlDisplay.tsx` exists and exports the component
- `src/sanity/queries.ts` exports `getProjectByPortalToken`
- `src/lib/rateLimit.ts` exports `checkRateLimit`
</verification>

<success_criteria>
- All vitest tests green
- Sanity schema compiles without errors
- Token generation works in browser context (Web Crypto API)
- Stage metadata covers all 6 stages with descriptions
- Portal token lookup query filters by token AND portalEnabled
</success_criteria>

<output>
After completion, create `.planning/phases/03-client-operations-portal/03-01-SUMMARY.md`
</output>
