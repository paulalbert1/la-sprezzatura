---
phase: 06-portal-features
plan: 02
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - src/pages/portal/project/[projectId].astro
  - src/components/portal/ProjectHeader.astro
  - src/components/portal/ConfidentialityBanner.astro
  - src/components/portal/MilestoneSection.astro
  - src/components/portal/MilestoneItem.astro
  - src/components/portal/ProgressBar.astro
  - src/components/portal/ProcurementTable.astro
  - src/components/portal/PostProjectBanner.astro
  - src/pages/portal/dashboard.astro
autonomous: true
requirements:
  - CLNT-06
  - CLNT-07
  - MILE-02
  - MILE-03
  - PROC-02
  - PORT-05

must_haves:
  truths:
    - "Client navigates to /portal/project/[id] and sees project title, pipeline status bar, engagement type label, and confidentiality notice"
    - "Milestones section shows progress summary (N of M complete) with a slim progress bar, compact 6-stage pipeline, and detailed milestone list sorted by date"
    - "Procurement table shows item names, status badges, install dates, MSRP, per-item savings, tracking links -- with total savings footer"
    - "Procurement section is hidden for Styling & Refreshing and Carpet Curating engagement types"
    - "Dashboard shows completed projects alongside active ones with 30-day visibility filtering"
    - "Confidentiality notice appears below project header"
  artifacts:
    - path: "src/pages/portal/project/[projectId].astro"
      provides: "Main project detail SSR page with engagement type gating"
      contains: "export const prerender = false"
    - path: "src/components/portal/ProjectHeader.astro"
      provides: "Project title + StatusBadge + engagement type label"
      contains: "StatusBadge"
    - path: "src/components/portal/ConfidentialityBanner.astro"
      provides: "Confidentiality notice banner"
      contains: "Please don't share your access link"
    - path: "src/components/portal/MilestoneSection.astro"
      provides: "Milestones section with progress bar and pipeline status"
      contains: "MilestoneTimeline"
    - path: "src/components/portal/MilestoneItem.astro"
      provides: "Individual milestone display with status dot, date, notes"
      contains: "formatRelativeDate"
    - path: "src/components/portal/ProcurementTable.astro"
      provides: "Procurement table with savings and tracking links"
      contains: "formatCurrency"
    - path: "src/components/portal/PostProjectBanner.astro"
      provides: "Project Complete banner for completed projects"
      contains: "Project Complete"
  key_links:
    - from: "src/pages/portal/project/[projectId].astro"
      to: "src/sanity/queries.ts"
      via: "getProjectDetail import and call"
      pattern: "getProjectDetail"
    - from: "src/components/portal/ProcurementTable.astro"
      to: "src/lib/formatCurrency.ts"
      via: "formatCurrency import"
      pattern: "import.*formatCurrency"
    - from: "src/components/portal/MilestoneSection.astro"
      to: "src/lib/milestoneUtils.ts"
      via: "sortMilestones, getMilestoneProgress imports"
      pattern: "import.*milestoneUtils"
    - from: "src/pages/portal/dashboard.astro"
      to: "src/lib/projectVisibility.ts"
      via: "isProjectVisible import for 30-day filtering"
      pattern: "import.*isProjectVisible"
---

<objective>
Build the project detail page at `/portal/project/[projectId]` with milestones section, procurement table, and all static portal components. Update the dashboard with 30-day completed project visibility filtering.

Purpose: This is the primary client-facing page -- the single scroll view where clients see their project status, milestones, and procurement. Engagement type gating controls which sections are visible. The dashboard update ensures completed projects appear for 30 days then auto-hide.

Output: Project detail page with milestone and procurement sections, updated dashboard, 7 new Astro components.
</objective>

<execution_context>
@/Users/paulalbert/.claude/get-shit-done/workflows/execute-plan.md
@/Users/paulalbert/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-portal-features/06-CONTEXT.md
@.planning/phases/06-portal-features/06-UI-SPEC.md
@.planning/phases/06-portal-features/06-01-SUMMARY.md

<interfaces>
<!-- Types and contracts from Plan 01 that this plan consumes -->

From src/lib/milestoneUtils.ts (created in Plan 01):
```typescript
export type MilestoneNote = { _key: string; text: string; clientName: string; timestamp: string };
export type Milestone = { _key: string; name: string; date: string | null; completed: boolean; description?: string; notes?: MilestoneNote[] };
export function sortMilestones(milestones: Milestone[]): Milestone[];
export function getMilestoneProgress(milestones: Milestone[]): { total: number; completed: number; percent: number };
export function getMilestoneStatus(milestone: Milestone, isFirstIncomplete: boolean): "completed" | "current" | "overdue" | "upcoming";
export function formatRelativeDate(dateStr: string): string;
```

From src/lib/formatCurrency.ts (created in Plan 01):
```typescript
export function formatCurrency(cents: number): string;
```

From src/lib/trackingUrl.ts (created in Plan 01):
```typescript
export function getTrackingInfo(trackingNumber: string): { carrier: "ups" | "fedex" | "usps" | "unknown"; url: string | null };
```

From src/lib/projectVisibility.ts (created in Plan 01):
```typescript
export function isProjectVisible(project: { completedAt?: string | null; projectStatus?: string }): boolean;
export function isProjectCompleted(project: { projectStatus?: string }): boolean;
export function isProjectReopened(project: { projectStatus?: string }): boolean;
```

From src/lib/artifactUtils.ts (created in Plan 01):
```typescript
export const ARTIFACT_TYPES: string[];
export function getArtifactLabel(type: string, customName?: string): string;
export function getArtifactBadgeStyle(type: string): string;
```

From src/sanity/queries.ts (extended in Plan 01):
```typescript
export const PROJECT_DETAIL_QUERY: string;
export async function getProjectDetail(projectId: string, clientId: string): Promise<any>;
export const PROJECTS_BY_CLIENT_QUERY: string; // now includes completedAt, projectStatus
```

From src/components/portal/MilestoneTimeline.astro (existing):
```typescript
interface Props { currentStage: StageKey; }
```

From src/components/portal/StatusBadge.astro (existing):
```typescript
interface Props { stage: string; isComplete: boolean; }
```

From src/components/portal/PortalLayout.astro (existing):
```typescript
interface Props { title: string; }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Project detail page + header + confidentiality + post-project banner</name>
  <files>
    src/pages/portal/project/[projectId].astro,
    src/components/portal/ProjectHeader.astro,
    src/components/portal/ConfidentialityBanner.astro,
    src/components/portal/PostProjectBanner.astro,
    src/components/portal/ProgressBar.astro
  </files>
  <read_first>
    src/pages/portal/dashboard.astro,
    src/components/portal/PortalLayout.astro,
    src/components/portal/StatusBadge.astro,
    src/components/portal/MilestoneTimeline.astro,
    src/lib/portalStages.ts,
    src/middleware.ts,
    src/sanity/queries.ts,
    src/styles/global.css,
    .planning/phases/06-portal-features/06-UI-SPEC.md
  </read_first>
  <action>
    **1. Create src/pages/portal/project/[projectId].astro:**

    ```
    export const prerender = false;
    ```

    Import: `PortalLayout`, `ProjectHeader`, `ConfidentialityBanner`, `PostProjectBanner`, `MilestoneSection`, `ProcurementTable`, `getProjectDetail`, `getProjectsByClientId`, `isProjectCompleted`, `isProjectReopened`.

    In frontmatter:
    - Get `clientId` from `Astro.locals.clientId!`
    - Get `projectId` from `Astro.params.projectId`
    - If no projectId, return `Astro.redirect("/portal/dashboard")`
    - Call `const project = await getProjectDetail(projectId, clientId)`
    - If no project, return `Astro.redirect("/portal/dashboard")`
    - Determine `isCompleted = isProjectCompleted(project)`
    - Determine `isReopened = isProjectReopened(project)`
    - Determine engagement type gating:
      ```
      const showProcurement = project.engagementType === "full-interior-design";
      const showCloseDocument = project.engagementType === "full-interior-design";
      ```
    - Determine if multi-project client: `const allProjects = await getProjectsByClientId(clientId); const isMultiProject = (allProjects?.length || 0) > 1;`

    Template structure (matching UI-SPEC layout):
    ```html
    <PortalLayout title={`${project.title} | La Sprezzatura`}>
      <main class="flex-1">
        <div class="max-w-2xl mx-auto px-6 py-12 md:py-20">
          {isMultiProject && (
            <a href="/portal/dashboard" class="inline-flex items-center text-xs text-stone uppercase tracking-widest font-body hover:text-terracotta transition-colors mb-8">
              &larr; Back to Dashboard
            </a>
          )}

          <p class="text-xs text-stone tracking-[0.2em] uppercase font-body mb-8 text-center">
            La Sprezzatura
          </p>

          <ProjectHeader project={project} />
          <ConfidentialityBanner />

          {isCompleted && !isReopened && (
            <PostProjectBanner completedAt={project.completedAt} />
          )}

          <!-- Milestones section (always visible) -->
          <MilestoneSection
            milestones={project.milestones || []}
            pipelineStage={project.pipelineStage}
            isCompleted={isCompleted && !isReopened}
          />

          <!-- Procurement section (Full Interior Design only) -->
          {showProcurement && (
            <ProcurementTable items={project.procurement || []} />
          )}

          <!-- Artifacts section placeholder (Plan 03) -->
          <!-- ArtifactSection will be added by Plan 03 -->

          <!-- Warranty section placeholder (Plan 05) -->

          <div class="mt-16 text-center">
            <a href="/portal/logout" class="text-xs text-stone hover:text-terracotta transition-colors uppercase tracking-widest font-body">
              Sign Out
            </a>
          </div>
        </div>
      </main>
    </PortalLayout>
    ```

    **2. Create ProjectHeader.astro:**

    Props: `project: { title: string; pipelineStage: string; engagementType: string }`

    Engagement type display mapping (same as dashboard):
    ```typescript
    const ENGAGEMENT_LABELS: Record<string, string> = {
      "full-interior-design": "Full Interior Design",
      "styling-refreshing": "Styling & Refreshing",
      "carpet-curating": "Carpet Curating",
    };
    ```

    Template:
    ```html
    <div class="text-center mb-8">
      <h1 class="font-heading text-3xl md:text-4xl font-light text-charcoal mb-3">
        {project.title}
      </h1>
      <div class="flex items-center justify-center gap-3 flex-wrap">
        <StatusBadge
          stage={STAGE_META[project.pipelineStage as StageKey]?.title || project.pipelineStage}
          isComplete={project.pipelineStage === "closeout"}
        />
        {project.engagementType && (
          <span class="text-xs text-stone uppercase tracking-widest font-body">
            {ENGAGEMENT_LABELS[project.engagementType] || project.engagementType}
          </span>
        )}
      </div>
    </div>
    ```

    **3. Create ConfidentialityBanner.astro:**

    No props needed. Static content.
    ```html
    <div class="border border-stone-light/20 bg-cream-dark px-5 py-3 text-center mb-8">
      <p class="text-xs text-stone font-body tracking-wide">
        This portal is private to you. Please don't share your access link.
      </p>
    </div>
    ```

    **4. Create PostProjectBanner.astro:**

    Props: `completedAt?: string`

    Format the completion date: `new Date(completedAt).toLocaleDateString("en-US", { month: "long", day: "numeric", year: "numeric" })`

    ```html
    <div class="bg-cream-dark border border-stone-light/20 px-6 py-6 text-center mb-8">
      <h2 class="font-heading text-xl font-light text-charcoal mb-2">
        Project Complete
      </h2>
      <p class="text-sm text-stone font-body">
        {completedAt
          ? `This project was completed on ${formattedDate}. You can view all details below.`
          : "This project has been completed. You can view all details below."}
      </p>
    </div>
    ```

    **5. Create ProgressBar.astro:**

    Props: `percent: number` (0-100)

    ```html
    <div class="w-full h-1 bg-cream-dark rounded-full overflow-hidden">
      <div
        class="h-full bg-terracotta rounded-full transition-all duration-500"
        style={`width: ${Math.min(100, Math.max(0, percent))}%`}
      />
    </div>
    ```
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - src/pages/portal/project/[projectId].astro contains `export const prerender = false`
    - src/pages/portal/project/[projectId].astro contains `getProjectDetail(projectId, clientId)`
    - src/pages/portal/project/[projectId].astro contains `engagementType === "full-interior-design"` for procurement gating
    - src/pages/portal/project/[projectId].astro contains `Astro.locals.clientId` for session identity
    - src/pages/portal/project/[projectId].astro contains `isMultiProject` logic with back link
    - src/components/portal/ProjectHeader.astro contains `StatusBadge` and `ENGAGEMENT_LABELS`
    - src/components/portal/ConfidentialityBanner.astro contains `Please don't share your access link`
    - src/components/portal/PostProjectBanner.astro contains `Project Complete`
    - src/components/portal/ProgressBar.astro contains `bg-terracotta` and `percent`
    - npm run build succeeds
  </acceptance_criteria>
  <done>Project detail page renders with header, confidentiality notice, post-project banner, and structural layout with engagement type gating. Milestones and procurement sections ready for wiring.</done>
</task>

<task type="auto">
  <name>Task 2: Milestones section + procurement table + dashboard 30-day filter</name>
  <files>
    src/components/portal/MilestoneSection.astro,
    src/components/portal/MilestoneItem.astro,
    src/components/portal/ProcurementTable.astro,
    src/pages/portal/dashboard.astro
  </files>
  <read_first>
    src/pages/portal/project/[projectId].astro,
    src/components/portal/MilestoneTimeline.astro,
    src/components/portal/StatusBadge.astro,
    src/components/portal/ProgressBar.astro,
    src/lib/milestoneUtils.ts,
    src/lib/formatCurrency.ts,
    src/lib/trackingUrl.ts,
    src/lib/portalStages.ts,
    src/lib/projectVisibility.ts,
    src/pages/portal/dashboard.astro,
    .planning/phases/06-portal-features/06-UI-SPEC.md
  </read_first>
  <action>
    **1. Create MilestoneSection.astro:**

    Props:
    ```typescript
    interface Props {
      milestones: Milestone[];
      pipelineStage: string;
      isCompleted: boolean;
    }
    ```

    Import `sortMilestones`, `getMilestoneProgress` from `milestoneUtils`. Import `MilestoneTimeline`, `MilestoneItem`, `ProgressBar`. Import `StageKey` from `portalStages`.

    In frontmatter:
    ```typescript
    const sorted = sortMilestones(milestones);
    const progress = getMilestoneProgress(sorted);
    // Find the first incomplete milestone index for "current" status
    const firstIncompleteIndex = sorted.findIndex(m => !m.completed);
    ```

    Template (section with 2xl vertical padding per UI-SPEC):
    ```html
    <section class="py-12 border-t border-stone-light/20">
      <div class="flex items-baseline justify-between mb-4">
        <h2 class="font-heading text-xl font-light text-charcoal">
          Milestones
        </h2>
        {progress.total > 0 && (
          <span class="text-xs text-stone font-body">
            {progress.completed} of {progress.total} complete
          </span>
        )}
      </div>

      {progress.total > 0 && (
        <div class="mb-6">
          <ProgressBar percent={progress.percent} />
        </div>
      )}

      <!-- Compact 6-stage pipeline status bar -->
      <div class="mb-8">
        <MilestoneTimeline currentStage={pipelineStage as StageKey} />
      </div>

      <!-- Detailed milestones list -->
      {sorted.length === 0 ? (
        <div class="text-center py-8">
          <p class="text-sm text-stone font-body font-light">
            No milestones added yet.
          </p>
          <p class="text-xs text-stone-light font-body mt-1">
            Liz will update this as your project progresses.
          </p>
        </div>
      ) : (
        <div class="space-y-0">
          {sorted.map((milestone, index) => (
            <MilestoneItem
              milestone={milestone}
              isFirstIncomplete={index === firstIncompleteIndex}
              isCompleted={isCompleted}
            />
          ))}
        </div>
      )}
    </section>
    ```

    **2. Create MilestoneItem.astro:**

    Props:
    ```typescript
    interface Props {
      milestone: Milestone;
      isFirstIncomplete: boolean;
      isCompleted: boolean;
    }
    ```

    Import `getMilestoneStatus`, `formatRelativeDate` from `milestoneUtils`.

    In frontmatter:
    ```typescript
    const status = getMilestoneStatus(milestone, isFirstIncomplete);
    const formattedDate = milestone.date
      ? new Date(milestone.date + "T00:00:00").toLocaleDateString("en-US", { month: "short", day: "numeric" })
      : null;
    const relativeDate = milestone.date ? formatRelativeDate(milestone.date) : null;
    const isOverdue = status === "overdue";
    ```

    Template -- vertical timeline style with status dots (per UI-SPEC milestone states):
    ```html
    <div class="flex gap-4 py-4">
      <!-- Status dot -->
      <div class="flex flex-col items-center">
        <div class:list={[
          "w-7 h-7 rounded-full flex items-center justify-center shrink-0",
          status === "completed" && "bg-terracotta text-white",
          status === "current" && "bg-cream ring-4 ring-terracotta/20 border-2 border-terracotta",
          status === "overdue" && "bg-cream border-2 border-stone-light",
          status === "upcoming" && "bg-cream-dark border-2 border-stone-light/40",
        ]}>
          {status === "completed" && (
            <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="3" stroke-linecap="round" stroke-linejoin="round" class="w-3.5 h-3.5">
              <polyline points="20 6 9 17 4 12" />
            </svg>
          )}
        </div>
      </div>

      <!-- Content -->
      <div class="flex-1 min-w-0">
        <div class="flex items-baseline justify-between gap-2">
          <h3 class:list={[
            "font-body text-sm",
            status === "completed" ? "text-stone" : "text-charcoal",
          ]}>
            {milestone.name}
          </h3>
          {formattedDate && (
            <div class="flex items-center gap-2 shrink-0">
              <span class:list={[
                "text-xs font-body",
                isOverdue ? "text-[#D4A08A]" : "text-stone",
              ]}>
                {formattedDate}
              </span>
              {!milestone.completed && relativeDate && (
                <span class:list={[
                  "text-xs font-body",
                  isOverdue ? "text-[#D4A08A] bg-stone-light/10 px-2 py-0.5 rounded" : "text-stone-light",
                ]}>
                  {isOverdue ? "Overdue" : relativeDate}
                </span>
              )}
            </div>
          )}
        </div>

        {milestone.description && (
          <p class="text-xs text-stone font-body mt-1 leading-relaxed">
            {milestone.description}
          </p>
        )}

        <!-- Existing notes -->
        {milestone.notes && milestone.notes.length > 0 && (
          <div class="mt-3 space-y-2">
            {milestone.notes.map(note => (
              <div class="bg-cream-dark px-3 py-2 text-xs font-body">
                <span class="text-charcoal tracking-wide">{note.clientName}</span>
                <span class="text-stone-light mx-1">&middot;</span>
                <span class="text-stone-light">
                  {new Date(note.timestamp).toLocaleDateString("en-US", { month: "short", day: "numeric", year: "numeric" })}
                </span>
                <p class="text-stone mt-1">{note.text}</p>
              </div>
            ))}
          </div>
        )}

        <!-- ClientNoteForm placeholder (Plan 03 adds interactive form) -->
      </div>
    </div>
    ```

    **3. Create ProcurementTable.astro:**

    Props:
    ```typescript
    interface Props {
      items: Array<{
        _key: string;
        name: string;
        status: string;
        installDate?: string;
        retailPrice?: number;
        savings?: number;
        trackingNumber?: string;
      }>;
    }
    ```

    Import `formatCurrency` from `formatCurrency`. Import `getTrackingInfo` from `trackingUrl`.

    In frontmatter:
    ```typescript
    // Status priority sort: pending=0, ordered=1, warehouse=2, in-transit=3, delivered=4, installed=5
    const STATUS_PRIORITY: Record<string, number> = {
      "pending": 0, "ordered": 1, "warehouse": 2, "in-transit": 3, "delivered": 4, "installed": 5,
    };
    const sorted = [...items].sort((a, b) => (STATUS_PRIORITY[a.status] ?? 99) - (STATUS_PRIORITY[b.status] ?? 99));
    const totalSavings = items.reduce((sum, item) => sum + (item.savings || 0), 0);

    const STATUS_LABELS: Record<string, string> = {
      "pending": "Pending", "ordered": "Ordered", "warehouse": "Warehouse",
      "in-transit": "In Transit", "delivered": "Delivered", "installed": "Installed",
    };
    const STATUS_STYLES: Record<string, string> = {
      "pending": "bg-stone-light/20 text-stone",
      "ordered": "bg-terracotta/10 text-terracotta",
      "warehouse": "bg-terracotta/10 text-terracotta",
      "in-transit": "bg-terracotta/10 text-terracotta",
      "delivered": "bg-emerald-50 text-emerald-700",
      "installed": "bg-emerald-50 text-emerald-700",
    };
    ```

    Template:
    ```html
    <section class="py-12 border-t border-stone-light/20">
      <h2 class="font-heading text-xl font-light text-charcoal mb-6">
        Procurement
      </h2>

      {items.length === 0 ? (
        <div class="text-center py-8">
          <p class="text-sm text-stone font-body font-light">No procurement items added yet.</p>
          <p class="text-xs text-stone-light font-body mt-1">Items will appear here as they're ordered.</p>
        </div>
      ) : (
        <div class="overflow-x-auto -mx-6 px-6">
          <table class="w-full text-sm font-body">
            <caption class="sr-only">Procurement items for this project</caption>
            <thead>
              <tr class="border-b border-stone-light/20">
                <th scope="col" class="text-left text-xs uppercase tracking-widest text-stone font-normal pb-3">Item</th>
                <th scope="col" class="text-center text-xs uppercase tracking-widest text-stone font-normal pb-3 w-[120px]">Status</th>
                <th scope="col" class="text-left text-xs uppercase tracking-widest text-stone font-normal pb-3 w-[100px] hidden md:table-cell">Install Date</th>
                <th scope="col" class="text-right text-xs uppercase tracking-widest text-stone font-normal pb-3 w-[100px]">MSRP</th>
                <th scope="col" class="text-right text-xs uppercase tracking-widest text-stone font-normal pb-3 w-[100px]">Savings</th>
                <th scope="col" class="text-left text-xs uppercase tracking-widest text-stone font-normal pb-3 w-[100px] hidden md:table-cell">Tracking</th>
              </tr>
            </thead>
            <tbody>
              {sorted.map(item => {
                const tracking = item.trackingNumber ? getTrackingInfo(item.trackingNumber) : null;
                return (
                  <tr class="border-b border-stone-light/10">
                    <td class="py-3 text-charcoal">{item.name}</td>
                    <td class="py-3 text-center">
                      <span class={`inline-flex px-3 py-1 rounded-full text-xs tracking-wide ${STATUS_STYLES[item.status] || "bg-stone-light/20 text-stone"}`}>
                        {STATUS_LABELS[item.status] || item.status}
                      </span>
                    </td>
                    <td class="py-3 text-stone hidden md:table-cell">
                      {item.installDate ? new Date(item.installDate + "T00:00:00").toLocaleDateString("en-US", { month: "short", day: "numeric" }) : "—"}
                    </td>
                    <td class="py-3 text-right text-charcoal">
                      {item.retailPrice ? formatCurrency(item.retailPrice) : "—"}
                    </td>
                    <td class="py-3 text-right text-emerald-700">
                      {item.savings && item.savings > 0 ? formatCurrency(item.savings) : "—"}
                    </td>
                    <td class="py-3 hidden md:table-cell">
                      {tracking?.url ? (
                        <a href={tracking.url} target="_blank" rel="noopener" class="text-xs text-terracotta hover:text-terracotta-light transition-colors underline underline-offset-2">
                          {tracking.carrier.toUpperCase()}
                        </a>
                      ) : item.trackingNumber ? (
                        <span class="text-xs text-stone">{item.trackingNumber.slice(0, 10)}...</span>
                      ) : "—"}
                    </td>
                  </tr>
                );
              })}
            </tbody>
          </table>

          {totalSavings > 0 && (
            <div class="mt-4 text-right">
              <span class="text-sm font-body text-emerald-700">
                You saved {formatCurrency(totalSavings)} vs. retail
              </span>
            </div>
          )}
        </div>
      )}
    </section>
    ```

    **4. Update dashboard.astro for 30-day visibility filtering:**

    Add import: `import { isProjectVisible } from "../../lib/projectVisibility";`

    Replace the existing active/completed separation logic:
    ```typescript
    // OLD: const activeProjects = projects.filter((p: any) => p.pipelineStage !== "closeout");
    // OLD: const completedProjects = projects.filter((p: any) => p.pipelineStage === "closeout");
    // NEW: Use projectStatus and 30-day visibility window
    const visibleProjects = projects.filter((p: any) => isProjectVisible(p));
    const activeProjects = visibleProjects.filter((p: any) => p.projectStatus !== "completed");
    const completedProjects = visibleProjects.filter((p: any) => p.projectStatus === "completed");
    ```

    This filters out completed projects older than 30 days (CLNT-07). The rest of the dashboard template remains the same -- it already renders active and completed sections.
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -20 && npx vitest run -x</automated>
  </verify>
  <acceptance_criteria>
    - src/components/portal/MilestoneSection.astro contains `import { sortMilestones, getMilestoneProgress }` and `MilestoneTimeline`
    - src/components/portal/MilestoneSection.astro contains `No milestones added yet` empty state
    - src/components/portal/MilestoneSection.astro contains `of {progress.total} complete` progress display
    - src/components/portal/MilestoneItem.astro contains `getMilestoneStatus` import
    - src/components/portal/MilestoneItem.astro contains `Overdue` text for overdue milestones
    - src/components/portal/MilestoneItem.astro contains `formatRelativeDate` for upcoming dates
    - src/components/portal/ProcurementTable.astro contains `import { formatCurrency }` and `import { getTrackingInfo }`
    - src/components/portal/ProcurementTable.astro contains `You saved` and `vs. retail`
    - src/components/portal/ProcurementTable.astro contains `STATUS_PRIORITY` for sorting
    - src/components/portal/ProcurementTable.astro contains `<caption class="sr-only">` for accessibility
    - src/components/portal/ProcurementTable.astro does NOT contain `clientCost` anywhere
    - src/pages/portal/dashboard.astro contains `import { isProjectVisible }` from projectVisibility
    - src/pages/portal/dashboard.astro contains `isProjectVisible` in filter logic
    - npm run build succeeds
    - npx vitest run -x passes
  </acceptance_criteria>
  <done>Milestones section shows sorted milestones with progress bar, status dots, overdue indicators, and relative dates. Procurement table shows items with status badges, MSRP, savings, tracking links, and total savings footer. Dashboard filters completed projects by 30-day visibility window. No client cost exposed anywhere.</done>
</task>

</tasks>

<verification>
- `npm run build` -- build succeeds with all new pages and components
- `npx vitest run -x` -- all tests pass
- `grep -r "clientCost" src/components/` -- returns no results (client cost never in components)
- `grep "Please don't share your access link" src/components/portal/ConfidentialityBanner.astro` -- confidentiality notice present
- `grep "isProjectVisible" src/pages/portal/dashboard.astro` -- 30-day filtering active
</verification>

<success_criteria>
- Project detail page at /portal/project/[projectId] renders with all structural sections
- Engagement type gating: procurement hidden for non-full-interior-design projects
- Milestones section shows progress, pipeline status bar, sorted milestone list with status dots
- Procurement table shows items, status badges, MSRP, savings, tracking links, total savings
- Dashboard uses 30-day visibility filtering for completed projects
- Confidentiality banner visible below project header
- Post-project banner shows for completed projects
- Build passes, tests pass, no client cost exposed in portal
</success_criteria>

<output>
After completion, create `.planning/phases/06-portal-features/06-02-SUMMARY.md`
</output>
