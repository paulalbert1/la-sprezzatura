---
phase: 11-rendering-studio-tool-and-design-options-gallery
plan: 04
type: execute
wave: 2
depends_on: ["11-01"]
files_modified:
  - src/components/portal/DesignOptionsSection.astro
  - src/components/portal/DesignOptionCard.astro
  - src/components/portal/DesignOptionLightbox.tsx
  - src/pages/portal/project/[projectId].astro
autonomous: false
requirements:
  - RNDR-05
must_haves:
  truths:
    - "Client sees a Design Options gallery section on their project page when promoted options exist"
    - "Gallery shows responsive grid: 1 column mobile, 2 tablet, 3 desktop"
    - "Gallery cards show 16:9 cropped images with caption, heart icon, and comment count"
    - "Client can click a card to open a lightbox with full uncropped image"
    - "Client can toggle favorite with heart icon (optimistic UI + API call)"
    - "Client can view and post comments in the lightbox"
    - "Confidentiality notice appears above the gallery with rendering-specific copy"
    - "Gallery section is hidden when there are 0 design options"
    - "Favorites and comments use the existing /api/rendering/react endpoint"
  artifacts:
    - path: "src/components/portal/DesignOptionsSection.astro"
      provides: "Gallery grid section with confidentiality notice"
      contains: "Design Options"
    - path: "src/components/portal/DesignOptionCard.astro"
      provides: "Individual gallery card with 16:9 crop, heart, comment count"
      contains: "aspect-[16/9]"
    - path: "src/components/portal/DesignOptionLightbox.tsx"
      provides: "React island lightbox with heart toggle and comment thread"
      exports: ["default"]
    - path: "src/pages/portal/project/[projectId].astro"
      provides: "Project page with Design Options section wired in"
      contains: "DesignOptionsSection"
  key_links:
    - from: "src/pages/portal/project/[projectId].astro"
      to: "src/components/portal/DesignOptionsSection.astro"
      via: "conditional render when designOptions.length > 0"
      pattern: "designOptions.length > 0"
    - from: "src/components/portal/DesignOptionsSection.astro"
      to: "src/components/portal/DesignOptionLightbox.tsx"
      via: "React island with client:load"
      pattern: "client:load"
    - from: "src/components/portal/DesignOptionLightbox.tsx"
      to: "/api/rendering/react"
      via: "fetch POST for favorites and comments"
      pattern: "api/rendering/react"
---

<objective>
Build the client portal Design Options gallery -- the client-facing section where promoted renderings are visible with favorites and comments.

Purpose: This completes RNDR-05. Clients viewing their project on the portal see a curated gallery of Liz's promoted design options. They can browse, favorite, and comment -- providing structured feedback on design directions. Only promoted renderings are visible; the rendering session, wizard inputs, and iteration history are never exposed.

Output: DesignOptionsSection.astro (gallery grid), DesignOptionCard.astro (individual card), DesignOptionLightbox.tsx (React island with heart toggle + comment thread), and project page wired to conditionally render the section.
</objective>

<execution_context>
@/Users/paulalbert/.claude/get-shit-done/workflows/execute-plan.md
@/Users/paulalbert/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-rendering-studio-tool-and-design-options-gallery/11-CONTEXT.md
@.planning/phases/11-rendering-studio-tool-and-design-options-gallery/11-RESEARCH.md
@.planning/phases/11-rendering-studio-tool-and-design-options-gallery/11-UI-SPEC.md
@.planning/phases/11-rendering-studio-tool-and-design-options-gallery/11-01-SUMMARY.md

<interfaces>
<!-- Key contracts from existing code -->

From src/sanity/queries.ts:
```typescript
export const DESIGN_OPTIONS_BY_PROJECT_QUERY = `
  *[_type == "designOption" && project._ref == $projectId] | order(sortOrder asc) {
    _id,
    blobPathname,
    caption,
    sortOrder,
    promotedAt,
    reactions[] {
      _key,
      clientId,
      type,
      text,
      createdAt
    }
  }
`;
```

From src/pages/api/rendering/react.ts:
```typescript
// POST body: { designOptionId, type: "favorite"|"unfavorite"|"comment", text?: string }
// Auth: portal session cookie (getSession), role === "client"
// Returns 200: { success: true }
```

From src/pages/api/blob-serve.ts:
```typescript
// GET /api/blob-serve?path={blobPathname}
// Auth: portal session cookie (getSession) -- already works for portal
// Returns: image stream with content-type header
```

From src/pages/portal/project/[projectId].astro:
```typescript
// Existing sections: ProjectHeader, ConfidentialityBanner, PostProjectBanner,
//   MilestoneSection, ContractorSection, ProcurementTable, ArtifactSection,
//   CloseDocument, WarrantyClaimForm
// clientId available from Astro.locals.clientId
// project fetched via getProjectDetail(projectId, clientId)
// isCommercial check: project.isCommercial (if available from schema)
```

From src/components/portal/ConfidentialityBanner.astro:
```astro
<!-- Existing pattern: div.border.border-stone-light/20.bg-cream-dark.px-5.py-3 -->
<!-- Phase 11 uses adapted copy inline, not this component directly -->
```

From src/styles/global.css:
```css
/* Color tokens: cream #FAF8F5, terracotta #C4836A, charcoal #2C2926, stone #8A8478, stone-light #B8B0A4 */
/* Fonts: --font-heading (Cormorant Garamond), --font-body (DM Sans) */
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: DesignOptionsSection, DesignOptionCard, and project page wiring</name>
  <files>
    src/components/portal/DesignOptionsSection.astro,
    src/components/portal/DesignOptionCard.astro,
    src/pages/portal/project/[projectId].astro
  </files>
  <read_first>
    src/pages/portal/project/[projectId].astro,
    src/sanity/queries.ts,
    src/components/portal/ConfidentialityBanner.astro,
    src/components/portal/MilestoneSection.astro,
    src/components/portal/ArtifactSection.astro,
    src/styles/global.css,
    .planning/phases/11-rendering-studio-tool-and-design-options-gallery/11-UI-SPEC.md (lines 753-838 for gallery section and card specs)
  </read_first>
  <action>
**1. Create `src/components/portal/DesignOptionsSection.astro`:**

Props interface:
```typescript
interface Props {
  options: Array<{
    _id: string;
    blobPathname: string;
    caption: string;
    sortOrder: number;
    promotedAt: string;
    reactions: Array<{ _key: string; clientId: string; type: string; text?: string; createdAt: string }>;
  }>;
  clientId: string;
  projectId: string;
  isCommercial?: boolean;
}
```

Compute enriched options in frontmatter:
```typescript
const enrichedOptions = options.map(opt => ({
  ...opt,
  favoriteCount: (opt.reactions || []).filter(r => r.type === "favorite").length,
  commentCount: (opt.reactions || []).filter(r => r.type === "comment").length,
  isFavorited: (opt.reactions || []).some(r => r.type === "favorite" && r.clientId === clientId),
}));
```

Layout (per UI spec lines 771-793):
```html
<section class="py-12 border-t border-stone-light/20" aria-label="Design Options">
  <!-- Confidentiality notice -->
  <div class="border border-stone-light/20 bg-cream-dark px-5 py-3 text-center mb-8">
    <p class="text-xs text-stone font-body tracking-wide">
      {isCommercial
        ? "These design concepts are confidential to your building project. Please do not share."
        : "These design concepts are confidential. Please do not share."}
    </p>
  </div>

  <!-- Section heading -->
  <h2 class="font-heading text-xl font-light text-charcoal mb-6">Design Options</h2>

  <!-- Gallery grid -->
  <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-4">
    {enrichedOptions.map((opt, index) => (
      <DesignOptionCard option={opt} index={index} />
    ))}
  </div>

  <!-- Lightbox React island -->
  <DesignOptionLightbox
    client:load
    options={enrichedOptions}
    clientId={clientId}
    projectId={projectId}
  />
</section>
```

Import DesignOptionCard and DesignOptionLightbox at top.

**2. Create `src/components/portal/DesignOptionCard.astro`:**

Props:
```typescript
interface Props {
  option: {
    _id: string;
    blobPathname: string;
    caption: string;
    favoriteCount: number;
    commentCount: number;
    isFavorited: boolean;
  };
  index: number;
}
```

Layout (per UI spec lines 799-830):
```html
<article
  class="group cursor-pointer"
  data-option-index={index}
  role="button"
  aria-label={`View ${option.caption || "design option"}`}
  tabindex="0"
>
  <!-- Image container: 16:9 crop -->
  <div class="overflow-hidden aspect-[16/9] relative bg-cream-dark">
    <img
      class="w-full h-full object-cover transition-transform duration-500 group-hover:scale-[1.02]"
      src={`/api/blob-serve?path=${encodeURIComponent(option.blobPathname)}`}
      alt={option.caption || "Design option"}
      loading="lazy"
    />
  </div>

  <!-- Card footer -->
  <div class="flex items-center justify-between pt-3 pb-1">
    <!-- Caption -->
    <p class="text-xs text-charcoal font-body truncate flex-1 mr-3">
      {option.caption || "Untitled"}
    </p>

    <!-- Interaction indicators -->
    <div class="flex items-center gap-3">
      <!-- Heart + count -->
      <div class="flex items-center gap-1">
        {option.isFavorited ? (
          <!-- Filled heart: terracotta -->
          <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="#C4836A" class="w-4 h-4">
            <path d="M11.645 20.91l-.007-.003-.022-.012a15.247 15.247 0 01-.383-.218 25.18 25.18 0 01-4.244-3.17C4.688 15.36 2.25 12.174 2.25 8.25 2.25 5.322 4.714 3 7.688 3A5.5 5.5 0 0112 5.052 5.5 5.5 0 0116.313 3c2.973 0 5.437 2.322 5.437 5.25 0 3.925-2.438 7.111-4.739 9.256a25.175 25.175 0 01-4.244 3.17 15.247 15.247 0 01-.383.219l-.022.012-.007.004-.003.001a.752.752 0 01-.704 0l-.003-.001z" />
          </svg>
        ) : (
          <!-- Outlined heart: stone -->
          <svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="#8A8478" class="w-4 h-4">
            <path stroke-linecap="round" stroke-linejoin="round" d="M21 8.25c0-2.485-2.099-4.5-4.688-4.5-1.935 0-3.597 1.126-4.312 2.733-.715-1.607-2.377-2.733-4.313-2.733C5.1 3.75 3 5.765 3 8.25c0 7.22 9 12 9 12s9-4.78 9-12z" />
          </svg>
        )}
        <span class="text-xs text-stone font-body">{option.favoriteCount}</span>
      </div>

      <!-- Comment count (only if > 0) -->
      {option.commentCount > 0 && (
        <div class="flex items-center gap-1">
          <svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="#8A8478" class="w-4 h-4">
            <path stroke-linecap="round" stroke-linejoin="round" d="M12 20.25c4.97 0 9-3.694 9-8.25s-4.03-8.25-9-8.25S3 7.444 3 12c0 2.104.859 4.023 2.273 5.48.432.447.74 1.04.586 1.641a4.483 4.483 0 01-.923 1.785A5.969 5.969 0 006 21c1.282 0 2.47-.402 3.445-1.087.81.22 1.668.337 2.555.337z" />
          </svg>
          <span class="text-xs text-stone font-body">{option.commentCount}</span>
        </div>
      )}
    </div>
  </div>
</article>
```

Add a `<script>` block at the bottom to dispatch a custom event on click/Enter:
```html
<script>
  document.addEventListener("astro:page-load", () => {
    document.querySelectorAll("[data-option-index]").forEach((card) => {
      const handler = () => {
        const index = parseInt(card.getAttribute("data-option-index") || "0", 10);
        window.dispatchEvent(new CustomEvent("open-design-lightbox", { detail: { index } }));
      };
      card.addEventListener("click", handler);
      card.addEventListener("keydown", (e) => {
        if ((e as KeyboardEvent).key === "Enter" || (e as KeyboardEvent).key === " ") {
          e.preventDefault();
          handler();
        }
      });
    });
  });
</script>
```

Hover state: `group-hover:scale-[1.02]` on image with `duration-500` transition. Respects `prefers-reduced-motion: reduce` via Tailwind `motion-safe:` prefix (or add a `@media (prefers-reduced-motion: reduce)` override).

**3. Update `src/pages/portal/project/[projectId].astro`:**

Add import at top:
```typescript
import DesignOptionsSection from "../../../components/portal/DesignOptionsSection.astro";
```

Add GROQ fetch for design options after the existing `getProjectDetail` call:
```typescript
import { sanityClient } from "sanity:client";
import { DESIGN_OPTIONS_BY_PROJECT_QUERY } from "../../../sanity/queries";

// Fetch design options for this project
const designOptions = await sanityClient.fetch(DESIGN_OPTIONS_BY_PROJECT_QUERY, { projectId }) || [];
```

Add DesignOptionsSection to the template, between MilestoneSection and ContractorSection (or between ContractorSection and ProcurementTable -- wherever fits the visual flow best). Use conditional rendering:

```astro
<!-- Design Options gallery (only if promoted options exist) -->
{designOptions.length > 0 && (
  <DesignOptionsSection
    options={designOptions}
    clientId={clientId}
    projectId={project._id}
    isCommercial={project.isCommercial}
  />
)}
```

Place this AFTER the MilestoneSection and ContractorSection blocks but BEFORE ProcurementTable:

```astro
<MilestoneSection ... />

{showProcurement && project.contractors && (
  <ContractorSection contractors={project.contractors} />
)}

<!-- Design Options gallery (only if promoted options exist) -->
{designOptions.length > 0 && (
  <DesignOptionsSection
    options={designOptions}
    clientId={clientId}
    projectId={project._id}
    isCommercial={project.isCommercial}
  />
)}

{showProcurement && (
  <ProcurementTable items={project.procurementItems || []} />
)}
```

Note: `project.isCommercial` may need to be added to the PROJECT_DETAIL_QUERY projection. Check if it's already included. If not, add `isCommercial` to the GROQ projection in the frontmatter fetch or use a separate query.
  </action>
  <verify>
    <automated>
      test -f src/components/portal/DesignOptionsSection.astro && test -f src/components/portal/DesignOptionCard.astro && grep -q "DesignOptionsSection" src/pages/portal/project/\\[projectId\\].astro && grep -q "DESIGN_OPTIONS_BY_PROJECT_QUERY" src/pages/portal/project/\\[projectId\\].astro && echo "PASS" || echo "FAIL"
    </automated>
  </verify>
  <acceptance_criteria>
    - `src/components/portal/DesignOptionsSection.astro` contains `"Design Options"` heading text
    - `src/components/portal/DesignOptionsSection.astro` contains `"These design concepts are confidential"` notice
    - `src/components/portal/DesignOptionsSection.astro` contains `grid-cols-1 sm:grid-cols-2 lg:grid-cols-3`
    - `src/components/portal/DesignOptionsSection.astro` contains `client:load` for DesignOptionLightbox
    - `src/components/portal/DesignOptionsSection.astro` contains `aria-label="Design Options"` on section
    - `src/components/portal/DesignOptionCard.astro` contains `aspect-[16/9]` for 16:9 crop
    - `src/components/portal/DesignOptionCard.astro` contains `blob-serve` URL pattern
    - `src/components/portal/DesignOptionCard.astro` contains `open-design-lightbox` custom event
    - `src/components/portal/DesignOptionCard.astro` contains heart SVG icon (filled and outlined)
    - `src/components/portal/DesignOptionCard.astro` contains `role="button"` for accessibility
    - `src/pages/portal/project/[projectId].astro` contains `import DesignOptionsSection`
    - `src/pages/portal/project/[projectId].astro` contains `DESIGN_OPTIONS_BY_PROJECT_QUERY`
    - `src/pages/portal/project/[projectId].astro` contains `designOptions.length > 0`
  </acceptance_criteria>
  <done>DesignOptionsSection renders responsive gallery grid with confidentiality notice, DesignOptionCard shows 16:9 cropped images with heart/comment indicators, project page conditionally renders gallery section when promoted options exist</done>
</task>

<task type="auto">
  <name>Task 2: DesignOptionLightbox React island with heart toggle and comments</name>
  <files>
    src/components/portal/DesignOptionLightbox.tsx
  </files>
  <read_first>
    src/components/portal/DesignOptionsSection.astro,
    src/components/portal/DesignOptionCard.astro,
    src/pages/api/rendering/react.ts,
    src/components/portfolio/Lightbox.astro,
    .planning/phases/11-rendering-studio-tool-and-design-options-gallery/11-UI-SPEC.md (lines 841-958 for lightbox and interaction specs)
  </read_first>
  <action>
Create `src/components/portal/DesignOptionLightbox.tsx` as a React component (default export for Astro island):

Props:
```typescript
interface DesignOption {
  _id: string;
  blobPathname: string;
  caption: string;
  favoriteCount: number;
  commentCount: number;
  isFavorited: boolean;
  reactions: Array<{
    _key: string;
    clientId: string;
    type: string;
    text?: string;
    createdAt: string;
  }>;
}

interface Props {
  options: DesignOption[];
  clientId: string;
  projectId: string;
}
```

**State:**
- `isOpen`: boolean (controlled by custom event listener)
- `currentIndex`: number (which option is displayed)
- `localFavorites`: `Record<string, boolean>` (optimistic favorite state per option _id)
- `localComments`: `Record<string, Array<{ clientName: string; text: string; createdAt: string }>>` (optimistic comments)
- `commentText`: string (current input value)
- `isSubmitting`: boolean (comment submit in progress)
- `commentError`: string | null (error message, auto-clears after 5s)

**Event listener:** Listen for `open-design-lightbox` custom event (dispatched by DesignOptionCard script):
```typescript
useEffect(() => {
  const handler = (e: CustomEvent) => {
    setCurrentIndex(e.detail.index);
    setIsOpen(true);
    document.body.style.overflow = "hidden"; // Lock scroll
  };
  window.addEventListener("open-design-lightbox", handler as EventListener);
  return () => {
    window.removeEventListener("open-design-lightbox", handler as EventListener);
    document.body.style.overflow = ""; // Unlock on cleanup
  };
}, []);
```

**Keyboard navigation:**
```typescript
useEffect(() => {
  if (!isOpen) return;
  const handler = (e: KeyboardEvent) => {
    if (e.key === "Escape") close();
    if (e.key === "ArrowLeft" && currentIndex > 0) setCurrentIndex(i => i - 1);
    if (e.key === "ArrowRight" && currentIndex < options.length - 1) setCurrentIndex(i => i + 1);
  };
  window.addEventListener("keydown", handler);
  return () => window.removeEventListener("keydown", handler);
}, [isOpen, currentIndex, options.length]);
```

**Touch swipe:** Track `touchStart` and `touchEnd` X positions. If delta > 50px, navigate left/right.

**Heart toggle (per UI spec lines 925-941):**
```typescript
const toggleFavorite = async (optionId: string, currentlyFavorited: boolean) => {
  // Optimistic update
  setLocalFavorites(prev => ({ ...prev, [optionId]: !currentlyFavorited }));

  try {
    await fetch("/api/rendering/react", {
      method: "POST",
      headers: { "Content-Type": "application/json" },
      body: JSON.stringify({
        designOptionId: optionId,
        type: currentlyFavorited ? "unfavorite" : "favorite",
      }),
    });
  } catch {
    // Revert on failure (silent -- no toast)
    setLocalFavorites(prev => ({ ...prev, [optionId]: currentlyFavorited }));
  }
};
```

Heart animation (per UI spec lines 925-941):
- When toggling to favorited: CSS keyframe `scale(1) -> scale(1.2) -> scale(1)` over 300ms ease-out
- When toggling to unfavorited: no animation, immediate change
- Inline style or CSS class toggle: `style={{ transform: animating ? "scale(1.2)" : "scale(1)", transition: "transform 0.3s ease-out" }}`

**Comment submission (per UI spec lines 943-958):**
```typescript
const submitComment = async () => {
  if (!commentText.trim() || isSubmitting) return;
  setIsSubmitting(true);
  setCommentError(null);

  try {
    const res = await fetch("/api/rendering/react", {
      method: "POST",
      headers: { "Content-Type": "application/json" },
      body: JSON.stringify({
        designOptionId: currentOption._id,
        type: "comment",
        text: commentText.trim(),
      }),
    });

    if (!res.ok) throw new Error();

    // Optimistic append
    setLocalComments(prev => ({
      ...prev,
      [currentOption._id]: [
        ...(prev[currentOption._id] || []),
        { clientName: "You", text: commentText.trim(), createdAt: new Date().toISOString() },
      ],
    }));
    setCommentText("");
  } catch {
    setCommentError("Failed to post comment. Try again.");
    setTimeout(() => setCommentError(null), 5000);
  } finally {
    setIsSubmitting(false);
  }
};
```

**Relative time helper:** Simple function returning "Just now", "2m ago", "1h ago", "3d ago" etc.

**Layout (per UI spec lines 841-921):**

```html
<!-- Fixed overlay -->
<div class="fixed inset-0 z-50" style={{ opacity: isOpen ? 1 : 0, visibility: isOpen ? "visible" : "hidden", transition: "opacity 0.25s ease, visibility 0.25s ease" }}>

  <!-- Dark backdrop -->
  <div class="absolute inset-0 bg-charcoal/90" onClick={close} />

  <!-- Content -->
  <div class="relative h-full flex flex-col items-center justify-center p-4 md:p-8">

    <!-- Close button -->
    <button class="absolute top-4 right-4 text-white/80 hover:text-white min-h-[44px] min-w-[44px]" onClick={close} aria-label="Close">
      <!-- Close X SVG 32x32 -->
    </button>

    <!-- Prev arrow (hidden when index === 0) -->
    {currentIndex > 0 && (
      <button class="absolute left-3 md:left-6 text-white/70 hover:text-white min-h-[44px] min-w-[44px]" onClick={() => setCurrentIndex(i => i - 1)} aria-label="Previous">
        <!-- ChevronLeft SVG 40x40 -->
      </button>
    )}

    <!-- Main image -->
    <div class="max-w-5xl max-h-[65vh] w-full flex items-center justify-center">
      <img
        class="max-w-full max-h-[65vh] object-contain"
        src={`/api/blob-serve?path=${encodeURIComponent(currentOption.blobPathname)}`}
        alt={currentOption.caption}
      />
    </div>

    <!-- Next arrow (hidden when index === last) -->
    {currentIndex < options.length - 1 && (
      <button class="absolute right-3 md:right-6 text-white/70 hover:text-white min-h-[44px] min-w-[44px]" onClick={() => setCurrentIndex(i => i + 1)} aria-label="Next">
        <!-- ChevronRight SVG 40x40 -->
      </button>
    )}

    <!-- Below image: interactive panel -->
    <div class="w-full max-w-5xl mt-4">

      <!-- Caption + heart row -->
      <div class="flex items-center justify-between mb-4">
        <p class="text-sm text-white/80 font-body">{currentOption.caption}</p>
        <button
          class="flex items-center gap-2 min-h-[44px] min-w-[44px] justify-center"
          onClick={() => toggleFavorite(currentOption._id, isFavorited)}
          aria-label={isFavorited ? "Remove from favorites" : "Add to favorites"}
          aria-pressed={isFavorited}
          role="button"
        >
          <!-- Heart SVG 20px: filled terracotta if favorited, outlined white/60 if not -->
          <span class="text-xs text-white/60">{adjustedFavoriteCount}</span>
        </button>
      </div>

      <!-- Comment thread -->
      <div class="max-h-[25vh] overflow-y-auto space-y-3 mb-4">
        {allComments.map(comment => (
          <div class="text-sm font-body">
            <span class="text-white/90 font-medium">{comment.clientName}</span>
            <span class="text-white/40 mx-1">.</span>
            <span class="text-white/40 text-xs">{relativeTime(comment.createdAt)}</span>
            <p class="text-white/70 mt-1">{comment.text}</p>
          </div>
        ))}
      </div>

      <!-- Comment input -->
      <div class="flex gap-2">
        <input
          class="flex-1 bg-white/10 text-white text-sm px-4 py-3 font-body border border-white/20 focus:border-white/40 focus:outline-none placeholder-white/30"
          placeholder="Leave a comment..."
          maxLength={500}
          value={commentText}
          onChange={e => setCommentText(e.target.value)}
          onKeyDown={e => e.key === "Enter" && submitComment()}
          aria-label="Leave a comment"
        />
        <button
          class="bg-terracotta text-white text-xs uppercase tracking-widest font-body px-6 py-3 hover:bg-terracotta-light transition-colors min-h-[44px] disabled:opacity-50"
          onClick={submitComment}
          disabled={!commentText.trim() || isSubmitting}
        >
          {isSubmitting ? "Posting..." : "Post Comment"}
        </button>
      </div>

      {commentError && (
        <p class="text-xs text-red-400 mt-1" role="alert">{commentError}</p>
      )}
    </div>

    <!-- Counter -->
    <div class="absolute bottom-4 left-1/2 -translate-x-1/2 text-white/60 text-xs font-body tracking-widest">
      {currentIndex + 1} / {options.length}
    </div>
  </div>
</div>
```

**Multi-client visibility (per UI spec lines 962-974):**
- Gallery card heart: filled if CURRENT client favorited (check option.isFavorited or localFavorites override)
- Favorite count: TOTAL count across all clients, adjusted by local optimistic changes
- Comments: show ALL comments with client names. Use `clientId` to determine which comments are from current client. For the current client's comments, show "You" as the name.

**Reduced motion (per UI spec Accessibility):**
- Lightbox open/close: respect `prefers-reduced-motion: reduce` (instant show/hide instead of opacity transition)
- Heart animation: disabled when reduced motion preferred
- Check with `window.matchMedia("(prefers-reduced-motion: reduce)").matches`

**Focus trap:**
- When lightbox opens, focus the close button
- Tab cycles through: close, prev, next, heart, comment input, submit

**Prefetch adjacent images:**
```typescript
useEffect(() => {
  if (!isOpen) return;
  // Prefetch prev and next images
  [currentIndex - 1, currentIndex + 1].forEach(i => {
    if (i >= 0 && i < options.length) {
      const img = new Image();
      img.src = `/api/blob-serve?path=${encodeURIComponent(options[i].blobPathname)}`;
    }
  });
}, [isOpen, currentIndex, options]);
```
  </action>
  <verify>
    <automated>
      grep -q "export default" src/components/portal/DesignOptionLightbox.tsx && grep -q "api/rendering/react" src/components/portal/DesignOptionLightbox.tsx && grep -q "open-design-lightbox" src/components/portal/DesignOptionLightbox.tsx && grep -q "ArrowLeft" src/components/portal/DesignOptionLightbox.tsx && grep -q "Leave a comment" src/components/portal/DesignOptionLightbox.tsx && echo "PASS" || echo "FAIL"
    </automated>
  </verify>
  <acceptance_criteria>
    - `src/components/portal/DesignOptionLightbox.tsx` contains `export default` (default export for Astro island)
    - `src/components/portal/DesignOptionLightbox.tsx` contains `open-design-lightbox` event listener
    - `src/components/portal/DesignOptionLightbox.tsx` contains `api/rendering/react` fetch call
    - `src/components/portal/DesignOptionLightbox.tsx` contains `"favorite"` and `"unfavorite"` type strings
    - `src/components/portal/DesignOptionLightbox.tsx` contains `"comment"` type string for comment submission
    - `src/components/portal/DesignOptionLightbox.tsx` contains `ArrowLeft` and `ArrowRight` keyboard handler
    - `src/components/portal/DesignOptionLightbox.tsx` contains `Escape` keyboard handler
    - `src/components/portal/DesignOptionLightbox.tsx` contains `"Leave a comment..."` placeholder
    - `src/components/portal/DesignOptionLightbox.tsx` contains `"Post Comment"` button text
    - `src/components/portal/DesignOptionLightbox.tsx` contains `"Posting..."` loading text
    - `src/components/portal/DesignOptionLightbox.tsx` contains `"Failed to post comment. Try again."` error text
    - `src/components/portal/DesignOptionLightbox.tsx` contains `min-h-[44px]` for WCAG touch targets
    - `src/components/portal/DesignOptionLightbox.tsx` contains `aria-label` attributes for accessibility
    - `src/components/portal/DesignOptionLightbox.tsx` contains `aria-pressed` on heart toggle
    - `src/components/portal/DesignOptionLightbox.tsx` contains `role="alert"` on error message
    - `src/components/portal/DesignOptionLightbox.tsx` contains `maxLength={500}` or `maxLength: 500` on comment input
    - `src/components/portal/DesignOptionLightbox.tsx` contains touch swipe detection (touchStart/touchEnd)
    - `src/components/portal/DesignOptionLightbox.tsx` contains `overflow` body scroll lock
  </acceptance_criteria>
  <done>DesignOptionLightbox renders full-screen lightbox with uncropped image, keyboard/touch navigation, heart toggle with optimistic UI and subtle animation, comment thread with submit/error handling, counter, close button, and accessibility features (focus trap, ARIA labels, reduced motion, 44px touch targets)</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visual verification of complete Phase 11 UI</name>
  <files>none</files>
  <what-built>
    Complete Phase 11 UI: Sanity Studio rendering tool (wizard, chat view, session list, Design Options tab, promote workflow) and client portal Design Options gallery (responsive grid, lightbox with favorites and comments).
  </what-built>
  <how-to-verify>
    1. Open Sanity Studio (`npx sanity dev`) and verify:
       - Rendering icon (sparkles) appears in sidebar
       - Sessions tab shows session list with New Session button and usage badge
       - Design Options tab shows promoted options grid
       - Click "New Session" to open wizard with 4 steps
       - Walk through Setup -> Upload -> Classify -> Describe
       - Click Generate (requires Gemini API key and valid session data)
       - After generation, verify chat view shows the result with action buttons

    2. Open the portal project page and verify:
       - Design Options section appears between milestones and procurement (if promoted options exist)
       - Gallery shows responsive grid (resize browser to verify 1/2/3 columns)
       - Confidentiality notice shows above the gallery
       - Click a card to open lightbox
       - Verify heart toggle, comment form, keyboard navigation (arrows, Escape)
       - Verify favorite/comment API calls work (check Network tab)

    3. Test edge cases:
       - Gallery hidden when 0 design options (no section rendered)
       - Scratchpad sessions show dashed border
       - Promote dialog requires project selection for scratchpad
       - Usage badge color changes based on threshold
  </how-to-verify>
  <action>User visually verifies the complete Phase 11 UI in both Sanity Studio and the client portal.</action>
  <verify>
    <automated>echo "CHECKPOINT: Manual verification required"</automated>
  </verify>
  <done>User confirms Studio tool and portal gallery are functioning correctly</done>
  <resume-signal>Type "approved" or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes (no type errors)
- Portal project page renders Design Options section when promoted options exist
- Gallery grid: 1 col mobile (<640px), 2 cols tablet (640-1023px), 3 cols desktop (>=1024px)
- Cards show 16:9 cropped images with caption, heart icon, comment count
- Lightbox opens on card click with full uncropped image
- Heart toggle calls /api/rendering/react with correct type
- Comment submit calls /api/rendering/react with type="comment"
- Keyboard navigation works: ArrowLeft, ArrowRight, Escape
- Touch swipe navigation works
- 44px minimum touch targets on heart, nav arrows, submit button
- Confidentiality notice shows correct copy (residential vs commercial)
- Gallery section absent when no design options exist
</verification>

<success_criteria>
- DesignOptionsSection.astro renders gallery grid with confidentiality notice
- DesignOptionCard.astro shows 16:9 crop with heart/comment indicators
- DesignOptionLightbox.tsx handles favorites (optimistic toggle + API), comments (submit + thread), navigation (keyboard + touch + arrows), and accessibility (ARIA labels, focus trap, reduced motion)
- Project page conditionally renders gallery section via DESIGN_OPTIONS_BY_PROJECT_QUERY
- Multi-client visibility: total counts shown, current client heart state tracked
- All copy matches the copywriting contract from 11-UI-SPEC.md
</success_criteria>

<output>
After completion, create `.planning/phases/11-rendering-studio-tool-and-design-options-gallery/11-04-SUMMARY.md`
</output>
