---
phase: 11-rendering-studio-tool-and-design-options-gallery
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/sanity/components/rendering/PromoteDialog.test.ts
  - src/sanity/components/rendering/UsageBadge.test.ts
  - src/components/portal/DesignOptionsSection.test.ts
  - src/components/portal/DesignOptionLightbox.test.ts
  - src/sanity/components/rendering/types.ts
  - src/sanity/components/rendering/RenderingToolPlugin.ts
  - src/sanity/components/rendering/RenderingTool.tsx
  - src/sanity/components/rendering/UsageBadge.tsx
  - sanity.config.ts
  - src/pages/api/blob-upload.ts
  - src/pages/api/blob-serve.ts
  - .env
  - .env.example
autonomous: true
requirements:
  - RNDR-04
must_haves:
  truths:
    - "Rendering tool icon (sparkles) appears in Sanity Studio sidebar alongside Structure"
    - "Clicking the Rendering sidebar icon shows a tool shell with Sessions and Design Options tabs"
    - "HEIC/HEIF images can be uploaded through the Blob upload endpoint"
    - "Studio tool components can read SANITY_STUDIO_API_SECRET environment variable"
    - "Both portal session auth and studio token auth can serve private Blob images"
    - "Wave 0 test stubs exist and are discoverable by vitest"
  artifacts:
    - path: "src/sanity/components/rendering/types.ts"
      provides: "Shared TypeScript interfaces for Studio tool and portal"
      exports: ["RenderingSession", "RenderingOutput", "DesignOption", "ImageInput", "UsageData", "WizardData"]
    - path: "src/sanity/components/rendering/RenderingToolPlugin.ts"
      provides: "Tool factory function for sanity.config.ts"
      exports: ["renderingTool"]
    - path: "src/sanity/components/rendering/RenderingTool.tsx"
      provides: "Main tool component with tab routing and state context"
      exports: ["RenderingTool"]
    - path: "src/sanity/components/rendering/UsageBadge.tsx"
      provides: "Color-coded usage counter badge"
      exports: ["UsageBadge"]
    - path: "sanity.config.ts"
      provides: "Studio config with rendering tool registered"
      contains: "renderingTool"
    - path: "src/sanity/components/rendering/PromoteDialog.test.ts"
      provides: "Wave 0 test stubs for promote/unpromote"
    - path: "src/sanity/components/rendering/UsageBadge.test.ts"
      provides: "Wave 0 test stubs for usage badge color tiers"
    - path: "src/components/portal/DesignOptionsSection.test.ts"
      provides: "Wave 0 test stubs for gallery rendering + hidden when 0"
    - path: "src/components/portal/DesignOptionLightbox.test.ts"
      provides: "Wave 0 test stubs for heart toggle + comment submit"
  key_links:
    - from: "sanity.config.ts"
      to: "src/sanity/components/rendering/RenderingToolPlugin.ts"
      via: "tools array import"
      pattern: "import.*renderingTool.*RenderingToolPlugin"
    - from: "src/sanity/components/rendering/RenderingToolPlugin.ts"
      to: "src/sanity/components/rendering/RenderingTool.tsx"
      via: "component reference"
      pattern: "component.*RenderingTool"
---

<objective>
Establish the foundation for Phase 11: Wave 0 test stubs, shared TypeScript types, Sanity Studio tool registration with shell UI (tabs + routing), UsageBadge component, HEIC upload support, dual-auth blob serving, and SANITY_STUDIO_ environment variable.

Purpose: Every subsequent plan (02: Wizard+SessionList, 03: ChatView+PromoteDialog, 04: PortalGallery) depends on the types, tool shell, and infrastructure established here. Building this first prevents duplicate work and ensures consistent contracts across all Studio and portal components. Wave 0 test stubs satisfy Nyquist compliance for the entire phase.

Output: Wave 0 test stubs for all 4 VALIDATION.md test files, a functioning Sanity Studio sidebar tool showing two tabs (Sessions/Design Options) with empty placeholder content, plus all shared infrastructure ready for implementation plans.
</objective>

<execution_context>
@/Users/paulalbert/.claude/get-shit-done/workflows/execute-plan.md
@/Users/paulalbert/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-rendering-studio-tool-and-design-options-gallery/11-CONTEXT.md
@.planning/phases/11-rendering-studio-tool-and-design-options-gallery/11-RESEARCH.md
@.planning/phases/11-rendering-studio-tool-and-design-options-gallery/11-UI-SPEC.md
@.planning/phases/11-rendering-studio-tool-and-design-options-gallery/11-VALIDATION.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From src/sanity/schemas/renderingSession.ts:
```typescript
// Sanity document type: "renderingSession"
// Field groups: setup, inputs, renderings, metadata
// Key fields: sessionTitle (string, required), project (ref to project),
//   aspectRatio (string: "16:9"|"1:1"|"4:3"), stylePreset (string),
//   description (text, required), images[] (array of renderingImage objects),
//   renderings[] (array of renderingOutput objects),
//   conversation[] (array of conversationEntry objects),
//   status ("idle"|"generating"|"complete"|"error"), createdBy (string), createdAt (datetime)
```

From src/sanity/schemas/designOption.ts:
```typescript
// Sanity document type: "designOption"
// Fields: project (ref, required), blobPathname (string, required),
//   caption (string), sourceSession (ref to renderingSession),
//   sourceRenderingIndex (number), sortOrder (number),
//   promotedAt (datetime), promotedBy (string),
//   reactions[] (array of { clientId, type: "favorite"|"comment", text, createdAt })
```

From src/sanity/queries.ts:
```typescript
export const RENDERING_SESSION_BY_ID_QUERY = `...`; // Full session fetch
export const RENDERING_SESSIONS_BY_PROJECT_QUERY = `...`; // Sessions by project
export const RENDERING_SESSIONS_BY_CREATOR_QUERY = `...`; // Sessions by creator
export const DESIGN_OPTIONS_BY_PROJECT_QUERY = `...`; // Design options by project
export const RENDERING_USAGE_QUERY = `...`; // Usage by doc ID
export const RENDERING_SETTINGS_QUERY = `...`; // Settings from siteSettings
```

From src/pages/api/blob-upload.ts:
```typescript
// Current allowedContentTypes: ["application/pdf", "image/jpeg", "image/png", "image/webp"]
// Missing: "image/heic", "image/heif" for iPhone photo support
```

From src/pages/api/blob-serve.ts:
```typescript
// Auth: getSession(context.cookies) -- portal session only
// Needs: additional x-studio-token auth path for Studio tool components
```

From sanity.config.ts:
```typescript
export default defineConfig({
  // plugins: [structureTool({...})],
  // schema: { types: schemaTypes },
  // document: { actions: (prev, context) => ... }
  // No `tools` array exists yet -- needs to be added
});
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 0: Create Wave 0 test stubs for Nyquist compliance</name>
  <files>
    src/sanity/components/rendering/PromoteDialog.test.ts,
    src/sanity/components/rendering/UsageBadge.test.ts,
    src/components/portal/DesignOptionsSection.test.ts,
    src/components/portal/DesignOptionLightbox.test.ts
  </files>
  <action>
Create 4 test stub files with `it.todo` placeholders that vitest can discover and run. These satisfy the Wave 0 requirements from VALIDATION.md. Real test implementations will be added as the corresponding components are built in Plans 02-04.

**1. Create `src/sanity/components/rendering/PromoteDialog.test.ts`:**

```typescript
import { describe, it } from "vitest";

describe("PromoteDialog", () => {
  it.todo("renders promote dialog with caption input for project-linked sessions");
  it.todo("shows project picker for scratchpad sessions");
  it.todo("disables Promote button when no project selected for scratchpad");
  it.todo("calls /api/rendering/promote with correct payload on promote");
  it.todo("calls /api/rendering/promote with unpromote: true on unpromote");
  it.todo("closes dialog and calls onPromoted callback on success");
  it.todo("shows error message on API failure");
});
```

**2. Create `src/sanity/components/rendering/UsageBadge.test.ts`:**

```typescript
import { describe, it } from "vitest";

describe("UsageBadge", () => {
  it.todo("renders green badge when usage < 80%");
  it.todo("renders amber badge when usage >= 80% and < 95%");
  it.todo("renders red/critical badge when usage >= 95%");
  it.todo("displays count/limit format like '5/20 this month'");
  it.todo("handles zero limit without division error");
  it.todo("renders compact variant with smaller font size");
});
```

**3. Create `src/components/portal/DesignOptionsSection.test.ts`:**

```typescript
import { describe, it } from "vitest";

describe("DesignOptionsSection", () => {
  it.todo("renders gallery grid with design option cards");
  it.todo("shows confidentiality notice above gallery");
  it.todo("uses residential copy when isCommercial is false");
  it.todo("uses commercial copy when isCommercial is true");
  it.todo("computes favoriteCount and commentCount from reactions");
  it.todo("computes isFavorited based on current clientId");
  it.todo("renders responsive grid with correct column classes");
});
```

**4. Create `src/components/portal/DesignOptionLightbox.test.ts`:**

```typescript
import { describe, it } from "vitest";

describe("DesignOptionLightbox", () => {
  it.todo("opens lightbox when open-design-lightbox event is dispatched");
  it.todo("closes lightbox on Escape key");
  it.todo("navigates to next option on ArrowRight key");
  it.todo("navigates to previous option on ArrowLeft key");
  it.todo("toggles favorite with optimistic UI update");
  it.todo("reverts favorite on API failure");
  it.todo("submits comment via /api/rendering/react");
  it.todo("appends comment optimistically on success");
  it.todo("shows error message on comment submit failure");
  it.todo("locks body scroll when open");
  it.todo("unlocks body scroll when closed");
});
```
  </action>
  <verify>
    <automated>
      npx vitest run src/sanity/components/rendering/PromoteDialog.test.ts src/sanity/components/rendering/UsageBadge.test.ts src/components/portal/DesignOptionsSection.test.ts src/components/portal/DesignOptionLightbox.test.ts --reporter=verbose 2>&1 | tail -20
    </automated>
  </verify>
  <done>All 4 test stub files exist, contain it.todo placeholders, and are discoverable by `npx vitest run` without errors. Wave 0 Nyquist compliance satisfied.</done>
</task>

<task type="auto">
  <name>Task 1: Types, UsageBadge, tool plugin, and infrastructure updates</name>
  <files>
    src/sanity/components/rendering/types.ts,
    src/sanity/components/rendering/UsageBadge.tsx,
    src/sanity/components/rendering/RenderingToolPlugin.ts,
    src/pages/api/blob-upload.ts,
    src/pages/api/blob-serve.ts,
    .env,
    .env.example
  </files>
  <read_first>
    src/sanity/schemas/renderingSession.ts,
    src/sanity/schemas/designOption.ts,
    src/sanity/queries.ts,
    src/pages/api/blob-upload.ts,
    src/pages/api/blob-serve.ts,
    src/sanity/components/BlobFileInput.tsx,
    src/lib/session.ts,
    .env,
    .env.example
  </read_first>
  <action>
**1. Create `src/sanity/components/rendering/types.ts`** with these exact interfaces:

```typescript
// -- Data types matching Sanity schema shapes --

export interface RenderingImage {
  _key: string;
  blobPathname: string;
  imageType: string;
  location?: string;
  notes?: string;
  copyExact: boolean;
}

export interface RenderingOutput {
  _key: string;
  blobPathname: string;
  prompt: string;
  textResponse: string;
  isPromoted: boolean;
  generatedAt: string;
  status: "generating" | "complete" | "error";
  errorMessage?: string;
  modelId: string;
  latencyMs: number;
  inputTokens: number;
  outputTokens: number;
  costEstimate: number;
  bytesStored: number;
}

export interface RenderingSession {
  _id: string;
  sessionTitle: string;
  project?: { _id: string; title: string } | null;
  aspectRatio: string;
  stylePreset?: string;
  description: string;
  status: "idle" | "generating" | "complete" | "error";
  lastError?: string;
  createdBy: string;
  createdAt: string;
  images: RenderingImage[];
  renderings: RenderingOutput[];
  conversation: ConversationEntry[];
}

export interface ConversationEntry {
  _key: string;
  role: "user" | "model";
  text: string;
  image?: string;
  timestamp: string;
}

export interface SessionListItem {
  _id: string;
  sessionTitle: string;
  project?: { _id: string; title: string } | null;
  status: string;
  createdAt: string;
  renderingCount: number;
}

export interface DesignOptionData {
  _id: string;
  blobPathname: string;
  caption: string;
  sortOrder: number;
  promotedAt: string;
  reactions: Reaction[];
}

export interface Reaction {
  _key: string;
  clientId: string;
  type: "favorite" | "comment";
  text?: string;
  createdAt: string;
}

export interface UsageData {
  sanityUserId: string;
  month: string;
  count: number;
  limit: number;
  remaining: number;
  bytesStored: number;
}

export interface WizardData {
  sessionTitle: string;
  projectId: string | null;
  aspectRatio: "16:9" | "1:1" | "4:3";
  stylePreset: string;
  images: WizardImage[];
  description: string;
}

export interface WizardImage {
  blobPathname: string;
  fileName: string;
  imageType: string;
  location: string;
  notes: string;
  copyExact: boolean;
  uploading: boolean;
  error?: string;
}

export interface ProjectOption {
  _id: string;
  title: string;
}

// -- Tool state types --

export type ToolView = "sessions" | "design-options";
export type ActiveView = "list" | "wizard" | "session-detail";

export interface ToolState {
  activeTab: ToolView;
  activeView: ActiveView;
  activeSessionId: string | null;
  wizardData: WizardData;
}

export type ToolAction =
  | { type: "SET_TAB"; tab: ToolView }
  | { type: "OPEN_WIZARD" }
  | { type: "CLOSE_WIZARD" }
  | { type: "OPEN_SESSION"; sessionId: string }
  | { type: "BACK_TO_LIST" }
  | { type: "UPDATE_WIZARD"; data: Partial<WizardData> }
  | { type: "RESET_WIZARD" };

// -- API helper type --

export function getStudioToken(): string {
  return (import.meta as any).env?.SANITY_STUDIO_API_SECRET || "";
}

export function getStudioHeaders(): Record<string, string> {
  return {
    "Content-Type": "application/json",
    "x-studio-token": getStudioToken(),
  };
}

export function getImageServeUrl(blobPathname: string, source: "studio" | "portal" = "portal"): string {
  if (source === "studio") {
    return `/api/blob-serve?path=${encodeURIComponent(blobPathname)}&source=studio`;
  }
  return `/api/blob-serve?path=${encodeURIComponent(blobPathname)}`;
}

export const INITIAL_WIZARD_DATA: WizardData = {
  sessionTitle: "",
  projectId: null,
  aspectRatio: "16:9",
  stylePreset: "",
  images: [],
  description: "",
};

export function isWizardDirty(data: WizardData): boolean {
  return (
    data.sessionTitle !== "" ||
    data.projectId !== null ||
    data.aspectRatio !== "16:9" ||
    data.stylePreset !== "" ||
    data.images.length > 0 ||
    data.description !== ""
  );
}
```

**2. Create `src/sanity/components/rendering/UsageBadge.tsx`:**

```typescript
import { Badge, Flex, Text } from "@sanity/ui";

interface UsageBadgeProps {
  count: number;
  limit: number;
  compact?: boolean;
}

export function UsageBadge({ count, limit, compact = false }: UsageBadgeProps) {
  const percentage = limit > 0 ? (count / limit) * 100 : 0;
  const label = `${count}/${limit} this month`;

  // Color thresholds: green <80%, amber 80-95%, red >=95%
  if (percentage >= 95) {
    return <Badge tone="critical" fontSize={compact ? 0 : 1}>{label}</Badge>;
  }
  if (percentage >= 80) {
    return (
      <Badge fontSize={compact ? 0 : 1} style={{ backgroundColor: "#D97706", color: "#FFFFFF" }}>
        {label}
      </Badge>
    );
  }
  return (
    <Badge fontSize={compact ? 0 : 1} style={{ backgroundColor: "#059669", color: "#FFFFFF" }}>
      {label}
    </Badge>
  );
}
```

**3. Create `src/sanity/components/rendering/RenderingToolPlugin.ts`:**

```typescript
import { SparklesIcon } from "@sanity/icons";
import { RenderingTool } from "./RenderingTool";

export const renderingTool = () => ({
  name: "rendering",
  title: "Rendering",
  icon: SparklesIcon,
  component: RenderingTool,
});
```

**4. Update `src/pages/api/blob-upload.ts`** -- add `"image/heic"` and `"image/heif"` to the `allowedContentTypes` array:

Change the `allowedContentTypes` from:
```typescript
allowedContentTypes: [
  "application/pdf",
  "image/jpeg",
  "image/png",
  "image/webp",
],
```
to:
```typescript
allowedContentTypes: [
  "application/pdf",
  "image/jpeg",
  "image/png",
  "image/webp",
  "image/heic",
  "image/heif",
],
```

**5. Update `src/pages/api/blob-serve.ts`** -- add studio token auth as an alternative to portal session auth. After the existing session check, add a fallback that checks for `x-studio-token` header:

Replace the auth block:
```typescript
const session = await getSession(context.cookies);
if (!session) {
  return new Response("Unauthorized", { status: 401 });
}
```
with:
```typescript
const session = await getSession(context.cookies);
const studioToken = context.request.headers.get("x-studio-token");
const isStudioAuth = studioToken && studioToken === import.meta.env.STUDIO_API_SECRET;
const isSourceStudio = context.url.searchParams.get("source") === "studio";

if (!session && !isStudioAuth && !isSourceStudio) {
  return new Response("Unauthorized", { status: 401 });
}
// Note: Studio tool passes source=studio param. In production,
// Studio is behind Sanity's own auth. The source param is a lightweight
// gate for dev. Full token validation can be added later if needed.
```

Actually, on second thought -- the Studio tool runs inside Sanity Studio which has its own authentication. The blob-serve endpoint should accept the `x-studio-token` header properly. Use this instead:

```typescript
const session = await getSession(context.cookies);
const studioToken = context.request.headers.get("x-studio-token");
const isStudioAuth = studioToken && studioToken === import.meta.env.STUDIO_API_SECRET;

if (!session && !isStudioAuth) {
  return new Response("Unauthorized", { status: 401 });
}
```

**6. Update `.env`** -- add `SANITY_STUDIO_API_SECRET` line immediately after the existing `STUDIO_API_SECRET` line (same value). The executor should read the current value of STUDIO_API_SECRET from .env and duplicate it:

Add this line (using the SAME value as STUDIO_API_SECRET):
```
SANITY_STUDIO_API_SECRET=<same value as STUDIO_API_SECRET>
```

**7. Update `.env.example`** -- add the SANITY_STUDIO_API_SECRET entry after STUDIO_API_SECRET (if STUDIO_API_SECRET is already in .env.example) or add both:

```
# Studio API Secret -- shared secret for rendering API routes
# STUDIO_API_SECRET is used server-side; SANITY_STUDIO_API_SECRET is the same value
# but prefixed for Sanity Studio client-side bundling (Vite only bundles SANITY_STUDIO_ prefix)
STUDIO_API_SECRET=your_studio_secret_here
SANITY_STUDIO_API_SECRET=your_studio_secret_here
```
  </action>
  <verify>
    <automated>
      grep -q "export interface RenderingSession" src/sanity/components/rendering/types.ts && grep -q "export function UsageBadge" src/sanity/components/rendering/UsageBadge.tsx && grep -q "export const renderingTool" src/sanity/components/rendering/RenderingToolPlugin.ts && grep -q "image/heic" src/pages/api/blob-upload.ts && grep -q "x-studio-token" src/pages/api/blob-serve.ts && grep -q "SANITY_STUDIO_API_SECRET" .env.example && echo "PASS" || echo "FAIL"
    </automated>
  </verify>
  <acceptance_criteria>
    - `src/sanity/components/rendering/types.ts` contains `export interface RenderingSession`
    - `src/sanity/components/rendering/types.ts` contains `export interface WizardData`
    - `src/sanity/components/rendering/types.ts` contains `export interface DesignOptionData`
    - `src/sanity/components/rendering/types.ts` contains `export function getStudioHeaders`
    - `src/sanity/components/rendering/types.ts` contains `export function getImageServeUrl`
    - `src/sanity/components/rendering/types.ts` contains `export function isWizardDirty`
    - `src/sanity/components/rendering/UsageBadge.tsx` contains `export function UsageBadge`
    - `src/sanity/components/rendering/UsageBadge.tsx` contains `backgroundColor: "#059669"` (green)
    - `src/sanity/components/rendering/UsageBadge.tsx` contains `backgroundColor: "#D97706"` (amber)
    - `src/sanity/components/rendering/UsageBadge.tsx` contains `tone="critical"` (red)
    - `src/sanity/components/rendering/RenderingToolPlugin.ts` contains `export const renderingTool`
    - `src/sanity/components/rendering/RenderingToolPlugin.ts` contains `icon: SparklesIcon`
    - `src/pages/api/blob-upload.ts` contains `image/heic`
    - `src/pages/api/blob-upload.ts` contains `image/heif`
    - `src/pages/api/blob-serve.ts` contains `x-studio-token`
    - `.env` contains `SANITY_STUDIO_API_SECRET`
    - `.env.example` contains `SANITY_STUDIO_API_SECRET`
  </acceptance_criteria>
  <done>All shared types exported, UsageBadge renders with 3-tier color coding, RenderingToolPlugin exports factory function with SparklesIcon, blob-upload accepts HEIC/HEIF, blob-serve accepts both portal session and studio token auth, SANITY_STUDIO_API_SECRET configured in both .env files</done>
</task>

<task type="auto">
  <name>Task 2: RenderingTool main component and sanity.config.ts registration</name>
  <files>
    src/sanity/components/rendering/RenderingTool.tsx,
    sanity.config.ts
  </files>
  <read_first>
    src/sanity/components/rendering/types.ts,
    src/sanity/components/rendering/RenderingToolPlugin.ts,
    src/sanity/components/rendering/UsageBadge.tsx,
    sanity.config.ts,
    src/sanity/queries.ts
  </read_first>
  <action>
**1. Create `src/sanity/components/rendering/RenderingTool.tsx`:**

The main tool component with:
- React context for tool state (useReducer)
- Tab bar: "Sessions" and "Design Options" tabs using `@sanity/ui` TabList/Tab
- Internal view routing: list (default), wizard (when "New Session" clicked), session-detail (when session card clicked)
- Placeholder content for each tab (real content comes in plans 02 and 03)
- UsageBadge fetching usage data from `/api/rendering/usage`
- Export the ToolContext so child components can useContext

Implementation details:

```typescript
import { createContext, useContext, useReducer, useState, useEffect, useCallback } from "react";
import { useCurrentUser, useClient } from "sanity";
import { Card, Tab, TabList, Flex, Stack, Text, Button, Spinner } from "@sanity/ui";
import { SparklesIcon, AddIcon, ImageIcon } from "@sanity/icons";
import { UsageBadge } from "./UsageBadge";
import type {
  ToolState, ToolAction, ToolView, UsageData,
  WizardData, INITIAL_WIZARD_DATA,
} from "./types";
import { getStudioHeaders } from "./types";

// -- Reducer --
function toolReducer(state: ToolState, action: ToolAction): ToolState {
  switch (action.type) {
    case "SET_TAB":
      return { ...state, activeTab: action.tab, activeView: "list", activeSessionId: null };
    case "OPEN_WIZARD":
      return { ...state, activeView: "wizard" };
    case "CLOSE_WIZARD":
      return { ...state, activeView: "list", wizardData: INITIAL_WIZARD_DATA };
    case "OPEN_SESSION":
      return { ...state, activeView: "session-detail", activeSessionId: action.sessionId };
    case "BACK_TO_LIST":
      return { ...state, activeView: "list", activeSessionId: null };
    case "UPDATE_WIZARD":
      return { ...state, wizardData: { ...state.wizardData, ...action.data } };
    case "RESET_WIZARD":
      return { ...state, wizardData: INITIAL_WIZARD_DATA };
    default:
      return state;
  }
}

const initialState: ToolState = {
  activeTab: "sessions",
  activeView: "list",
  activeSessionId: null,
  wizardData: INITIAL_WIZARD_DATA,
};

// -- Context --
interface ToolContextValue {
  state: ToolState;
  dispatch: React.Dispatch<ToolAction>;
  usage: UsageData | null;
  refreshUsage: () => void;
  sanityUserId: string;
}

export const ToolContext = createContext<ToolContextValue | null>(null);

export function useToolContext() {
  const ctx = useContext(ToolContext);
  if (!ctx) throw new Error("useToolContext must be used within RenderingTool");
  return ctx;
}

// -- Main Component --
export function RenderingTool() {
  const currentUser = useCurrentUser();
  const [state, dispatch] = useReducer(toolReducer, initialState);
  const [usage, setUsage] = useState<UsageData | null>(null);
  const [usageLoading, setUsageLoading] = useState(true);

  const sanityUserId = currentUser?.id || "";

  const refreshUsage = useCallback(async () => {
    if (!sanityUserId) return;
    try {
      const res = await fetch(
        `/api/rendering/usage?sanityUserId=${encodeURIComponent(sanityUserId)}`,
        { headers: getStudioHeaders() }
      );
      if (res.ok) {
        const data = await res.json();
        setUsage(data);
      }
    } catch {
      // Silently fail -- usage badge shows loading state
    } finally {
      setUsageLoading(false);
    }
  }, [sanityUserId]);

  useEffect(() => {
    refreshUsage();
  }, [refreshUsage]);

  if (!currentUser) {
    return (
      <Card height="fill" display="flex" style={{ alignItems: "center", justifyContent: "center" }}>
        <Spinner muted />
      </Card>
    );
  }

  const showTabs = state.activeView === "list";

  return (
    <ToolContext.Provider value={{ state, dispatch, usage, refreshUsage, sanityUserId }}>
      <Card height="fill" overflow="auto" sizing="border">
        {showTabs && (
          <Card paddingX={4} paddingTop={4} paddingBottom={2}>
            <TabList space={1}>
              <Tab
                aria-controls="sessions-panel"
                id="sessions-tab"
                label="Sessions"
                selected={state.activeTab === "sessions"}
                onClick={() => dispatch({ type: "SET_TAB", tab: "sessions" })}
              />
              <Tab
                aria-controls="design-options-panel"
                id="design-options-tab"
                label="Design Options"
                selected={state.activeTab === "design-options"}
                onClick={() => dispatch({ type: "SET_TAB", tab: "design-options" })}
              />
            </TabList>
          </Card>
        )}

        {/* Sessions tab - list view */}
        {state.activeTab === "sessions" && state.activeView === "list" && (
          <Card padding={4}>
            <Flex align="center" justify="space-between">
              <Button
                icon={AddIcon}
                text="New Session"
                tone="primary"
                onClick={() => dispatch({ type: "OPEN_WIZARD" })}
              />
              {usage && <UsageBadge count={usage.count} limit={usage.limit} />}
              {usageLoading && <Spinner muted />}
            </Flex>
            <Card padding={5} tone="transparent" style={{ marginTop: 16 }}>
              <Stack space={3} style={{ alignItems: "center", textAlign: "center" }}>
                <SparklesIcon style={{ fontSize: 48, opacity: 0.3 }} />
                <Text size={2} align="center">No rendering sessions yet</Text>
                <Text size={1} muted align="center">
                  Create your first session to start generating design renderings.
                </Text>
                <Button
                  text="Create Session"
                  tone="primary"
                  onClick={() => dispatch({ type: "OPEN_WIZARD" })}
                />
              </Stack>
            </Card>
          </Card>
        )}

        {/* Sessions tab - wizard view (placeholder for Plan 02) */}
        {state.activeTab === "sessions" && state.activeView === "wizard" && (
          <Card padding={4}>
            <Text size={2}>Wizard placeholder -- implemented in Plan 02</Text>
            <Button
              text="Back"
              mode="ghost"
              onClick={() => dispatch({ type: "CLOSE_WIZARD" })}
              style={{ marginTop: 16 }}
            />
          </Card>
        )}

        {/* Sessions tab - session detail view (placeholder for Plan 03) */}
        {state.activeTab === "sessions" && state.activeView === "session-detail" && (
          <Card padding={4}>
            <Text size={2}>Chat view placeholder -- implemented in Plan 03</Text>
            <Button
              text="Back"
              mode="ghost"
              onClick={() => dispatch({ type: "BACK_TO_LIST" })}
              style={{ marginTop: 16 }}
            />
          </Card>
        )}

        {/* Design Options tab (placeholder for Plan 03) */}
        {state.activeTab === "design-options" && state.activeView === "list" && (
          <Card padding={4}>
            <Flex align="center" justify="space-between">
              <Text size={3} weight="semibold">Design Options</Text>
            </Flex>
            <Card padding={5} tone="transparent" style={{ marginTop: 16 }}>
              <Stack space={3} style={{ alignItems: "center", textAlign: "center" }}>
                <ImageIcon style={{ fontSize: 48, opacity: 0.3 }} />
                <Text size={2} align="center">No design options yet</Text>
                <Text size={1} muted align="center">
                  Promote renderings from your sessions to make them visible to clients.
                </Text>
              </Stack>
            </Card>
          </Card>
        )}
      </Card>
    </ToolContext.Provider>
  );
}
```

Key design decisions:
- ToolContext exported so SessionList, WizardContainer, ChatView can import `useToolContext()`
- Usage data fetched once on mount, refreshed via `refreshUsage()` after generate/refine
- Placeholder views for wizard, session-detail, and design-options tab -- replaced by real components in plans 02 and 03
- Tab bar hidden when wizard or session-detail is active (per UI spec)

**2. Update `sanity.config.ts`:**

Add `tools` array with `renderingTool()`:

Add import at top:
```typescript
import { renderingTool } from "./src/sanity/components/rendering/RenderingToolPlugin";
```

Add `tools` property to `defineConfig` object, after the `plugins` array:
```typescript
tools: [renderingTool()],
```

The final defineConfig should look like:
```typescript
export default defineConfig({
  name: "la-sprezzatura",
  title: "La Sprezzatura",
  projectId: import.meta.env.PUBLIC_SANITY_PROJECT_ID,
  dataset: import.meta.env.PUBLIC_SANITY_DATASET,
  plugins: [
    structureTool({ ... }),
  ],
  tools: [renderingTool()],
  schema: { types: schemaTypes },
  document: { actions: ... },
});
```
  </action>
  <verify>
    <automated>
      grep -q "export function RenderingTool" src/sanity/components/rendering/RenderingTool.tsx && grep -q "export const ToolContext" src/sanity/components/rendering/RenderingTool.tsx && grep -q "renderingTool" sanity.config.ts && grep -q "tools:" sanity.config.ts && echo "PASS" || echo "FAIL"
    </automated>
  </verify>
  <acceptance_criteria>
    - `src/sanity/components/rendering/RenderingTool.tsx` contains `export function RenderingTool`
    - `src/sanity/components/rendering/RenderingTool.tsx` contains `export const ToolContext = createContext`
    - `src/sanity/components/rendering/RenderingTool.tsx` contains `export function useToolContext`
    - `src/sanity/components/rendering/RenderingTool.tsx` contains `useCurrentUser` import from "sanity"
    - `src/sanity/components/rendering/RenderingTool.tsx` contains `TabList` and `Tab` imports from "@sanity/ui"
    - `src/sanity/components/rendering/RenderingTool.tsx` contains `toolReducer`
    - `src/sanity/components/rendering/RenderingTool.tsx` contains `refreshUsage`
    - `sanity.config.ts` contains `import { renderingTool }` from the RenderingToolPlugin path
    - `sanity.config.ts` contains `tools: [renderingTool()]`
    - `npx tsc --noEmit` exits without errors on RenderingTool.tsx (or at minimum no errors in the rendering/ directory)
  </acceptance_criteria>
  <done>RenderingTool.tsx renders a functioning tool shell with Sessions/Design Options tabs, ToolContext provides state management to children, UsageBadge shows live usage data, sanity.config.ts registers the tool so it appears in the Studio sidebar with SparklesIcon</done>
</task>

</tasks>

<verification>
- `npx vitest run src/sanity/components/rendering/PromoteDialog.test.ts src/sanity/components/rendering/UsageBadge.test.ts src/components/portal/DesignOptionsSection.test.ts src/components/portal/DesignOptionLightbox.test.ts` passes (all todo stubs discovered)
- `npx tsc --noEmit` passes (no type errors in rendering components)
- Sanity Studio loads without errors: `npx sanity dev` starts and shows Rendering icon in sidebar
- Clicking the Rendering sidebar icon shows the tool with Sessions/Design Options tabs
- The Sessions tab shows "New Session" button and UsageBadge (or spinner if loading)
- `grep -q "image/heic" src/pages/api/blob-upload.ts` passes
- `grep -q "x-studio-token" src/pages/api/blob-serve.ts` passes
</verification>

<success_criteria>
- All 4 Wave 0 test stub files exist and run via vitest without errors
- RenderingTool appears in Sanity Studio sidebar with SparklesIcon
- Two tabs (Sessions, Design Options) are functional with tab switching
- Placeholder content renders for each view (list, wizard, session-detail, design-options)
- ToolContext with useReducer state management is exported for child components
- UsageBadge shows color-coded count/limit
- All shared TypeScript types exported from types.ts
- HEIC/HEIF images accepted by blob-upload
- blob-serve accepts both portal session and studio token auth
- SANITY_STUDIO_API_SECRET configured in .env and .env.example
</success_criteria>

<output>
After completion, create `.planning/phases/11-rendering-studio-tool-and-design-options-gallery/11-01-SUMMARY.md`
</output>
