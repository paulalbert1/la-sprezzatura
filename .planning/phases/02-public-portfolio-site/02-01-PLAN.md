---
phase: 02-public-portfolio-site
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/styles/global.css
  - src/sanity/schemas/project.ts
  - src/sanity/schemas/service.ts
  - src/sanity/schemas/siteSettings.ts
  - src/sanity/schemas/index.ts
  - src/sanity/queries.ts
  - src/sanity/image.ts
  - src/components/SanityImage.astro
  - src/components/seo/SEOHead.astro
  - src/components/seo/JsonLd.astro
  - src/components/layout/Header.astro
  - src/components/layout/Footer.astro
  - src/components/layout/MobileMenu.astro
  - src/components/layout/Navigation.astro
  - src/components/ui/Button.astro
  - src/components/ui/SectionHeading.astro
  - src/layouts/BaseLayout.astro
  - sanity.config.ts
  - astro.config.mjs
  - package.json
autonomous: true
requirements:
  - PORT-04
  - DSGN-01
  - DSGN-03
  - SEO-01
  - SEO-02
  - SEO-03
  - SEO-05

must_haves:
  truths:
    - "Sanity Studio at /admin shows Portfolio Projects, Services, and Site Settings with plain-English field labels"
    - "The site renders with warm neutral colors, editorial serif headings, and generous whitespace"
    - "All images from Sanity use responsive srcset with WebP/AVIF and blur-up LQIP placeholders"
    - "Every page has unique meta titles, OG tags, and LocalBusiness structured data"
    - "Navigation header and footer render on all pages with correct links"
  artifacts:
    - path: "src/styles/global.css"
      provides: "Tailwind v4 @theme design tokens -- colors, fonts, spacing"
      contains: "@theme"
    - path: "src/sanity/schemas/project.ts"
      provides: "Portfolio project document type with all fields including pipelineStage"
      contains: "defineType"
    - path: "src/sanity/schemas/siteSettings.ts"
      provides: "Global site config singleton"
      contains: "siteSettings"
    - path: "src/sanity/queries.ts"
      provides: "GROQ query library for all page data"
      exports: ["getProjects", "getProjectBySlug", "getFeaturedProjects", "getSiteSettings"]
    - path: "src/sanity/image.ts"
      provides: "Sanity image URL builder helper"
      exports: ["urlFor"]
    - path: "src/components/SanityImage.astro"
      provides: "Responsive image component with srcset + LQIP"
      min_lines: 30
    - path: "src/components/seo/SEOHead.astro"
      provides: "Per-page meta tags, OG tags, canonical URL"
      min_lines: 20
    - path: "src/components/seo/JsonLd.astro"
      provides: "LocalBusiness schema.org structured data"
      contains: "LocalBusiness"
    - path: "src/components/layout/Header.astro"
      provides: "Navigation header with transparent-to-sticky behavior"
      min_lines: 15
    - path: "src/components/layout/Footer.astro"
      provides: "Site footer with links and privacy policy link"
      min_lines: 15
    - path: "src/layouts/BaseLayout.astro"
      provides: "Extended base layout with SEOHead, fonts, header, footer"
      min_lines: 30
    - path: "sanity.config.ts"
      provides: "Sanity Studio with structureTool, singleton SiteSettings"
      contains: "structureTool"
  key_links:
    - from: "src/components/SanityImage.astro"
      to: "src/sanity/image.ts"
      via: "import urlFor"
      pattern: "urlFor"
    - from: "src/layouts/BaseLayout.astro"
      to: "src/components/seo/SEOHead.astro"
      via: "component inclusion"
      pattern: "SEOHead"
    - from: "src/layouts/BaseLayout.astro"
      to: "src/components/layout/Header.astro"
      via: "component inclusion"
      pattern: "Header"
    - from: "src/sanity/schemas/index.ts"
      to: "src/sanity/schemas/project.ts"
      via: "barrel export"
      pattern: "project"
---

<objective>
Build the design system foundation, Sanity CMS schemas, layout shell, and reusable utility components that all subsequent plans depend on.

Purpose: Every page in Plans 02-04 imports from this plan's outputs -- design tokens, layout components, image helpers, SEO components, and GROQ queries. Building these first prevents downstream plans from making assumptions about contracts.

Output: Design system tokens in global.css, three Sanity document types (project, service, siteSettings) with Studio customization, GROQ query library, responsive image component, SEO head component with JSON-LD, and full layout shell (header, footer, mobile menu, navigation).
</objective>

<execution_context>
@/Users/paulalbert/.claude/get-shit-done/workflows/execute-plan.md
@/Users/paulalbert/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-public-portfolio-site/02-CONTEXT.md
@.planning/phases/02-public-portfolio-site/02-RESEARCH.md
@.planning/phases/01-project-scaffold-and-staging-deploy/01-01-SUMMARY.md

<interfaces>
<!-- Phase 1 existing files this plan extends -->

From src/layouts/BaseLayout.astro (to be extended):
```astro
---
import "../styles/global.css";
interface Props { title: string; }
const { title } = Astro.props;
---
```

From src/styles/global.css (to be extended):
```css
@import "tailwindcss";
```

From src/sanity/schemas/index.ts (empty barrel to populate):
```typescript
export const schemaTypes: never[] = [];
```

From astro.config.mjs (to add fonts + sitemap):
```javascript
// Has: react(), sanity(), vercel(), tailwindcss()
// Needs: fonts config added
```

From sanity.config.ts (to add structureTool customization):
```typescript
// Has: basic structureTool()
// Needs: singleton SiteSettings, custom structure
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Design system tokens, Sanity schemas, and GROQ queries</name>
  <files>
    src/styles/global.css,
    src/sanity/schemas/project.ts,
    src/sanity/schemas/service.ts,
    src/sanity/schemas/siteSettings.ts,
    src/sanity/schemas/index.ts,
    src/sanity/queries.ts,
    src/sanity/image.ts,
    sanity.config.ts,
    astro.config.mjs,
    package.json
  </files>
  <action>
    All work happens in ~/Dropbox/GitHub/la-sprezzatura/.

    **1. Install Phase 2 dependencies:**
    ```
    npm install gsap resend astro-portabletext @astrojs/sitemap
    npm install @calcom/embed-react --force
    npm install -D vitest
    ```
    Add `"test": "vitest run"` to package.json scripts.

    **2. Design system tokens in global.css:**
    Extend the existing `@import "tailwindcss"` with a full `@theme` block. Per user decisions:
    - Colors: creamy off-white backgrounds (#FAF8F5), cream-dark (#F5F0EB), warm stone grays (#8A8478, light #B8B0A4, dark #6B6358), deep charcoal for text (#2C2926, light #4A4540), muted terracotta accent (#C4836A, light #D4A08A), white (#FFFFFF)
    - Typography: serif heading font (Cormorant Garamond) + sans body font (DM Sans). Define `--font-heading` and `--font-body` CSS variables
    - Spacing: generous section spacing (8rem default, 5rem small), content max-width 1200px
    - Animation keyframes: fade-in-up (opacity 0 + translateY 24px to visible)
    - Base styles after @theme: body uses font-body + charcoal color + cream background, headings use font-heading

    Use `@theme inline { }` for font variables that bridge from Astro Fonts API to Tailwind utilities (per research Pattern 2 / Pitfall 7).

    **3. Astro Fonts API in astro.config.mjs:**
    Add fonts config using `fontProviders.google()` for Cormorant Garamond (cssVariable: --font-heading) and DM Sans (cssVariable: --font-body). Import `fontProviders` from "astro/config".

    If Fonts API requires `experimental.fonts: true`, add that flag. Test with `npm run build` -- if it fails, fall back to Fontsource NPM packages with manual @font-face in global.css.

    **4. Sanity schemas:**

    Create `src/sanity/schemas/project.ts` -- Portfolio Project document type. Use the exact schema from the RESEARCH.md code example. Key fields:
    - title (string, required), slug (slug from title, required), heroImage (image with hotspot + lqip/palette metadata, required)
    - images (array of images with alt + caption fields, hotspot + lqip metadata)
    - roomType (string, predefined list: Living Room, Kitchen, Bedroom, Bathroom, Dining Room, Home Office, Outdoor, Full Home)
    - style (string, predefined list: Contemporary, Traditional, Transitional, Coastal, Modern)
    - location (string), description (text, 3 rows)
    - challenge, approach, outcome (each: array of block type -- Portable Text for rich narrative)
    - testimonial (object: quote text + author string)
    - completionDate (date), featured (boolean, default false), order (number)
    - pipelineStage (string, predefined list: Discovery, Concept, Design Development, Procurement, Installation, Closeout) -- hidden: true for Phase 2
    - All field labels in plain English per user decision ("Project Title" not "title", "Main Photo" not "heroImage", etc.)
    - Preview config: title, subtitle=location, media=heroImage

    Create `src/sanity/schemas/service.ts` -- Service offering document type:
    - title (string, required), slug (slug from title)
    - tagline (string -- brief one-liner)
    - description (array of block -- Portable Text)
    - features (array of strings -- "what's included" bullet points)
    - idealFor (string -- "who this is for")
    - order (number), icon (string -- emoji or icon name)
    - Preview: title, subtitle=tagline

    Create `src/sanity/schemas/siteSettings.ts` -- Singleton document type. Follow the exact schema from RESEARCH.md:
    - siteTitle (string, default "La Sprezzatura"), tagline (string)
    - contactEmail (string), contactPhone (string), studioLocation (string, description: general area not home address)
    - socialLinks (object: instagram url, pinterest url, houzz url)

    Update `src/sanity/schemas/index.ts` -- Import and export all three schema types in the array.

    Update `sanity.config.ts` -- Add structureTool customization per RESEARCH Pattern 10:
    - SiteSettings as singleton (S.document with fixed documentId "siteSettings")
    - Divider
    - Portfolio Projects list
    - Services list
    - Filter out siteSettings from default document type lists

    **5. GROQ query library (src/sanity/queries.ts):**
    Create centralized query file with typed exports per RESEARCH Pattern 3:
    - `getProjects()` -- all projects ordered by `order asc`, projecting id, title, slug, heroImage with asset URL + LQIP + dimensions, roomType, style, location, featured
    - `getProjectBySlug(slug)` -- single project with full field projection including images array with asset metadata, challenge/approach/outcome blocks, testimonial
    - `getFeaturedProjects()` -- featured=true, ordered, limited to 3, with hero image + LQIP
    - `getSiteSettings()` -- singleton fetch for global config
    - `getServices()` -- all services ordered by order asc

    **6. Image URL builder helper (src/sanity/image.ts):**
    Create helper that exports `urlFor(source)` function using `@sanity/image-url` with the Sanity client. This is used by SanityImage.astro and directly in pages.

    Use `import { sanityClient } from "sanity:client"` for the client reference.
  </action>
  <verify>
    <automated>cd ~/Dropbox/GitHub/la-sprezzatura && npm run build</automated>
  </verify>
  <done>
    - global.css has @theme block with full color palette, font references, spacing scale, and animation keyframes
    - Three Sanity schemas registered and Studio builds without errors
    - GROQ queries export 5 named functions
    - Image URL builder exports urlFor function
    - Fonts load via Astro Fonts API (or Fontsource fallback if experimental)
    - `npm run build` passes
  </done>
</task>

<task type="auto">
  <name>Task 2: Layout shell, SEO components, and reusable UI components</name>
  <files>
    src/components/seo/SEOHead.astro,
    src/components/seo/JsonLd.astro,
    src/components/SanityImage.astro,
    src/components/layout/Header.astro,
    src/components/layout/Footer.astro,
    src/components/layout/MobileMenu.astro,
    src/components/layout/Navigation.astro,
    src/components/ui/Button.astro,
    src/components/ui/SectionHeading.astro,
    src/layouts/BaseLayout.astro,
    src/pages/index.astro
  </files>
  <action>
    All work happens in ~/Dropbox/GitHub/la-sprezzatura/.

    **1. SanityImage.astro component:**
    Create per RESEARCH Pattern 5. Props: image (Sanity image object), alt (string), sizes (responsive sizes string, default "(max-width: 640px) 100vw, (max-width: 1200px) 80vw, 1200px"), widths (number array, default [400, 640, 800, 1200, 1600]), class (string), loading ("lazy"|"eager"), fetchpriority ("high"|"low"|"auto").

    Implementation:
    - Import urlFor from src/sanity/image.ts
    - Generate srcset by mapping widths through urlFor with .width(w).auto("format").quality(80)
    - Default src at 1200px wide
    - Extract LQIP from image.asset.metadata.lqip
    - Render wrapper div with blur-up placeholder img (if LQIP exists) + main img with srcset/sizes/loading/fetchpriority
    - LQIP img: absolute positioned, blurred, scaled slightly, aria-hidden
    - Main img: relative, removes LQIP sibling on load via onload handler

    **2. SEOHead.astro component:**
    Create per RESEARCH Pattern 9. Props: title (string), description (string), ogImage (string, default "/og-default.jpg"), type ("website"|"article"), noindex (boolean, default true for staging).

    Renders:
    - `<title>` tag
    - meta description, robots (noindex if staging)
    - canonical URL from Astro.url
    - Open Graph: og:title, og:description, og:image, og:type, og:url
    - Twitter Card: summary_large_image with title, description, image

    **3. JsonLd.astro component:**
    Props: none (static LocalBusiness data). Renders a `<script type="application/ld+json">` with:
    - @context: schema.org, @type: LocalBusiness
    - name: "La Sprezzatura Interior Design"
    - description about luxury interior design on Long Island and NYC
    - url: https://lasprezz.com
    - areaServed: New York City + Long Island
    - priceRange: "$$$"
    - Use `set:html={JSON.stringify(schema)}` for safe rendering

    **4. Navigation.astro component:**
    Shared navigation links used in both Header and MobileMenu. Links: Home (/), Portfolio (/portfolio), Services (/services), About (/about), Contact (/contact). Render as `<nav>` with `<a>` tags. Accept a `mobile` boolean prop to toggle between horizontal (desktop) and vertical (mobile) styling. Apply `font-heading` for editorial feel. Generous letter-spacing per user decision.

    **5. Header.astro component:**
    Per user decisions:
    - Minimal header, transparent by default on home page, overlaying hero imagery
    - Logo: "La Sprezzatura" in heading serif font (left side)
    - Desktop: Navigation component (right side), widely spaced links
    - Mobile: hamburger button that triggers MobileMenu
    - Sticky behavior: on scroll, header gets warm background (cream-dark or similar), subtle shadow
    - Use `position: fixed` + `top: 0` + transparent initial state
    - Scroll detection via small inline script using `scroll` event on window
    - Accept `transparent` boolean prop (true on home page, false on inner pages)

    **6. MobileMenu.astro component:**
    Per user decisions:
    - Full-screen overlay menu triggered by hamburger button in Header
    - Large tap targets, editorial typography (heading serif font)
    - Warm background matching site palette
    - Close button (X) prominently placed
    - Navigation links vertically stacked, large font size
    - Toggled via JS: hamburger click opens, X click or link click closes
    - Use `fixed inset-0` positioning with high z-index

    **7. Footer.astro component:**
    - Site name "La Sprezzatura" in heading font
    - Navigation links (same as header)
    - Contact info from siteSettings (email, phone, location) -- hardcode reasonable defaults for now since siteSettings may not have content yet
    - Social media links (Instagram, Pinterest, Houzz) -- render only if URLs exist
    - Privacy policy link (/privacy)
    - Copyright line
    - Warm, subtle design -- cream-dark background or subtle border-top
    - NOT in main navigation per user decision (privacy link is footer-only)

    **8. Button.astro component:**
    CTA button with variants. Props: href (string), variant ("primary"|"secondary"|"outline"), size ("sm"|"md"|"lg"), class (string).
    - Primary: terracotta background, cream text, hover darkens
    - Secondary: charcoal background, cream text
    - Outline: transparent with charcoal border, hover fills
    - All: generous padding, heading font, uppercase tracking, smooth transition
    - Renders as `<a>` if href provided, `<button>` otherwise

    **9. SectionHeading.astro component:**
    Editorial section header. Props: tagline (string, small uppercase text above heading), class (string).
    - Slot for heading text (renders as `<h2>` in heading font)
    - Optional tagline renders as small uppercase tracking-wide text in stone color above heading
    - Generous margin-bottom

    **10. Extend BaseLayout.astro:**
    Expand the existing layout to include all new components. Props: title (string), description (string), ogImage (string), noindex (boolean), transparentHeader (boolean, default false).

    Structure:
    ```
    <html lang="en">
      <head>
        <meta charset="UTF-8" />
        <meta name="viewport" ... />
        <Font cssVariable="--font-heading" preload />
        <Font cssVariable="--font-body" preload />
        <SEOHead title, description, ogImage, noindex />
        <JsonLd />
      </head>
      <body class="bg-cream text-charcoal font-body">
        <Header transparent={transparentHeader} />
        <MobileMenu />
        <main>
          <slot />
        </main>
        <Footer />
      </body>
    </html>
    ```

    If Astro Fonts API Font component import doesn't work (experimental), remove Font imports and rely on the font loading from astro.config.mjs fonts config.

    **11. Update src/pages/index.astro:**
    Replace the Phase 1 placeholder with a minimal home page that uses BaseLayout with transparentHeader=true. Just render "La Sprezzatura" as an H1 with a "Coming soon" message. This validates the full layout pipeline. The real home page content comes in Plan 02.
  </action>
  <verify>
    <automated>cd ~/Dropbox/GitHub/la-sprezzatura && npm run build</automated>
  </verify>
  <done>
    - BaseLayout renders with header, footer, mobile menu, SEO head, and JSON-LD on every page
    - Header shows "La Sprezzatura" logo and navigation links (Home, Portfolio, Services, About, Contact)
    - Mobile hamburger menu opens full-screen overlay with large navigation links
    - SanityImage component generates srcset with 5 width variants and auto-format
    - SEOHead outputs unique title, description, OG tags per page
    - JsonLd outputs valid LocalBusiness schema.org markup
    - Button component renders with primary/secondary/outline variants
    - Index page renders with full layout shell
    - `npm run build` passes
  </done>
</task>

</tasks>

<verification>
- `cd ~/Dropbox/GitHub/la-sprezzatura && npm run build` completes without errors
- Sanity Studio at /admin shows three document types when running `npm run dev`
- Built output includes pages with proper HTML structure (head with meta, body with header/main/footer)
- global.css contains @theme block with color, font, and spacing tokens
</verification>

<success_criteria>
1. Design tokens are defined and Tailwind utilities (bg-cream, text-charcoal, font-heading, etc.) work in components
2. Three Sanity schemas are registered and Studio renders them with plain-English labels
3. SanityImage component produces responsive srcset URLs with auto-format
4. SEOHead generates unique meta + OG tags per page
5. Layout shell (header + footer + mobile menu) renders correctly
6. GROQ query library exports all five query functions
7. `npm run build` passes cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/02-public-portfolio-site/02-01-SUMMARY.md`
</output>
