---
phase: 02-public-portfolio-site
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - src/pages/index.astro
  - src/pages/portfolio/index.astro
  - src/pages/portfolio/[slug].astro
  - src/components/home/Hero.astro
  - src/components/home/FeaturedProjects.astro
  - src/components/portfolio/ProjectCard.astro
  - src/components/portfolio/ProjectGallery.astro
  - src/components/portfolio/Lightbox.astro
  - src/components/ui/FilterPills.astro
  - src/components/ui/ProcessStep.astro
autonomous: true
requirements:
  - PORT-01
  - PORT-02
  - PORT-03
  - SITE-01
  - DSGN-02

must_haves:
  truths:
    - "A visitor can browse the home page and see a hero image, featured projects, about teaser, process preview, and CTA"
    - "A visitor can view the portfolio gallery with 4-5 project cards and filter by room type or style using pill buttons"
    - "Clicking a project card navigates to a dedicated project page with full-bleed hero, narrative sections, gallery, and testimonial"
    - "The lightbox opens when clicking gallery images on a project page, showing full-size detail"
    - "All pages are mobile-responsive with proper layout at phone, tablet, and desktop sizes"
  artifacts:
    - path: "src/pages/index.astro"
      provides: "Complete home page with hero, featured projects, about teaser, process preview, CTA"
      min_lines: 60
    - path: "src/pages/portfolio/index.astro"
      provides: "Portfolio gallery with filter pills and project grid"
      min_lines: 40
    - path: "src/pages/portfolio/[slug].astro"
      provides: "Dynamic project detail pages generated from Sanity slugs"
      contains: "getStaticPaths"
    - path: "src/components/home/Hero.astro"
      provides: "Full-bleed hero section with image and text overlay"
      min_lines: 15
    - path: "src/components/home/FeaturedProjects.astro"
      provides: "2-3 featured project editorial cards"
      min_lines: 20
    - path: "src/components/portfolio/ProjectCard.astro"
      provides: "Portfolio grid card with image, title, location"
      min_lines: 15
    - path: "src/components/portfolio/ProjectGallery.astro"
      provides: "Scrolling image gallery on project pages"
      min_lines: 15
    - path: "src/components/ui/FilterPills.astro"
      provides: "Room type and style filter buttons"
      min_lines: 10
  key_links:
    - from: "src/pages/index.astro"
      to: "src/sanity/queries.ts"
      via: "getFeaturedProjects import"
      pattern: "getFeaturedProjects"
    - from: "src/pages/portfolio/index.astro"
      to: "src/sanity/queries.ts"
      via: "getProjects import"
      pattern: "getProjects"
    - from: "src/pages/portfolio/[slug].astro"
      to: "src/sanity/queries.ts"
      via: "getProjectBySlug import"
      pattern: "getProjectBySlug"
    - from: "src/pages/portfolio/index.astro"
      to: "src/components/ui/FilterPills.astro"
      via: "component inclusion"
      pattern: "FilterPills"
    - from: "src/pages/portfolio/[slug].astro"
      to: "src/components/portfolio/ProjectGallery.astro"
      via: "component inclusion"
      pattern: "ProjectGallery"
---

<objective>
Build the home page and complete portfolio experience -- the two most visually important parts of the site that showcase Liz's design work.

Purpose: The home page is the first impression and the portfolio is the core value proposition. These pages are where the luxury aesthetic, image optimization, and editorial layout must be strongest. Everything else (services, about, contact) refers visitors back to the portfolio.

Output: Complete home page with hero, featured projects, about teaser, process preview, and CTA. Portfolio gallery page with filtering. Dynamic project detail pages with full-bleed imagery, narrative sections, scrolling gallery, and lightbox.
</objective>

<execution_context>
@/Users/paulalbert/.claude/get-shit-done/workflows/execute-plan.md
@/Users/paulalbert/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-public-portfolio-site/02-CONTEXT.md
@.planning/phases/02-public-portfolio-site/02-RESEARCH.md
@.planning/phases/02-public-portfolio-site/02-01-SUMMARY.md

<interfaces>
<!-- Components and utilities created by Plan 01 that this plan consumes -->

From src/layouts/BaseLayout.astro:
```astro
interface Props {
  title: string;
  description: string;
  ogImage?: string;
  noindex?: boolean;
  transparentHeader?: boolean;
}
```

From src/components/SanityImage.astro:
```astro
interface Props {
  image: any;
  alt: string;
  sizes?: string;
  widths?: number[];
  class?: string;
  loading?: "lazy" | "eager";
  fetchpriority?: "high" | "low" | "auto";
}
```

From src/components/ui/Button.astro:
```astro
interface Props {
  href?: string;
  variant?: "primary" | "secondary" | "outline";
  size?: "sm" | "md" | "lg";
  class?: string;
}
```

From src/components/ui/SectionHeading.astro:
```astro
interface Props {
  tagline?: string;
  class?: string;
}
<!-- Slot: heading text rendered as h2 -->
```

From src/sanity/queries.ts:
```typescript
export async function getProjects(): Promise<Project[]>;
export async function getProjectBySlug(slug: string): Promise<Project>;
export async function getFeaturedProjects(): Promise<Project[]>;
export async function getSiteSettings(): Promise<SiteSettings>;
```

From src/sanity/image.ts:
```typescript
export function urlFor(source: any): ImageUrlBuilder;
```

From src/styles/global.css:
```css
/* Available Tailwind utilities: bg-cream, bg-cream-dark, text-charcoal, text-stone, text-terracotta, font-heading, font-body */
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Home page with hero, featured projects, about teaser, process preview, and CTA</name>
  <files>
    src/pages/index.astro,
    src/components/home/Hero.astro,
    src/components/home/FeaturedProjects.astro,
    src/components/ui/ProcessStep.astro
  </files>
  <action>
    All work happens in ~/Dropbox/GitHub/la-sprezzatura/.

    **1. Hero.astro component:**
    Per user decisions: full-bleed hero image with minimal text overlay.
    - Full viewport height (100vh or 100svh for mobile)
    - Background: either a Sanity-sourced hero image (via SanityImage with loading="eager" and fetchpriority="high") or a placeholder gradient in warm neutrals if no Sanity content exists yet
    - Text overlay: "La Sprezzatura" in heading serif font (large, elegant) + tagline below
    - Subtle dark gradient overlay at bottom to ensure text readability over any image
    - Text positioned toward bottom-left or center, with generous padding
    - Down-arrow or scroll indicator at bottom (subtle, animated with CSS)
    - Props: image (optional Sanity image), title (string), tagline (string)
    - For now, if no Sanity image is available, use a solid warm gradient background (cream to cream-dark) so the page works without CMS content

    **2. FeaturedProjects.astro component:**
    Per user decisions: 2-3 featured projects as large editorial cards below hero.
    - Accepts `projects` array prop
    - If projects array is empty (no Sanity content yet), render nothing or a placeholder
    - Each card: large SanityImage of hero photo, project title in heading font, location in stone color, link to /portfolio/[slug]
    - Layout: responsive grid -- 1 column on mobile, 2-3 columns on desktop
    - Cards should feel editorial -- generous padding, subtle hover effect (image zoom or overlay shift)
    - Image hover: subtle scale(1.03) transition per user decisions
    - Each card links to `/portfolio/${project.slug.current}`

    **3. ProcessStep.astro component:**
    Reusable step component for the design process preview on home page (and full process page in Plan 03).
    - Props: number (string/number), title (string), description (string), class (string)
    - Renders: step number (large, terracotta accent color), title (heading font), brief description (body font, stone color)
    - Compact enough for a row of 3-4 on home page, expandable for full detail on process page

    **4. Complete index.astro (home page):**
    Replace the minimal placeholder from Plan 01 with the full home page. Per user decisions, the sections in order:

    a. **Hero** -- Full-bleed hero using Hero.astro with transparentHeader={true} on BaseLayout
    b. **Featured Projects** -- FeaturedProjects.astro with data from getFeaturedProjects(). Wrap in a section with SectionHeading tagline="Portfolio" heading "Our Work"
    c. **About Teaser** -- Brief paragraph about Liz/the studio with a "Learn more" Button linking to /about. Use asymmetric layout -- text on one side, image placeholder on the other. Heading font for the quote or teaser line.
    d. **Process Preview** -- 3-4 ProcessStep components showing abbreviated design process (Discovery, Concept, Design, Installation). SectionHeading tagline="How We Work" heading "Our Process". Link to full /process page.
    e. **Testimonial** -- If available from Sanity, display a client quote in large italic heading font with attribution. Centered, generous vertical padding.
    f. **CTA Section** -- "Ready to transform your space?" with Button linking to /contact. Warm background (cream-dark), centered text.

    Use generous vertical spacing between sections (py-section or py-section-sm classes based on the design tokens). Apply `data-animate="fade-up"` attributes to sections for GSAP animation in Plan 04.

    Fetch data in frontmatter:
    ```
    const featuredProjects = await getFeaturedProjects();
    const settings = await getSiteSettings();
    ```

    Handle empty state gracefully -- if no Sanity content exists yet, the page should still render beautifully with placeholder content. Do NOT crash if queries return empty results.

    SEO: title="La Sprezzatura | Luxury Interior Design", description about luxury interior design on Long Island and NYC.
  </action>
  <verify>
    <automated>cd ~/Dropbox/GitHub/la-sprezzatura && npm run build</automated>
  </verify>
  <done>
    - Home page renders with all 6 sections (hero, featured projects, about teaser, process preview, testimonial, CTA)
    - Hero takes full viewport height with text overlay
    - Featured projects section renders cards or gracefully handles empty state
    - Process preview shows 3-4 steps
    - CTA section links to /contact
    - Mobile layout stacks sections vertically with appropriate spacing
    - `npm run build` passes
  </done>
</task>

<task type="auto">
  <name>Task 2: Portfolio gallery with filters and project detail pages with lightbox</name>
  <files>
    src/pages/portfolio/index.astro,
    src/pages/portfolio/[slug].astro,
    src/components/portfolio/ProjectCard.astro,
    src/components/portfolio/ProjectGallery.astro,
    src/components/portfolio/Lightbox.astro,
    src/components/ui/FilterPills.astro
  </files>
  <action>
    All work happens in ~/Dropbox/GitHub/la-sprezzatura/.

    **1. FilterPills.astro component:**
    Per user decisions: simple pill/tag filter, NOT dropdown.
    - Props: roomTypes (string array), styles (string array)
    - Renders an "All" button + a pill for each unique room type and style
    - Styling: pill-shaped buttons with cream-dark background, charcoal text, rounded-full, generous padding
    - Active state: terracotta background, cream text
    - Uses `data-filter` attribute on each pill for client-side JS
    - Inline `<script>` block (per RESEARCH "Client-Side Portfolio Filter" example):
      - Listen on `astro:page-load` event (for view transitions compatibility)
      - On pill click: toggle active class, filter project cards by matching `data-room` or `data-style` attributes
      - "All" resets to show everything
      - Simple show/hide via display toggle -- no framework needed

    **2. ProjectCard.astro component:**
    Per user decisions: editorial cards, NOT lightbox from grid.
    - Props: project (object with title, slug, heroImage, roomType, style, location)
    - Renders as `<a>` linking to `/portfolio/${project.slug.current}`
    - SanityImage for hero photo with hover scale(1.03) transition
    - Title in heading font, location in stone color below
    - Entire card wrapped in `<article>` with `data-room={project.roomType}` and `data-style={project.style}` for filtering
    - Image container with overflow-hidden for clean zoom effect
    - If no hero image, render a placeholder warm gradient

    **3. Portfolio gallery page (src/pages/portfolio/index.astro):**
    Per user decisions: masonry or editorial grid with 4-5 projects.
    - Fetch all projects via getProjects() in frontmatter
    - Extract unique roomTypes and styles from projects for FilterPills
    - Page hero: smaller than home hero -- heading "Portfolio" with SectionHeading, brief intro text
    - FilterPills component with extracted categories
    - Grid of ProjectCard components: CSS grid, 1 col mobile, 2 cols tablet/desktop
    - Editorial grid feel: consider making the first card span 2 columns on desktop for visual hierarchy (CSS grid span)
    - Handle empty state: if no projects in Sanity, show a message like "Portfolio coming soon"
    - Apply `data-animate="fade-up"` to grid items for GSAP in Plan 04
    - SEO: title="Portfolio | La Sprezzatura Interior Design", description about portfolio

    **4. ProjectGallery.astro component:**
    - Props: images (array of Sanity image objects with alt, caption)
    - Renders full-width images in a vertical scroll layout (per user decision: scrolling gallery)
    - Each image uses SanityImage with lazy loading
    - Each image is clickable -- triggers lightbox
    - Each image has a `data-lightbox-index` attribute for lightbox navigation
    - Optional captions below images in small stone-colored text

    **5. Lightbox.astro component:**
    Per user decisions: optional lightbox for detail zoom within project pages.
    - Self-contained component with inline `<script>`
    - Fixed overlay (inset-0, black background with 90% opacity, high z-index)
    - Large image display centered in viewport
    - Navigation: prev/next arrows, close button (X)
    - Keyboard support: Escape closes, left/right arrows navigate
    - Touch support: swipe left/right on mobile
    - Image loaded via Sanity URL at high resolution (1600px+ width)
    - Open trigger: `[data-lightbox-index]` click events
    - Close trigger: X button, Escape key, click outside image
    - Initially hidden, shown via JS
    - Use GSAP-free CSS transitions for open/close (opacity + scale) since GSAP is Plan 04

    **6. Project detail page (src/pages/portfolio/[slug].astro):**
    Per RESEARCH Pattern 4 for getStaticPaths.
    - Fetch all slugs for static path generation
    - Fetch full project data via getProjectBySlug(slug)
    - Layout per user decisions: "each project should feel like a magazine spread"
      a. Full-bleed hero image (SanityImage, loading="eager", fetchpriority="high") -- full viewport width, 70-80vh height
      b. Project title overlay on hero in heading font, location + room type below
      c. Narrative section: if challenge/approach/outcome Portable Text blocks exist, render with astro-portabletext. Three subsections with headings "The Challenge", "Our Approach", "The Result". If blocks are empty, skip gracefully.
      d. ProjectGallery with remaining images
      e. Lightbox component
      f. Testimonial section: if testimonial exists, render quote in italic heading font with client name
      g. "Back to Portfolio" link and "Next Project" link if applicable
    - Handle missing fields gracefully -- not all projects may have testimonials or full narratives
    - SEO: title="`${project.title} | La Sprezzatura Interior Design`", description from project.description or generated from location + roomType, ogImage from heroImage URL
    - Mobile: full-width images, stacked layout, readable text sizes
    - `data-animate="fade-up"` on narrative sections and gallery for Plan 04
  </action>
  <verify>
    <automated>cd ~/Dropbox/GitHub/la-sprezzatura && npm run build</automated>
  </verify>
  <done>
    - Portfolio gallery page renders with FilterPills and project grid (or empty state message)
    - Filter pills toggle visibility of project cards by room type/style
    - Project detail page generates static paths from Sanity slugs
    - Project page shows full-bleed hero, narrative (if exists), gallery, testimonial (if exists)
    - Lightbox opens on gallery image click with prev/next navigation and keyboard/touch support
    - Portable Text renders rich content from challenge/approach/outcome fields
    - All pages responsive: proper layout at 375px, 768px, and 1440px widths
    - `npm run build` passes (or handles empty Sanity data gracefully)
  </done>
</task>

</tasks>

<verification>
- `cd ~/Dropbox/GitHub/la-sprezzatura && npm run build` completes without errors
- Home page has 6 distinct sections visible in the built HTML
- Portfolio gallery page renders with filter UI
- Project detail pages generate via getStaticPaths (or build succeeds with empty projects)
- All pages use BaseLayout with proper SEO meta tags
</verification>

<success_criteria>
1. Home page renders all 6 sections with proper luxury aesthetic and generous spacing
2. Portfolio gallery displays projects in an editorial grid with working pill filters
3. Project detail pages render full magazine-spread layout with Portable Text narratives
4. Lightbox opens gallery images with keyboard and touch navigation
5. Empty Sanity data does not crash any page
6. Mobile-first responsive layout works at phone, tablet, and desktop sizes
7. `npm run build` passes
</success_criteria>

<output>
After completion, create `.planning/phases/02-public-portfolio-site/02-02-SUMMARY.md`
</output>
