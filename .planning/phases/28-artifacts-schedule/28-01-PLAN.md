---
phase: 28-artifacts-schedule
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/gantt/GanttChart.tsx
  - src/lib/gantt/ganttTransforms.ts
  - src/lib/gantt/ganttTypes.ts
  - src/lib/gantt/ganttColors.ts
  - src/lib/gantt/ganttDates.ts
  - src/lib/gantt/gantt.css
  - src/lib/gantt/frappe-gantt.css
  - src/lib/gantt/ganttTransforms.test.ts
  - src/lib/gantt/ganttColors.test.ts
  - src/lib/gantt/ganttDates.test.ts
  - src/sanity/components/gantt/GanttScheduleView.tsx
  - src/sanity/components/gantt/GanttChart.tsx
autonomous: true
requirements: []

must_haves:
  truths:
    - "Gantt transform functions, types, colors, dates, CSS, and wrapper component exist in src/lib/gantt/"
    - "Studio GanttScheduleView imports shared code from src/lib/gantt/ instead of local lib/"
    - "All existing Gantt unit tests pass from their new location in src/lib/gantt/"
    - "Studio Gantt chart still renders correctly (no regressions)"
  artifacts:
    - path: "src/lib/gantt/ganttTransforms.ts"
      provides: "Pure transform functions for project data to Gantt tasks"
      exports: ["transformProjectToGanttTasks"]
    - path: "src/lib/gantt/ganttTypes.ts"
      provides: "GanttTask, GanttLink, ScheduleConflict, ResolvedContractor types"
      exports: ["GanttTask", "GanttLink"]
    - path: "src/lib/gantt/ganttColors.ts"
      provides: "Contractor palette and category color constants"
      exports: ["CONTRACTOR_PALETTE", "CATEGORY_COLORS"]
    - path: "src/lib/gantt/ganttDates.ts"
      provides: "Sanity date parsing and serialization"
      exports: ["parseSanityDate", "serializeSanityDate"]
    - path: "src/lib/gantt/GanttChart.tsx"
      provides: "Base Frappe Gantt wrapper component (read-only)"
      exports: ["GanttChart"]
    - path: "src/lib/gantt/gantt.css"
      provides: "Custom Gantt theme CSS"
    - path: "src/lib/gantt/frappe-gantt.css"
      provides: "Frappe Gantt base CSS"
  key_links:
    - from: "src/sanity/components/gantt/GanttScheduleView.tsx"
      to: "src/lib/gantt/GanttChart.tsx"
      via: "lazy import"
      pattern: 'import.*from.*\.\./\.\./\.\./lib/gantt'
    - from: "src/lib/gantt/GanttChart.tsx"
      to: "src/lib/gantt/ganttTypes.ts"
      via: "type import"
      pattern: 'import.*GanttTask.*from.*ganttTypes'
---

<objective>
Relocate shared Gantt code from `src/sanity/components/gantt/` to `src/lib/gantt/` so both Studio and admin can import the same transform functions, types, colors, dates, CSS, and base wrapper component. Update Studio imports to point to the new shared location.

Purpose: D-11 requires extracting reusable Gantt layers to a shared location. D-12 requires Studio to import from the shared location. This is the foundation for the admin ScheduleEditor in Plan 05.
Output: `src/lib/gantt/` directory with all shared Gantt code, Studio imports updated, all existing tests passing.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/28-artifacts-schedule/28-CONTEXT.md
@.planning/phases/28-artifacts-schedule/28-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From src/sanity/components/gantt/lib/ganttTypes.ts:
```typescript
export type GanttTask = { id: string; text: string; start: Date; end?: Date; ... };
export type GanttLink = { source: string; target: string; conflict?: boolean };
```

From src/sanity/components/gantt/GanttChart.tsx:
```typescript
export function GanttChart({ tasks, links }: GanttChartProps): JSX.Element;
```

From src/sanity/components/gantt/GanttScheduleView.tsx:
```typescript
const GanttChart = lazy(() => import("./GanttChart").then((m) => ({ default: m.GanttChart })));
// Also imports from ./lib/ganttTypes, uses useGanttData hook
```

From src/sanity/components/gantt/lib/ganttDates.ts:
```typescript
export function parseSanityDate(dateString: string): Date;
export function serializeSanityDate(date: Date): string;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Move shared Gantt files to src/lib/gantt/</name>
  <files>
    src/lib/gantt/GanttChart.tsx,
    src/lib/gantt/ganttTransforms.ts,
    src/lib/gantt/ganttTypes.ts,
    src/lib/gantt/ganttColors.ts,
    src/lib/gantt/ganttDates.ts,
    src/lib/gantt/gantt.css,
    src/lib/gantt/frappe-gantt.css,
    src/lib/gantt/ganttTransforms.test.ts,
    src/lib/gantt/ganttColors.test.ts,
    src/lib/gantt/ganttDates.test.ts,
    src/sanity/components/gantt/GanttChart.tsx
  </files>
  <read_first>
    src/sanity/components/gantt/GanttChart.tsx,
    src/sanity/components/gantt/lib/ganttTransforms.ts,
    src/sanity/components/gantt/lib/ganttTypes.ts,
    src/sanity/components/gantt/lib/ganttColors.ts,
    src/sanity/components/gantt/lib/ganttDates.ts,
    src/sanity/components/gantt/gantt.css,
    src/sanity/components/gantt/frappe-gantt.css,
    src/sanity/components/gantt/lib/ganttTransforms.test.ts,
    src/sanity/components/gantt/lib/ganttColors.test.ts,
    src/sanity/components/gantt/lib/ganttDates.test.ts
  </read_first>
  <action>
Create the directory `src/lib/gantt/`.

Copy the following files from `src/sanity/components/gantt/` to `src/lib/gantt/`, updating internal import paths:

1. `src/sanity/components/gantt/lib/ganttTypes.ts` -> `src/lib/gantt/ganttTypes.ts` (no import changes needed -- this file has no local imports)
2. `src/sanity/components/gantt/lib/ganttColors.ts` -> `src/lib/gantt/ganttColors.ts` (update import of `ganttTypes` from `"./ganttTypes"` -- should already be correct relative path)
3. `src/sanity/components/gantt/lib/ganttDates.ts` -> `src/lib/gantt/ganttDates.ts` (no import changes needed)
4. `src/sanity/components/gantt/lib/ganttTransforms.ts` -> `src/lib/gantt/ganttTransforms.ts` (update imports: `"./ganttTypes"`, `"./ganttColors"`, `"./ganttDates"` -- should remain the same relative paths since they are now siblings)
5. `src/sanity/components/gantt/gantt.css` -> `src/lib/gantt/gantt.css` (CSS file, no imports to change)
6. `src/sanity/components/gantt/frappe-gantt.css` -> `src/lib/gantt/frappe-gantt.css` (CSS file, no imports to change)
7. `src/sanity/components/gantt/GanttChart.tsx` -> `src/lib/gantt/GanttChart.tsx` (update imports: CSS imports from `"./frappe-gantt.css"` and `"./gantt.css"`, type import from `"./ganttTypes"` instead of `"./lib/ganttTypes"`)

Copy test files:
8. `src/sanity/components/gantt/lib/ganttTransforms.test.ts` -> `src/lib/gantt/ganttTransforms.test.ts` (update imports to `"./ganttTransforms"`, `"./ganttTypes"`, `"./ganttColors"`, `"./ganttDates"` as needed)
9. `src/sanity/components/gantt/lib/ganttColors.test.ts` -> `src/lib/gantt/ganttColors.test.ts` (update imports)
10. `src/sanity/components/gantt/lib/ganttDates.test.ts` -> `src/lib/gantt/ganttDates.test.ts` (update imports)

After copying and updating imports, delete the old files from `src/sanity/components/gantt/lib/` (ganttTypes.ts, ganttColors.ts, ganttDates.ts, ganttTransforms.ts, and their test files). Also delete `src/sanity/components/gantt/GanttChart.tsx`, `src/sanity/components/gantt/gantt.css`, and `src/sanity/components/gantt/frappe-gantt.css`.

Then create a re-export barrel at `src/sanity/components/gantt/GanttChart.tsx` that re-exports from the shared location:
```typescript
// Re-export from shared location for backward compatibility
export { GanttChart } from "../../../lib/gantt/GanttChart";
```

This ensures the lazy import in GanttScheduleView continues to work without changes to that file's import path.

IMPORTANT: In the new `src/lib/gantt/GanttChart.tsx`, the CSS imports must be:
```typescript
import "./frappe-gantt.css";
import "./gantt.css";
```
And the type import must be:
```typescript
import type { GanttTask, GanttLink } from "./ganttTypes";
```
  </action>
  <verify>
    <automated>npx vitest run src/lib/gantt/ -x</automated>
  </verify>
  <acceptance_criteria>
    - `src/lib/gantt/ganttTypes.ts` exists and contains `export type GanttTask`
    - `src/lib/gantt/ganttColors.ts` exists and contains `export const CONTRACTOR_PALETTE`
    - `src/lib/gantt/ganttDates.ts` exists and contains `export function parseSanityDate`
    - `src/lib/gantt/ganttTransforms.ts` exists and contains `export function transformProjectToGanttTasks`
    - `src/lib/gantt/GanttChart.tsx` exists and contains `export function GanttChart`
    - `src/lib/gantt/GanttChart.tsx` contains `import "./frappe-gantt.css"`
    - `src/lib/gantt/GanttChart.tsx` contains `import "./gantt.css"`
    - `src/lib/gantt/gantt.css` exists
    - `src/lib/gantt/frappe-gantt.css` exists
    - `src/lib/gantt/ganttTransforms.test.ts` exists
    - `src/lib/gantt/ganttColors.test.ts` exists
    - `src/lib/gantt/ganttDates.test.ts` exists
    - `src/sanity/components/gantt/GanttChart.tsx` contains `export { GanttChart } from "../../../lib/gantt/GanttChart"`
    - `src/sanity/components/gantt/lib/ganttTypes.ts` does NOT exist (deleted)
    - `npx vitest run src/lib/gantt/ -x` exits 0
  </acceptance_criteria>
  <done>All shared Gantt files relocated to src/lib/gantt/, old files deleted, re-export barrel in place, all tests pass from new location</done>
</task>

<task type="auto">
  <name>Task 2: Update Studio GanttScheduleView imports to use shared location</name>
  <files>
    src/sanity/components/gantt/GanttScheduleView.tsx,
    src/sanity/components/gantt/hooks/useGanttData.ts
  </files>
  <read_first>
    src/sanity/components/gantt/GanttScheduleView.tsx,
    src/sanity/components/gantt/hooks/useGanttData.ts,
    src/sanity/components/gantt/GanttLegend.tsx,
    src/sanity/components/gantt/GanttEmptyState.tsx,
    src/sanity/components/gantt/ScaleToggle.tsx
  </read_first>
  <action>
Update imports in Studio Gantt files that referenced the old `./lib/` paths:

**GanttScheduleView.tsx**: The lazy import of `GanttChart` already works via the re-export barrel created in Task 1 (`./GanttChart` resolves to the barrel). Check if this file imports anything else from `./lib/` (e.g., types). If it does, update those imports to `"../../../lib/gantt/ganttTypes"` (or whichever shared module). Per D-12, Studio-specific code (useClient, @sanity/ui chrome) stays in `src/sanity/components/gantt/`.

**hooks/useGanttData.ts**: This file likely imports from `../lib/ganttTransforms`, `../lib/ganttTypes`, `../lib/ganttColors`, `../lib/ganttDates`. Update all such imports to `"../../../../lib/gantt/ganttTransforms"`, `"../../../../lib/gantt/ganttTypes"`, etc. (path from `src/sanity/components/gantt/hooks/` to `src/lib/gantt/`).

**GanttLegend.tsx**: If it imports from `./lib/ganttTypes` or `./lib/ganttColors`, update to `"../../../lib/gantt/ganttTypes"`, `"../../../lib/gantt/ganttColors"`.

**ScaleToggle.tsx, GanttEmptyState.tsx**: Check for imports from `./lib/` and update similarly.

After updating all imports, the `src/sanity/components/gantt/lib/` directory should be empty (if not already deleted in Task 1). If the directory still exists, delete it.

Run the full test suite to verify no regressions.
  </action>
  <verify>
    <automated>npx vitest run -x</automated>
  </verify>
  <acceptance_criteria>
    - `src/sanity/components/gantt/hooks/useGanttData.ts` contains imports from `../../../../lib/gantt/` (not from `../lib/`)
    - `src/sanity/components/gantt/GanttScheduleView.tsx` does NOT contain `from "./lib/`
    - `src/sanity/components/gantt/lib/` directory does NOT exist (fully cleaned up)
    - `npx vitest run -x` exits 0 (all tests pass, no regressions)
  </acceptance_criteria>
  <done>All Studio Gantt files import from shared src/lib/gantt/ location, old lib/ directory removed, full test suite green</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | This plan is a code relocation with no new trust boundaries -- no API routes, no user input, no auth changes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-00 | Tampering | src/lib/gantt/ (shared code) | accept | Code relocation only -- same trust level as before. No new attack surface. Existing tests verify correctness. |
</threat_model>

<verification>
1. `npx vitest run src/lib/gantt/ -x` -- all relocated tests pass
2. `npx vitest run -x` -- full suite green, no regressions from import updates
3. `ls src/sanity/components/gantt/lib/` returns "No such file or directory" (fully cleaned up)
4. `grep -r "from.*\./lib/gantt" src/sanity/components/gantt/` returns no matches (all imports updated)
</verification>

<success_criteria>
All shared Gantt code lives in src/lib/gantt/ and is importable by both Studio and admin components. Studio GanttScheduleView renders identically to before. All existing tests pass from their new location. No references to the old src/sanity/components/gantt/lib/ directory remain.
</success_criteria>

<output>
After completion, create `.planning/phases/28-artifacts-schedule/28-01-SUMMARY.md`
</output>
