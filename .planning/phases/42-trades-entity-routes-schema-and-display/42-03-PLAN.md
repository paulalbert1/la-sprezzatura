---
phase: 42-trades-entity-routes-schema-and-display
plan: 03
type: execute
wave: 1
depends_on: [42-02]
files_modified:
  - src/components/admin/WorkOrderComposeModal.tsx
  - src/components/admin/ContractorChipSendAction.tsx
  - src/pages/admin/projects/[projectId]/index.astro
autonomous: true
requirements: [TRAD-03]
gap_closure: true
gap_source: 42-VERIFICATION.md
gaps_addressed:
  - "Truth #11: Work order contractor-picker header / chip context shows {Contractor|Vendor} derived label"

must_haves:
  truths:
    - "WorkOrderComposeModal header renders 'To {name} · {email} · {Contractor|Vendor}' using relationshipLabel(contractor.relationship)"
  artifacts:
    - path: "src/components/admin/WorkOrderComposeModal.tsx"
      provides: "contractor prop includes relationship field; header renders derived entity type label"
      contains: "relationship?: string | null"
    - path: "src/components/admin/ContractorChipSendAction.tsx"
      provides: "contractor prop includes relationship field passed through to WorkOrderComposeModal"
      contains: "relationship?: string | null"
    - path: "src/pages/admin/projects/[projectId]/index.astro"
      provides: "ContractorChipSendAction call passes pc.contractor.relationship"
      contains: "relationship: pc.contractor.relationship"
  key_links:
    - from: "src/pages/admin/projects/[projectId]/index.astro"
      to: "src/components/admin/ContractorChipSendAction.tsx"
      via: "contractor prop with relationship field"
      pattern: "relationship.*pc\\.contractor\\.relationship"
    - from: "src/components/admin/ContractorChipSendAction.tsx"
      to: "src/components/admin/WorkOrderComposeModal.tsx"
      via: "contractor prop spread (line 167)"
      pattern: "contractor=\\{contractor\\}"
    - from: "src/components/admin/WorkOrderComposeModal.tsx"
      to: "src/lib/relationshipLabel"
      via: "import + header render"
      pattern: "relationshipLabel\\(contractor\\.relationship\\)"
---

<objective>
Close TRAD-03 gap: the WorkOrderComposeModal header must show the derived Contractor|Vendor label per the UI-SPEC Copywriting Contract.

Purpose: The verification report confirmed that all other surfaces (list, detail, popover, delete button, meta line) render the derived entity type label. The WorkOrderComposeModal header — the compose panel that opens when a user clicks "Send work order" on a contractor chip — is the only surface that does not. The contractor prop type lacks `relationship`, no import of `relationshipLabel` exists, and the header renders `To {name} · {email}` with no entity type context.

Output: WorkOrderComposeModal header renders `To {name} · {email} · {Contractor|Vendor}`. The `relationship` field flows from the project detail page GROQ projection (already available as `pc.contractor.relationship`) through ContractorChipSendAction and into WorkOrderComposeModal.
</objective>

<execution_context>
@/Users/paulalbert/.claude/get-shit-done/workflows/execute-plan.md
@/Users/paulalbert/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/42-trades-entity-routes-schema-and-display/42-VERIFICATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Thread relationship through the work order send chain and render derived label in modal header</name>

  <read_first>
    - src/components/admin/WorkOrderComposeModal.tsx — lines 89-94 (contractor prop type), line 1 (imports), line 336 (header paragraph that needs the label)
    - src/components/admin/ContractorChipSendAction.tsx — lines 46-52 (contractor prop type), line 167 (WorkOrderComposeModal contractor prop pass-through)
    - src/pages/admin/projects/[projectId]/index.astro — lines 333-350 (ContractorChipSendAction JSX, contractor object literal)
    - src/lib/relationshipLabel.ts — confirm export name is `relationshipLabel` (line 11)
  </read_first>

  <files>
    src/components/admin/WorkOrderComposeModal.tsx,
    src/components/admin/ContractorChipSendAction.tsx,
    src/pages/admin/projects/[projectId]/index.astro
  </files>

  <action>
**WorkOrderComposeModal.tsx — three edits:**

1. Add `relationship?: string | null` to the contractor prop type object at lines 89-94. The existing fields are `_id`, `name`, `email`, `trades?`. Add `relationship?` after `email`:
   ```ts
   contractor: {
     _id: string;
     name: string;
     email: string;
     relationship?: string | null;
     trades?: string[];
   };
   ```

2. Import `relationshipLabel` from `../../lib/relationshipLabel`. Add this import near the top of the file alongside other local imports. The path resolves from `src/components/admin/` to `src/lib/`.

3. Update the header paragraph at line 336. Current:
   ```tsx
   To {contractor.name} · {contractor.email}
   ```
   Replace with:
   ```tsx
   To {contractor.name} · {contractor.email} · {relationshipLabel(contractor.relationship)}
   ```
   The `relationshipLabel` function returns "Contractor" when relationship is null/undefined (per D-04 fallback), so this is safe for records without a relationship set.

**ContractorChipSendAction.tsx — one edit:**

4. Add `relationship?: string | null` to the contractor prop type at lines 46-52. The existing fields are `_id`, `_key`, `name`, `email`, `trades?`. Add `relationship?` after `email`:
   ```ts
   contractor: {
     _id: string;
     _key: string;
     name: string;
     email: string;
     relationship?: string | null;
     trades?: string[];
   };
   ```
   No other changes needed — the `contractor` prop is spread directly to `WorkOrderComposeModal` at line 167 (`contractor={contractor}`), so the field flows through automatically.

**src/pages/admin/projects/[projectId]/index.astro — one edit:**

5. In the `ContractorChipSendAction` JSX at lines 342-348, the contractor object literal is:
   ```tsx
   contractor={{
     _id: pc.contractor._id,
     _key: pc._key,
     name: pc.contractor.name,
     email: pc.contractor.email || "",
     trades: pc.contractor.trades,
   }}
   ```
   Add `relationship: pc.contractor.relationship ?? null` after `email: pc.contractor.email || "",`:
   ```tsx
   contractor={{
     _id: pc.contractor._id,
     _key: pc._key,
     name: pc.contractor.name,
     email: pc.contractor.email || "",
     relationship: pc.contractor.relationship ?? null,
     trades: pc.contractor.trades,
   }}
   ```
   `pc.contractor.relationship` is already present in the `projectContractors` GROQ projection (confirmed in queries.ts line 953 per VERIFICATION.md Truth #4 fix). No GROQ or API changes are required.
  </action>

  <verify>
    <automated>
      cd /Users/paulalbert/Dropbox/GitHub/la-sprezzatura && npx astro check --root . 2>&1 | tail -20
    </automated>
  </verify>

  <acceptance_criteria>
    - `grep -n "relationship.*string.*null" src/components/admin/WorkOrderComposeModal.tsx` returns a hit in the contractor prop block (lines ~89-94 area)
    - `grep -n "relationshipLabel" src/components/admin/WorkOrderComposeModal.tsx` returns at least 2 hits: one import line and one in the header paragraph
    - `grep -n "contractor\.email.*relationshipLabel\|relationshipLabel.*contractor" src/components/admin/WorkOrderComposeModal.tsx` returns a hit in the header p tag (line ~336 area) confirming the label is rendered inline
    - `grep -n "relationship.*string.*null" src/components/admin/ContractorChipSendAction.tsx` returns a hit in the contractor prop type block (lines ~46-52 area)
    - `grep -n "relationship.*pc\.contractor\.relationship" src/pages/admin/projects/\[projectId\]/index.astro` returns a hit confirming the field is passed in the ContractorChipSendAction contractor object
    - `npx astro check` exits 0 with no type errors
  </acceptance_criteria>

  <done>
    All five edits applied. WorkOrderComposeModal header renders `To {name} · {email} · {Contractor|Vendor}`. ContractorChipSendAction contractor prop accepts `relationship`. Project detail page passes `pc.contractor.relationship ?? null` into ContractorChipSendAction. No TypeScript errors introduced.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GROQ projection → component prop | relationship string from Sanity passes through Astro to React |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-42-03-01 | Information Disclosure | WorkOrderComposeModal header | accept | relationship is display metadata only (Contractor/Vendor); no PII, no access control impact; rendered only to authenticated admin users |
</threat_model>

<verification>
After applying the 5 edits:

1. `grep -n "relationship" src/components/admin/WorkOrderComposeModal.tsx` — must show the prop type field and the `relationshipLabel(contractor.relationship)` call in the header paragraph
2. `grep -n "relationship" src/components/admin/ContractorChipSendAction.tsx` — must show the prop type field
3. `grep -n "relationship.*pc\.contractor" src/pages/admin/projects/\[projectId\]/index.astro` — must show the pass-through in the ContractorChipSendAction call
4. `npx astro check` exits 0
5. `npx vitest run src/lib/relationshipLabel.test.ts` — existing 6 tests still pass (no changes to the label function itself)
</verification>

<success_criteria>
Truth #11 satisfied: WorkOrderComposeModal header shows `To {contractor.name} · {contractor.email} · {Contractor|Vendor}` where the derived label comes from `relationshipLabel(contractor.relationship)`. The `relationship` field is threaded from `pc.contractor.relationship` (GROQ projection) through ContractorChipSendAction into WorkOrderComposeModal. TRAD-03 moves from partial to satisfied.
</success_criteria>

<output>
After completion, create `.planning/phases/42-trades-entity-routes-schema-and-display/42-03-SUMMARY.md`
</output>
