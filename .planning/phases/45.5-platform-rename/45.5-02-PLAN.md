---
phase: 45.5-platform-rename
plan: 45.5-02
type: execute
wave: 2
depends_on: [45.5-01]
files_modified:
  - src/lib/sendUpdate/emailTemplate.ts
  - src/lib/workOrder/emailTemplate.ts
  - src/lib/sendUpdate/__snapshots__/emailTemplate.test.ts.snap
  - src/lib/workOrder/__snapshots__/emailTemplate.test.ts.snap
  - tests/email-snapshots/scaffold.spec.ts-snapshots
  - src/lib/email/system.ts
autonomous: true
requirements: []

must_haves:
  truths:
    - "Send Update email footer shows tenant wordmark unchanged ('La Sprezzatura' on the existing dark line) plus a small 'Sent via Sprezza Hub' attribution at the very bottom (~10px, gray, centered)."
    - "Work Order email footer (HTML and plain-text MIME) shows tenant wordmark unchanged plus 'Sent via Sprezza Hub' attribution; plain-text variant no longer says 'Linha Studio'."
    - "Vitest snapshot diffs show ONLY the footer line change — no drift in heading, body, button, or other regions."
    - "src/lib/email/system.ts exists and exports SYSTEM_FROM_NAME='Sprezza Hub', SYSTEM_FROM_EMAIL='noreply@lasprezz.com', and a formatSystemSender() helper. No callers; forward-looking stub for D-4."
    - "grep -rn 'Linha' src/ across the entire repo (excluding docs/superpowers/specs and snapshot directories) returns zero hits."
    - "npm run test:email and npm run test:visual exit 0 (both target the snapshots regenerated in this plan). Full npm test (Vitest) shows no NEW failures vs the pre-edit baseline captured in Plan 45.5-01 Task 0 at /tmp/45.5-test-baseline.txt; tenantAudit.test.ts and the regenerated email snapshot tests specifically exit 0."
  artifacts:
    - path: src/lib/sendUpdate/emailTemplate.ts
      provides: "Send Update email HTML with Sprezza Hub footer attribution"
      contains: "Sent via Sprezza Hub"
    - path: src/lib/workOrder/emailTemplate.ts
      provides: "Work Order email HTML and plain text with Sprezza Hub footer attribution"
      contains: "Sent via Sprezza Hub"
    - path: src/lib/email/system.ts
      provides: "System/account email sender constants (forward-looking, D-4)"
      exports: ["SYSTEM_FROM_NAME", "SYSTEM_FROM_EMAIL", "formatSystemSender"]
    - path: src/lib/sendUpdate/__snapshots__/emailTemplate.test.ts.snap
      provides: "Regenerated Vitest snapshots reflecting new footer"
    - path: src/lib/workOrder/__snapshots__/emailTemplate.test.ts.snap
      provides: "Regenerated Vitest snapshots reflecting new footer"
  key_links:
    - from: src/lib/sendUpdate/emailTemplate.ts
      to: snapshot file
      via: vitest toMatchSnapshot()
      pattern: "Sent via Sprezza Hub"
    - from: src/lib/workOrder/emailTemplate.ts
      to: snapshot file (HTML + plain text)
      via: vitest toMatchSnapshot()
      pattern: "Sent via Sprezza Hub"
---

<objective>
Replace the `Linha Studio` / `La Sprezzatura · Linha Studio` co-brand lines in both email templates with the D-2 pattern: tenant wordmark stays unchanged, a small `Sent via Sprezza Hub` attribution lands at the very bottom (~10px gray, centered). Regenerate Vitest snapshots and Playwright PNG baselines as the audit trail (D-6). Add a forward-looking `src/lib/email/system.ts` stub for D-4 (system-email sender constants — no callers today).

Purpose: Closes the last 5 `Linha` references in the codebase (Success Criteria #1, #3) and architecturally documents the D-4 tenant-vs-platform sender split before Phase 46's react-email migration.

Output: Two email template files updated, snapshots regenerated, system.ts stub created. Linha is dead-clean across the repo.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/45.5-platform-rename/45.5-CONTEXT.md
@.planning/phases/45.5-platform-rename/45.5-PATTERNS.md
@.planning/phases/45.5-platform-rename/45.5-01-SUMMARY.md
@src/lib/sendUpdate/emailTemplate.ts
@src/lib/workOrder/emailTemplate.ts
</context>

<interfaces>
<!-- New module shape for src/lib/email/system.ts (D-4 forward-looking stub) -->
<!-- Pure constants module. No imports. No side effects. No callers in this phase. -->

```typescript
// src/lib/email/system.ts
//
// System / account email sender constants (D-4).
//
// Tenant emails (Send Update, Work Order, portal/building/workorder access invites)
// use the tenant's siteSettings.defaultFromName + defaultFromEmail.
//
// System emails (password reset, signup confirmation, billing notices) use these
// constants. Sender domain is decoupled from app brand per phase 45-03's app-rename
// decision: lasprezz.com is the sending domain for the foreseeable future.
//
// As of phase 45.5 there are no system emails in the codebase. These constants
// exist so that when system emails are introduced, the tenant-vs-platform sender
// split is already architecturally documented.

export const SYSTEM_FROM_NAME = "Sprezza Hub";
export const SYSTEM_FROM_EMAIL = "noreply@lasprezz.com";

/**
 * Format the system sender as the canonical "Display Name <addr>" string
 * accepted by Resend's `from` field.
 */
export function formatSystemSender(): string {
  return `${SYSTEM_FROM_NAME} <${SYSTEM_FROM_EMAIL}>`;
}
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Update Send Update email footer co-brand</name>
  <files>src/lib/sendUpdate/emailTemplate.ts</files>
  <read_first>Read lines 320-368 of `src/lib/sendUpdate/emailTemplate.ts` (the buildSendUpdateEmail return string). The two Linha hits are line 331 (header micro-line under the wordmark) and line 349 (footer co-brand under Elizabeth's signature). The line 330 `La Sprezzatura` wordmark and line 361 `La Sprezzatura · Darien, CT` location line are TENANT brand and stay.</read_first>
  <action>
Per D-2 (`Sent via Sprezza Hub` attribution, ~10px gray centered) and the visual treatment matched to the existing footer color palette:

1. **Line 331** — the `Linha Studio` micro-line under the wordmark in the *header* block. Per D-2 the platform attribution moves to the *footer*, so this header line should be removed entirely. Delete the entire `<p>` element on line 331. The header keeps just the `La Sprezzatura` wordmark on line 330.

2. **Line 349** — the footer co-brand under Elizabeth's signature. Currently: `<p style="font-family: Arial, sans-serif; font-size: 12px; color: #9A8F82; margin: 0 0 1.75rem;">La Sprezzatura · Linha Studio</p>`. Change to: `<p style="font-family: Arial, sans-serif; font-size: 12px; color: #9A8F82; margin: 0 0 1.75rem;">La Sprezzatura</p>` (remove `· Linha Studio`, keep the tenant wordmark line that pairs with Elizabeth's signature).

3. **Add the new `Sent via Sprezza Hub` attribution row** at the very bottom of the email — INSIDE the existing bottom `<div style="padding: 1rem 2.5rem; border-top: 0.5px solid #EDE8E2; text-align: center;">` block (currently lines 360-362), append a second `<p>` AFTER the existing `La Sprezzatura · Darien, CT` line. Wording, casing, and visual treatment per D-2/D-3:
   ```html
   <p style="font-family: Arial, sans-serif; font-size: 10px; color: #C5BDB4; margin: 0.5rem 0 0; letter-spacing: 0.06em;">Sent via Sprezza Hub</p>
   ```
   Color `#C5BDB4` matches the existing low-contrast `Darien, CT` line so the attribution recedes visually. Font-size 10px per D-2.

Net diff: line 331 deleted, line 349 trimmed, one new `<p>` inside the existing bottom row. No layout changes; no new container; no other style modifications.
  </action>
  <verify>
    <automated>cd /Users/paulalbert/Dropbox/GitHub/la-sprezzatura && grep -c "Linha" src/lib/sendUpdate/emailTemplate.ts | awk '{ if ($1 == 0) print "PASS"; else { print "FAIL: " $1; exit 1 } }' && grep -c "Sent via Sprezza Hub" src/lib/sendUpdate/emailTemplate.ts | awk '{ if ($1 == 1) print "PASS"; else { print "FAIL"; exit 1 } }' && grep -c "La Sprezzatura" src/lib/sendUpdate/emailTemplate.ts | awk '{ if ($1 >= 2) print "PASS"; else { print "FAIL: tenant wordmark missing"; exit 1 } }'</automated>
    - Zero `Linha` hits in file
    - Exactly one `Sent via Sprezza Hub` line
    - At least 2 `La Sprezzatura` hits remain (the line-330 wordmark + the line-361 location line)
  </verify>
  <done>Send Update header has the tenant wordmark only; footer signature shows `La Sprezzatura` (no `· Linha Studio`); a new `Sent via Sprezza Hub` 10px attribution sits at the very bottom of the email.</done>
</task>

<task type="auto">
  <name>Task 2: Update Work Order email footer co-brand (HTML + plain text)</name>
  <files>src/lib/workOrder/emailTemplate.ts</files>
  <read_first>Read lines 30-76 of `src/lib/workOrder/emailTemplate.ts`. Three Linha hits: line 39 (header micro-line under `LA SPREZZATURA` wordmark), line 54 (HTML footer co-brand under fromDisplayName), line 74 (plain-text MIME footer line). Lines 38, 53, 55, 73, 75 are tenant content (wordmark, name, location) and stay. Note `emailTemplate.test.ts:94` asserts text contains `"La Sprezzatura"` — that survives because line 73 is unchanged.</read_first>
  <action>
Match the visual treatment from Task 1 so both templates look consistent.

1. **Line 39** — `<div style="font-size:11.5px;color:#9E8E80;margin-top:4px;">Linha Studio</div>` in the header block. Delete this entire `<div>`. Header keeps just the `LA SPREZZATURA` wordmark on line 38.

2. **Line 54** — `<div style="font-size:12px;color:#9E8E80;margin-top:4px;">La Sprezzatura &middot; Linha Studio</div>` in the footer. Change to: `<div style="font-size:12px;color:#9E8E80;margin-top:4px;">La Sprezzatura</div>` (remove `&middot; Linha Studio`).

3. **Add the new `Sent via Sprezza Hub` attribution row.** The current footer block (lines 52-56) ends with `Darien, CT` on line 55 inside the same `<td>` opened on line 52. Append a new `<div>` AFTER line 55 (still inside that `<td>`):
   ```html
   <div style="font-size:10px;color:#C5BDB4;margin-top:12px;letter-spacing:0.06em;text-align:center;">Sent via Sprezza Hub</div>
   ```
   Centered, 10px, low-contrast — matches Task 1 treatment.

4. **Line 74** — plain-text variant. Current plain-text body (lines 67-75):
   ```
   ${firstName},

   Your work order for ${project.title} is ready.

   View the latest version: ${link}

   La Sprezzatura
   Linha Studio
   Darien, CT
   ```
   Replace `Linha Studio\n` with `` (delete the line) AND append `\n\nSent via Sprezza Hub` at the very end. Resulting plain-text footer:
   ```
   La Sprezzatura
   Darien, CT

   Sent via Sprezza Hub
   ```
   The `emailTemplate.test.ts:94` assertion `expect(text).toContain("La Sprezzatura")` still passes because line 73 is intact.
  </action>
  <verify>
    <automated>cd /Users/paulalbert/Dropbox/GitHub/la-sprezzatura && grep -c "Linha" src/lib/workOrder/emailTemplate.ts | awk '{ if ($1 == 0) print "PASS"; else { print "FAIL: " $1; exit 1 } }' && grep -c "Sent via Sprezza Hub" src/lib/workOrder/emailTemplate.ts | awk '{ if ($1 == 2) print "PASS"; else { print "FAIL: expected 2 (HTML + plain text), got " $1; exit 1 } }' && grep -c "LA SPREZZATURA" src/lib/workOrder/emailTemplate.ts | awk '{ if ($1 == 1) print "PASS"; else { print "FAIL: tenant wordmark"; exit 1 } }'</automated>
    - Zero `Linha` hits in file
    - Exactly 2 `Sent via Sprezza Hub` (HTML branch + plain-text branch)
    - `LA SPREZZATURA` tenant wordmark survives on line 38
    - `La Sprezzatura` (mixed case) survives on lines 54 and 73
  </verify>
  <done>Work Order HTML and plain-text both show tenant wordmark + Sprezza Hub attribution; no Linha references; tenant wordmark untouched.</done>
</task>

<task type="auto">
  <name>Task 3: Create forward-looking system-email constants stub</name>
  <files>src/lib/email/system.ts</files>
  <read_first>Confirm `src/lib/email/` directory does not yet exist (`ls src/lib/email 2>/dev/null` returns nothing). If it does, that's fine — just write the new file inside it.</read_first>
  <action>
Create `src/lib/email/system.ts` with the contents shown in the `<interfaces>` block above. Pure module: no imports, no side effects, no callers in this phase. JSDoc on the `formatSystemSender` helper. Top-of-file comment explains the tenant-vs-platform sender split per D-4.

Do NOT add a barrel `index.ts`. Do NOT wire any caller. Phase 46+ will consume this when system emails are introduced.
  </action>
  <verify>
    <automated>cd /Users/paulalbert/Dropbox/GitHub/la-sprezzatura && test -f src/lib/email/system.ts && grep -q 'SYSTEM_FROM_NAME = "Sprezza Hub"' src/lib/email/system.ts && grep -q 'SYSTEM_FROM_EMAIL = "noreply@lasprezz.com"' src/lib/email/system.ts && grep -q 'export function formatSystemSender' src/lib/email/system.ts && echo PASS</automated>
    - File exists at the expected path
    - Both constants exported with the exact values from D-3/D-4
    - `formatSystemSender` helper exported
    - No callers in the codebase: `grep -rn "from.*lib/email/system" src/` returns zero hits (forward-looking stub)
  </verify>
  <done>`src/lib/email/system.ts` exists, exports the three names, has zero callers, compiles cleanly under TypeScript (verified by Task 4's `npm test` run which type-checks via Vitest).</done>
</task>

<task type="auto">
  <name>Task 4: Regenerate Vitest snapshots and verify only footer changed</name>
  <files>src/lib/sendUpdate/__snapshots__/emailTemplate.test.ts.snap, src/lib/workOrder/__snapshots__/emailTemplate.test.ts.snap</files>
  <read_first>Read both `.snap` files briefly so you understand the existing snapshot shape (5 permutations for sendUpdate, 2 baselines for workOrder). After regen, you'll diff against this baseline to confirm only the footer regions changed.</read_first>
  <action>
1. **First, run the email tests WITHOUT update mode** to see the failure diffs:
   ```
   npm run test:email
   ```
   Expect 7 snapshot failures (5 sendUpdate + 2 workOrder). Inspect each diff and confirm the only changes are:
   - Removal of the `Linha Studio` micro-line under the header wordmark (sendUpdate + workOrder)
   - Removal of `· Linha Studio` from the footer co-brand line
   - Addition of the new `Sent via Sprezza Hub` 10px attribution row
   - For workOrder plain-text snap: removal of `Linha Studio\n` and addition of `\n\nSent via Sprezza Hub`

   No diffs in heading, body copy, button, action items, milestones, procurement, pending, or signature regions. If unrelated drift appears, STOP and re-read your edits before regenerating.

2. **Regenerate snapshots:**
   ```
   npx vitest run src/lib/sendUpdate src/lib/workOrder -u --reporter=basic
   ```
   All snapshot tests pass. The `emailTemplate.test.ts:94` text-MIME assertion `expect(text).toContain("La Sprezzatura")` still passes (line 73 of source is unchanged).

3. **Idempotency check** — run again WITHOUT `-u`:
   ```
   npm run test:email
   ```
   All green. Re-running `-u` produces zero further changes.

4. **Manual diff sanity check:**
   ```
   git diff src/lib/sendUpdate/__snapshots__/emailTemplate.test.ts.snap | grep -E '^[+-]' | grep -vE '^(\+\+\+|---)' | head -40
   ```
   Confirm the diff is dominated by `Linha Studio` removals and `Sent via Sprezza Hub` additions. No unexpected churn.
  </action>
  <verify>
    <automated>cd /Users/paulalbert/Dropbox/GitHub/la-sprezzatura && npx vitest run src/lib/sendUpdate src/lib/workOrder --reporter=basic 2>&1 | tail -20 && grep -c "Linha" src/lib/sendUpdate/__snapshots__/emailTemplate.test.ts.snap src/lib/workOrder/__snapshots__/emailTemplate.test.ts.snap | awk -F: '{ sum += $2 } END { if (sum == 0) print "PASS"; else { print "FAIL: stale Linha in snaps"; exit 1 } }' && grep -l "Sent via Sprezza Hub" src/lib/sendUpdate/__snapshots__/emailTemplate.test.ts.snap src/lib/workOrder/__snapshots__/emailTemplate.test.ts.snap | wc -l | awk '{ if ($1 == 2) print "PASS"; else { print "FAIL: attribution not in both snaps"; exit 1 } }'</automated>
    - `npx vitest run src/lib/sendUpdate src/lib/workOrder` exits 0 (snapshots match the regenerated source)
    - Both `.snap` files contain `Sent via Sprezza Hub`
    - Neither `.snap` file contains `Linha`
    - `git diff` over the snap files shows only footer-region changes
  </verify>
  <done>Vitest snapshots regenerated, idempotent, contain Sprezza Hub attribution and zero Linha references. The diff is the audit trail per D-6.</done>
</task>

<task type="auto">
  <name>Task 5: Regenerate Playwright PNG baselines and run full verification</name>
  <files>tests/email-snapshots/scaffold.spec.ts-snapshots/</files>
  <action>
1. **Regenerate Playwright PNG baselines.** The scaffold spec from Phase 45-05 may or may not visually shift (the scaffold component itself doesn't include the email template footer being changed, but regenerating ensures any incidental drift is captured):
   ```
   npm run test:visual:update
   ```
   Inspect any PNG diffs. If unrelated drift appears (font rendering, antialiasing), accept the new baselines — that's expected at the few-pixel level.

2. **Run the scoped suites that this plan directly affects** — these MUST exit 0 because Task 4 just regenerated their inputs:
   ```
   npm run test:email
   npm run test:visual
   ```

3. **Run the full Vitest suite and compare against the Plan 45.5-01 Task 0 baseline.** Phase 45-01 carried ~65 pre-existing test failures that are out of scope for 45.5; the gate is "no NEW failures introduced," not "absolute exit 0":
   ```
   AFTER=$(npx vitest run --reporter=basic 2>&1 | grep -E "^[[:space:]]*Tests:" | tail -1 || echo "Tests: 0 failed")
   BEFORE=$(cat /tmp/45.5-test-baseline.txt)
   echo "Pre-edit baseline (from Plan 45.5-01 Task 0): $BEFORE"
   echo "Post-edit result:                              $AFTER"
   if [ "$BEFORE" = "$AFTER" ]; then
     echo "PASS: no new test failures introduced"
   else
     echo "WARN: test count differs from baseline. Inspect."
     diff <(echo "$BEFORE") <(echo "$AFTER") || true
   fi
   ```
   Note: the regenerated email snapshot tests (Task 4) are now part of the AFTER count and MUST be passing. Specifically run:
   ```
   npx vitest run src/lib/sendUpdate src/lib/workOrder src/lib/tenant/tenantAudit.test.ts --reporter=basic
   ```
   That subset MUST exit 0 — it covers the regenerated snaps + the admin-chrome regression sentinel.

4. **Run the global Linha grep (Success Criterion #1) on the full repo:**
   ```
   grep -rn "Linha" src/ docs/ scripts/ --include="*.ts" --include="*.tsx" --include="*.astro" --include="*.json" --include="*.md" 2>/dev/null | grep -v "docs/superpowers/specs/" | grep -v "__snapshots__"
   ```
   MUST return zero hits. (Snapshot directories are excluded only because the regenerated snaps no longer contain Linha — the grep above filters them as belt-and-suspenders.)

5. **Confirm tenant brand surfaces still untouched:**
   ```
   git diff --stat src/components/portal src/pages/portal src/pages/building src/pages/workorder src/components/home src/components/layout src/components/seo src/pages/about.astro src/pages/contact.astro src/pages/index.astro src/pages/portfolio src/pages/privacy.astro src/pages/process.astro src/pages/services.astro src/config/tenants.json package.json src/sanity/schemas/siteSettings.ts
   ```
   MUST be empty.
  </action>
  <verify>
    <automated>set -e; npm run test:email --silent && npm run test:visual --silent && npx vitest run src/lib/sendUpdate src/lib/workOrder src/lib/tenant/tenantAudit.test.ts --reporter=basic && AFTER=$(npx vitest run --reporter=basic 2>&1 | grep -E "^[[:space:]]*Tests:" | tail -1 || echo "Tests: 0 failed") && BEFORE=$(cat /tmp/45.5-test-baseline.txt) && echo "BEFORE: $BEFORE" && echo "AFTER:  $AFTER" && test "$(grep -rn 'Linha' src/ docs/ scripts/ --include='*.ts' --include='*.tsx' --include='*.astro' --include='*.json' --include='*.md' 2>/dev/null | grep -v 'docs/superpowers/specs/' | grep -v '__snapshots__' | wc -l | tr -d ' ')" = "0" && test -z "$(git diff --stat src/components/portal src/pages/portal src/pages/building src/pages/workorder src/components/home src/components/layout src/components/seo src/pages/about.astro src/pages/contact.astro src/pages/index.astro src/pages/portfolio src/pages/privacy.astro src/pages/process.astro src/pages/services.astro src/config/tenants.json package.json src/sanity/schemas/siteSettings.ts)" && echo PASS</automated>
    - `npm run test:email` exits 0 (snaps regenerated in Task 4)
    - `npm run test:visual` exits 0 (Playwright baselines regenerated in step 1)
    - Scoped Vitest run on `src/lib/sendUpdate src/lib/workOrder src/lib/tenant/tenantAudit.test.ts` exits 0 (regression sentinels)
    - Full Vitest `Tests:` line ≤ baseline from `/tmp/45.5-test-baseline.txt` — no NEW failures introduced (Phase 45-01 had ~65 pre-existing failures out of scope)
    - Linha grep returns zero hits across the entire codebase (excluding frozen specs and snapshot dirs)
    - `git diff --stat` over tenant brand surfaces returns empty
  </verify>
  <done>
    - Scoped suites (`test:email`, `test:visual`, the regenerated snap tests + tenantAudit) all green
    - Full Vitest run shows no NEW failures vs `/tmp/45.5-test-baseline.txt` (Phase 45-01 pre-existing failures remain out of scope)
    - Linha is dead-clean across the repo (Success Criterion #1 satisfied)
    - Tenant brand surfaces byte-identical (Success Criterion #5 satisfied)
    - Snapshot diffs (Vitest + Playwright) are the audit trail (Success Criterion #6 satisfied)
  </done>
</task>

</tasks>

<verification>
After all tasks complete, the entire Phase 45.5 success criteria 1-8 must hold across both plans:

1. `grep -ri "Linha" src/ docs/ scripts/ --include="*.ts" --include="*.tsx" --include="*.astro" --include="*.json" --include="*.md" | grep -v "docs/superpowers/specs/" | grep -v "__snapshots__"` → zero hits ✓
2. Admin login + AdminNav + admin trades render `Sprezza Hub` (Plan 45.5-01) ✓
3. Email footer (Send Update + Work Order, both HTML and plain text) shows tenant wordmark + `Sent via Sprezza Hub` ✓
4. `src/lib/email/system.ts` exports the three names (forward-looking; no callers) ✓
5. Tenant brand surfaces byte-identical: `git diff --stat` over portal/marketing/tenants.json/package.json/sender domains is empty ✓
6. Vitest snaps + Playwright baselines regenerated, committed as audit trail ✓
7. `npm run test:email`, `npm run test:visual`, and scoped Vitest (regenerated snaps + tenantAudit) all exit 0. Full `npm test` (Vitest) shows no NEW failures vs `/tmp/45.5-test-baseline.txt` (Phase 45-01 carried ~65 pre-existing failures out of scope for 45.5). ✓
8. No commits used `--no-verify` (project rule from CLAUDE.md) ✓
</verification>

<success_criteria>
- 5 source files modified + 2 Vitest snap files regenerated + Playwright baselines regenerated + 1 new file (`src/lib/email/system.ts`)
- Linha is gone from the entire codebase except frozen specs and `.planning/` memory
- Both email templates show tenant brand dominance + small Sprezza Hub attribution per D-2
- D-4 architecturally documented in code via the system.ts stub
- Scoped suites green (test:email, test:visual, regenerated snaps, tenantAudit). Full Vitest run no worse than `/tmp/45.5-test-baseline.txt` (no NEW failures). No `--no-verify` commits.
</success_criteria>

<output>
After completion, write `.planning/phases/45.5-platform-rename/45.5-02-SUMMARY.md` covering:
- The 5 source-file substitutions made (file:line → before/after summary)
- New file: `src/lib/email/system.ts` with the three exports
- Snapshot diff summary (which snaps regenerated, dominant change being Linha removal + Sprezza Hub attribution addition)
- Test suite results: pre-edit baseline from `/tmp/45.5-test-baseline.txt`, post-edit `Tests:` line, and confirmation that `npm run test:email`, `npm run test:visual`, and scoped Vitest (regenerated snaps + tenantAudit) all exit 0
- Confirmation that tenant brand surfaces are byte-identical
- Confirmation that Linha is dead-clean across the repo
- Hand-off note: Phase 46 react-email migration can now author new components against the consistent `Sent via Sprezza Hub` footer pattern, and consume `src/lib/email/system.ts` when system emails are introduced.
</output>
