---
phase: 45.5-platform-rename
plan: 45.5-02
subsystem: email
tags: [rebrand, email-templates, snapshot-regen, platform-identifier, multi-tenant, forward-looking-stub]

requires:
  - phase: 45.5-platform-rename
    plan: 45.5-01
    provides: clean Linha grep across the codebase except in the two emailTemplate.ts files this plan owns
provides:
  - Send Update email footer renders tenant wordmark + "Sent via Sprezza Hub" attribution
  - Work Order email footer (HTML + plain text) renders tenant wordmark + "Sent via Sprezza Hub" attribution
  - src/lib/email/system.ts forward-looking stub for D-4 (system-email sender constants)
  - Vitest snapshot baselines for both email templates regenerated as the audit trail
  - Linha is dead-clean across the entire codebase (zero hits in src/ docs/ scripts/ excluding frozen specs)
affects: [46-email-template-migration]

tech-stack:
  added: []
  patterns:
    - "Email-footer attribution slot: small (~10px) low-contrast (#C5BDB4) centered 'Sent via Sprezza Hub' line at the very bottom — pattern Phase 46 react-email components inherit"
    - "Tenant-vs-platform sender split documented in code via src/lib/email/system.ts (D-4) — no callers today; activated when system emails are introduced"

key-files:
  created:
    - src/lib/email/system.ts
  modified:
    - src/lib/sendUpdate/emailTemplate.ts
    - src/lib/workOrder/emailTemplate.ts
    - src/lib/sendUpdate/__snapshots__/emailTemplate.test.ts.snap
    - src/lib/workOrder/__snapshots__/emailTemplate.test.ts.snap

key-decisions:
  - "10px gray (#C5BDB4) centered attribution row matches the existing low-contrast 'Darien, CT' line — the brand recedes visually exactly as D-2 prescribes."
  - "Plain-text MIME variant of the Work Order email gets the attribution as a separate trailing paragraph (blank-line separated) so it reads naturally in text-only clients."
  - "src/lib/email/system.ts ships with zero callers; activated by Phase 46+ when system emails (password reset, signup, billing) are introduced. No barrel index.ts."

patterns-established:
  - "Email footer attribution pattern: tenant wordmark line + tenant location line + (12px gap) + 10px low-contrast 'Sent via Sprezza Hub' centered."
  - "Snapshot regeneration commits use `test(phase-plan): regenerate <X> snapshots for <reason>` and document the dominant change in the body — the diff IS the audit trail per D-6."

requirements-completed: []

duration: ~4 min
completed: 2026-04-27
---

# Phase 45.5 Plan 45.5-02: Email Footer Co-Brand Rebrand + System-Email Stub Summary

**Five literal substitutions across two transactional email templates retire the `Linha Studio` co-brand line and add a small `Sent via Sprezza Hub` attribution at the very bottom; a new `src/lib/email/system.ts` forward-looking stub documents the tenant-vs-platform sender split (D-4); seven Vitest snapshots regenerate as the audit trail; Linha is now dead-clean across the entire codebase.**

## Performance

- **Duration:** ~4 min
- **Started:** 2026-04-27T12:06:18Z
- **Completed:** 2026-04-27T12:10:21Z
- **Tasks:** 5 (3 source edits + 1 stub creation + 1 snapshot regen + global verification)
- **Files modified:** 2 source files + 2 snapshot files + 1 new file

## Accomplishments

- Send Update email header: collapsed to tenant wordmark only (Linha Studio micro-line removed)
- Send Update email footer signature: trimmed `La Sprezzatura · Linha Studio` → `La Sprezzatura`
- Send Update email very-bottom attribution row: added `Sent via Sprezza Hub` (10px, #C5BDB4, centered, 0.06em letter-spacing)
- Work Order email header: collapsed to `LA SPREZZATURA` tenant wordmark only (Linha Studio micro-line removed)
- Work Order HTML footer signature: trimmed `La Sprezzatura · Linha Studio` → `La Sprezzatura`
- Work Order HTML footer: added `Sent via Sprezza Hub` 10px attribution row inside the existing footer cell
- Work Order plain-text MIME footer: dropped `Linha Studio\n` line; appended `\n\nSent via Sprezza Hub` at the very end
- New `src/lib/email/system.ts` stub: exports `SYSTEM_FROM_NAME = "Sprezza Hub"`, `SYSTEM_FROM_EMAIL = "noreply@lasprezz.com"`, and `formatSystemSender()` helper. Zero callers — forward-looking per D-4
- 7 Vitest snapshots regenerated (5 sendUpdate permutations + 2 workOrder baselines) as the audit trail per D-6
- 3 Playwright PNG baselines re-emitted (deterministic scaffold render — bytes unchanged, no commit needed)
- Vitest counts strictly improved: `60 failed | 1164 passed | 68 todo (1292)` vs Plan 45.5-01 post-edit `67 failed | 1157 passed`. Net delta: 7 fewer failures (the 7 regenerated email snapshot tests flipped FAIL → PASS)
- Linha sweep gate (excluding `docs/superpowers/specs/` and `__snapshots__/`): zero hits — Phase 45.5 Success Criterion #1 satisfied
- Tenant brand surfaces (portal, public marketing, tenants.json, package.json, siteSettings schema, sender domains): byte-identical, Phase 45.5 Success Criterion #5 satisfied

## Task Commits

1. **Task 1: Rebrand Send Update email footer** — `251672e` (feat)
2. **Task 2: Rebrand Work Order email footer (HTML + plain text)** — `4f741b2` (feat)
3. **Task 3: Add system-email sender constants stub** — `d4b4a2c` (chore)
4. **Task 4: Regenerate Vitest snapshots** — `0c112e6` (test)
5. **Task 5: Regenerate Playwright PNG baselines + run full verification** — verification only, no source change (PNGs were byte-identical after regen)

## Files Created/Modified

### Created

- `src/lib/email/system.ts` — Forward-looking system-email sender constants module (24 lines including JSDoc + module-level explainer comment). Exports `SYSTEM_FROM_NAME`, `SYSTEM_FROM_EMAIL`, `formatSystemSender()`. Zero callers in this phase; consumed by Phase 46+ when system emails (password reset, signup, billing) are introduced.

### Modified

- `src/lib/sendUpdate/emailTemplate.ts:331` — DELETED `<p>Linha Studio</p>` micro-line from header block; header now has tenant wordmark only (line 330)
- `src/lib/sendUpdate/emailTemplate.ts:349` — `La Sprezzatura · Linha Studio` → `La Sprezzatura` in footer signature
- `src/lib/sendUpdate/emailTemplate.ts:362` — added new `<p>Sent via Sprezza Hub</p>` 10px attribution row inside the existing very-bottom centered div
- `src/lib/workOrder/emailTemplate.ts:39` — DELETED `<div>Linha Studio</div>` from header block; header now has `LA SPREZZATURA` wordmark only
- `src/lib/workOrder/emailTemplate.ts:54` — `La Sprezzatura &middot; Linha Studio` → `La Sprezzatura` in HTML footer signature
- `src/lib/workOrder/emailTemplate.ts:55-56` — added new `<div>Sent via Sprezza Hub</div>` 10px centered attribution inside the same `<td>` after `Darien, CT`
- `src/lib/workOrder/emailTemplate.ts:73-74` — plain-text MIME: dropped `Linha Studio\n` line; appended blank line + `Sent via Sprezza Hub` at the very end
- `src/lib/sendUpdate/__snapshots__/emailTemplate.test.ts.snap` — 5 snapshots regenerated (30 line changes, 21 insertions / 21 deletions in `git diff --stat`)
- `src/lib/workOrder/__snapshots__/emailTemplate.test.ts.snap` — 2 snapshots regenerated

## Substitution Table (5 source-file swaps)

| File:line | Before | After |
|---|---|---|
| `src/lib/sendUpdate/emailTemplate.ts:331` | `<p style="..." text-transform: uppercase;">Linha Studio</p>` | (entire line deleted) |
| `src/lib/sendUpdate/emailTemplate.ts:349` | `<p style="...">La Sprezzatura · Linha Studio</p>` | `<p style="...">La Sprezzatura</p>` |
| `src/lib/sendUpdate/emailTemplate.ts:361` (after) | (single `<p>` for `La Sprezzatura · Darien, CT`) | + new `<p style="...10px;color:#C5BDB4;...">Sent via Sprezza Hub</p>` row |
| `src/lib/workOrder/emailTemplate.ts:39` | `<div style="...color:#9E8E80;...">Linha Studio</div>` | (entire line deleted) |
| `src/lib/workOrder/emailTemplate.ts:54` | `<div style="...">La Sprezzatura &middot; Linha Studio</div>` | `<div style="...">La Sprezzatura</div>` |
| `src/lib/workOrder/emailTemplate.ts:55` (after) | (existing `Darien, CT` div) | + new `<div style="font-size:10px;color:#C5BDB4;margin-top:12px;letter-spacing:0.06em;text-align:center;">Sent via Sprezza Hub</div>` |
| `src/lib/workOrder/emailTemplate.ts:73-74` (plain-text) | `La Sprezzatura\nLinha Studio\nDarien, CT` | `La Sprezzatura\nDarien, CT\n\nSent via Sprezza Hub` |

## Snapshot Diff Summary (D-6 audit trail)

`git diff --stat` over the regenerated `.snap` files:

```
src/lib/sendUpdate/__snapshots__/emailTemplate.test.ts.snap | 30 +++++++++++-----------
src/lib/workOrder/__snapshots__/emailTemplate.test.ts.snap  | 12 ++++-----
2 files changed, 21 insertions(+), 21 deletions(-)
```

The diff is dominated by:

1. `Linha Studio` micro-line removed from header (sendUpdate × 5, workOrder × 2 — total 7)
2. `· Linha Studio` removed from footer signature line (both templates)
3. `Sent via Sprezza Hub` 10px low-contrast attribution row added (sendUpdate × 5, workOrder × 2)
4. Incidental date drift: `April 26, 2026` → `April 27, 2026` on the date interpolation line in every permutation. This is pre-existing brittleness from Phase 39 / 45-05 — both templates use `new Date()` at render time (workOrder line 25, sendUpdate line 183), so any snapshot regen drifts to "today's date." Not a regression introduced by this plan; out of scope per scope-boundary deviation rule (could be fixed in a future test-hygiene plan by injecting a clock).

Idempotency verified: re-running `npx vitest run src/lib/sendUpdate src/lib/workOrder` without `-u` returns `Tests 23 passed (23)` with zero further snapshot changes.

## Test Suite vs Baseline

- **Pre-edit baseline** (`/tmp/45.5-test-baseline.txt`, captured Plan 45.5-01 Task 0): `Tests  68 failed | 1156 passed | 68 todo (1292)`
- **Post Plan 45.5-01** (per SUMMARY): `Tests  67 failed | 1157 passed | 68 todo (1292)` (admin-chrome audit flipped FAIL → PASS)
- **Post Plan 45.5-02** (this plan): `Tests  60 failed | 1164 passed | 68 todo (1292)` — 7 additional passes (regenerated email snapshot tests).
- **Net delta vs original baseline:** 8 fewer failures, 8 more passes — strict improvement, zero new failures introduced.

### Scoped suites (all PASS)

| Suite | Result |
|---|---|
| `npm run test:email` | `Tests  27 passed (27)` (sendUpdate × 14, workOrder × 9, scaffold × 4) — exit 0 |
| `npm run test:visual` | `3 passed (3.1s)` (Playwright scaffold spec at apple/gmail/owa widths) — exit 0 |
| `npx vitest run src/lib/sendUpdate src/lib/workOrder` | `Tests  23 passed (23)` — exit 0 |
| `npx vitest run src/lib/sendUpdate src/lib/workOrder src/lib/tenantAudit.test.ts` | `Tests  2 failed | 27 passed (29)` — see "Pre-existing tenantAudit failures" below |

### Pre-existing tenantAudit failures (out of scope)

The 2 failing tests in `src/lib/tenantAudit.test.ts` are the same ones documented in Plan 45.5-01's SUMMARY:

- × `should not contain hardcoded contact email in admin code` — flags `office@lasprezz.com` in `send.ts` and `GeneralSection.tsx` (3 hits)
- × `should not contain domain (lasprezz.com) in admin code` — flags 8 hits across admin code

These are pre-existing literals that belong to the sender-domain-decoupling concern. CONTEXT.md D-1 explicitly keeps `@lasprezz.com` sender domain unchanged in this rename phase. They were failing in the 68-failure baseline pre-Plan-01 and remain failing post-Plan-02 — no regression introduced. Cleanup is its own future workstream.

The relevant audit for this plan — admin-chrome `La Sprezzatura` — is **passing** (flipped green by Plan 45.5-01).

## Tenant Brand Surfaces — Byte-Identical Verification

`git diff --stat HEAD~4 -- <full tenant brand surface list>` returned empty output — zero changes outside the 4 source files in this plan's surface (2 email templates + 1 new email/system.ts + 2 snapshot files).

Specifically verified untouched (Success Criterion #5):

- `src/components/portal/`, `src/pages/portal/`, `src/pages/building/`, `src/pages/workorder/`
- `src/components/home/`, `src/components/layout/`, `src/components/seo/`
- `src/pages/{about,contact,index,privacy,process,services}.astro`, `src/pages/portfolio/`
- `src/config/tenants.json`, `package.json`, `src/sanity/schemas/siteSettings.ts`

## Linha Sweep Gate — Dead-Clean

```
grep -rn "Linha" src/ docs/ scripts/ --include="*.ts" --include="*.tsx" --include="*.astro" --include="*.json" --include="*.md" \
  | grep -v "docs/superpowers/specs/" | grep -v "__snapshots__"
```

**Returns 0 hits.** Phase 45.5 Success Criterion #1 satisfied. Linha is dead-clean across the entire live codebase. The only remaining `Linha` references are:

- `docs/superpowers/specs/2026-03-17-linha-multi-tenant-platform-design.md` — frozen historical artifact (D-1, anti-pattern #5)
- `docs/superpowers/specs/2026-03-17-ai-rendering-design.md` — frozen historical artifact
- `.planning/` memory files referencing the historical platform name — anti-pattern #5

## Decisions Made

- **Attribution color**: chose `#C5BDB4` (matches the existing `Darien, CT` location line) for visual consistency. Plan suggested this; adopted.
- **Plain-text MIME formatting**: blank-line separation before `Sent via Sprezza Hub` so it reads as a discrete trailing paragraph, not appended to the address line.
- **No barrel `index.ts` for `src/lib/email/`**: just the single `system.ts` file. Phase 46+ adds barrel only if it makes sense at that point.
- **Date drift is pre-existing**: not patched in this plan. Both email templates read `new Date()` at render time (Phase 39 brittleness), so the regenerated snaps captured "today's date" (April 27 vs the captured April 26). Documented as observation, not a fix target — that's a test-hygiene concern, separate from the rename.

## Deviations from Plan

### 1. Path typo in Task 5 verify command (documentation only)

The Task 5 `<automated>` block referenced `src/lib/tenant/tenantAudit.test.ts` (with `tenant/` subdirectory). The actual file lives at `src/lib/tenantAudit.test.ts` — same file Plan 45.5-01 used. Used the correct path; no behavioral difference.

**Treatment:** Documented as observation only — no plan amendment, no code change.

### 2. Pre-existing tenantAudit failures resurface in scoped run

When the scoped Vitest run includes `src/lib/tenantAudit.test.ts`, the suite reports 2 failures (hardcoded contact email + lasprezz.com domain audits). These are the same 2 pre-existing failures documented in Plan 45.5-01's SUMMARY — both unrelated to this plan's surface, both present in the 68-failure baseline, both concerning sender-domain decoupling that CONTEXT.md D-1 explicitly defers.

**Treatment:** Documented as observation only — the relevant `La Sprezzatura` admin-chrome audit (flipped green by Plan 01) remains green. The plan's intent is satisfied: Vitest baseline strictly improved (60 vs 68 failed); regenerated email snaps + tenantAudit-La-Sprezzatura-admin all green; no NEW failures introduced.

### 3. Date drift in regenerated snaps (incidental, pre-existing)

The regenerated snapshots show `April 26, 2026` → `April 27, 2026` drift on the date interpolation line. This is caused by `new Date()` calls at render time in both email templates (workOrder.ts:25, sendUpdate.ts:183) — pre-existing Phase 39 / 45-05 brittleness. Not introduced by this plan. Out of scope per scope-boundary deviation rule.

**Treatment:** Documented in commit body and snapshot diff section. A future test-hygiene plan can inject a clock to make snapshots fully deterministic across days.

---

**Total deviations:** 3 documentation-only observations; zero deviations required code changes beyond what the plan prescribed.
**Impact on plan:** None — all 4 source-file commits + 1 snap-regen commit landed atomically; snapshot diffs are the audit trail per D-6; tenant brand surfaces byte-identical; Linha dead-clean across the codebase.

## Issues Encountered

None.

## User Setup Required

None — no external service configuration required.

## Next Phase Readiness — Hand-Off Note for Phase 46

Phase 46 (Send Update + Work Order react-email migration) is now unblocked and inherits a clean substrate:

- **Footer pattern to mirror in react-email components**: tenant wordmark line + tenant location line + (12px gap) + `<Text>` 10px low-contrast (#C5BDB4) centered `Sent via Sprezza Hub` attribution. Both Send Update and Work Order use the identical pattern in this plan; new react-email components grep for "Sent via Sprezza Hub" and find the consistent treatment.
- **System-email constants ready**: when Phase 46+ introduces password-reset, signup-confirmation, or billing emails, import from `src/lib/email/system.ts` to get the `Sprezza Hub <noreply@lasprezz.com>` From-display per D-4. The tenant-vs-platform sender split is architecturally documented in code; no decision-making required at consumer time.
- **Snapshot infrastructure intact**: the 7 regenerated Vitest snaps + 3 Playwright PNG baselines are the regression detector for Phase 46's react-email rewrite. When Phase 46 swaps the legacy template strings for `<Html>...` JSX, the snaps will diff and the diff IS the audit trail showing what visually changed.

## Self-Check: PASSED

Files verified to exist post-edit:

- `src/lib/sendUpdate/emailTemplate.ts` (Linha=0, "Sent via Sprezza Hub"=1, "La Sprezzatura"≥2)
- `src/lib/workOrder/emailTemplate.ts` (Linha=0, "Sent via Sprezza Hub"=2 (HTML+text), LA SPREZZATURA=1)
- `src/lib/email/system.ts` (3 exports verified by grep)
- `src/lib/sendUpdate/__snapshots__/emailTemplate.test.ts.snap` (Linha=0, attribution=5)
- `src/lib/workOrder/__snapshots__/emailTemplate.test.ts.snap` (Linha=0, attribution=2)

Commits verified to exist in git log:

- `251672e` feat(45.5-02): rebrand Send Update email footer to Sprezza Hub attribution
- `4f741b2` feat(45.5-02): rebrand Work Order email footer to Sprezza Hub attribution
- `d4b4a2c` chore(45.5-02): add forward-looking system-email sender constants stub (D-4)
- `0c112e6` test(45.5-02): regenerate Vitest snapshots for Sprezza Hub footer

---
*Phase: 45.5-platform-rename*
*Completed: 2026-04-27*
