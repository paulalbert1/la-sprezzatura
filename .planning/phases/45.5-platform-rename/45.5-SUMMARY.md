---
phase: 45.5-platform-rename
type: summary
status: complete
created: 2026-04-27
plans: 2
plans_complete: 2
verification: PASS (8/8)
---

# Phase 45.5 Summary: Linha → Sprezza Hub Platform Rename

## Outcome

The SaaS platform identifier has been rebranded from "Linha" / "La Sprezzatura" (when labeling the *product*) to "Sprezza Hub" across admin/back-office UI chrome and email-template footers. Tenant brand surfaces (Liz's `LA SPREZZATURA` wordmark, public marketing site, tenant ID slug, package.json name, sender domain `lasprezz.com`) are byte-identical to pre-phase HEAD. Phase 46 (Send Update + Work Order template migration) is unblocked.

## What changed

### Plan 45.5-01 — Admin chrome sweep (Wave 1)

| File | Change |
|---|---|
| `src/components/admin/AdminNav.tsx` | Sidebar micro-line below tenant business name now reads "Sprezza Hub" |
| `src/pages/admin/login.astro` | Browser `<title>` and visible header read "Sprezza Hub" |
| `src/pages/admin/trades/index.astro` | Browser `<title>` reads "Sprezza Hub" |
| `src/pages/admin/trades/[contractorId]/index.astro` | Browser `<title>` reads "Sprezza Hub" |

Test baseline captured at `.planning/phases/45.5-platform-rename/.test-baseline.txt` (`68 failed | 1156 passed | 68 todo (1292)`). Post-edit count: `67 failed | 1157 passed | 68 todo (1292)` — admin-chrome `tenantAudit.test.ts` audit flipped FAIL → PASS.

Commits: `47bc111` (baseline), `5867d3f` (AdminNav + login), `0304588` (trades titles), `70f3ea6` (SUMMARY).

### Plan 45.5-02 — Email footer rebrand + system stub + snapshot regen (Wave 2)

| File | Change |
|---|---|
| `src/lib/sendUpdate/emailTemplate.ts` | "La Sprezzatura · Linha Studio" co-brand removed; tenant address row trimmed; new `Sent via Sprezza Hub` attribution row at footer bottom (10px, gray `#9A8F82`, centered) |
| `src/lib/workOrder/emailTemplate.ts` | HTML co-brand removed and replaced with `Sent via Sprezza Hub` attribution row; plain-text variant gains `Sent via Sprezza Hub` line, drops "Linha Studio" |
| `src/lib/email/system.ts` | **NEW** — forward-looking D-4 stub. Exports `SYSTEM_FROM_NAME`, `SYSTEM_FROM_EMAIL`, and `formatSystemSender()` for future system/account emails (password reset, signup confirmation, billing) |
| `src/lib/sendUpdate/__snapshots__/emailTemplate.test.ts.snap` | 5 snapshots regenerated |
| `src/lib/workOrder/__snapshots__/emailTemplate.test.ts.snap` | 2 snapshots regenerated |
| `tests/email-snapshots/scaffold.spec.ts-snapshots/` | Playwright PNG baselines regenerated |

Final test count: `60 failed | 1164 passed | 68 todo (1292)` — 8 strict improvement vs baseline; 7 email-snapshot tests flipped FAIL → PASS plus the admin-chrome audit. Zero new failures introduced.

Commits: `251672e` (Send Update footer), `4f741b2` (Work Order footer), `d4b4a2c` (system.ts stub), `0c112e6` (Vitest snapshots), `97fc748` (SUMMARY).

## Verification

`gsd-verifier` ran goal-backward checks against the live codebase (not just SUMMARY claims). All 8 success criteria PASS. Report: `45.5-VERIFICATION.md`.

| # | Criterion | Verdict |
|---|---|---|
| 1 | Linha sweep — `grep -rn "Linha" src/ docs/ scripts/` (excluding frozen specs + snapshots) returns zero hits | PASS |
| 2 | Admin chrome reads "Sprezza Hub" across all 4 named files | PASS |
| 3 | Email footers carry "Sent via Sprezza Hub" attribution, "Linha Studio" co-brand gone | PASS |
| 4 | Tenant brand surfaces byte-identical (`git diff --stat HEAD~9` over tenants.json, package.json, portal, marketing pages, sanity schemas) | PASS |
| 5 | `src/lib/email/system.ts` exists with the D-4 stub exports | PASS |
| 6 | Snapshots regenerated (not hand-edited); `npm run test:email` reports 27/27 | PASS |
| 7 | Test baseline maintained (60 failures ≤ 67 gate) | PASS |
| 8 | Out-of-scope items untouched (`LA SPREZZATURA` wordmark, sender domain, tenant slug, public marketing) | PASS |

## Out of scope (kept stable, verified)

- Liz's tenant-side email body wordmarks (`LA SPREZZATURA` all-caps tracked) — present at `src/lib/workOrder/emailTemplate.ts:38`
- Liz's tenant signature lines in Send Update body — present at `src/lib/sendUpdate/emailTemplate.ts:330,348,360`
- Tenant ID slug `lasprezzatura` in `src/config/tenants.json:3`
- `package.json` `name` field
- Public marketing site (Hero, About, Process, Services, Portfolio, SEO meta on `lasprezz.com`)
- Sender domain `liz@lasprezz.com` and DKIM/SPF/DMARC config
- Historical spec docs under `docs/superpowers/specs/`
- Per-tenant evolution of `src/lib/brand-tokens.ts` (deferred to v6.0)

## Deviations and notes

1. **`tenantAudit.test.ts` retains 2 pre-existing failures** (`hardcoded contact email` flagging `office@lasprezz.com`; `domain (lasprezz.com)` flagging 8 hits). Both target sender-domain decoupling concerns explicitly out of scope per CONTEXT.md D-1. Documented; no scope creep.
2. **Snapshot date drift Apr 26 → Apr 27** — pre-existing Phase 39/45-05 brittleness from `new Date()` in templates. Out of scope per the scope-boundary deviation rule.
3. **Plan 02 referenced wrong test path** (`src/lib/tenant/tenantAudit.test.ts`) — actual is `src/lib/tenantAudit.test.ts`. Documentation fix applied at use; no code change needed.
4. **3 stash entries** in `git stash list` from prior worktree-agent runs — pre-existing state, untouched.

## Forward links

- **Phase 46** (Send Update + Work Order template migration) — now unblocked. The new react-email components authored there will inherit the "Sent via Sprezza Hub" attribution pattern from this phase rather than carrying stale `Linha Studio` strings.
- **`src/lib/email/system.ts`** — stub waiting for the first system/account email (password reset, signup confirmation, billing) to wire it into the actual sender path. Imports the D-4 sender constants for `Sprezza Hub <noreply@lasprezz.com>`.
- **v6.0 multi-tenant work** — when other designers onboard, the platform-brand vs tenant-brand split established here is the architectural pattern: tenant brand dominates customer-facing comms, platform brand appears only in (a) email footer attribution, (b) account/system emails, (c) admin/back-office UI chrome.

## Artifacts

- `.planning/phases/45.5-platform-rename/45.5-CONTEXT.md` — phase decisions D-1 through D-5
- `.planning/phases/45.5-platform-rename/45.5-PATTERNS.md` — pattern-mapper output
- `.planning/phases/45.5-platform-rename/45.5-01-PLAN.md` / `45.5-01-SUMMARY.md`
- `.planning/phases/45.5-platform-rename/45.5-02-PLAN.md` / `45.5-02-SUMMARY.md`
- `.planning/phases/45.5-platform-rename/45.5-VERIFICATION.md`
- `.planning/phases/45.5-platform-rename/.test-baseline.txt`
