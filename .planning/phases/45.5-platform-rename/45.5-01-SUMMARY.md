---
phase: 45.5-platform-rename
plan: 45.5-01
subsystem: ui
tags: [rebrand, admin-chrome, platform-identifier, multi-tenant]

requires:
  - phase: 45-email-foundations
    provides: tenantAudit.test.ts admin-chrome regression sentinel
provides:
  - Admin chrome (sidebar, login, trades pages) renders "Sprezza Hub"
  - tenantAudit.test.ts admin-chrome `La Sprezzatura` audit flips from failing to passing
  - Plan 45.5-02 inherits a clean Linha grep except in the two emailTemplate.ts files it owns
affects: [45.5-02, 46-email-template-migration]

tech-stack:
  added: []
  patterns:
    - "Three-layer brand model (tenant brand / platform identifier / public marketing) — admin chrome is platform identifier surface"

key-files:
  created: []
  modified:
    - src/components/admin/AdminNav.tsx
    - src/pages/admin/login.astro
    - src/pages/admin/trades/index.astro
    - src/pages/admin/trades/[contractorId]/index.astro
    - .planning/phases/45.5-platform-rename/.test-baseline.txt

key-decisions:
  - "Login screen h1 collapsed from 'Studio Login' → 'Sign In' to match the new <title> 'Sign In | Sprezza Hub' (Plan recommended this for consistency; adopted)."
  - "Tenant-controlled {businessName} on AdminNav line 48 left untouched — that is tenant data, not platform chrome."

patterns-established:
  - "Platform identifier rename rule: when admin chrome reads '— La Sprezzatura Admin', replace with '— Sprezza Hub'."
  - "Tenant ID slug `lasprezzatura` and tenant content (siteTitle, businessName) are untouched in this and future rename phases."

requirements-completed: []

duration: ~7 min
completed: 2026-04-27
---

# Phase 45.5 Plan 45.5-01: Linha → Sprezza Hub Admin-Chrome Sweep Summary

**Five literal substitutions across four admin-chrome files swap the platform identifier `Linha Studio` / `La Sprezzatura Admin` for `Sprezza Hub`, flipping the `tenantAudit.test.ts` admin-chrome regression sentinel from failing to passing while leaving all tenant brand surfaces byte-identical.**

## Performance

- **Duration:** ~7 min
- **Started:** 2026-04-27T11:55:00Z (approx)
- **Completed:** 2026-04-27T12:02:57Z
- **Tasks:** 4 (Task 0 baseline + Tasks 1-2 edits + Task 3 verification)
- **Files modified:** 4 source files + 1 baseline tracking file

## Accomplishments

- Admin sidebar (every `/admin/*` page) now renders `Sprezza Hub` under the tenant business name
- Admin login screen `<title>` is `Sign In | Sprezza Hub`; visible header reads `Sprezza Hub`; h1 reads `Sign In` (collapsed from `Studio Login` for chrome/title consistency)
- Admin trades index browser tab title: `Trades — Sprezza Hub`
- Admin trades detail browser tab title: `New Trade — Sprezza Hub` / `{contractor} — {Contractor|Vendor} — Sprezza Hub`
- `tenantAudit.test.ts` admin-chrome `La Sprezzatura` audit: now PASSES (was failing pre-edit; this is the regression sentinel for this plan)
- Vitest counts strictly improved: 68 failed → 67 failed (1 fewer failure; net effect of admin-chrome audit flip)
- Linha sweep gate (excluding `docs/superpowers/specs/` + the two `emailTemplate.ts` files Plan 45.5-02 owns): zero hits

## Task Commits

1. **Task 0: Capture pre-edit Vitest baseline + confirm enumeration** — `47bc111` (chore)
2. **Task 1: Rename in AdminNav and admin login** — `5867d3f` (feat)
3. **Task 2: Rename admin trades browser titles** — `0304588` (feat)
4. **Task 3: Verify full sweep + tests vs baseline** — verification only, no commit

## Files Created/Modified

- `src/components/admin/AdminNav.tsx:55` — `Linha Studio` → `Sprezza Hub`
- `src/pages/admin/login.astro:24` — `<title>Studio Login | La Sprezzatura</title>` → `<title>Sign In | Sprezza Hub</title>`
- `src/pages/admin/login.astro:29` — visible header `La Sprezzatura` → `Sprezza Hub`
- `src/pages/admin/login.astro:32` — h1 `Studio Login` → `Sign In`
- `src/pages/admin/trades/index.astro:29` — `title="Trades — La Sprezzatura Admin"` → `title="Trades — Sprezza Hub"`
- `src/pages/admin/trades/[contractorId]/index.astro:51` — `"New Trade — La Sprezzatura Admin"` → `"New Trade — Sprezza Hub"`
- `src/pages/admin/trades/[contractorId]/index.astro:52` — `${pageTitle} — ${relLabel ?? "Trade"} — La Sprezzatura Admin` → `${pageTitle} — ${relLabel ?? "Trade"} — Sprezza Hub`
- `.planning/phases/45.5-platform-rename/.test-baseline.txt` — pre-edit Vitest baseline tracking file (`Tests  68 failed | 1156 passed | 68 todo (1292)`)

## Substitution Table (5 literal swaps)

| File:line | Before | After |
|---|---|---|
| `src/components/admin/AdminNav.tsx:55` | `Linha Studio` | `Sprezza Hub` |
| `src/pages/admin/login.astro:24` | `<title>Studio Login \| La Sprezzatura</title>` | `<title>Sign In \| Sprezza Hub</title>` |
| `src/pages/admin/login.astro:29` (+ :32 h1) | header `La Sprezzatura` / h1 `Studio Login` | header `Sprezza Hub` / h1 `Sign In` |
| `src/pages/admin/trades/index.astro:29` | `title="Trades — La Sprezzatura Admin"` | `title="Trades — Sprezza Hub"` |
| `src/pages/admin/trades/[contractorId]/index.astro:51-52` | `"New Trade — La Sprezzatura Admin"` / `${pageTitle} — ${relLabel ?? "Trade"} — La Sprezzatura Admin` | `"New Trade — Sprezza Hub"` / `${pageTitle} — ${relLabel ?? "Trade"} — Sprezza Hub` |

## Pre-pass Enumeration Confirmation

Task 0's grep `grep -rn "La Sprezzatura" src/components/admin src/pages/admin src/layouts` returned exactly the 7 hits the planner enumerated (5 platform-chrome → renamed, 2 tenant-content in `SettingsPage.test.tsx` → left alone). No new hits surfaced; no Task 1/2 action-block extension was needed.

## Test Suite vs Baseline

- **Pre-edit baseline** (`/tmp/45.5-test-baseline.txt` and `.planning/phases/45.5-platform-rename/.test-baseline.txt`): `Tests  68 failed | 1156 passed | 68 todo (1292)`
- **Post-edit result** (`/tmp/45.5-test-after.txt`): `Tests  67 failed | 1157 passed | 68 todo (1292)`
- **Delta:** 1 fewer failure. The single test that flipped from FAIL → PASS is the `tenantAudit.test.ts` admin-chrome `La Sprezzatura` audit — this is exactly the regression sentinel the plan named.

## tenantAudit.test.ts Status (Regression Sentinel)

`npx vitest run src/lib/tenantAudit.test.ts` reports **6 tests, 4 passed / 2 failed**. The 2 passing audits relevant to this plan's surface:

- ✓ `should not contain business name (La Sprezzatura) in admin code` — **PASSES** (was failing pre-edit; this is the plan's regression sentinel — flipped green by this plan)
- ✓ `should not contain hardcoded sender email in admin code` — passes (no admin code change here)

The 2 failing audits are pre-existing and out of scope for this plan:

- × `should not contain hardcoded contact email in admin code` — flags `office@lasprezz.com` literals in `src/pages/api/admin/work-orders/[id]/send.ts` and `src/components/admin/settings/GeneralSection.tsx`
- × `should not contain domain (lasprezz.com) in admin code` — flags 8 hits across `send.ts`, `SendUpdateModal.tsx`, `GeneralSection.tsx`

These are pre-existing failures captured in the 68-failure baseline. They were failing before this plan started and remain failing after — no regression introduced. They concern hardcoded sender domain literals (per CONTEXT.md D-1, sender domain `@lasprezz.com` is decoupled from app brand and stays unchanged in this rename phase). Cleanup of those literals is its own future workstream.

## Tenant Brand Surfaces — Byte-Identical Verification

`git diff --stat` against the full tenant-brand surface list (portal layout, public marketing pages, tenants.json, package.json, sender domains, siteSettings schema) returned empty output — zero changes outside the 4 admin-chrome files in this plan's surface.

Specifically verified untouched:
- `src/components/portal/`, `src/pages/portal/`, `src/pages/building/`, `src/pages/workorder/`
- `src/components/home/`, `src/components/layout/`, `src/components/seo/`
- `src/pages/{about,contact,index,privacy,process,services}.astro`, `src/pages/portfolio/`
- `src/config/tenants.json`, `package.json`, `src/sanity/schemas/siteSettings.ts`

## Linha Handoff to Plan 45.5-02

After this plan, the unfiltered Linha grep over `src/ docs/ scripts/` (excluding `docs/superpowers/specs/`) returns exactly the 5 expected hits in the two email template files:

- `src/lib/sendUpdate/emailTemplate.ts:331` — `Linha Studio` (uppercase footer micro-line)
- `src/lib/sendUpdate/emailTemplate.ts:349` — `La Sprezzatura · Linha Studio` co-brand
- `src/lib/workOrder/emailTemplate.ts:39` — header `Linha Studio` micro-mark
- `src/lib/workOrder/emailTemplate.ts:54` — `La Sprezzatura · Linha Studio` co-brand
- `src/lib/workOrder/emailTemplate.ts:74` — plain-text `Linha Studio`

These five hits are **Plan 45.5-02's territory** and will be replaced with the tenant wordmark + `Sent via Sprezza Hub` attribution per CONTEXT.md D-2.

## Decisions Made

- **Login h1 wording**: collapsed `Studio Login` → `Sign In` to align with the new `<title>` (the plan's recommended path; no separate decision required from the user).
- **AdminNav line 48 untouched**: `{businessName}` is tenant-controlled `siteSettings.siteTitle`, not platform chrome — left alone per D-5.
- **No proactive cleanup of unrelated tenantAudit failures**: the hardcoded contact email + domain audits flag pre-existing literals that belong to a separate concern (sender domain decoupling). Out of scope for this plan; logged as observation only.

## Deviations from Plan

### Plan-prescribed automated verify command interpretation

The Task 3 `<automated>` block ends by running `npx vitest run src/lib/tenantAudit.test.ts --reporter=basic`. That command exits non-zero because two pre-existing audits (hardcoded contact email + lasprezz.com domain) fail — both unrelated to this plan's surface and present in the 68-failure baseline.

The plan's `<done>` and `<success_criteria>` resolve this nuance: "tenantAudit.test.ts specifically green" refers to the **admin-chrome** regression sentinel (the `La Sprezzatura` audit), which this plan flipped from FAIL to PASS. PATTERNS.md line 125 confirms: "After this phase, admin source has Sprezza Hub, so the audit keeps passing without modification" — singular, specifically the La Sprezzatura admin-chrome audit.

**Treatment:** Documented as observation only — no code change. The plan's intent is satisfied (admin-chrome audit green; baseline strictly improved by 1; no new failures). The two pre-existing audit failures remain for a future cleanup phase.

### Recovery from accidental `git stash pop`

While running an experimental `git stash && ... && git stash pop` to confirm the tenantAudit.test.ts pre-edit status, `git stash` reported "No local changes to save" but `git stash pop` then popped a pre-existing stash (`stash@{0}: On worktree-agent-a0a4b87f: pre-reset snapshot`) into the working tree, generating heavy merge conflicts.

**Fix:** Used `git read-tree --reset -u HEAD` to restore the working tree to HEAD (which still contained all 3 of this plan's commits intact). Then re-stashed the two untracked residue items (`src/sanity/components/`, `vercel.json`) into a new stash entry (`stash@{0}: On main: 45.5-01-executor: residue from accidental stash pop`) so they remain recoverable for whoever owns the original worktree work.

**Outcome:** zero impact on this plan's commits or files. HEAD-state and post-edit grep/test results all confirmed correct after recovery. Documented per the destructive_git_prohibition guidance — the residue is preserved in stash, not deleted.

---

**Total deviations:** 1 documentation-interpretation observation + 1 in-session recovery (no code change in either case)
**Impact on plan:** None — all four admin-chrome edits committed atomically and verified; tenant-brand surfaces byte-identical; baseline strictly improved.

## Issues Encountered

- During verification, an experimental stash/pop sequence accidentally surfaced an old stash; recovered cleanly via `git read-tree --reset -u HEAD` (see deviation above).

## User Setup Required

None — no external service configuration required.

## Next Phase Readiness

- Plan 45.5-02 (`emailTemplate.ts` footer co-brand + snapshot regen) is unblocked. All admin-chrome `Linha Studio` and `La Sprezzatura Admin` references are gone; the only remaining `Linha` hits are exactly the 5 in the two `emailTemplate.ts` files Plan 45.5-02 owns.
- Phase 46 (Send Update + Work Order react-email migration) can now author new components without inheriting stale `Linha Studio` / `La Sprezzatura · Linha Studio` co-brand strings — the consistent platform identifier in the codebase is now `Sprezza Hub`.

## Self-Check: PASSED

Files verified to exist post-edit:
- src/components/admin/AdminNav.tsx (line 55: `Sprezza Hub`)
- src/pages/admin/login.astro (line 24: `Sign In | Sprezza Hub`; line 29: `Sprezza Hub`)
- src/pages/admin/trades/index.astro (line 29: `Trades — Sprezza Hub`)
- src/pages/admin/trades/[contractorId]/index.astro (lines 51-52: `Sprezza Hub`)
- .planning/phases/45.5-platform-rename/.test-baseline.txt (`68 failed | 1156 passed | 68 todo`)

Commits verified to exist in git log:
- 47bc111 chore(45.5-01): capture pre-edit Vitest baseline
- 5867d3f feat(45.5-01): rename platform identifier to Sprezza Hub in AdminNav and admin login
- 0304588 feat(45.5-01): rename admin trades browser titles to Sprezza Hub

---
*Phase: 45.5-platform-rename*
*Completed: 2026-04-27*
