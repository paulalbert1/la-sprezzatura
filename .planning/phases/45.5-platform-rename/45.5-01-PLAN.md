---
phase: 45.5-platform-rename
plan: 45.5-01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/admin/AdminNav.tsx
  - src/pages/admin/login.astro
  - src/pages/admin/trades/index.astro
  - src/pages/admin/trades/[contractorId]/index.astro
autonomous: true
requirements: []

must_haves:
  truths:
    - "Admin sidebar (every /admin/* page) renders 'Sprezza Hub' under the tenant business name."
    - "Admin login screen <title> and visible header read 'Sprezza Hub' (not 'La Sprezzatura' platform label)."
    - "Admin trades index and detail browser tab titles read 'Sprezza Hub' (not 'La Sprezzatura Admin')."
    - "grep -rn 'Linha' src/ docs/ scripts/ excluding docs/superpowers/specs and the two emailTemplate.ts files (Plan 45.5-02 territory) returns zero hits."
    - "Tenant brand surfaces are byte-identical: portal layout, public marketing pages, tenants.json, package.json, sender domains untouched."
    - "Vitest run after edits shows no NEW failures vs the pre-edit baseline captured in Task 0 (Phase 45-01 carried ~65 pre-existing failures that are out of scope)."
  artifacts:
    - path: src/components/admin/AdminNav.tsx
      provides: "Admin nav brand micro-line"
      contains: "Sprezza Hub"
    - path: src/pages/admin/login.astro
      provides: "Admin login chrome"
      contains: "Sprezza Hub"
    - path: src/pages/admin/trades/index.astro
      provides: "Admin trades index browser title"
      contains: "Sprezza Hub"
    - path: src/pages/admin/trades/[contractorId]/index.astro
      provides: "Admin trades detail browser title"
      contains: "Sprezza Hub"
  key_links:
    - from: src/components/admin/AdminNav.tsx
      to: every /admin/* page
      via: AdminLayout sidebar render
      pattern: "Sprezza Hub"
---

<objective>
Sweep the platform identifier `Linha` and `La Sprezzatura` (where it labels the *product*, not the *tenant*) out of admin/back-office UI chrome. Replace with `Sprezza Hub` (Title Case per D-3).

Purpose: Phase 46 starts the email template react-email migration. Renaming now prevents new components inheriting stale `Linha Studio` / `La Sprezzatura · Linha Studio` co-brand strings (Success Criteria #1-2).

Output: Four admin chrome files updated. Zero `Linha` references remain in admin/source surfaces (the two emailTemplate.ts files are Plan 45.5-02's territory). Tenant brand surfaces untouched.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/45.5-platform-rename/45.5-CONTEXT.md
@.planning/phases/45.5-platform-rename/45.5-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 0: Capture pre-edit test baseline and confirm admin-chrome enumeration</name>
  <files>(setup only — no source edits)</files>
  <action>
This phase inherits ~65 pre-existing test failures from Phase 45-01 (deferred-items log). The downstream test gates compare against this baseline rather than asserting absolute zero, so we capture it once here and reuse from both plans.

1. **Capture pre-edit Vitest baseline** to `/tmp/45.5-test-baseline.txt`:
   ```
   cd /Users/paulalbert/Dropbox/GitHub/la-sprezzatura
   npx vitest run --reporter=basic 2>&1 | grep -E "^[[:space:]]*Tests:" | tail -1 > /tmp/45.5-test-baseline.txt || echo "Tests: 0 failed" > /tmp/45.5-test-baseline.txt
   echo "Captured pre-edit test baseline:"
   cat /tmp/45.5-test-baseline.txt
   ```
   Persist the file at `/tmp/45.5-test-baseline.txt`. Plan 45.5-02 reads the same file to gate its post-edit comparison.

2. **Confirm admin-chrome pre-pass enumeration** (deterministic — already run during planning).
   The planner ran `grep -rn "La Sprezzatura" src/components/admin src/pages/admin src/layouts`. The complete result set, with classification:

   | File:line | String | Classification | Action |
   |---|---|---|---|
   | `src/pages/admin/login.astro:24` | `<title>Studio Login \| La Sprezzatura</title>` | Platform chrome | Rename (Task 1) |
   | `src/pages/admin/login.astro:29` | `La Sprezzatura` (h1 visible header) | Platform chrome | Rename (Task 1) |
   | `src/pages/admin/trades/index.astro:29` | `title="Trades — La Sprezzatura Admin"` | Platform chrome | Rename (Task 2) |
   | `src/pages/admin/trades/[contractorId]/index.astro:51` | `"New Trade — La Sprezzatura Admin"` | Platform chrome | Rename (Task 2) |
   | `src/pages/admin/trades/[contractorId]/index.astro:52` | `${pageTitle} — ${relLabel ?? "Trade"} — La Sprezzatura Admin` | Platform chrome | Rename (Task 2) |
   | `src/components/admin/settings/SettingsPage.test.tsx:24` | `siteTitle: "La Sprezzatura"` | **Tenant content** (test fixture for Liz's biz `siteSettings.siteTitle`) | **Leave alone** |
   | `src/components/admin/settings/SettingsPage.test.tsx:122` | `target: { value: "La Sprezzatura Studios" }` | **Tenant content** (typed-input test value) | **Leave alone** |

   Re-run the same grep to confirm no new hits since planning:
   ```
   grep -rn "La Sprezzatura" src/components/admin src/pages/admin src/layouts
   ```
   The output MUST match the table above exactly (5 platform-chrome hits + 2 tenant-content hits = 7 total). If any additional hit appears, STOP and classify it before proceeding to Task 1 — extend the action block of Task 1 or Task 2 accordingly.

3. **Confirm AdminNav platform label** (separate file, separate pattern):
   ```
   grep -n "Linha\|Sprezza" src/components/admin/AdminNav.tsx
   ```
   Expected: line 55 contains `Linha Studio`. (PATTERNS.md catalogued this; reconfirm before Task 1 edits it.)
  </action>
  <verify>
    <automated>cd /Users/paulalbert/Dropbox/GitHub/la-sprezzatura && test -s /tmp/45.5-test-baseline.txt && cat /tmp/45.5-test-baseline.txt && test "$(grep -rn 'La Sprezzatura' src/components/admin src/pages/admin src/layouts | wc -l | tr -d ' ')" = "7"</automated>
    - `/tmp/45.5-test-baseline.txt` exists and is non-empty
    - Pre-pass admin-chrome grep returns exactly 7 hits matching the table above
    - AdminNav.tsx line 55 confirmed to contain `Linha Studio`
  </verify>
  <done>Baseline captured to `/tmp/45.5-test-baseline.txt`. Admin-chrome enumeration confirmed deterministic (5 to rename, 2 tenant-content to leave). Tasks 1-2 may proceed.</done>
</task>

<task type="auto">
  <name>Task 1: Rename Linha → Sprezza Hub in AdminNav and admin login</name>
  <files>src/components/admin/AdminNav.tsx, src/pages/admin/login.astro</files>
  <read_first>Read both files in full before editing. Confirm line numbers match the Task 0 enumeration (AdminNav line 55; login.astro lines 24, 29).</read_first>
  <action>
Per D-3 (Title Case `Sprezza Hub`) and D-5 (admin chrome is platform surface):

1. `src/components/admin/AdminNav.tsx` line 55: replace the literal `Linha Studio` with `Sprezza Hub`. Do NOT touch `{businessName}` on line 48 (that is tenant-controlled `siteSettings.siteTitle`, not platform chrome).

2. `src/pages/admin/login.astro`:
   - Line 24 `<title>Studio Login | La Sprezzatura</title>` → `<title>Sign In | Sprezza Hub</title>`
   - Line 29 visible header `La Sprezzatura` → `Sprezza Hub`
   - Line 32 `Studio Login` → keep as `Sign In` if you collapsed the title above, OR keep `Studio Login` as the visible h1 if you prefer the existing copy. Recommend: change h1 to `Sign In` for consistency (the title bar should match the screen heading).

Do NOT touch `AdminLoginForm.tsx`, body classes, or any styles. String-only edits.
  </action>
  <verify>
    <automated>grep -n "Linha\|La Sprezzatura" src/components/admin/AdminNav.tsx src/pages/admin/login.astro | grep -v '^#' | wc -l | awk '{ if ($1 == 0) print "PASS"; else { print "FAIL: " $1 " stale refs"; exit 1 } }'</automated>
    - `grep -n "Sprezza Hub" src/components/admin/AdminNav.tsx` returns line 55
    - `grep -n "Sprezza Hub" src/pages/admin/login.astro` returns at least 2 hits (title + header)
    - `grep -n "Linha\|La Sprezzatura" src/components/admin/AdminNav.tsx src/pages/admin/login.astro` returns zero hits
  </verify>
  <done>AdminNav and admin login screen render `Sprezza Hub` only. No `Linha` or `La Sprezzatura` platform labels remain in either file.</done>
</task>

<task type="auto">
  <name>Task 2: Rename admin trades page browser titles</name>
  <files>src/pages/admin/trades/index.astro, src/pages/admin/trades/[contractorId]/index.astro</files>
  <read_first>Read both files. Confirm: `index.astro` line 29 has `title="Trades — La Sprezzatura Admin"`; `[contractorId]/index.astro` lines 51-52 have the conditional `New Trade — La Sprezzatura Admin` / interpolated variant. These match the Task 0 enumeration table.</read_first>
  <action>
1. `src/pages/admin/trades/index.astro` line 29: change `title="Trades — La Sprezzatura Admin"` to `title="Trades — Sprezza Hub"`. Leave `pageTitle="Trades"` and `breadcrumbs` untouched.

2. `src/pages/admin/trades/[contractorId]/index.astro` lines 51-52: change both branches:
   - `"New Trade — La Sprezzatura Admin"` → `"New Trade — Sprezza Hub"`
   - `${pageTitle} — ${relLabel ?? "Trade"} — La Sprezzatura Admin` → `${pageTitle} — ${relLabel ?? "Trade"} — Sprezza Hub`

Do NOT touch any tenant-content fields (relationship, contractorData, checklists, meta segments). Do NOT change `pageTitle` itself — that is tenant-content (the contractor's display name).
  </action>
  <verify>
    <automated>grep -nE "La Sprezzatura Admin|Linha" src/pages/admin/trades/index.astro src/pages/admin/trades/\[contractorId\]/index.astro | grep -v '^#' | wc -l | awk '{ if ($1 == 0) print "PASS"; else { print "FAIL"; exit 1 } }'</automated>
    - `grep -n "Sprezza Hub" src/pages/admin/trades/index.astro` returns line 29
    - `grep -n "Sprezza Hub" src/pages/admin/trades/[contractorId]/index.astro` returns lines 51 and 52
    - `grep -nE "La Sprezzatura Admin|Linha" src/pages/admin/trades/` returns zero hits
  </verify>
  <done>Both admin trades pages render `— Sprezza Hub` in the browser tab title. Tenant data references (contractor names, relationships) untouched.</done>
</task>

<task type="auto">
  <name>Task 3: Verify the full Linha sweep and run tests against baseline</name>
  <files>(verification only)</files>
  <action>
Run the gating Linha grep that defines Success Criterion #1, plus tenant-brand byte-identical checks, plus the test suite compared against the pre-edit baseline from Task 0.

1. **Gating Linha grep** (the one that determines pass/fail) — across all source/docs/scripts, excluding frozen historical specs AND the two emailTemplate.ts files that Plan 45.5-02 owns:
   ```
   grep -rn "Linha" src/ docs/ scripts/ --include="*.ts" --include="*.tsx" --include="*.astro" --include="*.json" --include="*.md" 2>/dev/null \
     | grep -v "docs/superpowers/specs/" \
     | grep -v "src/lib/sendUpdate/emailTemplate.ts" \
     | grep -v "src/lib/workOrder/emailTemplate.ts"
   ```
   This MUST return zero lines. **This is the gate for THIS plan.**

   For information only — the unfiltered grep:
   ```
   grep -rn "Linha" src/ docs/ scripts/ --include="*.ts" --include="*.tsx" --include="*.astro" --include="*.json" --include="*.md" 2>/dev/null | grep -v "docs/superpowers/specs/"
   ```
   will still show the 5 expected `Linha` hits in the two emailTemplate.ts files. Plan 45.5-02 closes those in the next wave; they are NOT this plan's concern.

2. **Tenant brand untouched** — confirm public marketing, portal, tenant config, sender domains are byte-identical:
   ```
   git diff --stat src/components/home src/components/layout src/components/seo src/pages/about.astro src/pages/contact.astro src/pages/index.astro src/pages/portfolio src/pages/privacy.astro src/pages/process.astro src/pages/services.astro src/components/portal src/pages/portal src/pages/building src/pages/workorder src/config/tenants.json package.json src/sanity/schemas/siteSettings.ts
   ```
   MUST output nothing (zero lines = zero changes outside admin chrome).

3. **Run the test suite and compare against the Task 0 baseline:**
   ```
   AFTER=$(npx vitest run --reporter=basic 2>&1 | grep -E "^[[:space:]]*Tests:" | tail -1 || echo "Tests: 0 failed")
   BEFORE=$(cat /tmp/45.5-test-baseline.txt)
   echo "Pre-edit baseline: $BEFORE"
   echo "Post-edit result:  $AFTER"
   if [ "$BEFORE" = "$AFTER" ]; then
     echo "PASS: no new test failures introduced"
   else
     echo "WARN: test count differs from baseline. Visually inspect."
     echo "Phase 45-01 carried ~65 pre-existing failures that are out of scope for 45.5."
     echo "The gate is 'no NEW failures introduced,' not 'absolute exit 0.'"
     diff <(echo "$BEFORE") <(echo "$AFTER") || true
   fi
   ```
   Additionally, the `tenantAudit.test.ts` admin-chrome audit MUST pass (admin source no longer matches its forbidden `La Sprezzatura` pattern). If that specific test regresses, STOP — that IS a regression caused by this plan. No snapshot tests are touched by this plan.
  </action>
  <verify>
    <automated>set -e; cd /Users/paulalbert/Dropbox/GitHub/la-sprezzatura && test "$(grep -rn 'Linha' src/ docs/ scripts/ --include='*.ts' --include='*.tsx' --include='*.astro' --include='*.json' --include='*.md' 2>/dev/null | grep -v 'docs/superpowers/specs/' | grep -v 'src/lib/sendUpdate/emailTemplate.ts' | grep -v 'src/lib/workOrder/emailTemplate.ts' | wc -l | tr -d ' ')" = "0" && test -z "$(git diff --stat src/components/home src/components/layout src/components/seo src/pages/about.astro src/pages/contact.astro src/pages/index.astro src/pages/portfolio src/pages/privacy.astro src/pages/process.astro src/pages/services.astro src/components/portal src/pages/portal src/pages/building src/pages/workorder src/config/tenants.json package.json src/sanity/schemas/siteSettings.ts)" && AFTER=$(npx vitest run --reporter=basic 2>&1 | grep -E "^[[:space:]]*Tests:" | tail -1 || echo "Tests: 0 failed") && BEFORE=$(cat /tmp/45.5-test-baseline.txt) && echo "BEFORE: $BEFORE" && echo "AFTER: $AFTER" && npx vitest run src/lib/tenant/tenantAudit.test.ts --reporter=basic</automated>
    - The Linha grep (excluding email templates and frozen specs) returns zero hits
    - `git diff --stat` over tenant-brand surfaces returns empty
    - Post-edit `Tests:` line printed alongside `BEFORE` baseline; executor inspects diff if any
    - `tenantAudit.test.ts` passes (regression sentinel for this plan)
  </verify>
  <done>
    - Admin chrome renders `Sprezza Hub` everywhere it formerly said `Linha Studio` or `La Sprezzatura Admin`
    - Linha sweep is clean except for the two emailTemplate.ts files (Plan 45.5-02's responsibility)
    - Tenant brand surfaces untouched
    - Vitest result: ≤ baseline failures from `/tmp/45.5-test-baseline.txt` (no NEW failures introduced). Pre-existing failures from Phase 45-01 are out of scope. `tenantAudit.test.ts` specifically green.
  </done>
</task>

</tasks>

<verification>
After all tasks complete:

1. `grep -rn "Linha" src/ docs/ scripts/ --include="*.ts" --include="*.tsx" --include="*.astro" --include="*.json" --include="*.md" | grep -v "docs/superpowers/specs/" | grep -v "src/lib/sendUpdate/emailTemplate.ts" | grep -v "src/lib/workOrder/emailTemplate.ts"` → zero hits
2. `grep -rn "La Sprezzatura Admin\|La Sprezzatura$" src/components/admin src/pages/admin src/layouts` → zero hits (post-hoc regression check; Task 0 enumerated all hits and Tasks 1-2 fixed them deterministically)
3. Vitest post-edit `Tests:` line ≤ baseline captured in `/tmp/45.5-test-baseline.txt` — no NEW failures vs Phase 45-01's pre-existing ~65
4. `tenantAudit.test.ts` exits 0 (regression sentinel: admin-chrome audit must remain green)
5. `git diff --stat src/components/portal src/pages/portal src/pages/building src/pages/workorder src/components/home src/components/layout src/components/seo src/pages/{about,contact,index,privacy,process,services}.astro src/pages/portfolio src/config/tenants.json package.json` → empty (tenant brand untouched)
</verification>

<success_criteria>
- 4 files modified; nothing else
- AdminNav, admin login, admin trades index + detail render `Sprezza Hub`
- Linha references in admin chrome: zero
- Tenant brand surfaces (portal, marketing, tenant ID, sender domains, package.json): byte-identical
- Acceptance: pre-edit baseline = `/tmp/45.5-test-baseline.txt` (Phase 45-01 carried ~65 pre-existing failures). Post-edit result MUST be ≤ baseline failures (no new failures introduced). `tenantAudit.test.ts` MUST be green specifically.
- No commits with `--no-verify`
</success_criteria>

<output>
After completion, write `.planning/phases/45.5-platform-rename/45.5-01-SUMMARY.md` covering:
- The 5 literal substitutions made (file:line → before/after)
- Pre-pass grep confirmation that no other admin chrome titles needed touching (Task 0 enumeration table holds)
- Pre-edit baseline value from `/tmp/45.5-test-baseline.txt` and post-edit `Tests:` line — confirm no new failures
- `tenantAudit.test.ts` result (regression sentinel)
- Confirmation that Linha references remain only in the two emailTemplate.ts files (handed off to Plan 45.5-02)
</output>
