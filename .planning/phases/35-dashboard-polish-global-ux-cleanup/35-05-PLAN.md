---
phase: 35-dashboard-polish-global-ux-cleanup
plan: 05
type: execute
wave: 2
depends_on: [01]
files_modified:
  - src/components/admin/DashboardTasksCard.tsx
  - src/components/admin/DashboardTasksCard.test.tsx
  - src/components/admin/ProjectTasks.tsx
  - src/components/admin/ProjectTasks.test.tsx
  - src/components/admin/ClientActionItemsList.tsx
  - src/components/admin/ClientActionItemsList.test.tsx
autonomous: true
requirements:
  - DASH-20
  - DASH-21
  - DASH-22
must_haves:
  truths:
    - "DashboardTasksCard header renders a visible `+ Add task` outline button, right-aligned"
    - "ProjectTasks header renders a visible `+ Add task` outline button, right-aligned"
    - "ClientActionItemsList (the client Tasks card) header renders a visible `+ Add task` outline button, right-aligned"
    - "All three cards hide completed tasks by default"
    - "When completed tasks exist, each card renders a `Show completed (N)` inline link at the card bottom that expands to show completed tasks and swaps copy to `Hide completed`"
    - "When NO completed tasks exist, the reveal link is absent"
    - "Reload of the page resets the reveal state to hidden (useState, no persistence)"
    - "The old footer `+ Add task` band on DashboardTasksCard (currently in dashboard.astro lines 254-297 area OR inside the card component itself) is REMOVED; the header button replaces it"
  artifacts:
    - path: "src/components/admin/DashboardTasksCard.tsx"
      provides: "Tasks card with header Add-task button and hide-completed toggle"
    - path: "src/components/admin/ProjectTasks.tsx"
      provides: "Project-detail Tasks card with header Add-task button and hide-completed toggle"
    - path: "src/components/admin/ClientActionItemsList.tsx"
      provides: "Client Tasks card with header Add-task button and hide-completed toggle"
  key_links:
    - from: "All three Tasks cards"
      to: "setShowCompleted(true)"
      via: "inline button at card bottom"
      pattern: "setShowCompleted"
    - from: "All three Tasks cards"
      to: "displayTasks / sortedTasks"
      via: "filter applies `!task.completed || showCompleted`"
      pattern: "\\!.*\\.completed.*showCompleted\\|showCompleted.*\\|.*\\!.*completed"
---

<objective>
Apply the same three-requirement pattern to all three Tasks cards (dashboard + project detail + client):

1. **DASH-20** — visible `+ Add task` primary CTA in the card header (right-aligned), replacing any footer-band Add-task UI that may currently exist.
2. **DASH-21 / DASH-22** — hide completed tasks by default; expose a bottom-centered `Show completed (N)` reveal link; reload resets state to hidden.

Per D-16 and D-17: header-right placement, useState only (no sessionStorage, no URL param). Purely display-layer; no schema or API changes.

Depends only on Plan 01 (absolute-date format already applied to task date chips). Parallel-safe with Plans 02 and 03 (no file overlap).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/35-dashboard-polish-global-ux-cleanup/35-CONTEXT.md
@.planning/phases/35-dashboard-polish-global-ux-cleanup/35-UI-SPEC.md
@.planning/phases/35-01-SUMMARY.md

<interfaces>
From src/components/admin/DashboardTasksCard.tsx (current state):
- `localTasks: TaskItem[]` state at line 27 — includes completed + incomplete
- `filteredTasks` line 44-46 — filters by `filterProject`
- `sortedTasks` line 49-58 — sorts completed to bottom (keep this; we'll filter them OUT by default)
- `displayTasks = sortedTasks.slice(0, 8)` line 64 — note the cap
- Existing Add task input/form lives somewhere in the component — confirm by full-file read. UI-SPEC says the current footer band is in "dashboard.astro lines 254-297" but that's outdated — the component owns its own rendering. Grep `handleAddTask` / `addTask` / `<form` / `Add task` in DashboardTasksCard.tsx to locate.

From src/components/admin/ProjectTasks.tsx (current state, per grep):
- Uses `localTasks`, `handleToggle` pattern
- Already has some "Add task" flow (line 96 adds a new task to state); confirm where the input lives

From src/components/admin/ClientActionItemsList.tsx:
- Same pattern; line 89 shows an add-task state update

All three components already have "add task" logic internally — this plan is REPOSITIONING the trigger to the header and wiring the hide-completed toggle, not building add-task from scratch.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: DashboardTasksCard — header Add-task button + hide-completed toggle</name>
  <files>
    src/components/admin/DashboardTasksCard.tsx,
    src/components/admin/DashboardTasksCard.test.tsx
  </files>
  <read_first>
    - src/components/admin/DashboardTasksCard.tsx full file (locate current Add task UI, handleAddTask, filter chain)
    - .planning/phases/35-dashboard-polish-global-ux-cleanup/35-CONTEXT.md D-16, D-17
    - .planning/phases/35-dashboard-polish-global-ux-cleanup/35-UI-SPEC.md "Header-right action button" (line 160+), "Reveal link" (line 170+), "Tasks card (modified ...)" (line 192+)
  </read_first>
  <behavior>
    - Header-right slot renders `+ Add task` outline button (lucide `Plus` icon 14px + label). On click: reveals the inline add-task input (existing flow) or directly focuses it if already rendered. Exact interaction: if the current card has a dedicated "show add-task input" state, the header button toggles it. If add-task is always inline, the header button becomes a no-op secondary trigger that scrolls/focuses the input.
    - Completed tasks filtered OUT of the primary `displayTasks` list by default
    - `useState<boolean> showCompleted` — when true, completed tasks are appended below active tasks (the existing sort already pushes completed to the bottom; the filter controls visibility)
    - Reveal link at card bottom:
      - When `showCompleted === false` AND `completedCount > 0`: render `Show completed ({N})` link (text-[13px] text-stone hover:text-charcoal hover:underline)
      - When `showCompleted === true`: render `Hide completed` (no count)
      - When `completedCount === 0`: render nothing
    - Reload resets state (component remounts with showCompleted=false)
    - The old footer band for Add-task (if any) is REMOVED
  </behavior>
  <action>
    Read full DashboardTasksCard.tsx first. Identify:
    - Where the current Add task input/form is rendered (save snippet)
    - The current cap `.slice(0, 8)` — decide whether the cap applies to (active only) or (active + revealed completed). Per UI-SPEC the cap is content-level, so keep it on the COMBINED list after the showCompleted filter (so completed tasks can push active tasks off the visible list when revealed — consistent with "show the full set on demand"). Document decision in an inline code comment.

    Write DashboardTasksCard.test.tsx first (create if missing; use the existing `RegenerateLinkDialog.test.tsx` or `AdminModal.test.tsx` as a harness reference). Test cases:
    1. Renders `+ Add task` button in header slot
    2. With 3 active + 2 completed tasks: default render shows only 3 active rows; completed rows absent from DOM
    3. Renders `Show completed (2)` reveal link at bottom when completed tasks exist
    4. Clicking reveal reveals completed rows and swaps copy to `Hide completed`
    5. With 0 completed tasks: reveal link is ABSENT
    6. Clicking the header `+ Add task` button triggers the add-task input path (existing behavior — mock whatever callback is involved)
    7. Keyboard accessibility on reveal link: tabbable, Enter/Space activates

    Run tests — MUST fail.

    Modify DashboardTasksCard.tsx:
    - Add `const [showCompleted, setShowCompleted] = useState(false);`
    - After `sortedTasks`, compute `const activeTasks = sortedTasks.filter(t => !t.completed);` and `const completedTasks = sortedTasks.filter(t => t.completed);`
    - Define `const visibleTasks = showCompleted ? [...activeTasks, ...completedTasks].slice(0, 8) : activeTasks.slice(0, 8);` (keeping the 8 cap; document the combined-list-cap decision).
    - Render the card header as a flex row: `<h2>Tasks</h2>` on the left, the `+ Add task` button on the right. Preserve the existing legacy header padding (`pt-[18px]`, `mb-[14px]`) — do NOT modify per UI-SPEC "Inherited Exceptions".
    - Add-task header button (outline variant per UI-SPEC):
      ```tsx
      <button
        type="button"
        onClick={handleAddTaskClick}
        className="inline-flex items-center gap-1.5 text-[13px] font-body text-terracotta border border-terracotta/40 rounded-md px-3 py-1 hover:bg-terracotta/5 transition-colors focus:outline-none focus:ring-1 focus:ring-terracotta"
      >
        <Plus size={14} />
        Add task
      </button>
      ```
      `handleAddTaskClick` wires to whatever add-task mechanism currently exists (toggle inline input visibility OR focus the existing input).
    - Remove the old Add-task footer band if present (grep + read for the current add-task UI and relocate / delete).
    - Render the reveal link at card bottom when `completedTasks.length > 0`:
      ```tsx
      {completedTasks.length > 0 && (
        <div className="border-t border-stone-light/10 text-center py-3">
          <button
            type="button"
            onClick={() => setShowCompleted(s => !s)}
            className="text-[13px] font-body text-stone hover:text-charcoal hover:underline underline-offset-2 bg-transparent border-0"
          >
            {showCompleted ? "Hide completed" : `Show completed (${completedTasks.length})`}
          </button>
        </div>
      )}
      ```

    Re-run tests — MUST pass.
  </action>
  <verify>
    <automated>npx vitest run src/components/admin/DashboardTasksCard.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "showCompleted" src/components/admin/DashboardTasksCard.tsx` returns >= 3 matches (useState + conditional renders)
    - `grep -n "Show completed" src/components/admin/DashboardTasksCard.tsx` returns >= 1 match
    - `grep -n "Hide completed" src/components/admin/DashboardTasksCard.tsx` returns 1 match
    - `grep -n "Add task" src/components/admin/DashboardTasksCard.tsx` returns >= 1 match (header button label)
    - `grep -n "border-terracotta" src/components/admin/DashboardTasksCard.tsx` returns >= 1 match (outline button styling)
    - Test file has >= 7 test cases; all pass
    - The `.slice(0, 8)` cap is still present (grep `slice(0, 8)` >= 1 match)
  </acceptance_criteria>
  <done>Dashboard Tasks card has header Add-task CTA, hides completed by default, reveal link toggles visibility; tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: ProjectTasks — header Add-task button + hide-completed toggle</name>
  <files>
    src/components/admin/ProjectTasks.tsx,
    src/components/admin/ProjectTasks.test.tsx
  </files>
  <read_first>
    - src/components/admin/ProjectTasks.tsx full file
    - src/components/admin/DashboardTasksCard.tsx (post-Task-1 — mirror the pattern)
    - .planning/phases/35-dashboard-polish-global-ux-cleanup/35-UI-SPEC.md "Tasks card" rows
  </read_first>
  <behavior>
    - Same as Task 1 but for ProjectTasks: header Add-task button (right-aligned), completed hidden by default, `Show completed (N)` / `Hide completed` reveal at card bottom, reload resets state.
    - No cap in ProjectTasks currently (read the file — if there's a `.slice(0, N)` apply the same combined-list-cap rule; otherwise no cap, render all).
    - Current add-task mechanism in ProjectTasks (form at line 96 area) — repurpose or keep; the header button MUST be visible and clickable and MUST trigger the same add-task path.
  </behavior>
  <action>
    Write ProjectTasks.test.tsx first with test cases mirroring DashboardTasksCard.test.tsx:
    1. Renders `+ Add task` button in header
    2. Default: completed tasks hidden
    3. Renders `Show completed (N)` when completed tasks exist
    4. Reveal toggles visibility and copy swaps to `Hide completed`
    5. No completed tasks -> no reveal link
    6. Add-task header click triggers add-task flow

    Run tests — MUST fail.

    Modify ProjectTasks.tsx following the same pattern as Task 1:
    - `const [showCompleted, setShowCompleted] = useState(false);`
    - Partition `localTasks` into active/completed; render active by default; append completed when `showCompleted`.
    - Add header-right `+ Add task` button (copy the exact className from DashboardTasksCard to keep visual parity).
    - Relocate or remove any existing footer Add-task UI.
    - Render the reveal link at card bottom with same classes as Task 1.

    Re-run tests — MUST pass.
  </action>
  <verify>
    <automated>npx vitest run src/components/admin/ProjectTasks.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "showCompleted" src/components/admin/ProjectTasks.tsx` returns >= 3 matches
    - `grep -n "Show completed\|Hide completed" src/components/admin/ProjectTasks.tsx` returns >= 2 matches
    - `grep -n "Add task" src/components/admin/ProjectTasks.tsx` returns >= 1 match
    - `grep -n "border-terracotta" src/components/admin/ProjectTasks.tsx` returns >= 1 match
    - Test file has >= 6 test cases; all pass
  </acceptance_criteria>
  <done>Project-detail Tasks card matches dashboard Tasks card behavior; tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: ClientActionItemsList — header Add-task button + hide-completed toggle</name>
  <files>
    src/components/admin/ClientActionItemsList.tsx,
    src/components/admin/ClientActionItemsList.test.tsx
  </files>
  <read_first>
    - src/components/admin/ClientActionItemsList.tsx full file
    - src/components/admin/DashboardTasksCard.tsx (post-Task-1 — mirror the pattern)
    - .planning/phases/35-dashboard-polish-global-ux-cleanup/35-CONTEXT.md D-16, D-17 (client Tasks card explicitly mentioned)
  </read_first>
  <behavior>
    - Same as Tasks 1 and 2. Client Tasks card is the third surface called out by DASH-22.
    - Uses `item.completed` field (component calls them "items" not "tasks", but same semantic — check component nomenclature and name the state accordingly).
    - Preserve existing add-item API flow — header button is a new trigger into the same code path.
  </behavior>
  <action>
    Write ClientActionItemsList.test.tsx first with 6+ test cases mirroring Task 1's pattern, adapted to the component's `item` nomenclature.

    Run tests — MUST fail.

    Modify ClientActionItemsList.tsx:
    - `const [showCompleted, setShowCompleted] = useState(false);`
    - Partition items into active/completed
    - Header-right `+ Add task` button (label reads `+ Add task` — use that exact copy per UI-SPEC, even if the component internally calls them "items"; DASH-22 explicitly says "Add task" on the client Tasks card)
    - Bottom reveal link with count
    - Remove any existing footer add-item band

    Re-run tests — MUST pass.
  </action>
  <verify>
    <automated>npx vitest run src/components/admin/ClientActionItemsList.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "showCompleted" src/components/admin/ClientActionItemsList.tsx` returns >= 3 matches
    - `grep -n "Show completed\|Hide completed" src/components/admin/ClientActionItemsList.tsx` returns >= 2 matches
    - `grep -n "Add task" src/components/admin/ClientActionItemsList.tsx` returns >= 1 match (exact copy per UI-SPEC)
    - `grep -n "border-terracotta" src/components/admin/ClientActionItemsList.tsx` returns >= 1 match
    - Test file has >= 6 test cases; all pass
  </acceptance_criteria>
  <done>Client Tasks card matches dashboard + project Tasks card behavior; tests green. Full suite of Tasks cards consistent.</done>
</task>

</tasks>

<verification>
Manual UAT:
- [ ] On /admin dashboard, Tasks card shows `+ Add task` button in header; clicking it triggers add-task flow
- [ ] Completed tasks are hidden on initial page load; reveal link appears when completed tasks exist
- [ ] Clicking `Show completed (N)` reveals completed rows; copy swaps to `Hide completed`
- [ ] Page reload returns to hidden state
- [ ] Same behavior on /admin/projects/[id] Tasks card
- [ ] Same behavior on /admin/clients/[id] Tasks card (the action-items list)
- [ ] Header rhythm is preserved (no visual breakage from the Add-task button insertion)

Automated:
- [ ] `npx vitest run src/components/admin/DashboardTasksCard.test.tsx src/components/admin/ProjectTasks.test.tsx src/components/admin/ClientActionItemsList.test.tsx` all green
- [ ] All acceptance-criteria grep checks pass
</verification>

<success_criteria>
DASH-20, DASH-21, DASH-22 all satisfied across three surfaces with consistent visual treatment and interaction.
</success_criteria>

<output>
After completion, create `.planning/phases/35-dashboard-polish-global-ux-cleanup/35-05-SUMMARY.md`
</output>
