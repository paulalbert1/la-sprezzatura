---
phase: 35-dashboard-polish-global-ux-cleanup
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - src/components/admin/ui/CardFilterInput.tsx
  - src/components/admin/ui/CardFilterInput.test.tsx
  - src/components/admin/UpcomingDeliveriesCard.tsx
  - src/components/admin/UpcomingDeliveriesCard.test.tsx
  - src/pages/admin/dashboard.astro
  - src/sanity/queries.ts
autonomous: true
requirements:
  - DASH-11
  - DASH-12
  - DASH-13
  - DASH-14
  - DASH-15
must_haves:
  truths:
    - "Dashboard card title reads `Upcoming Deliveries` (not `Deliveries`)"
    - "Upcoming Deliveries card shows ONLY undelivered items by default"
    - "A `Show delivered (N)` inline link appears at card bottom when >=1 delivered item exists; clicking reveals delivered rows and swaps copy to `Hide delivered`; reload resets to hidden"
    - "A filter input in the card header live-filters rows per keystroke, case-insensitive, matching client name, item name, project name, tracking number, and carrier"
    - "Each delivery row renders in the visual order: client name (lead) -> item -> project (secondary) -> status/ETA (right)"
    - "Carrier ETA slot renders `ETA {MMM d} · {FedEx|UPS|USPS}` ONLY when Ship24 returns a tracked carrier; renders nothing otherwise"
    - "Empty states render correct copy: `No deliveries match your filter.` when filter hides all; `All caught up — no undelivered items.` when none outstanding"
  artifacts:
    - path: "src/components/admin/ui/CardFilterInput.tsx"
      provides: "Shared filter input component used by Deliveries + Projects cards"
      exports: ["default (CardFilterInput)"]
    - path: "src/components/admin/UpcomingDeliveriesCard.tsx"
      provides: "React island replacing the inline Astro deliveries card"
      exports: ["default (UpcomingDeliveriesCard)"]
  key_links:
    - from: "src/pages/admin/dashboard.astro"
      to: "src/components/admin/UpcomingDeliveriesCard.tsx"
      via: "<UpcomingDeliveriesCard client:load deliveries={...} />"
      pattern: "UpcomingDeliveriesCard"
    - from: "src/components/admin/UpcomingDeliveriesCard.tsx"
      to: "src/components/admin/ui/CardFilterInput.tsx"
      via: "import default"
      pattern: "CardFilterInput"
    - from: "src/sanity/queries.ts"
      to: "getAdminDashboardData"
      via: "delivered items included in response (with `delivered: true` flag) so client-side disclosure works"
      pattern: "deliveries"
---

<objective>
Rebuild the Dashboard Deliveries card into the "Upcoming Deliveries" React island required by DASH-11..15:

- Rename title to "Upcoming Deliveries" (D-10).
- Show only undelivered items by default, with a `Show delivered (N)` inline reveal at the card bottom (D-05, D-06).
- Re-order row layout: client name lead, item, project (D-11).
- Show carrier ETA only when Ship24 provides a tracked carrier from {FedEx, UPS, USPS}; otherwise collapsed (D-07).
- Free-text filter input in the card header, live per-keystroke, matches client/item/project/tracking/carrier (D-01, D-02, D-04).

Depends on Plan 01 for the absolute-date format convention (`format(parseISO(d), "MMM d")`) and for the baseline relative-time purge already complete on the dashboard.

Output: a new `UpcomingDeliveriesCard` React island, a shared `CardFilterInput` primitive in `src/components/admin/ui/`, and a dashboard query update that includes delivered items.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/35-dashboard-polish-global-ux-cleanup/35-CONTEXT.md
@.planning/phases/35-dashboard-polish-global-ux-cleanup/35-UI-SPEC.md
@.planning/phases/35-01-SUMMARY.md

<interfaces>
From src/pages/admin/dashboard.astro (current Deliveries card lines 186-230):
- `displayDeliveries = data.deliveries.slice(0, 6)`
- Row shape (inferred): `{ name, projectTitle, projectId, expectedDeliveryDate, status }` — confirm from queries.ts
- `DELIVERY_STATUS[d.status]` provides `{ bg, text, dot, label }` classes — keep, pass into the new component
- `formatDeliveryDate(dateStr)` returns `ETA MMM d` — this becomes the carrier-ETA renderer

From src/lib/ship24.ts (carrier detection):
- Read full file to confirm response shape. Look for a `carrier` or `courier` field on the tracking response. Only values matching `/^(fedex|ups|usps)$/i` trigger ETA rendering per D-07.
- If the module exposes a `getTracking(trackingNumber)` function returning `{ carrier?: string, estimatedDeliveryDate?: string }`, use it. If not, wire the dashboard query to pre-resolve carrier at SSR time (preferred — avoids client-side Ship24 calls).

From src/sanity/queries.ts:
- `getAdminDashboardData(client)` currently returns `{ deliveries: [...] }` filtered to undelivered. Extend to either:
  (a) return ALL deliveries (past 30 days delivered + all undelivered) with a `delivered: boolean` flag on each row, OR
  (b) add a second array `deliveredDeliveries: [...]` alongside the existing `deliveries`.
- Pick whichever requires fewer GROQ changes. Per CONTEXT.md line 99: "delivered items already available via existing query (or add a `delivered: true` variant if current query filters them out — check during planning)". Check during Task 3.

From src/components/admin/ui/AdminModal.tsx (style reference):
- Tailwind primitive pattern: `rounded-xl border border-stone-light/40 bg-white`. Match for CardFilterInput.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create shared CardFilterInput primitive with tests</name>
  <files>
    src/components/admin/ui/CardFilterInput.tsx,
    src/components/admin/ui/CardFilterInput.test.tsx
  </files>
  <read_first>
    - src/components/admin/ui/AdminToast.tsx (shadcn-style Tailwind primitive conventions)
    - src/components/admin/ui/TagInput.tsx (input primitive style reference)
    - src/components/admin/QuickAssignTypeahead.tsx lines 230-270 (typeahead input reference per UI-SPEC: "match the snappy typeahead")
    - .planning/phases/35-dashboard-polish-global-ux-cleanup/35-UI-SPEC.md "<CardFilterInput />" section (line 149+) and "Filter input" interaction-states row
    - .planning/phases/35-dashboard-polish-global-ux-cleanup/35-CONTEXT.md D-01, D-04
  </read_first>
  <behavior>
    - Renders a controlled text input styled per UI-SPEC: `text-[13px] font-body text-charcoal bg-transparent border border-stone-light/40 rounded-md px-3 py-1.5 pl-8 focus:outline-none focus:ring-1 focus:ring-terracotta placeholder:text-stone-light`
    - Leading `Search` icon from lucide-react at 14px, color `text-stone-light`, positioned absolute inside `pl-8` wrapper
    - When `value.length > 0`: renders a right-aligned clickable `X` icon (lucide `X`, 14px, `text-stone-light hover:text-charcoal`) that calls `onChange("")` when clicked
    - When `value.length === 0`: no X icon
    - Props: `{ value: string; onChange: (next: string) => void; placeholder: string; ariaLabel?: string; className?: string }`
    - `className` is merged onto the outer wrapper (for width overrides by parent)
    - No debounce — onChange fires synchronously on every keystroke
    - `aria-label` falls back to `placeholder` if not provided
  </behavior>
  <action>
    Write CardFilterInput.test.tsx FIRST with vitest + @testing-library/react covering:
    1. Renders placeholder text
    2. Typing triggers onChange with new value
    3. X button absent when value empty
    4. X button present when value non-empty
    5. Clicking X calls onChange("")
    6. aria-label falls back to placeholder

    Run `npx vitest run src/components/admin/ui/CardFilterInput.test.tsx` — MUST fail (file does not exist).

    Then create CardFilterInput.tsx. Export default. Use `lucide-react` imports for `Search` and `X`. Match exact Tailwind classes from UI-SPEC "Visual" row. Input `type="text"`, `autoComplete="off"`, `spellCheck={false}`.

    Re-run test — MUST pass.
  </action>
  <verify>
    <automated>npx vitest run src/components/admin/ui/CardFilterInput.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "export default function CardFilterInput\|export default CardFilterInput" src/components/admin/ui/CardFilterInput.tsx` returns 1 match
    - `grep -n "lucide-react" src/components/admin/ui/CardFilterInput.tsx` returns 1 match
    - `grep -n "Search" src/components/admin/ui/CardFilterInput.tsx` returns >= 1 match (icon import)
    - `grep -n "focus:ring-terracotta" src/components/admin/ui/CardFilterInput.tsx` returns 1 match (UI-SPEC focus ring)
    - `grep -n "text-\[13px\]\|text-[13px]" src/components/admin/ui/CardFilterInput.tsx` returns >= 1 match (UI-SPEC typography)
    - Test file has >= 6 test cases; all pass
  </acceptance_criteria>
  <done>Reusable filter input primitive ships with tests green and exact UI-SPEC styling.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build UpcomingDeliveriesCard React island (filter + delivered disclosure + row layout + ETA gating)</name>
  <files>
    src/components/admin/UpcomingDeliveriesCard.tsx,
    src/components/admin/UpcomingDeliveriesCard.test.tsx
  </files>
  <read_first>
    - src/components/admin/DashboardTasksCard.tsx (React island pattern for dashboard cards)
    - src/pages/admin/dashboard.astro lines 186-230 (current Deliveries card HTML to port)
    - src/components/admin/ui/CardFilterInput.tsx (just created in Task 1)
    - src/lib/ship24.ts (carrier field names, response shape)
    - src/sanity/queries.ts (`getAdminDashboardData` return type — specifically the deliveries array shape)
    - .planning/phases/35-dashboard-polish-global-ux-cleanup/35-CONTEXT.md D-02, D-05, D-06, D-07, D-10, D-11
    - .planning/phases/35-dashboard-polish-global-ux-cleanup/35-UI-SPEC.md "Primary Visual Anchors" (Upcoming Deliveries row), "Typography", "Color", "Copywriting Contract", "Interaction States" rows
  </read_first>
  <behavior>
    - Props: `{ deliveries: DeliveryRow[] }` where `DeliveryRow = { _id: string; name: string; clientName: string | null; projectTitle: string; projectId: string; expectedDeliveryDate: string | null; status: string; delivered: boolean; trackingNumber?: string; carrier?: "fedex" | "ups" | "usps" | string | null }`
    - Default render: only rows where `delivered === false`
    - Filter state: `useState<string>("")` — case-insensitive substring match against `name || ""`, `clientName || ""`, `projectTitle || ""`, `trackingNumber || ""`, `carrier || ""` — matches any of them
    - Show-delivered state: `useState<boolean>(false)` — when `true`, also render rows where `delivered === true` (subject to same filter)
    - Row visual order (left-to-right): client name (14px `text-sm` `text-charcoal` `font-medium`) -> item (11px `text-stone-light`) -> project (11px `text-stone-light`), status/ETA column right-aligned
    - When a row's `carrier` is one of `["fedex","ups","usps"]` (case-insensitive) AND `expectedDeliveryDate` present: render `ETA {format(parseISO(expectedDeliveryDate), "MMM d")} · {Carrier}` where Carrier maps to `FedEx|UPS|USPS`. Otherwise render no ETA element at all (return null from the ETA slot)
    - Empty states:
      - `deliveries.filter(r => !r.delivered).length === 0` AND no filter text -> "All caught up — no undelivered items."
      - Filter text present AND zero matches -> "No deliveries match your filter."
    - Reveal link at card bottom inside `border-t border-stone-light/10` divider, centered, `py-3`:
      - When `showDelivered === false` AND `deliveredCount > 0`: render `Show delivered ({N})` as text-[13px] text-stone hover:text-charcoal hover:underline
      - When `showDelivered === true`: render `Hide delivered` (no count)
      - When `deliveredCount === 0`: render nothing
    - No debounce. No sessionStorage. Filter and showDelivered reset on reload (no persistence).
  </behavior>
  <action>
    Write UpcomingDeliveriesCard.test.tsx FIRST with vitest + @testing-library/react. Required cases:
    1. Renders card title "Upcoming Deliveries"
    2. With all undelivered items: renders all rows, shows no "Show delivered" link
    3. With mix of delivered/undelivered: default render shows only undelivered; "Show delivered (N)" link present where N = count of delivered
    4. Clicking "Show delivered (N)" reveals delivered rows and swaps copy to "Hide delivered"
    5. Typing in filter narrows visible rows by client name match (case-insensitive)
    6. Typing in filter matches tracking number
    7. Row with `carrier: "fedex"` and expectedDeliveryDate renders `ETA {MMM d} · FedEx`
    8. Row with `carrier: null` OR `carrier: "dhl"` renders no ETA element
    9. Empty state: no undelivered + no filter -> "All caught up — no undelivered items."
    10. Empty state: filter with no matches -> "No deliveries match your filter."
    11. Row visual order: first column text is client name, second is item, third is project (assert by checking DOM order of text nodes in the row's left block)

    Run tests — MUST fail (file does not exist).

    Create UpcomingDeliveriesCard.tsx:
    - Import `useState` from react, `format, parseISO` from date-fns, `CardFilterInput` from `./ui/CardFilterInput`
    - Use card chrome matching existing dashboard cards: outer `bg-white rounded-xl border border-stone-light/40 overflow-hidden`
    - Header: `px-5 pt-[18px] pb-0` matching legacy header rhythm per UI-SPEC "Inherited Exceptions" note. Title `<h2>` uses the SAME inline font style object as the existing Active Projects card (`font-family: var(--font-sans); font-size: 10.5px; font-weight: 500; color: #9E8E80; letter-spacing: 0.1em; text-transform: uppercase;`) — copy verbatim from dashboard.astro line 190. Title text: `Upcoming Deliveries`.
    - Below title: `<hr>` divider identical to dashboard.astro line 193, then CardFilterInput with `placeholder="Filter deliveries…"`
    - Row render: each row is an `<a href={`/admin/projects/${d.projectId}#procurement`}>` with `flex items-center gap-3 px-5 py-3 hover:bg-cream/50 border-b border-stone-light/10 last:border-b-0`. Left block (`flex-1 min-w-0`): client name (`text-sm font-medium text-charcoal truncate`), below it item name (`text-[11px] text-stone-light truncate`), below item the project title (`text-[11px] text-stone-light truncate`). Right block (`flex flex-col items-end gap-1 shrink-0`): existing status pill (reuse `DELIVERY_STATUS` mapping — import from dashboard.astro if exported, otherwise re-declare locally using the same bg/text/dot/label values), then the carrier-ETA slot (only rendered when conditions met).
    - Carrier label map: `{ fedex: "FedEx", ups: "UPS", usps: "USPS" }` (keys lowercased for matching).
    - Reveal link at bottom: inside `border-t border-stone-light/10 text-center py-3`, a `<button type="button">` styled as `text-[13px] font-body text-stone hover:text-charcoal hover:underline underline-offset-2 cursor-pointer bg-transparent border-0`.

    Re-run tests — MUST pass.
  </action>
  <verify>
    <automated>npx vitest run src/components/admin/UpcomingDeliveriesCard.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "Upcoming Deliveries" src/components/admin/UpcomingDeliveriesCard.tsx` returns 1 match
    - `grep -n "Filter deliveries" src/components/admin/UpcomingDeliveriesCard.tsx` returns 1 match (`Filter deliveries…`)
    - `grep -n "Show delivered" src/components/admin/UpcomingDeliveriesCard.tsx` returns >= 1 match
    - `grep -n "Hide delivered" src/components/admin/UpcomingDeliveriesCard.tsx` returns 1 match
    - `grep -n "All caught up" src/components/admin/UpcomingDeliveriesCard.tsx` returns 1 match (empty-state copy)
    - `grep -n "No deliveries match your filter" src/components/admin/UpcomingDeliveriesCard.tsx` returns 1 match
    - `grep -n "FedEx\|UPS\|USPS" src/components/admin/UpcomingDeliveriesCard.tsx` returns >= 3 matches (carrier label map)
    - `grep -n "fedex\|ups\|usps" src/components/admin/UpcomingDeliveriesCard.tsx` returns >= 3 matches (lowercase keys for matching)
    - `grep -n "CardFilterInput" src/components/admin/UpcomingDeliveriesCard.tsx` returns >= 2 matches (import + usage)
    - Test file contains >= 11 test cases; all pass
  </acceptance_criteria>
  <done>UpcomingDeliveriesCard React island with full DASH-11..15 behavior and all tests green.</done>
</task>

<task type="auto">
  <name>Task 3: Wire UpcomingDeliveriesCard into dashboard.astro + extend dashboard query for delivered items</name>
  <files>
    src/pages/admin/dashboard.astro,
    src/sanity/queries.ts
  </files>
  <read_first>
    - src/pages/admin/dashboard.astro lines 1-50 (imports) and 186-230 (Deliveries card to replace)
    - src/sanity/queries.ts -- specifically the `getAdminDashboardData` function and its `deliveries` GROQ projection
    - src/lib/ship24.ts (confirm carrier field name for the GROQ/server enrichment)
    - src/components/admin/UpcomingDeliveriesCard.tsx (just built — confirm prop shape)
    - .planning/phases/35-dashboard-polish-global-ux-cleanup/35-CONTEXT.md D-06 (delivered items load on first expand vs up-front)
  </read_first>
  <action>
    **queries.ts changes:**

    1. Locate `getAdminDashboardData` and its `deliveries` GROQ block. Determine whether the current query already returns delivered items or filters them out (likely filters by `status != "delivered"` or similar).

    2. Modify the GROQ projection so it returns ALL deliveries (delivered + undelivered), added field `delivered: status == "delivered"` (or whatever the delivered sentinel value is in the schema — read the procurement status taxonomy from Phase 32 CONTEXT.md if uncertain). Also include `clientName` (join from the related project's client), `trackingNumber`, and `carrier` on each row. Example GROQ addition:

       ```groq
       *[_type == "procurementItem" && ...] {
         _id, name, projectId, projectTitle, expectedDeliveryDate, status, trackingNumber,
         "delivered": status == "delivered",
         "clientName": project->client->name,
         "carrier": carrier
       }
       ```

    3. If `carrier` is not a stored Sanity field but comes from a server-side Ship24 lookup, add a post-GROQ enrichment step: for each delivery row with a `trackingNumber`, call `getTracking(trackingNumber)` (or equivalent function in src/lib/ship24.ts), set `carrier` to the returned carrier if the tracking service identifies one of `fedex|ups|usps`, else `null`. Do this with `Promise.all` to avoid sequential network latency. If Ship24 is not available in the dashboard loader context, omit enrichment and leave `carrier: null` — then the card simply never renders ETA, which is the correct D-07 fallback.

    4. Update the TypeScript return type for `getAdminDashboardData` to include the new fields on each delivery row.

    **dashboard.astro changes:**

    1. Add import in frontmatter: `import UpcomingDeliveriesCard from "../../components/admin/UpcomingDeliveriesCard.tsx";`

    2. Replace the entire Deliveries card block (lines 186-230, the `{/* Deliveries Card */}` div and its contents) with:
       ```astro
       <UpcomingDeliveriesCard client:load deliveries={data.deliveries} />
       ```
       Remove the `displayDeliveries = data.deliveries.slice(0, 6)` variable at line 36 — the React island handles its own rendering cap (no cap needed; filter + delivered disclosure make "top N" truncation obsolete). If desired, add an internal `slice(0, 20)` inside the card to keep DOM size reasonable; note in code comment.

    3. The `formatDeliveryDate` local helper (lines 68-75) is no longer referenced — remove it. Confirm with grep.

    4. `DELIVERY_STATUS` constant: if referenced only by the removed card, move the declaration into UpcomingDeliveriesCard.tsx (or export it from a shared module). If referenced elsewhere in dashboard.astro, leave in place and pass via prop (extending the component interface). Prefer in-component declaration — fewer touch points.

    5. Preserve the layout: UpcomingDeliveriesCard sits in the left column below Active Projects (same slot as the removed Deliveries card).
  </action>
  <verify>
    <automated>npx vitest run && grep -n "UpcomingDeliveriesCard" src/pages/admin/dashboard.astro && grep -n "formatDeliveryDate\|displayDeliveries" src/pages/admin/dashboard.astro; [ $? -eq 1 ] || exit 1</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "import UpcomingDeliveriesCard" src/pages/admin/dashboard.astro` returns 1 match
    - `grep -n "<UpcomingDeliveriesCard" src/pages/admin/dashboard.astro` returns 1 match
    - `grep -n "client:load" src/pages/admin/dashboard.astro | grep UpcomingDeliveriesCard` returns 1 match (hydration directive present)
    - `grep -n "Deliveries</h2>" src/pages/admin/dashboard.astro` returns 0 matches (old card gone — the new card owns its title)
    - `grep -n "formatDeliveryDate\|displayDeliveries" src/pages/admin/dashboard.astro` returns 0 matches
    - `grep -n "delivered\|clientName\|trackingNumber\|carrier" src/sanity/queries.ts` returns >= 1 match each (new fields projected)
    - Dev server `pnpm dev` or `npm run dev` compiles without errors (manual during execution; not gated)
    - `npx vitest run` passes all tests
  </acceptance_criteria>
  <done>Dashboard renders the new Upcoming Deliveries React island; GROQ projection carries delivered flag + clientName + trackingNumber + carrier; old inline card and its helpers removed.</done>
</task>

</tasks>

<verification>
Manual UAT on /admin dashboard:
- [ ] Card title reads "Upcoming Deliveries"
- [ ] Typing in the filter box narrows rows instantly (tested with client name, item name, project name, tracking number substring)
- [ ] Each row shows client name (prominent), item (small), project (small) in that vertical order
- [ ] Rows with Ship24 carrier == fedex/ups/usps show `ETA MMM d · FedEx/UPS/USPS`; rows without show nothing in the ETA slot
- [ ] By default only undelivered rows render; `Show delivered (N)` link appears at the bottom when any delivered items exist
- [ ] Clicking reveal swaps copy to `Hide delivered` and shows delivered rows; reload resets to hidden
- [ ] Empty states render the correct copy per UI-SPEC

Automated:
- [ ] `npx vitest run` all green
- [ ] All acceptance-criteria grep checks from tasks pass
</verification>

<success_criteria>
All of DASH-11 through DASH-15 satisfied. Card is fully self-contained React island; hydration does not require separate Ship24 calls on the client (carrier resolved server-side at SSR). Reload always returns to the default undelivered-only view.
</success_criteria>

<output>
After completion, create `.planning/phases/35-dashboard-polish-global-ux-cleanup/35-02-SUMMARY.md`
</output>
