---
phase: 35-dashboard-polish-global-ux-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/trades.ts
  - src/lib/trades.test.ts
  - src/lib/dashboardUtils.ts
  - src/lib/dashboardUtils.test.ts
  - src/pages/admin/dashboard.astro
  - src/components/admin/QuickAssignTypeahead.tsx
  - src/components/admin/EntityListPage.tsx
  - src/components/portal/ContractorSection.astro
autonomous: true
requirements:
  - DASH-10
  - DASH-19
must_haves:
  truths:
    - "No relative-time badges (`Nd`, `Nw`, `formatDistanceToNow`, `in Nd`, `today`, `{N}d overdue`) render on any admin surface"
    - "Every admin trade label renders in sentence case via formatTrade() (e.g., `electrical-rough-in` -> `Electrical rough-in`)"
    - "Dashboard `days in stage` value (Active Projects card, currently line 170 rendering `{daysInStage}d`) is replaced with absolute stage-changed date"
    - "Dashboard overdue task/milestone dates render as `MMM d` in red-600 with no `overdue` suffix"
  artifacts:
    - path: "src/lib/trades.ts"
      provides: "formatTrade(slug) utility exporting a sentence-case label for any trade slug"
      exports: ["formatTrade", "TRADE_LABELS"]
    - path: "src/lib/trades.test.ts"
      provides: "Vitest coverage for known-slug map, unknown-slug fallback, empty/undefined input"
  key_links:
    - from: "src/components/admin/QuickAssignTypeahead.tsx"
      to: "src/lib/trades.ts"
      via: "import { formatTrade }"
      pattern: "formatTrade\\("
    - from: "src/components/admin/EntityListPage.tsx"
      to: "src/lib/trades.ts"
      via: "import { formatTrade } used in formatTrades() wrapper"
      pattern: "formatTrade\\("
    - from: "src/pages/admin/dashboard.astro"
      to: "src/lib/dashboardUtils.ts"
      via: "absolute-date helpers replacing getDaysOverdueStr/getDaysUntilStr"
      pattern: "format\\(parseISO"
---

<objective>
Lay the two cross-cutting foundations other Phase 35 plans depend on:

1. A single `formatTrade(slug)` utility that every trade render site uses (D-14, D-15, DASH-19).
2. A project-wide purge of relative-time strings (`Nd`, `in Nd`, `today`, `Nd overdue`, `formatDistanceToNow`) from every admin surface, replaced with absolute `MMM d` dates (D-08, D-09, DASH-10).

Purpose: downstream plans (Upcoming Deliveries overhaul, Projects filter, Tasks cards) can freely render trade pills and dates without re-doing this work.

Output: new `src/lib/trades.ts` + test file; every admin render site that previously rendered raw trade slugs or relative-time strings now reads sentence-case labels and absolute dates.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/35-dashboard-polish-global-ux-cleanup/35-CONTEXT.md
@.planning/phases/35-dashboard-polish-global-ux-cleanup/35-UI-SPEC.md

<interfaces>
<!-- Existing helpers and render sites the executor needs to touch. -->

From src/lib/dashboardUtils.ts (read to confirm signatures before modifying):
- `getDaysInStage(stageChangedAt: string | null): number`
- `isMilestoneOverdue(dueDate: string | null): boolean`
- `getOverdueBannerData(...)` -- keep (used by overdue banner long-form copy)

From src/pages/admin/dashboard.astro (local helpers to remove):
- `getDaysOverdueStr(dateStr)` lines 77-84 -- DELETE call sites, DELETE helper
- `getDaysUntilStr(dateStr)` lines 86-93 -- DELETE call sites, DELETE helper
- `formatDeliveryDate(dateStr)` lines 68-75 -- KEEP (Plan 02 replaces its consumers)
- `formatMilestoneDate(dateStr)` lines 59-66 -- KEEP (reuse for absolute dates)

From src/components/admin/QuickAssignTypeahead.tsx:
- Line 285 renders `{trade}` raw -- replace with `{formatTrade(trade)}`
- Line 181 sends `trade` slug in the API payload -- KEEP as slug (server-side stays slug-based)

From src/components/admin/EntityListPage.tsx:
- Line 84 `function formatTrades(trades: string[] | undefined): React.ReactNode` -- rewrite to call `formatTrade(slug)` for each pill
- Line 192 `{formatTrades(entity.trades)}` -- no change

From src/components/portal/ContractorSection.astro:
- Line 45 renders `{trade}` raw inside a pill -- replace with `formatTrade(trade)` (portal surface, but per D-14 all render sites swap; portal is a render site)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create formatTrade utility with full test coverage</name>
  <files>src/lib/trades.ts, src/lib/trades.test.ts</files>
  <read_first>
    - src/lib/dashboardUtils.ts (module style, exports pattern)
    - src/lib/formatCurrency.ts (confirm util module conventions)
    - src/lib/formatCurrency.test.ts (confirm vitest style for pure utils)
    - src/components/admin/EntityDetailForm.tsx lines 240-280 (current trade array usage -- read to enumerate slug universe)
    - .planning/phases/35-dashboard-polish-global-ux-cleanup/35-CONTEXT.md D-14, D-15
    - .planning/phases/35-dashboard-polish-global-ux-cleanup/35-UI-SPEC.md "formatTrade(slug) utility" section (line 184+)
  </read_first>
  <behavior>
    - Known slug `electrical-rough-in` -> `Electrical rough-in` (first letter capital, rest of hyphenated word lowercase, hyphens preserved)
    - Known slug `painting` -> `Painting`
    - Known slug `hvac` -> `HVAC` (acronym preserved -- document in TRADE_LABELS map)
    - Known slug `general-contractor` -> `General contractor`
    - Unknown slug `foo-bar-baz` -> `Foo bar baz` (fallback: replace hyphens/underscores with spaces, capitalize only the first letter of the whole string, rest lowercase)
    - Unknown slug `FOO_BAR` -> `Foo bar` (upper input is still normalized)
    - Empty string -> returns `""` (do not throw)
    - `undefined` input -> returns `""` (type signature accepts `string | undefined`)
    - Never returns raw slug with hyphens intact (except when hyphens are semantic in a known-map entry like `rough-in`)
    - Never returns all-caps output (except known acronyms in the TRADE_LABELS map)
  </behavior>
  <action>
    Write src/lib/trades.test.ts FIRST with 8+ cases covering every bullet in <behavior>. Tests must import `formatTrade` and `TRADE_LABELS` from `./trades`. Run `npx vitest run src/lib/trades.test.ts` — MUST fail (module does not exist).

    Then create src/lib/trades.ts exporting:

    ```ts
    export const TRADE_LABELS: Record<string, string> = {
      "electrical-rough-in": "Electrical rough-in",
      "electrical-finish": "Electrical finish",
      "plumbing-rough-in": "Plumbing rough-in",
      "plumbing-finish": "Plumbing finish",
      "hvac": "HVAC",
      "painting": "Painting",
      "flooring": "Flooring",
      "tile": "Tile",
      "drywall": "Drywall",
      "framing": "Framing",
      "general-contractor": "General contractor",
      "cabinetry": "Cabinetry",
      "countertops": "Countertops",
      "millwork": "Millwork",
      "demolition": "Demolition",
      "landscaping": "Landscaping",
      "roofing": "Roofing",
      "windows": "Windows",
      "wallpaper": "Wallpaper",
    };

    export function formatTrade(slug: string | undefined | null): string {
      if (!slug) return "";
      if (TRADE_LABELS[slug]) return TRADE_LABELS[slug];
      const normalized = slug.replace(/[-_]/g, " ").toLowerCase().trim();
      if (!normalized) return "";
      return normalized.charAt(0).toUpperCase() + normalized.slice(1);
    }
    ```

    Before committing the final TRADE_LABELS list, run `grep -rn "trades:\s*\[" src/lib/gantt/ src/components/admin/EntityDetailForm.tsx` to enumerate any trade slugs actually referenced in test fixtures and code, and add any missing ones to the map. Do NOT omit slugs found in the codebase.

    Re-run `npx vitest run src/lib/trades.test.ts` — MUST pass.
  </action>
  <verify>
    <automated>npx vitest run src/lib/trades.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "export function formatTrade" src/lib/trades.ts` returns exactly one match
    - `grep -n "export const TRADE_LABELS" src/lib/trades.ts` returns exactly one match
    - `npx vitest run src/lib/trades.test.ts` reports `>= 8` tests passing, 0 failing
    - Test file contains assertions matching every bullet in <behavior>
    - `TRADE_LABELS["electrical-rough-in"] === "Electrical rough-in"` (verifiable via test)
    - `formatTrade("FOO_BAR") === "Foo bar"` (verifiable via test)
    - `formatTrade(undefined) === ""` (verifiable via test)
    - `formatTrade("") === ""` (verifiable via test)
  </acceptance_criteria>
  <done>formatTrade + TRADE_LABELS exported, all tests green, known slug map covers every slug referenced elsewhere in src/.</done>
</task>

<task type="auto">
  <name>Task 2: Swap every admin + portal trade render site to formatTrade()</name>
  <files>
    src/components/admin/QuickAssignTypeahead.tsx,
    src/components/admin/EntityListPage.tsx,
    src/components/portal/ContractorSection.astro
  </files>
  <read_first>
    - src/lib/trades.ts (just created in Task 1 — confirm export surface)
    - src/components/admin/QuickAssignTypeahead.tsx full file (understand trade dropdown render + selectedContractor.trades data flow)
    - src/components/admin/EntityListPage.tsx lines 80-200 (current formatTrades implementation + call site)
    - src/components/portal/ContractorSection.astro lines 38-50 (current trade pill render)
    - .planning/phases/35-dashboard-polish-global-ux-cleanup/35-UI-SPEC.md "Quick-assign multi trade" row (line 210)
    - .planning/phases/35-dashboard-polish-global-ux-cleanup/35-CONTEXT.md D-14
  </read_first>
  <action>
    **QuickAssignTypeahead.tsx:**
    1. Add import: `import { formatTrade } from "../../lib/trades";` (relative path — confirm correct depth from components/admin/).
    2. Line 285 — replace `{trade}` render text with `{formatTrade(trade)}`. The `key={trade}` and the `onClick={() => handleTradeSelect(trade)}` callback MUST continue using the raw slug (server-side API and trade state remain slug-based per D-14 "slugs stay in Sanity").
    3. Confirm no other raw-trade render sites in the file (search for `{trade`).

    **EntityListPage.tsx:**
    1. Add import: `import { formatTrade } from "../../lib/trades";`
    2. Rewrite the `formatTrades(trades: string[] | undefined)` helper at line 84 so each rendered pill's label text comes from `formatTrade(slug)`. Preserve existing pill wrapper styling (do not change className or DOM shape); only the visible text changes.
    3. The search / filter string (existing code at line 54: `entity.trades.join(" ").toLowerCase()`) MUST continue to match against raw slugs AND formatted labels. Update to: `[...entity.trades, ...entity.trades.map(formatTrade)].join(" ").toLowerCase()` so typing either `electrical-rough-in` OR `electrical rough-in` matches.

    **ContractorSection.astro:**
    1. Add import at top of the frontmatter script: `import { formatTrade } from "../../lib/trades";`
    2. Line 45 — replace `{trade}` with `{formatTrade(trade)}`.

    Run project tests to ensure nothing broke.
  </action>
  <verify>
    <automated>npx vitest run && grep -rn "^\s*{trade}\s*$\|>\s*{trade}\s*<\|>\s*{trade}$" src/components/admin/QuickAssignTypeahead.tsx src/components/admin/EntityListPage.tsx src/components/portal/ContractorSection.astro; [ $? -eq 1 ]</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "formatTrade" src/components/admin/QuickAssignTypeahead.tsx` returns >= 1 match
    - `grep -n "formatTrade" src/components/admin/EntityListPage.tsx` returns >= 1 match
    - `grep -n "formatTrade" src/components/portal/ContractorSection.astro` returns >= 1 match
    - `grep -n '{trade}' src/components/admin/QuickAssignTypeahead.tsx` returns 0 matches for raw render (key/onClick props may still contain `{trade}` in attribute position, but NOT as body render text)
    - `grep -n 'handleTradeSelect(trade)' src/components/admin/QuickAssignTypeahead.tsx` returns 1 match (raw slug still used for state/API)
    - `npx vitest run` passes all tests (no regressions)
  </acceptance_criteria>
  <done>Every admin and portal trade render site emits sentence-case labels via formatTrade. Raw slugs remain on the wire (API payloads, keys, state). Test suite green.</done>
</task>

<task type="auto">
  <name>Task 3: Purge all relative-time strings from admin dashboard</name>
  <files>
    src/pages/admin/dashboard.astro,
    src/lib/dashboardUtils.ts,
    src/lib/dashboardUtils.test.ts
  </files>
  <read_first>
    - src/pages/admin/dashboard.astro full file (identify all relative-time render sites)
    - src/lib/dashboardUtils.ts full file (check which helpers are still needed after purge)
    - src/lib/dashboardUtils.test.ts (update tests if helpers removed)
    - .planning/phases/35-dashboard-polish-global-ux-cleanup/35-CONTEXT.md D-08, D-09
    - .planning/phases/35-dashboard-polish-global-ux-cleanup/35-UI-SPEC.md "Absolute-date badge" section (line 176+) and "Overdue date" copywriting row
  </read_first>
  <action>
    **dashboard.astro changes:**

    1. **Active Projects card days-in-stage (line 169-171):** Currently renders `{daysInStage}d` in red when stale. Replace with absolute `Since {MMM d}` using stage changed date:
       ```astro
       <span class={`text-[11px] font-body shrink-0 ${daysStale ? "text-red-600 font-semibold" : "text-stone-light"}`}>
         {project.stageChangedAt ? `Since ${format(parseISO(project.stageChangedAt), "MMM d")}` : ""}
       </span>
       ```
       Keep `getDaysInStage` import (used only to compute `daysStale` boolean for coloring). Do NOT render the number.

    2. **Remove local helper `getDaysOverdueStr(dateStr)` lines 77-84** — delete the function. Grep confirms no remaining call sites after step 1.

    3. **Remove local helper `getDaysUntilStr(dateStr)` lines 86-93** — delete. Grep confirms no remaining call sites.

    4. **Overdue banner single-item copy (lines 107-124):** Currently renders `{overdueBanner.firstMilestone.daysOverdue} days overdue`. Per D-08 the long-form banner keeps its wording (UI-SPEC copywriting row: "The existing overdue banner keeps its longer copy"). LEAVE banner copy as-is.

    5. Run a final sweep: `grep -n "\${\|{.*}d\b\|{.*}w\b\|formatDistanceToNow\|overdue\"\|in {.*}d" src/pages/admin/dashboard.astro` — confirm no `{daysX}d` / `{daysX}w` / `formatDistanceToNow` remain outside the banner's literal "days overdue" text node.

    **dashboardUtils.ts changes:**

    1. Search for `formatDistanceToNow` imports across `src/**` (admin-only scope):
       ```bash
       grep -rn "formatDistanceToNow" src/pages/admin/ src/components/admin/
       ```
       For each hit: replace with `format(parseISO(d), "MMM d")` where `d` is the date variable. Non-admin files (src/pages/portal, src/components/portal, src/lib/sendUpdate) are out of scope for Phase 35 per DASH-10 ("from admin UI where they add noise").

    2. In dashboardUtils.ts: if `getDaysInStage` or `isMilestoneOverdue` still have callers, KEEP them (they compute booleans, not visible strings). If any helper is now unreferenced, remove it AND its test case.

    3. Update dashboardUtils.test.ts to reflect any helper removals. Run `npx vitest run src/lib/dashboardUtils.test.ts` — MUST pass.
  </action>
  <verify>
    <automated>grep -rn "formatDistanceToNow\|getDaysOverdueStr\|getDaysUntilStr" src/pages/admin/ src/components/admin/ 2>/dev/null | grep -v ".test." | grep -v "\.planning/"; [ $? -eq 1 ] && npx vitest run src/lib/dashboardUtils.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn "formatDistanceToNow" src/pages/admin/ src/components/admin/` returns 0 matches (excluding .test. files if any mock the import)
    - `grep -n "getDaysOverdueStr\|getDaysUntilStr" src/pages/admin/dashboard.astro` returns 0 matches
    - `grep -n "{daysInStage}d" src/pages/admin/dashboard.astro` returns 0 matches (the `Nd` badge must be gone)
    - `grep -n 'Since ${format' src/pages/admin/dashboard.astro` OR `grep -n 'Since.*MMM d' src/pages/admin/dashboard.astro` returns >= 1 match (absolute replacement present)
    - `grep -n "days overdue" src/pages/admin/dashboard.astro` returns 1-2 matches (banner long-form copy preserved per D-08)
    - `npx vitest run src/lib/dashboardUtils.test.ts` passes
    - `npx vitest run` passes all tests (no regressions)
  </acceptance_criteria>
  <done>Admin dashboard renders only absolute dates. `getDaysOverdueStr`/`getDaysUntilStr` helpers deleted. `formatDistanceToNow` absent from every admin file. Overdue banner long-form copy preserved per CONTEXT D-08.</done>
</task>

</tasks>

<verification>
- `grep -rn "formatDistanceToNow" src/pages/admin/ src/components/admin/` returns 0 matches
- `grep -rn "{.*[Dd]ays.*}d\b\|getDaysOverdueStr\|getDaysUntilStr" src/pages/admin/ src/components/admin/` returns 0 matches (outside banner literal text)
- `grep -rn "formatTrade" src/components/admin/ src/components/portal/` returns >= 3 matches (QuickAssign, EntityList, ContractorSection)
- `npx vitest run` all tests pass, including new trades.test.ts
- Manual visual scan of /admin (Liz's dev URL): no `Nd`, `Nw`, or `in Nd` badges; trade pills on any contractor page show sentence case.
</verification>

<success_criteria>
- formatTrade utility exists and is used everywhere an admin or portal surface renders a trade slug as visible text
- Raw trade slugs remain the on-wire/state representation (API payloads, React keys, component state)
- Zero relative-time strings on admin dashboard, admin project/client detail, admin contractor pages (except overdue banner long-form copy)
- Absolute dates (`MMM d` format, red-600 for overdue, stone-light otherwise) replace every removed relative-time string
- Test suite green
</success_criteria>

<output>
After completion, create `.planning/phases/35-dashboard-polish-global-ux-cleanup/35-01-SUMMARY.md`
</output>
