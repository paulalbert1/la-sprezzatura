---
phase: 35-dashboard-polish-global-ux-cleanup
plan: 03
type: execute
wave: 3
depends_on: [01, 02]
files_modified:
  - src/components/admin/ActiveProjectsCard.tsx
  - src/components/admin/ActiveProjectsCard.test.tsx
  - src/pages/admin/dashboard.astro
autonomous: true
requirements:
  - DASH-16
must_haves:
  truths:
    - "Projects card in the admin dashboard has a free-text filter input in its header that live-filters rows per keystroke"
    - "Filter matches project name, client name, and stage label (case-insensitive substring) per D-03"
    - "Filter resets on page reload (no persistence)"
    - "Empty state reads `No projects match your filter.` when filter hides all rows"
  artifacts:
    - path: "src/components/admin/ActiveProjectsCard.tsx"
      provides: "React island replacing the inline Astro Active Projects card, with filter behavior"
      exports: ["default (ActiveProjectsCard)"]
  key_links:
    - from: "src/pages/admin/dashboard.astro"
      to: "src/components/admin/ActiveProjectsCard.tsx"
      via: "<ActiveProjectsCard client:load projects={...} />"
      pattern: "ActiveProjectsCard"
    - from: "src/components/admin/ActiveProjectsCard.tsx"
      to: "src/components/admin/ui/CardFilterInput.tsx"
      via: "import default"
      pattern: "CardFilterInput"
---

<objective>
Add a free-text filter to the Active Projects card on the admin dashboard per DASH-16 / D-03 / D-04. Extract the card into a React island `ActiveProjectsCard` so filter state can live in React without client:only gymnastics.

Depends on Plan 01 for the absolute-date replacement already applied to the Active Projects stage-date render (`Since {MMM d}` instead of `{N}d`). Reuses the `CardFilterInput` primitive from Plan 02.

Output: `ActiveProjectsCard.tsx` + test; dashboard.astro updated to mount it.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/35-dashboard-polish-global-ux-cleanup/35-CONTEXT.md
@.planning/phases/35-dashboard-polish-global-ux-cleanup/35-UI-SPEC.md
@.planning/phases/35-01-SUMMARY.md

<interfaces>
From src/pages/admin/dashboard.astro lines 131-183 (Active Projects card — current render):
- `displayProjects = data.activeProjects.slice(0, 8)`
- Each project has: `{ _id, title, clientName, pipelineStage, stageChangedAt }`
- `STAGE_META[project.pipelineStage].title` — stage label for display
- `STAGE_COLORS[project.pipelineStage]` — `{ bg, text }` class pair for stage pill
- `getDaysInStage(project.stageChangedAt)` — used only for `daysStale` coloring after Plan 01
- After Plan 01: renders `Since ${format(parseISO(stageChangedAt), "MMM d")}` on the right

From src/lib/portalStages.ts:
- `STAGE_META` map: `{ [stageKey]: { title: string, ...} }`
- `STAGE_COLORS` may live here too (confirm) — export for use in React component

From src/components/admin/ui/CardFilterInput.tsx (created in Plan 02, Task 1):
- Default export. Props: `{ value, onChange, placeholder, ariaLabel?, className? }`
- **This plan depends on Plan 02 for CardFilterInput existence.** Execution order: Plan 02 Task 1 before Plan 03 Task 1.
</interfaces>

**NOTE ON DEPENDENCY:** This plan is in Wave 2 alongside Plan 02. Both need `CardFilterInput`. Plan 02 Task 1 creates it. Plan 03 consumes it. Since same wave, execute-phase must run Plan 02 Task 1 before Plan 03 Task 1. If parallel execution is attempted and `CardFilterInput` is missing when Plan 03 starts, Plan 03 Task 1's test will fail at import — at that point the executor should wait / re-sync. Alternatively, move Plan 03 to wave 3 (safer; costs one extra wave). **Planner recommendation: keep wave 2 with the understanding that Plan 02 Task 1 is the critical-path primitive; if the orchestrator serializes plans within a wave, order Plan 02 before Plan 03.**
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build ActiveProjectsCard React island with filter</name>
  <files>
    src/components/admin/ActiveProjectsCard.tsx,
    src/components/admin/ActiveProjectsCard.test.tsx
  </files>
  <read_first>
    - src/pages/admin/dashboard.astro lines 131-184 (current card HTML to port)
    - src/components/admin/DashboardTasksCard.tsx (React island card pattern)
    - src/components/admin/UpcomingDeliveriesCard.tsx (sibling card from Plan 02 — match card chrome)
    - src/components/admin/ui/CardFilterInput.tsx (primitive from Plan 02)
    - src/lib/portalStages.ts full file (STAGE_META / STAGE_COLORS exports)
    - src/lib/dashboardUtils.ts (getDaysInStage signature)
    - .planning/phases/35-dashboard-polish-global-ux-cleanup/35-CONTEXT.md D-03, D-04
    - .planning/phases/35-dashboard-polish-global-ux-cleanup/35-UI-SPEC.md "Primary Visual Anchors" (Projects row), "Copywriting Contract" row for Projects filter placeholder + empty state
  </read_first>
  <behavior>
    - Props: `{ projects: ProjectRow[]; totalCount: number }` where `ProjectRow = { _id, title, clientName: string | null, pipelineStage: string, stageChangedAt: string | null }`
    - Default render: all provided projects, sliced to 8 (same cap as current dashboard.astro `slice(0, 8)`)
    - Filter state: `useState<string>("")` — case-insensitive substring match against `title`, `clientName || ""`, AND `STAGE_META[pipelineStage]?.title || ""`
    - Empty states:
      - No projects provided -> "No active projects" (preserve existing copy)
      - Filter text present AND zero matches -> "No projects match your filter."
    - Row visual: preserve existing layout verbatim — project title (left, `text-[13.5px] font-medium`), client name below (`text-[11.5px] text-stone-light`), stage pill with `STAGE_COLORS[stage]` classes, right-aligned absolute-date `Since {MMM d}` from stageChangedAt (red-600 font-semibold when `daysStale`, else `text-stone-light`)
    - Filter resets on reload (useState, no persistence)
    - When filter is non-empty, the `totalCount > 8` footer "View all N projects" link keeps its behavior (link still points to /admin/projects)
  </behavior>
  <action>
    Write ActiveProjectsCard.test.tsx FIRST with vitest + @testing-library/react:
    1. Renders card title "Active Projects"
    2. Renders all passed projects up to 8 default
    3. Typing in filter narrows to matching rows by title
    4. Typing in filter matches client name
    5. Typing in filter matches stage label (e.g., "Construction" matches `pipelineStage: "construction"`)
    6. Filter is case-insensitive
    7. Filter with no matches -> "No projects match your filter." empty state
    8. Empty projects prop -> "No active projects" (no filter input displayed? — per UI-SPEC the filter input is always in the header; render it but show empty-state below)
    9. Stage pill renders with correct STAGE_COLORS classes (assert presence of the expected bg class)
    10. Date column renders `Since ` prefix + formatted date

    Run tests — MUST fail.

    Create ActiveProjectsCard.tsx:
    - Match card chrome verbatim with dashboard.astro lines 132-184 (outer `bg-white rounded-xl border border-stone-light/40 overflow-hidden`, header padding `px-5 pt-[18px] pb-0`, title inline style copied verbatim, `<hr>` divider)
    - Title text: `Active Projects` (unchanged — DASH-16 does not rename)
    - Below title: `<hr>`, then CardFilterInput with `placeholder="Filter projects…"`
    - Row render: `<a href={`/admin/projects/${p._id}`}>` preserving all existing classes from dashboard.astro lines 150-173, substituting `Since ${format(parseISO(p.stageChangedAt), "MMM d")}` for the right-aligned date
    - Footer "View all N projects" link: render only when `totalCount > 8` (reference existing line 177-183)
    - Import STAGE_META, STAGE_COLORS from `../../lib/portalStages` (confirm path during Task). If STAGE_COLORS is defined inline in dashboard.astro currently, move it to portalStages.ts as a first step and export.

    Re-run tests — MUST pass.
  </action>
  <verify>
    <automated>npx vitest run src/components/admin/ActiveProjectsCard.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "Active Projects" src/components/admin/ActiveProjectsCard.tsx` returns >= 1 match
    - `grep -n "Filter projects" src/components/admin/ActiveProjectsCard.tsx` returns 1 match (`Filter projects…`)
    - `grep -n "No projects match your filter" src/components/admin/ActiveProjectsCard.tsx` returns 1 match
    - `grep -n "CardFilterInput" src/components/admin/ActiveProjectsCard.tsx` returns >= 2 matches (import + usage)
    - `grep -n "Since " src/components/admin/ActiveProjectsCard.tsx` returns 1 match (absolute-date label prefix)
    - `grep -n "STAGE_META\|STAGE_COLORS" src/components/admin/ActiveProjectsCard.tsx` returns >= 2 matches
    - Test file has >= 10 test cases; all pass
  </acceptance_criteria>
  <done>Active Projects card is a React island with filter; all tests green; visual parity with previous inline Astro card.</done>
</task>

<task type="auto">
  <name>Task 2: Wire ActiveProjectsCard into dashboard.astro (remove inline card)</name>
  <files>src/pages/admin/dashboard.astro</files>
  <read_first>
    - src/pages/admin/dashboard.astro full file
    - src/components/admin/ActiveProjectsCard.tsx (prop shape from Task 1)
  </read_first>
  <action>
    1. Add import in frontmatter: `import ActiveProjectsCard from "../../components/admin/ActiveProjectsCard.tsx";`
    2. Remove the `displayProjects = data.activeProjects.slice(0, 8)` variable (line 34 area) — the React island caps internally.
    3. Replace the entire Active Projects card block (dashboard.astro lines 131-184, the `{/* Active Projects Card */}` div) with:
       ```astro
       <ActiveProjectsCard
         client:load
         projects={data.activeProjects}
         totalCount={data.activeProjects.length}
       />
       ```
    4. If STAGE_COLORS was previously declared inline in dashboard.astro (`STAGE_COLORS = { ... }`) and was referenced ONLY by this card, remove the inline declaration. If it's still referenced elsewhere (e.g., by an overdue banner), keep it. After moving to portalStages.ts in Task 1, prefer single source of truth — delete the inline copy if safe.
    5. Preserve left-column layout ordering: ActiveProjectsCard remains above UpcomingDeliveriesCard (Plan 02).
  </action>
  <verify>
    <automated>grep -n "import ActiveProjectsCard" src/pages/admin/dashboard.astro && grep -n "<ActiveProjectsCard" src/pages/admin/dashboard.astro && ! grep -n "Active Projects</h2>" src/pages/admin/dashboard.astro && npx vitest run</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "import ActiveProjectsCard" src/pages/admin/dashboard.astro` returns 1 match
    - `grep -n "<ActiveProjectsCard" src/pages/admin/dashboard.astro` returns 1 match with `client:load`
    - `grep -n "Active Projects</h2>" src/pages/admin/dashboard.astro` returns 0 matches (inline card is gone)
    - `grep -n "displayProjects" src/pages/admin/dashboard.astro` returns 0 matches
    - `npx vitest run` all tests pass
    - Dashboard renders in dev server without runtime errors
  </acceptance_criteria>
  <done>ActiveProjectsCard island mounted on dashboard; inline card removed; layout preserved.</done>
</task>

</tasks>

<verification>
Manual UAT:
- [ ] Active Projects card shows filter input below the title
- [ ] Typing "construction" narrows rows to projects in construction stage
- [ ] Typing a client name substring narrows rows accordingly
- [ ] Typing a project name substring narrows rows accordingly
- [ ] Clearing the filter restores the full (capped-at-8) list
- [ ] Reload resets the filter to empty
- [ ] Empty-state copy on no match reads "No projects match your filter."

Automated:
- [ ] `npx vitest run src/components/admin/ActiveProjectsCard.test.tsx` passes
- [ ] All acceptance-criteria grep checks pass
</verification>

<success_criteria>
DASH-16 satisfied: Active Projects card has a working free-text filter matching project/client/stage. Visual parity with prior card. Filter state does not persist across reloads.
</success_criteria>

<output>
After completion, create `.planning/phases/35-dashboard-polish-global-ux-cleanup/35-03-SUMMARY.md`
</output>
