---
phase: 35-dashboard-polish-global-ux-cleanup
plan: 04
type: execute
wave: 4
depends_on: [01, 02, 03]
files_modified:
  - src/pages/admin/dashboard.astro
  - src/components/admin/QuickAssignTypeahead.tsx
  - src/components/admin/QuickAssignTypeahead.test.tsx
  - src/sanity/queries.ts
autonomous: true
requirements:
  - DASH-17
  - DASH-18
must_haves:
  truths:
    - "The dashboard Contractor card has a visible `+ Add new contractor` primary CTA that routes to `/admin/contractors/new`"
    - "When a contractor has exactly one trade, the quick-assign flow skips the trade-picker step and assigns directly with that single trade"
    - "When a contractor has >= 2 trades, the trade-picker continues to show (no regression)"
    - "When a contractor has zero trades, the existing `No trades listed` fallback continues to render (no regression)"
  artifacts:
    - path: "src/components/admin/QuickAssignTypeahead.tsx"
      provides: "Updated quick-assign with single-trade bypass logic"
    - path: "src/pages/admin/dashboard.astro"
      provides: "Contractor card header with Add-new-contractor CTA"
  key_links:
    - from: "src/pages/admin/dashboard.astro"
      to: "/admin/contractors/new"
      via: "Anchor element on Contractor card header"
      pattern: "href=\"/admin/contractors/new\""
    - from: "src/components/admin/QuickAssignTypeahead.tsx"
      to: "assignContractor() direct call when trades.length === 1"
      via: "handleSelect branch bypassing selectingTrade state"
      pattern: "trades\\.length === 1"
---

<objective>
Two small but load-bearing contractor flows:

1. **DASH-17** — add a `+ Add new contractor` button to the dashboard Contractor card that routes to `/admin/contractors/new` (the existing full contractor create page — no modal, no inline form per D-12).
2. **DASH-18** — in `QuickAssignTypeahead`, skip the trade-picker step when the selected contractor has exactly one trade. The single trade is auto-selected and the assign proceeds immediately (per D-13).

Depends on Plan 01 (formatTrade used in toast confirmation copy) and Plans 02/03 (both modify dashboard.astro — serialize after them to avoid merge conflict). The file overlap on dashboard.astro is the serialization driver.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/35-dashboard-polish-global-ux-cleanup/35-CONTEXT.md
@.planning/phases/35-dashboard-polish-global-ux-cleanup/35-UI-SPEC.md
@.planning/phases/35-01-SUMMARY.md

<interfaces>
From src/components/admin/QuickAssignTypeahead.tsx:
- `SearchResult` type at line 11 includes `trade?: string; trades?: string[]`
- `handleSelect(entity)` triggers `setSelectingTrade(entity)` for contractors — this is where the single-trade bypass branches in (around line 199-205 area)
- `assignContractor(selectedContractor, trade)` lines 171-206 — existing function that executes the assignment; call directly with the single trade when bypassing
- `handleTradeSelect(trade)` line 208-211 — called by the picker; single-trade bypass calls `assignContractor` directly instead of routing through the picker
- Line 285 renders `{formatTrade(trade)}` already (after Plan 01 Task 2) — confirm during read

From src/pages/admin/dashboard.astro:
- Contractor card location: search for "Contractor" / "ContactCard" in the frontmatter and body. Per CONTEXT.md line 74-77, `ContactCardWrapper` renders contractor contact details. The "Dashboard Contractor card" is a card section on the dashboard that lists contractors — NOT the hover popover. Inspect dashboard.astro first to find the card (may be a section rendering a list of contractors; UI-SPEC speaks of a "Dashboard Contractor card" at line 134).

**CRITICAL:** If dashboard.astro does NOT currently contain a standalone Contractor card (only contact-popover hover triggers on other cards), this plan must first CREATE the Contractor card. Reading dashboard.astro during the "read_first" phase will surface this. If the card needs to be created, follow the Active Projects / Upcoming Deliveries card chrome verbatim. The card should list the most recent N contractors (top 6) with name + trades pills rendered via `formatTrade`, and have the `+ Add new contractor` CTA in the header-right slot.

From src/components/admin/ui/AdminToast.tsx:
- Existing toast primitive. After a successful single-trade auto-assign, fire a toast: `Assigned {contractorName} as {formatTrade(trade)}.` per UI-SPEC copywriting row.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Single-trade bypass in QuickAssignTypeahead</name>
  <files>
    src/components/admin/QuickAssignTypeahead.tsx,
    src/components/admin/QuickAssignTypeahead.test.tsx
  </files>
  <read_first>
    - src/components/admin/QuickAssignTypeahead.tsx full file (understand selectingTrade state machine, handleSelect, handleTradeSelect, assignContractor)
    - src/lib/trades.ts (formatTrade — used in toast copy)
    - src/components/admin/ui/AdminToast.tsx (toast invocation API)
    - .planning/phases/35-dashboard-polish-global-ux-cleanup/35-CONTEXT.md D-13
    - .planning/phases/35-dashboard-polish-global-ux-cleanup/35-UI-SPEC.md "Quick-assign single trade" and "Quick-assign multi trade" interaction state rows
  </read_first>
  <behavior>
    - When user selects a contractor from the typeahead AND `contractor.trades?.length === 1`: skip `setSelectingTrade`, call `assignContractor(contractor, contractor.trades[0])` directly, fire success toast `Assigned {contractor.name} as {formatTrade(contractor.trades[0])}.`
    - When `contractor.trades?.length >= 2`: existing behavior — set selectingTrade so the picker renders
    - When `contractor.trades?.length === 0` OR undefined: existing fallback — `No trades listed` branch (confirm line 288-292 continues to render)
    - Non-contractor entities (clients, etc.) — unchanged; single-trade bypass is contractor-only
  </behavior>
  <action>
    Create `src/components/admin/QuickAssignTypeahead.test.tsx` if missing (check first — there's a `RegenerateLinkDialog.test.tsx` so the test-harness pattern exists; mirror it). Tests required:
    1. Selecting a contractor with 1 trade calls `assignContractor` once with that trade, does NOT render the trade picker
    2. Selecting a contractor with 2+ trades renders the trade picker (does not bypass)
    3. Selecting a contractor with 0 trades shows `No trades listed`
    4. Single-trade bypass triggers toast with copy `Assigned {name} as {formattedTrade}.`
    5. Non-contractor entity selection is unaffected

    Mock the assignment API call and toast function. Use @testing-library/react.

    Run tests — MUST fail (bypass not implemented).

    Modify QuickAssignTypeahead.tsx:
    - Locate the contractor branch of `handleSelect` (search for `selectingTrade` setter near line 195-205).
    - Before setting `selectingTrade`, add:
      ```tsx
      if (entity.type === "contractor" && entity.trades?.length === 1) {
        const onlyTrade = entity.trades[0];
        void assignContractor(entity, onlyTrade);
        // toast fires inside assignContractor's success path if that's the existing pattern;
        // otherwise fire it here via the existing toast import
        return;
      }
      ```
    - Confirm `assignContractor` returns a Promise and fires a success toast — if it does NOT, add the toast call at the call site with copy `Assigned ${entity.name} as ${formatTrade(onlyTrade)}.`.
    - Import `formatTrade` if not already imported (Plan 01 Task 2 added it).
    - The trade picker (lines 275-295 area) is unchanged for multi-trade case.

    Re-run tests — MUST pass.
  </action>
  <verify>
    <automated>npx vitest run src/components/admin/QuickAssignTypeahead.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "trades.length === 1\|trades?.length === 1" src/components/admin/QuickAssignTypeahead.tsx` returns 1 match
    - `grep -n "formatTrade" src/components/admin/QuickAssignTypeahead.tsx` returns >= 1 match
    - Test file has >= 5 test cases covering single/multi/zero-trade paths
    - All QuickAssignTypeahead tests pass
    - Existing multi-trade picker behavior unchanged (assertable via the `>= 2 trades` test case)
  </acceptance_criteria>
  <done>Single-trade contractors auto-assign on selection; multi-trade flow preserved; toast confirmation uses formatTrade label.</done>
</task>

<task type="auto">
  <name>Task 2: Add Contractor card + `+ Add new contractor` CTA to dashboard</name>
  <files>src/pages/admin/dashboard.astro</files>
  <read_first>
    - src/pages/admin/dashboard.astro full file (determine if a Contractor card already exists or needs creation)
    - src/components/admin/ContactCardWrapper.tsx (if Contractor card reuses popover pattern for contact details)
    - src/sanity/queries.ts (find existing contractor query OR determine if `getAdminDashboardData` needs a `contractors` field added)
    - src/lib/trades.ts (formatTrade for trade pills)
    - .planning/phases/35-dashboard-polish-global-ux-cleanup/35-CONTEXT.md D-12 and "Dashboard Contractor card" in code_context
    - .planning/phases/35-dashboard-polish-global-ux-cleanup/35-UI-SPEC.md "Header-right action button" section (line 160+), "Primary Visual Anchors" (Tasks row shows the pattern), and Copywriting Contract row `Add-contractor primary CTA`
  </read_first>
  <action>
    **Decision tree based on what `dashboard.astro` currently shows:**

    **If a Contractor card already exists:** Add an `+ Add new contractor` CTA in the header-right slot. Copy:
    ```astro
    <a
      href="/admin/contractors/new"
      class="inline-flex items-center gap-1.5 text-[13px] font-body text-terracotta border border-terracotta/40 rounded-md px-3 py-1 hover:bg-terracotta/5 transition-colors focus:outline-none focus:ring-1 focus:ring-terracotta"
    >
      <svg /* lucide Plus icon, 14px, inline */ >...</svg>
      Add new contractor
    </a>
    ```
    Place anchor in the card header row, right-aligned, same vertical center as the card title. Do NOT create a second header band (UI-SPEC line 71).

    **If NO Contractor card exists:** Create one matching the sibling card chrome (Active Projects / Upcoming Deliveries). Place it in the right column below DashboardTasksCard, or in the left column below Upcoming Deliveries (pick whichever is visually balanced — the right column is lighter; add there). Card body:
    - Query the top 6 recent contractors from Sanity. If `getAdminDashboardData` does not already include contractors, extend it (parallel to the deliveries extension in Plan 02 Task 3):
      ```groq
      "contractors": *[_type == "contractor"] | order(_createdAt desc) [0...6] {
        _id, name, company, trades
      }
      ```
    - For each contractor: render name, company (small), and trade pills — each pill's text comes from `formatTrade(slug)` (import the util in the Astro frontmatter).
    - Row link: `/admin/contractors/{contractorId}`.
    - Card title: `Contractors` (plain — no rename required by requirements; Phase 40 will rename to "Contractor / Vendor").
    - Header-right CTA: the `+ Add new contractor` anchor described above.

    **Either path:** The anchor MUST have `href="/admin/contractors/new"`. No `rel="noopener"` needed (same-origin).
  </action>
  <verify>
    <automated>grep -n 'href="/admin/contractors/new"' src/pages/admin/dashboard.astro && grep -n "Add new contractor" src/pages/admin/dashboard.astro</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n 'href="/admin/contractors/new"' src/pages/admin/dashboard.astro` returns 1 match
    - `grep -n "Add new contractor" src/pages/admin/dashboard.astro` returns 1 match
    - `grep -n "text-terracotta\|border-terracotta" src/pages/admin/dashboard.astro` returns >= 2 matches (outline button styling per UI-SPEC)
    - `grep -n "focus:ring-terracotta" src/pages/admin/dashboard.astro` returns >= 1 match
    - The button/anchor sits inside a card header (verify by visual inspection or by matching adjacent `<h2>` title in same parent block — can be spot-checked manually)
    - Clicking the link in dev routes to the existing `/admin/contractors/new` page (manual check)
    - No new modal introduced (UI-SPEC explicitly forbids this — grep for `AdminModal` added in this change should return 0 new occurrences on this file)
  </acceptance_criteria>
  <done>Dashboard Contractor card has a working `+ Add new contractor` CTA routing to the full create page.</done>
</task>

</tasks>

<verification>
Manual UAT:
- [ ] Dashboard Contractor card shows `+ Add new contractor` CTA in the header-right slot
- [ ] Clicking the CTA navigates to /admin/contractors/new (existing full create page)
- [ ] Quick-assign: pick a contractor with a single trade -> instantly assigned, toast confirms with sentence-case trade label
- [ ] Quick-assign: pick a contractor with multiple trades -> existing trade-picker step still renders
- [ ] Quick-assign: pick a contractor with no trades -> `No trades listed` fallback still renders

Automated:
- [ ] `npx vitest run src/components/admin/QuickAssignTypeahead.test.tsx` all green
- [ ] All acceptance-criteria grep checks pass
</verification>

<success_criteria>
DASH-17 satisfied (entry from dashboard). DASH-18 satisfied (single-trade bypass). No regression in multi-trade or zero-trade assignment paths.
</success_criteria>

<output>
After completion, create `.planning/phases/35-dashboard-polish-global-ux-cleanup/35-04-SUMMARY.md`
</output>
